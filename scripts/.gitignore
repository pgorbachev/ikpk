node_modules/

# Локальное состояние прогонов импорта (TD-8): персистентный кеш идемпотентности медиа
# между отдельными прогонами процесса. Машинное состояние конкретного хоста, не артефакт
# репозитория — коммитить нечего, разные окружения (dev/VPS) имеют разный кеш.
.media-cache.json
