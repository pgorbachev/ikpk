# Приёмка отличимости Youtube / Rutube (social-accounts)

Свидетельство сценария «приёмка отличимости» (`openspec/changes/social-accounts/specs/…`):
кадр всего подвала при целевом размере марок, в каждой поддерживаемой теме.

| Файл | Тема |
|---|---|
| `footer-distinctiveness-default-1280.png` | тема по умолчанию (без `data-theme`) |
| `footer-distinctiveness-dark-1280.png` | `:root[data-theme='dark']` |

Снято с `web/dist` (chromium, 1280×900, `contentinfo`). Подтверждение владельца —
задача 3.17 в `tasks.md`.
