# Tech Debt

Зафиксированные технические долги проекта. Ссылки в коде имеют вид `TODO(TD-N)`.

**Закрыто 2026-08-17 — подвижные major-теги GitHub Actions.** Все внешние `uses:` в
`.github/workflows/` закреплены полными SHA; читаемая major-версия сохранена рядом
комментарием. Общая проверка `dependency-workflow-integrity.test.ts` запрещает вернуть
подвижный ref и сообщает точные файл и строку. Dependabot продолжает обновлять
экосистему `github-actions`, но изменение SHA теперь всегда видно в diff.

**Про пропуски в нумерации.** Номер занимается в той ветке, где запись написана, а ветки
сливаются не в порядке нумерации — поэтому в этом файле на неслитой ветке пропуски
нормальны и означают «номер занят другой веткой», а не «запись удалена». Пропуск в
нумерации сам по себе — не дефект; дефект — **необъяснённый** пропуск, потому что
следующий исполнитель либо переиспользует занятый номер и получит коллизию при merge, либо
потратит время на поиск потерянной записи. Поэтому занятые в других ветках номера
перечисляются здесь, а новый номер берётся **за максимумом по всем живым ветках**, а не за
максимумом в этом файле:

**Состояние на 2026-08-13, после merge PR #98: пропусков в этом файле больше нет —
номера идут 1…30 подряд, и это ожидаемый исход, а не признак того, что правило выше
перестало действовать.** История такая:

- **TD-20, TD-21, TD-23, TD-24, TD-25** были заняты веткой
  `change/schedule-month-filter-impl` и на 2026-08-13 приехали в `main` вместе с ней
  (squash-коммит `1363684`). До этого в файле этой ветки на их месте стоял объяснённый
  пропуск;
- **TD-22** занят этой веткой (`change/serving-cache-headers-impl`); в
  `change/schedule-month-filter-impl` он был пропущен симметрично, и после её merge
  пропуск закрылся с этой стороны;
- **TD-26…TD-30** заведены этой веткой **за максимумом по всем живым ветвям**, а не за
  максимумом в файле, — именно поэтому они начались с 26, хотя в файле старшим тогда был
  TD-19, и именно поэтому при merge не возникло коллизии, а только конфликт вставки в одно
  место файла, разрешаемый складыванием обоих наборов;
- в `main` на 2026-08-13 старший номер — **TD-30** (после merge этой ветки будет он же).

---

## TD-1 — Дублирование логики пагинации в statyi/index.astro

**Файлы:** `web/src/pages/statyi/index.astro`
**Метки в коде:** `TODO(TD-1)` (2 места — серверная `buildPaginationTokens` и клиентская `getPaginationTokens`)

**Проблема:**
Алгоритм построения токенов пагинации продублирован дважды: серверная функция для SSG initial render и клиентская для динамической перестройки при фильтрации. При изменении алгоритма нужно обновлять оба места.

**Решение:**
Вынести в `web/src/lib/pagination.ts` как чистую функцию и импортировать из обоих мест (Astro компилирует `<script>` с поддержкой ESM).

---

## TD-2 — Все статьи как `<template>` в HTML на странице /statyi

**Файлы:** `web/src/pages/statyi/index.astro`
**Метки в коде:** `TODO(TD-2)`

**Проблема:**
Клиентская фильтрация/поиск работает через скрытые `<template>` теги — все статьи рендерятся в HTML при сборке. При росте каталога страница будет тяжёлой (сейчас 68 статей — приемлемо).

**Решение (на выбор):**
1. Статические пагинированные страницы `/statyi/page/2` — нет JS-поиска.
2. JSON-индекс `/statyi-index.json` — клиент грузит один раз, рендерит карточки динамически. Сохраняет поиск/фильтрацию без раздутого HTML.

**Когда актуально:** при росте каталога свыше ~200 статей.

---

## TD-3 — Пагинация /statyi без fallback при отключённом JS

**Файлы:** `web/src/pages/statyi/index.astro`
**Метки в коде:** нет (архитектурный долг)

**Проблема:**
Пагинация реализована через `<button>` + клиентский JS. При отключённом JavaScript или ошибке загрузки скрипта пользователь и поисковый бот видят только первые 6 статей — нет ни ссылок, ни URL для остальных страниц.

**Решение (на выбор):**
1. Статические страницы `/statyi/page/2`, `/statyi/page/3` — работают без JS, индексируются поисковиками.
2. `<a href="?page=2">` с прогрессивным улучшением через JS — сохраняет URL-навигацию и работает без скриптов.

**Когда актуально:** сейчас некритично (SEO покрывается SSG), но желательно до роста каталога.

---

## TD-4 — TopNav переполняется по горизонтали при увеличенном корневом кегле

**Файлы:** `web/src/components/topnav/TopNav.astro`
**Метки в коде:** нет (тест зафиксирован через `test.fixme` в `web/tests/a11y.spec.ts`, describe `Root font-size scaling (a11y text zoom)`)

**Проблема:**
После снятия `font-size: 16px` с `html` (фикс дефекта — `web/src/styles/base.css`) корневой кегль страницы теперь действительно масштабируется с пользовательской настройкой браузера. `.topnav-bar` — плоский `display:flex` без `flex-wrap`, а `.topnav-menu`, `.topnav-phone`, кнопка CTA и блок инструментов держат `white-space: nowrap`/`flex-shrink:0`. При росте кегля (проверено симуляцией `document.documentElement.style.fontSize='32px'`, т.е. 200%) содержимое шапки перестаёт помещаться и визуально выходит за пределы viewport — воспроизводится на всех проверенных страницах (`/`, `/statyi/`, `/raspisanie-i-tseny/`) и обоих viewport'ах (desktop 1280px, mobile 375px).

**Почему не исправлено в рамках этой задачи:**
Раскладка уже держится на грани даже при базовом кегле (16px) — эмпирически проверено несколько вариантов минимального фикса:
- `flex-wrap: wrap` на `.topnav-bar` — устраняет переполнение при зуме, но одновременно ломает раскладку при обычном 16px (высота бара выросла с ~73px до ~127px на desktop 1280px: `.topnav-menu` с `flex:1` и `min-width:auto` уже забирает почти всю доступную ширину при 16px, включение wrap меняет алгоритм переноса и сразу утаскивает `.topnav-actions` на вторую строку);
- `min-width: 0` на `.topnav-menu` + `flex-wrap: wrap` на внутреннем `ul` — та же картина (высота баз-кегля выросла до ~102px);
- снятие `white-space: nowrap` с `.topnav-link` — при 16px пункт меню «Об институте» (два слова) уже переносится на 2 строки сам по себе (высота 60.8px против 38.4px у остальных пунктов).

Любой из вариантов — уже визуальное изменение обычного (не увеличенного) состояния шапки, а такие изменения по правилам проекта (`AGENTS.md`, «Изменения облика: мокапы до внедрения») требуют мокапов и согласования, а не точечного red/green-фикса. Настоящая правка требует пересмотра плотности контента шапки на десктопе (например: раньше прятать телефон/поиск, укорачивать подписи, либо контейнерные запросы) — отдельная работа с дизайн-решением.

**Решение (на выбор для последующей работы):**
1. Мокап компактной desktop-шапки (что прячется первым при недостатке места) → внедрение через container queries или доп. брейкпоинты.
2. Рассмотреть отдельный, более узкий набор пунктов меню / скрытие второстепенных элементов (тема/поиск) на промежуточных ширинах — сейчас это уже частично есть по viewport (`@media (max-width: 1440px)`, `@media (max-width: 900px)`), но не по кеглю.

**Когда актуально:** до того, как реальные пользователи с включённым увеличением текста (Chrome/Edge «Размер шрифта», iOS «Крупный текст» и т.п.) начнут пользоваться сайтом с шапкой — сейчас баг реален, но обнаруживается только при значительном (~200%) увеличении кегля.

---

## TD-5 — Секции вне шапки не готовы к росту корневого кегля

**Файлы:** секции главной (`hero-d-*`, `feature-card`, `adv-cta-*`, `segment-card`, `upcoming-card`, `teacher-card`, `cta-band-*` — см. `web/src/pages/index.astro` и соответствующие компоненты), карточки расписания (`schedule-card-*`, `web/src/pages/raspisanie-i-tseny/index.astro` и компоненты)
**Метки в коде:** нет (тест зафиксирован через `test.fixme` в `web/tests/a11y.spec.ts`)

**Проблема:**
При симуляции увеличенного кегля (`document.documentElement.style.fontSize='32px'`, 200%) горизонтальную прокрутку страницы создают не только шапка (TD-4), но и содержимое конкретных секций:
- главная (`/`) — переполняется на **обоих** viewport'ах (desktop и mobile): `hero-d-*`, `feature-card`, `adv-cta-copy`, `segment-card`, `upcoming-card`, `teacher-card`, `cta-band-actions` и др.;
- `/raspisanie-i-tseny/` — переполняется на **mobile**: карточки `schedule-card-*` (688 затронутых узлов в проверке).

`/statyi/` и десктопный `/raspisanie-i-tseny/` при той же симуляции чисты вне шапки — это подтверждено проходящим тестом (см. ниже).

**Почему не исправлено в рамках этой задачи:** объём — компоненты используют фиксированные rem-размеры (ширины карточек, отступы) без учёта роста кегля; это отдельная, более широкая работа по компонентам главной и расписания, а не точечный дефект.

**Гейт:** `web/tests/a11y.spec.ts`, describe `Root font-size scaling (a11y text zoom)`, тест `content outside the header has no new horizontal overflow at 2x root font-size`. Для `/statyi/` (desktop+mobile) и `/raspisanie-i-tseny/` (desktop) тест реальный и зелёный — ловит регресс (проверено негативно: временная инъекция `main { min-width: 2000px }` валила именно эти три комбинации). Для `/` (оба viewport) и `/raspisanie-i-tseny/` (mobile) тест зафиксирован как `test.fixme` с ссылкой на этот пункт — снять fixme по мере исправления конкретных секций.

**Решение (на выбор):** аудит и починка по компонентам (по одному) с проверкой на 2x кегля; либо системная мера — `clamp()`/`min()` для критичных ширин, либо контейнерные запросы там, где сетка карточек жёстко завязана на px/rem-ширину.

**Когда актуально:** вместе с TD-4 — оба обнаружены одной и той же проверкой роста кегля.

---
## TD-6 — Браузерные проверки не сверяют код ответа

**Метки в коде:** нет

**Проблема:**
Измерено на этой ветке: из переходов `page.goto` в `web/tests/*.spec.ts` **81 не
захватывает ответ вовсе** (плюс 21 через helper, см. ниже — всего 102) — `a11y.spec.ts` 2 из 2, `site.spec.ts` 19 из 20,
`compare.spec.ts` 58 из 60, `compat.spec.ts` 2 из 3. Проверка, выполненная на странице
404, при этом не падает: страница маленькая, нарушений axe на ней нет, DOM-утверждения не
находят элементов и молчат либо ждут таймаут, а причина в отчёте выглядит как поломка
компонента.

Это не гипотеза. На ветке `feat/demo-mode-and-hero-photo` (PR #27) ровно так проверялись
**10 шаблонов доступности из 14**: адреса шли со слэшем на конце при
`trailingSlash: 'never'`, и «36 проверок доступности» в описании PR означали одну
проверенную главную. Там это закрыто обязательной сверкой кода ответа; здесь, в `main`,
проверки остались прежними — сборка отдаёт обе формы адреса, поэтому дефект пока не
проявляется, но зависит от конфигурации, а не от самих проверок.

К этим 81 добавляются **21 переход через `web/tests/helpers/navigation.ts`** — по нему
ходят все пять parity-спек (`parity-audit.spec.ts` 12, `schedule-parity.spec.ts` 3,
`seminar-parity.spec.ts` 2, `institute-parity.spec.ts` 2, `course-group-parity.spec.ts` 2),
ни одной сверки кода ответа. Первая редакция этой записи выводила их из-под правила,
считая, что helper распознаёт неотданную страницу по разметке. **Это неверно:**
`navigation.ts:6-9` считает переход успешным, едва виден `header`, а **своя** страница 404
несёт полную шапку — `404.astro` собирается через `BaseLayout`, который рендерит `TopNav`
(на ветке с медиа это видно и в собранном `dist/404.html`). Опознаётся helper'ом только
404 **предпросмотра Astro** — по заголовку `404: Not Found` (`navigation.ts:11`), которого
на своей 404 нет. Итого под правило попадает **102** перехода, а не 81.

Что действительно устроено иначе и переделки не требует: `compare.spec.ts:84-85` сверяет
`status < 400`, и там, где не сверяет, проверки падают на пустых `title`/`h1` — то есть не
молчат.

**Отдельный дефект того же класса в `parity-compare.test.ts`:** три теста начинаются с
`if (!REMOTE_PARITY_ENABLED || !remoteOriginReachable) return;` (`:171`, `:265`, `:275`), а
`PARITY_REMOTE` выставлен только в `test:build:remote` (`package.json:18`), не в обычном
`test:build` (`:17`). В штатном прогоне тест «original and local homepage keep same main
institute heading» отчитывается как **passed**, не выполнив ни одного сравнения — то есть
«не смогла проверить» выдаётся за «дефектов нет». Правильное поведение — `skip` с явной
причиной либо падение, если удалённая сверка объявлена обязательной.

**Решение:**
Захватывать ответ у каждого перехода и сверять с ожидаемым кодом: `200` там, где
проверяется содержимое; ожидаемый `301`/`404` там, где предмет — сам ответ, без следования
редиректу. Правило записано в `AGENTS.md`, раздел «Дефекты: red/green и негативная проверка
гейтов»; оно действует на новые проверки и на те, которых касается работа, поэтому долг
закрывается постепенно, а не одной переделкой набора.

**Когда актуально:** при слиянии PR #27 (там появляется `trailingSlash: 'never'`, и любой
адрес со слэшем начинает отдавать 404) — то есть до него, иначе часть проверок станет
вакуумной молча.
## TD-7 — Откат релиза выполняется руками

**Метки в коде:** `scripts/deploy-web.sh` (сообщение при провале health-check)

**Проблема:**
Провал health-check теперь роняет деплой ненулевым кодом, но symlink `current` к этому
моменту уже переключён: сломанный релиз остаётся активным, и восстановление — ручное
(перевести symlink на предыдущий каталог в `releases/`, перезагрузить nginx).

**Решение:** автоматический откат — запомнить предыдущую цель symlink до переключения,
при провале health-check вернуть её и перезагрузить nginx, затем завершиться ненулевым
кодом. Отдельная работа, потому что требует решения, что считать провалом (одна
страница или набор) и как отличать провал релиза от недоступности сети.

**Когда актуально:** до первого боевого деплоя.

---

## TD-8 — Импорт в CMS сообщает об ошибках и завершается нулём

**Метки в коде:** `scripts/import.ts`

**Проблема:**
`report.errors` и `report.missingRelations` печатаются в итоговом отчёте (`TOTALS: … ❌ N
errors`), после чего `main()` завершается нормально — код выхода 0. Импорт с потерянными
сущностями и оборванными связями выглядит успешным. Отдельно: `catch` при загрузке файла
(`import.ts:246`) создаёт сущность без изображения, не увеличивая никакого счётчика.

Тот же класс, что закрыт для `make-derivatives`, `gen-redirects`, `refresh-catalog`,
`recover-collapsibles`, `download-media` и `localize-upload-links`, но здесь ставка выше:
пишется в CMS заказчика, и повторный запуск не идемпотентен.

**Решение:** ненулевой код выхода при непустых `errors`/`missingRelations`, счётчик
неудачных загрузок файлов, осознанный обход отдельным флагом.

**Когда актуально:** до следующего запуска импорта в CMS заказчика.

---

## TD-9 — Обе advisory закрыты; остаётся процессный долг и порог гейта

**Файлы:** `web/package.json`, `web/package-lock.json`
**Метки в коде:** нет

**Техническая часть закрыта полностью:**

- **js-yaml 4.0.0–4.3.0**, high — [GHSA-5p4m-2wfm-xmqj](https://github.com/advisories/GHSA-5p4m-2wfm-xmqj)
  (CVE-2026-59870). Приходил транзитивно через `astro` и `@astrojs/internal-helpers`.
  Закрыт подъёмом до 4.3.1, коммит `5c3f4b9`;
- **postcss ≤8.5.22**, moderate — [GHSA-fxqj-rqcc-2cmp](https://github.com/advisories/GHSA-fxqj-rqcc-2cmp).
  Закрыт подъёмом до 8.5.26, коммит `5d48da8`.

`npm run audit:prod` в `web/` выходит с кодом 0.

**Смежная ловушка, стоившая отдельного коммита:** npm 11 вырезает из lockfile записи,
без которых `npm ci` в CI падает (`01f8981` их вернул). Та же версия npm уже кусалась
на апгрейде Astro. Правило: lockfile правится под npm 10.

**Что осталось — и это важнее закрытых advisory.**

*Первое, процессное.* С красными гейтами аудита были смержены PR #32 и #33. Гейт, мимо
которого мержат, гейтом быть перестаёт: в следующий раз им так же пренебрегут при
настоящей уязвимости. Причина не выяснена — либо проверка не required, либо
использовался обход. Тот же механизм описан в TD-11 применительно к нестабильному
бюджету: две разные дороги к одному итогу — «красное перестало значить красное».

*Второе, про сам порог.* Гейт запускается с `--audit-level=high`. Пока advisory были
открыты, это скрывало moderate-уязвимость: postcss висел, а гейт при нём был бы
зелёным, если бы не js-yaml. Сейчас открытых нет, поэтому вопрос не горит, но остаётся:
**зелёный `audit:prod` означает «выше порога ничего нет», а не «уязвимостей нет»**, и
читается он как второе.

**Решение:**
1. Выяснить, почему прошёл мерж при красном required-гейте, и закрыть эту возможность.
2. Решить осознанно: понижаем порог до moderate или явно записываем, что moderate
   принимаются и почему.

**Когда актуально:** пункт 1 — до следующего мержа в `main`; пункт 2 — при ближайшем
касании зависимостей.

---

## TD-10 — Контраст проваливается во время анимации смены темы

**Файлы:** `web/src/styles/motion.css` (переход `.topnav`),
`web/src/components/topnav/HeaderTools.astro`, `web/src/components/topnav/TopNav.astro`
(переход дорожки), `web/tests/a11y.spec.ts` (гейт)
**Метки в коде:** комментарии у правил тумблера и у ожидания в тесте

**Проблема:**
При каждом переключении темы контраст дорожки тумблера к фону шапки падает **до
1.04:1** и держится ниже порога 3:1 около **150 мс**. Измерено покадрово на реальном
клике:

```
t=115 мс  дорожка rgb(66,122,70) на фоне rgb(111,113,111)  → 1.04:1   дно
t=607 мс  осело                                             → 3.41:1
```

Причина — рассинхрон двух переходов на одном клике: `.topnav` перекрашивается
`background-color 0.3s` (motion.css, «плавная смена темы для крупных поверхностей»),
дорожка — `background 0.15s`. Фон проезжает через светимость, совпадающую с дорожкой.
Обводка `#9aa39a` не спасает: она статична, а сравнивается с тем же уезжающим фоном и
проваливается вместе с ним.

**Синхронизация длительностей это НЕ чинит.** Переход из светлой темы (6.42:1) в
тёмную (3.41:1) при любом плавном кроссфейде проходит через точку, где фон и дорожка
равны по светимости. Дело не в скорости, а в самом факте интерполяции между двумя
палитрами.

**Принятое решение:** переходные кадры анимации считаются вне области WCAG 1.4.11 —
стандарт описывает состояния компонента, а не промежуточные кадры. Устойчивые
состояния порог проходят (см. таблицу замеров в `base.css`). Пользователи с
`prefers-reduced-motion: reduce` этого окна не видят вовсе: переходы у них выключены
(`motion.css`).

**Следствие для гейта, уже исправленное:** тест ждал 200 мс при переходе 300 мс и
поэтому иногда мерил неосевшее состояние — независимое ревью воспроизвело падения на
исправном коде (2 из 15 и 2 из 20 повторов). Ожидание поднято до 350 мс. У меня
локально на 20 повторах флап не воспроизводился — то есть частота зависит от машины, и
«три зелёных прогона подряд» доказательством надёжности не были.

**Что осталось открытым:**
1. Признание переходных кадров вне области — решение, а не факт. Альтернатива —
   убрать плавную смену фона шапки; это изменение облика и по правилам проекта требует
   мокапов, поэтому не сделано походя.
2. Мобильный `.drawer-theme` на тот же провал не проверялся: он вне `.topnav`, и
   переход у его подложки может быть другим.
3. Гейт по-прежнему мерит отрисованный контрол, а не правила CSS, поэтому зависит от
   того, какое состояние успело установиться. Сочетание «тёмная тема + выключено»
   устойчиво недостижимо и проверяется только расчётом.

**Когда актуально:** при изменении палитры тумблера, длительностей переходов в
`motion.css` или появлении третьего режима темы.

---

## TD-11 — Бюджет total-blocking-time нестабилен на общих раннерах

**Файлы:** `web/lighthouserc.cjs` (бюджеты), `.github/workflows/lighthouse.yml`
**Метки в коде:** нет

**Проблема:**
Гейт «Lighthouse budgets» упал на главной по одной метрике:

```
total-blocking-time  ожидалось <=200, получено 771
```

Перезапуск **на том же SHA, без единого изменения кода**, дал зелёный результат.
Значит метрика недетерминирована: разброс превышает бюджет в 3.8 раза, то есть
результат гейта определяется загрузкой раннера, а не качеством сборки.

Доказательство, что дело не в коде: падения пришлись на коммиты `5c3f4b9` и `01f8981`,
а их отличие от зелёного `afd0dec` — только `web/package-lock.json`, 9 строк, подъём
`js-yaml` (сборочная зависимость, в браузер не уезжает).

Почему это долг, а не «просто перезапустить»: гейт, который краснеет случайно, учит
перезапускать не глядя. После нескольких таких случаев настоящее падение TBT тоже
спишут на шум — и в этот момент гейт перестанет работать, оставаясь формально
включённым. Это тот же механизм, который уже дал мерж PR #32 и #33 с красным аудитом
(см. TD-9).

**Решение (на выбор):**
1. Брать по TBT не медиану 5 прогонов, а перцентиль/минимум — метрика чувствительна к
   выбросам сильнее прочих, и медиана их не гасит.
2. Поднять бюджет TBT до значения, которое реально держится на общих раннерах, и
   записать, почему именно такое — вместо бюджета, который проходит через раз.
3. Мерить TBT на выделенном раннере либо вынести его из блокирующих проверок в
   отчётные, оставив блокирующими стабильные метрики.

Вариант 2 без вариантов 1 или 3 — самый дешёвый и самый вредный: он снимает симптом,
не отвечая на вопрос, какое значение TBT мы считаем нормой.

**Наблюдения (пополняется, чтобы «просто перезапустить» имело цену):**

| Дата | SHA | Ветка | Метрика | Отношение к бюджету |
|---|---|---|---|---|
| ранее | `5c3f4b9`, `01f8981` | — | TBT 771 при бюджете 200 | 3.9× |
| 18.08.2026 | `ad31096` | `change/cms-content-publication` | TBT 1060 при бюджете 200 | **5.3×** |
| 22.08.2026 | `d34e729` | PR #156 | TBT 1083 при бюджете 200 | 5.4× |

Частота, измеренная по последним **100** прогонам workflow `lighthouse.yml`: 87 success,
3 failure, 10 cancelled — то есть ~3 %. Разброс при этом растёт: 3.9× → 5.3× → 5.4×.

Доказательство, что дело снова не в коде, — сверка **входов сборки**, а не «на код не похоже»:
у всех четырёх голов (`63c60cc` и `ad31096` этой ветки, `f736c9f` в
`change/cms-content-authoring`, `4c3746b` в `docs/rule-branch-from-fresh-main`)
`git diff --name-only 634dad5 <head> -- web/ scripts/ cms/` пуст — то есть входы сборки
совпадают с базовым коммитом, а различия лежат только в `openspec/**` и `docs/**`.
Хронология: успешный прогон `63c60cc` — 13:11:40Z, падение `ad31096` — 13:32:28Z, то есть
**через 20 минут 48 секунд**; соседние ветки прошли тот же джоб в 13:31:36Z и 13:32:02Z.

Две формулировки первой редакции этой записи были неверны, и это исправление, а не уточнение:
«за полтора часа до падения» (фактически 21 минута) и «собранный сайт во всех четырёх прогонах
байт-в-байт один» — последнее по этим прогонам **не доказано вовсе**: `lighthouse.yml` не
сохраняет ни артефакт сборки, ни контрольную сумму, поэтому равенство вывода из них не следует.
Проверяемое утверждение — равенство входов, оно выше.

**Случай 22.08.2026 (PR #156), внесён из `main`.** Тот же гейт, та же метрика, та же главная:

```
total-blocking-time  ожидалось <=200, получено 1083
```

Доказательство, что дело снова не в коде, сильнее прошлого: упавший `d34e729`
отличается от прошедшего `3d71ba8` **одним тестовым файлом** —
`web/tests/payment-transport.spec.ts`, 5 добавленных строк, правка регулярного
выражения в утверждении. Этот файл в сборку не входит вовсе, в браузер не уезжает и на
время блокировки главного потока повлиять не может ни при какой раскладке. Прошедший и
упавший прогоны разделяют шесть минут.

**Третий случай, 23.08.2026 (PR #155).** Тот же гейт, та же метрика, та же главная:

```
total-blocking-time  ожидалось <=200, получено 431.8864
```

**Этот случай ломает довод варианта 2 иначе, чем предыдущий.** Второй случай показал, что
«поднять бюджет до значения, которое держится» дорожает — понадобилось бы больше 1083.
Третий показал, что такого значения нет вовсе: наблюдённый ряд по TBT на главной —
**771, 1083, 431.9** при бюджете 200. Разброс не растёт монотонно, он просто широкий, то
есть подобрать «реально держащееся» число не удастся ни сверху, ни снизу: любой бюджет между
431 и 1083 будет проходить через раз, а бюджет выше 1083 не стережёт ничего.

Доказательство, что дело не в коде, здесь самое сильное из трёх на момент этой записи. Упавший
`e1951a2400bf8924f8cf211cacca6cb44fb112b9` отличается от прошедшего
`5e55697672d1837ff55b8330b43e0218a0994e5f` **двумя файлами, оба под `docs/`**:
`docs/design/mockups/demo-followups/README.md` и
`docs/design/mockups/demo-followups/mockup-code.patch.txt`. Ни один из них не входит в
сборку, не попадает в браузер и не может повлиять на время блокировки главного потока ни при
какой раскладке — это markdown и текстовый файл с патчем. Прошедший прогон завершился
2026-08-23T08:06:11Z, упавший — примерно двадцать минут позже, то есть около 08:26 UTC.

Отличие от прошлых случаев не только в доказательстве: те падения приходились на изменение
lockfile и тестового файла, то есть кто-то мог заподозрить хотя бы косвенное влияние. Здесь
влияние исключено по типу файлов, а не по рассуждению о содержании.

**Гейт входит в required-контексты `main`** (девятый из двенадцати), поэтому его ложное
падение переводит PR в `BLOCKED` — то есть шум прибора блокирует merge, а не просто мешает
читать ленту.

**Четвёртый случай, 23.08.2026 (PR #136), около 10:19 UTC — позже третьего в тот же день.**
Тот же гейт, та же метрика, та же главная:

```
total-blocking-time  ожидалось <=200, получено 601.5
```

Доказательство сильнее всех предыдущих на момент этой записи, и дальше усиливать его нечем: PR
не меняет **ни одного** файла, доходящего до браузера. Состав дельты — девять файлов
`openspec/**` и `docs/tech-debt.md`, то есть markdown целиком; ни исходников, ни стилей, ни
конфигурации сборки, ни тестов. Прежние случаи приходилось защищать доводом «этот тестовый файл
в сборку не входит»; здесь в сборку не входит **ничто из изменённого**, и вход Lighthouse
побайтово тот же, что у `main`. Если гейт по TBT способен покраснеть на таком PR, он не отвечает
про сборку ни в каком случае.

**Пятый случай, 23.08.2026 — на PR, который вносил четвёртую по счёту редакцию этой записи
(#160), позже четвёртого случая в тот же день.**

```
total-blocking-time  ожидалось <=200, получено 285.5
```

Диффом этого PR является **один файл — сам `docs/tech-debt.md`**. Markdown в сборку не входит,
в браузер не уезжает и на время блокировки главного потока не влияет никак. То есть гейт
покраснел на изменении, которое физически не может на него повлиять, — и покраснел на записи о
том, что он краснеет случайно.

Отдельно стоит записать, что **`285.5` ближе всего к бюджету из пяти датированных случаев**.
Соблазн прочитать это как «почти прошло, дело в мелочи» — ошибка того же рода, что и «поднять
бюджет»: близость к порогу ничего не говорит о причине, потому что предмет измерения тут вообще
не менялся.

**Пересчитанный ряд, а не два независимых.** Третья и четвёртая правки этой записи считали
каждая свой хвост наблюдений и разошлись тем же способом, что уже случалось раньше в этом файле
(см. запись о случаях 18.08 и 22.08 выше): ветка `change/cms-content-publication` знала о
случаях `ранее`, 18.08, 22.08 и о своём собственном (PR #136), но не о PR #155 и PR #160;
`main` знал о PR #155 и PR #160, но не о параллельном случае на PR #136. Слиты обе редакции, а
не выбрана одна сторона.

Полный ряд по TBT на главной при бюджете 200, по датам: **771 (ранее, без точной привязки),
1060 (18.08), 1083 (22.08, PR #156), 431.9 (23.08 08:26, PR #155), 601.5 (23.08 10:19, PR #136),
285.5 (23.08, PR #160)** — **шесть** наблюдений, все выше бюджета. Прежняя редакция считала
четыре и не называла 1060 — тот же класс недосчёта, только внутри одного файла, а не между
ветками. Разброс 285.5–1083 — почти четырёхкратный между крайними, и они не сдвинулись от
пересчёта. Это окончательно закрывает вариант 2: «значения, которое держится», в этих данных не
существует, а любой бюджет внутри разброса проходит через раз.

**Седьмой случай, 23.08.2026 около 22:18 UTC — второе падение на том же PR #136, после
коммита второго раунда ревью.** Тот же гейт, та же метрика, та же главная:

```
total-blocking-time  ожидалось <=200, получено 414.1198
```

Диффом между прошедшим и упавшим прогоном на этом PR — снова только `openspec/**` и
`docs/tech-debt.md`: проверено `git diff --name-only origin/main...origin/change/cms-content-publication`
— ни одного файла вне них. Это тот же PR, что дал `601.5` (случай четвёртый), теперь на более
поздней голове с тем же составом диффа — то есть один и тот же класс изменений дал гейту два
разных числа, 601.5 и 414.1, оба выше бюджета и оба внутри уже известного разброса. Дублирует
вывод случая четвёртого, а не добавляет новый: PR без единого файла в сборке продолжает получать
случайные значения TBT.

Полный ряд стал таким: **771, 1060, 1083, 431.9, 601.5, 285.5, 414.1** — **семь** наблюдений.
Разброс не сдвинулся: 285.5–1083.

**Когда актуально:** при следующем ложном падении. Фактически падений **шесть**, у каждого есть
дата и пара SHA до/после (недатированное «771, ранее» в счёт перезапусков не входит): 18.08,
22.08 (PR #156), 23.08 трижды (PR #155, дважды PR #136) и ещё раз 23.08 (PR #160, на PR про этот
самый долг). Шесть перезапусков потрачены, два из них — на одном и том же PR. Вариант 3 (вынести
TBT из блокирующих в отчётные) остаётся единственным, который не делает вид, что мы знаем норму.

---

## TD-12 — Две страницы обещают форму, которой нет

**Файлы:** `web/src/pages/sotrudnichestvo-s-nami.astro`, `web/src/pages/oplata.astro`
(текст приходит из контента, `discovery/entities/static_pages.json`)
**Метки в коде:** нет

**Проблема:**
Обе страницы сохранили подводку замещаемого сайта, рассчитанную на форму:

- `/sotrudnichestvo-s-nami` — «Готовы к сотрудничеству? **Кликайте на кнопку,
  заполняйте форму** и мы вернемся к вам с предложением!». Формы на странице нет и
  не было ни разу за время rebuild;
- `/oplata` — «Кликайте на кнопку, **выбирайте направление** и записывайтесь к нам на
  обучение!». Пока кнопка вела в расписание, обещание «выбирайте направление»
  выполнялось. После фикса адресов (PR #37) она ведёт на блок контактов той же
  страницы — честнее по назначению, но подводка стала неверной.

Второй пункт — прямое следствие моего же фикса, и он должен быть назван, а не
замолчан: фикс улучшил одно и ухудшил другое.

**Статус `/oplata` (обновлено, находка целевого ревью PR #90):** change
`payment-request-flow`, которым первая редакция этой записи объясняла закрытие
пункта `/oplata`, **удалён** — он был замещён `online-payment-flow` и не
реализуется. `online-payment-flow` держит требование «Устаревшая подводка перед
кнопкой заменена» (`specs/online-payment/spec.md`) и закрывает пункт `/oplata` после
своей реализации; до неё подводка остаётся неверной на `main`, как описано выше.

**Почему `/sotrudnichestvo-s-nami` не закрыт ничем:** ни удалённый
`payment-request-flow`, ни его преемник `online-payment-flow` эту страницу не
упоминают ни в proposal, ни в design, ни в tasks — оба ограничены оплатой со страницы
«Оплата». Описание PR #37 утверждало обратное для прежнего change — утверждение было
неверным, найдено независимым ревью, и остаётся неверным для его преемника по той же
причине (области разные).

**Решение (на выбор):**
1. Расширить объём формной работы на сотрудничество: механизм там идентичен оплате —
   модалка, свой эндпоинт, те же состояния, согласие, демо-режим, гейт. Требует
   решения владельца о разделении возможности на общий транспорт и специфику.
2. До появления форм на `/sotrudnichestvo-s-nami` — переписать её подводку под то, что
   страница реально делает. Дёшево и снимает ложь немедленно, но текст расходится с
   замещаемым сайтом, то есть это отступление от паритета и его надо назвать.

**Когда актуально:** пункт 2 для `/sotrudnichestvo-s-nami` — сразу, это одна правка
текста; пункт 1 — вместе с решением по объёму формной работы. Для `/oplata` отдельного
решения не нужно — снимается реализацией `online-payment-flow`.

---

---

## TD-13 — Остановку публикации нечем заметить

**Файлы:** `.github/workflows/deploy.yml`, `.github/workflows/nightly.yml`
**Метки в коде:** нет

**Проблема:**
Публикация переведена с `push: main` на событие `workflow_run` о завершении `Tests`
(change `deploy-gated-on-tests`). Вместе с гейтом появился класс отказа, которого раньше
физически не существовало: **связь между двумя workflow по имени может тихо не
сработать.**

Если событие не доставится или `Tests` не запустится вовсе (фильтры путей, отмена в
очереди, сбой на стороне GitHub), не стартует ни один джоб. А значит не будет и красного
прогона в Actions — того самого сигнала, по которому замечают поломку. Сайт просто
перестанет обновляться, и узнают об этом, когда кто-нибудь заметит устаревший контент.

Раньше этого риска не было: `push` вёл к публикации напрямую, и отказ был виден красным.

**Это цена осознанного решения, а не недосмотр.** Отказ безопасный — лучше не
опубликовать, чем опубликовать непроверенное, — и он назван в `design.md` change'а. Но
названный риск и замеченный риск это разные вещи: сейчас его ловит только разовая ручная
проверка сразу после merge, постоянного наблюдения нет. В `nightly.yml` про публикацию
нет ни строки (проверено).

**Решение:** лёгкая периодическая проверка — сравнить SHA последнего успешного `Tests` на
`main` с тем, что фактически опубликовано (метка в собранном выводе либо ответ живого
сайта), и краснеть при расхождении старше разумного окна. Место — `nightly.yml`.

Проверка обязана различать «расхождения нет» и «я не смогла проверить»: недоступность
сайта или отсутствие метки — это провал, а не тишина.

**Когда актуально:** после первой реальной публикации по новому пути. Найдено независимым
ревью последствий, merge change'а не блокирует.

**Демо-сборка внутри `Tests` (change `deploy-gated-on-tests`, решение 6) окно НЕ
расширяет — это измерено, а не предположено.** Сверка на `push` в `main`, до и после
merge #94: прогон 31577505010 (`a414b067c3a9f818bbae984c46412187cdec6cad`, до демо-шага)
против 31579350161 (`504e197dfd85ba7756623dfbe0e4b88ba920b25e`, после). Демо-шаг лежит в
джобе `unit-and-build`, который идёт параллельно `e2e-smoke` и не является критическим
путём:

| джоб | до демо-шага | после |
|---|---|---|
| `Unit and build tests` (демо-шаг здесь) | 103 s | 110 s |
| `Playwright smoke (desktop + mobile)` — критический путь | 152 s | 191 s |

`unit-and-build` вырос на 7 s, критический путь остался за `e2e-smoke`, запас между ними
вырос до 81 s. **Приписывать демо-сборке рост итога нельзя:** разброс самого `e2e-smoke`
(152 s против 191 s при медиане 176 s по пяти прогонам) на порядок больше вклада
демо-шага, и по одиночной паре прогонов из этих же данных выходит любой знак.

**Условие, при котором окно всё же начнёт расширяться:** `unit-and-build` становится
длиннее `e2e-smoke`, то есть должен съесть текущий запас в 81 s. Это условие, а не факт —
до тех пор демо-сборка на TD-13 не влияет.

Первая публикация по обновлённому прогону (с демо-шагом внутри `Tests`) прошла успешно —
живое наблюдение, не только сверка длительностей: прогон публикации на `504e197…` (после
merge #94) и на `55346de2144726501c3753e2e952f501f13319f8` (после merge #99) оба
`conclusion=success` (`gh run list --workflow=deploy.yml`, databaseId 31579602046 и
31580416601 соответственно).

---

## TD-14 — Прототипы каркаса не защищены в CI

**Файлы:** `web/tests/seo-package.test.ts` (`describe.skipIf(!isDemoBuildForPrototypes)`),
`web/tests/a11y.spec.ts`, `.github/workflows/`
**Метки в коде:** нет

**Проблема:**
Десять проверок architecture-прототипов (`/preview/{editorial,faculty,modular}` и
страницы семинара/расписания) обёрнуты в `skipIf`, потому что CI собирает production
без `DEMO_FORMS` — каталога `dist/preview` нет. Architecture-маршруты также не входят
в Playwright a11y-прогон. Все GitHub checks зелёные, даже если вся функциональность
прототипов сломана. Это не вакуумный early-return (тот уже убран), но и не защита.

**Решение (на выбор):**
1. Job с `npm run build:demo` + `vitest --config vitest.build.config.ts` (и при желании
   axe по `/preview/*`).
2. Пока job нет — этот долг остаётся явным; молчаливый skip в CI не считать достаточным.

**Когда актуально:** до мержа любых правок `/preview/*`, либо сразу после решения
владельца по job. Найдено ревью PR #36.



## TD-15. Юнит-прогон показал флак после добавления WASM-парсера в тесты

**Симптом.** На ветке `fix/json-ld-dist-gate` полный `npm test` один раз упал на
`repo-hygiene.test.ts` («генератор редиректов падает на конфликте в карте адресов»),
а изолированный прогон того же файла дал 9 passed, и повторный полный прогон — 141
passed. Воспроизвести падение больше не удалось, точный текст ассерта не сохранён.

**Классификация:** флаки-тест, а не дефект генератора. Изолированно зелёный, при
повторе зелёный.

**Правдоподобная причина.** Проверка проводки JSON-LD стала разбирать компоненты
парсером Astro (`@astrojs/compiler`, WASM), то есть юнит-набор потяжелел. Тест про
генератор редиректов запускает отдельный процесс (`npx tsx scripts/gen-redirects.ts`),
и такие тесты первыми страдают от конкуренции за ресурсы. Связь именно правдоподобная:
доказательства нет, поскольку падение не воспроизвелось.

**Что уже сделано.** Разбор ограничен файлами, содержащими `<script` (87 файлов `.astro`
против 14 с тегами), — нагрузка снизилась, гейт при этом не ослаблен: файл без такой
подстроки не может содержать элемент script вовсе. Фильтр по `ld+json` был бы неверен:
тип бывает задан выражением, и такой фильтр вернул бы обход, который этот гейт закрывает.

**Решение:** при повторении — сохранить текст ассерта и вывод прогона, затем либо
изолировать процессные тесты (`fileParallelism`/отдельный проект vitest), либо заменить
запуск `npx tsx` на прямой вызов функции.

**Когда актуально:** при следующем таком падении. Отмечено, потому что молчаливый
перезапуск скрывает повторяющуюся цену.

---

## TD-16 — Восемь файлов браузерных проверок не исполняются гейтом публикации

**Файлы:** `web/tests/{compare,compat,course-group-parity,institute-parity,parity-audit,schedule-parity,seminar-parity,visual-baseline}.spec.ts`,
`.github/workflows/test.yml`, `.github/workflows/nightly.yml`, `web/playwright.config.ts`
**Метки в коде:** нет. Список дублируется как ожидаемая константа `ACKNOWLEDGED_DEBT` в
`web/tests/browser-test-gating.test.ts` и в задаче `2.19`
`openspec/changes/schedule-month-filter/tasks.md` (мета-гейт).

Обновление на 2026-08-12: гейт-тест **опубликован** — он лежит в ветке реализации фильтра
месяца `change/schedule-month-filter-impl@0a6ccdc379bdb693bb9f9d07ffde118da723c73a`
(PR #98), `web/tests/browser-test-gating.test.ts:40-49`, и его константа перечисляет
ровно те же восемь файлов, что и эта запись. Прежняя редакция этого абзаца говорила, что
теста нет ни на `main`, ни на запушенных ветках, и это было верно на момент написания:
по правилу проекта «тесты по спеке пишутся раньше кода» PR с одними красными тестами в
`main` не сливается, поэтому ветка тестов до выхода реализации не публиковалась. Теперь
сверка по origin возможна, и расхождение между этим списком и константой стало
наблюдаемым — но пока только на ветке: в `main` (`17836810fe59f4947b544ee3942c24056ef25ff4`)
гейт-теста ещё нет, там есть лишь спека, требующая его.

**Проблема:**
Гейт публикации — ровно один workflow, `Tests`: `.github/workflows/deploy.yml:11-14`
подписывается на его `workflow_run` (`branches: [main]`). Транзитивно от `Tests`
исполняются три файла браузерных проверок Playwright (job `e2e-smoke` в
`.github/workflows/test.yml`): `site.spec.ts` (`test:e2e:smoke`), `a11y.spec.ts`
(`test:e2e:a11y`), `article-catalog.spec.ts` (`test:e2e:catalog`, подключён отдельным
шагом в PR #88, коммит `490b10f`).

Остальные восемь файлов `web/tests/*.spec.ts` этим гейтом не исполняются:

- `compare.spec.ts` — в `web/package.json` есть только `test:compare:attached`
  (требует уже поднятого сервера через `playwright.attached.config.ts`); ни один
  workflow его не вызывает;
- `compat.spec.ts` — исполняется, но `.github/workflows/nightly.yml`
  (`test:e2e:compat`), который триггерится `cron`/`workflow_dispatch`, а не событием
  `Tests`, и в гейт публикации не входит;
- `course-group-parity.spec.ts`, `institute-parity.spec.ts`, `parity-audit.spec.ts`,
  `schedule-parity.spec.ts`, `seminar-parity.spec.ts` — в `web/package.json` для них
  вообще нет npm-скрипта; запустить можно только прямым вызовом
  `npx playwright test tests/<file>.spec.ts`;
- `visual-baseline.spec.ts` — есть `test:e2e:visual` и `test:e2e:visual:update`, ни
  один workflow их не вызывает.

Причина, по которой это легко пропустить: `web/playwright.config.ts:9` собирает
`**/*.spec.ts` только при запуске БЕЗ явного списка файлов — так сделано, чтобы
Playwright не пытался собрать заодно файлы vitest (`*.test.ts`) и не падал до старта
проверок. Все npm-скрипты вызывают playwright с конкретным именем файла, поэтому новый
`*.spec.ts` может лежать в репозитории, компилироваться, проходить локально и выглядеть
частью гейта — и никогда не запуститься ни в одном workflow. Ровно так уже было с
`article-catalog.spec.ts` до PR #88: коммит `490b10f` прямо называет находку — файл «не
запускался НИЧЕМ, ни скриптом `web/`, ни шагом workflow».

**Почему это опасно:** каждый из восьми файлов физически неотличим от исполняемого — тот
же каталог, то же расширение, тот же паттерн `describe`/`test`. Ревьюер, оценивающий
покрытие по составу `web/tests/`, увидит одиннадцать файлов и решит, что гейт публикации
существенно шире, чем есть на самом деле. Долг уже был кодом (отсутствие шага в workflow
+ поведение `playwright.config.ts`), но не был виден нигде, кроме одновременного чтения
обоих файлов, — то есть только тому, кто уже подозревает проблему.

**Измерено на `a35b4051a7f707c2c89f373f83e61e4799a3f248` (2026-08-11):**
`web/tests/*.spec.ts` — 11 файлов; исполняются гейтом публикации — 3 (`site`, `a11y`,
`article-catalog`); исполняется, но вне гейта публикации — 1 (`compat`, только через
`nightly.yml`); не исполняются никаким workflow вовсе — 7 (`compare`,
`course-group-parity`, `institute-parity`, `parity-audit`, `schedule-parity`,
`seminar-parity`, `visual-baseline`). Итого восемь файлов вне гейта публикации, считая
`compat.spec.ts` сиротой по критерию «нужен для выкладки» — так же посчитано в задаче
`2.19`.

**Решение:** для каждого файла — либо подключить шагом в `test.yml` (как сделано для
`article-catalog.spec.ts` в PR #88), либо осознанно оставить вне гейта публикации с
названной причиной (как `compat.spec.ts` — дорогой кросс-браузерный прогон, сейчас в
`nightly.yml`).

**Связь с мета-гейтом:** список этих восьми файлов зафиксирован как ожидаемая константа в
задаче `2.19` change `schedule-month-filter` — и сама спека change **уже в `main`**
(`17836810fe59f4947b544ee3942c24056ef25ff4`, PR #95), то есть требование к мета-гейту
больше не живёт в непубликованной ветке. Мета-гейт состоит из трёх частей: список файлов
вне гейта совпадает с фактическим состоянием (не отстал и не забежал вперёд); список
живой — имя отсутствующего файла и имя файла, ставшего исполняемым, роняют проверку;
признаки фильтра месяца не встречаются в файлах из списка долга и встречаются хотя бы в
одном исполняемом файле.

**Важно для читателя этой записи:** третья часть в `main` описана уже во **второй**
редакции. Первая сверяла ТЕКСТ файлов, и это было отменено как дефект — `text.includes`
удовлетворяется строкой в комментарии, то есть сценарии можно удалить, а гейт останется
зелёным. Действующая редакция требует сверки с набором тестов, **собранным той же
командой, которой их запускает гейт публикации** (`playwright test … --list
--reporter=json`), с устойчивой меткой в имени теста, симметрией «объявлено ↔ собрано» и
непризнанием `skip`/`fixme`. Реализация в PR #98 на
`0a6ccdc379bdb693bb9f9d07ffde118da723c73a` пока соответствует **отменённой** редакции —
это открытая находка по тому PR, а не состояние, которое эта запись фиксирует как норму.

Оба списка — здесь и в константе теста — обязаны совпадать, иначе разойдутся молча; это
прямо требование части «(б) по живости списка». С публикацией ветки реализации сверка
стала возможна по origin, но автоматической она станет только когда гейт-тест окажется в
`main`: до тех пор расхождение ловится чтением, а не прогоном.

**Когда актуально:** до реализации задачи `2.19`; после её реализации всякое
расхождение между этим списком и константой в коде должно ловиться автоматически, а не
вручную при следующем чтении `docs/tech-debt.md`.

---

## TD-17. Проверки прототипов зависят от горизонта снапшота данных

**Дата записи:** 2026-08-11. **Файлы:** `web/tests/demo-prototypes.test.ts`,
`web/src/components/home/sections/{EventLine,EventTeacher,UpcomingModular}.astro`,
`web/src/pages/preview/[variant]/seminar.astro`.

**Проблема.** Блоки анонсов на прототипах рендерятся условно: `{next && …}`, где `next` —
из `getUpcomingSeminars(limit, now = new Date())`, то есть от даты СБОРКИ. Данные у нас
снапшот, и его горизонт конечен: последняя активная дата — **2027-09-17** (замерено
2026-08-11 по `discovery/entities/schedule_entries.json`; активных событий 63, текущих или
будущих 62, из них с преподавателем 62).

После этой даты, при полностью исправном коде, страницы прототипов перестанут содержать
`data-event-line`, `data-event-teacher` и `data-upcoming="modular"`, а шапка
`/preview/<id>/seminar` уйдёт в ветку «даты набора уточняются». Четвёртое место того же
класса — обязательный блок «ближайшие семинары» в проверке состава прототипа: его тоже
нечем наполнить без будущих событий. Все четыре найдены не рассуждением, а мутацией:
сдвиг снапшота на пять лет назад ронял 7 проверок из 13 до починки и 3 из 13 после
починки только блоков анонсов.

**Почему это долг, а не дефект.** С 2026-08-11 (PR #94) проверки прототипов входят в
обязательный прогон `Tests`, то есть в гейт публикации. Требование «маркер есть» остановило
бы выкладку боевого сайта по календарю, а сообщение соврало бы про причину. Поэтому
ожидание теперь **выводится из тех же данных той же чистой функцией**: есть будущие
события — маркер обязан быть, нет — обязан отсутствовать. Проверяются оба исхода, так что
сломанный прототип по-прежнему ловится.

**Что осталось незакрытым.** Сборка вызывает `getUpcomingSeminars()` в момент сборки, а
проверка — в момент прогона. Разойтись они могут на событии, истекающем между сборкой и
проверкой; в CI это секунды, но окно есть. Полностью снять его можно, только зафиксировав
дату сборки явно (например, `BUILD_DATE` в окружении, читаемая и сборкой, и проверкой) —
это отдельная работа, затрагивающая не только прототипы.

**Настоящее лечение.** Обновить снапшот расписания до реальных данных: демо-стенд с
событиями из прошлого бесполезен для показа независимо от гейтов.

## TD-18. Ложное красное от таймаута vitest в обязательном прогоне

**Дата записи:** 2026-08-11. **Файл:** `web/tests/repo-hygiene.test.ts`, проверка
«генератор редиректов падает на конфликте в карте адресов».

**Симптом.** В составе полного `npm test` проверка упала по дефолтному таймауту vitest
(5000 мс), показав **8071 мс**. Следующий прогон той же командой — **1433 мс**, зелёный.
Изолированный прогон файла — **1294…1394 мс** (три замера).

**Классификация: ложное КРАСНОЕ, не дефект генератора.** Код не менялся между падением и
успехом; менялось состояние кэша и загрузка машины. Цена выше обычной: файл лежит в
обязательном прогоне `Tests`, то есть такое красное останавливает выкладку боевого сайта.

**Что охлаждается — измерено, а не предположено.** Кэш tsx тут не при чём: его удаление
(`rm -rf $TMPDIR/tsx-502`) не изменило времени вовсе — 1294…1335 мс, как на прогретом.
Разница приходит из холодного кэша модулей и параллельного прогона 16 файлов: в упавшем
прогоне `transform 1.32 с`, `import 2.88 с` против `39 мс` и `65 мс` в изолированном.
Способ воспроизведения: полный `npm test` первым прогоном после сборок, когда кэш ФС занят
другим; повтор той же команды уже зелёный.

**Что сделано.** Проверке задан явный `{ timeout: 30_000 }` — примерно 3,7 запаса к худшему
наблюдённому. Большой таймаут здесь бесплатен: длительность теста — это длительность
подпроцесса `npx tsx scripts/gen-redirects.ts`, на успехе тест возвращается сразу, а
стерегомый дефект (генератор вышел с нулём на конфликте) определяется кодом возврата, а не
временем.

**Что осталось.** В том же файле есть вторая проверка с подпроцессами — «деплой отказывается
работать без явного режима форм», три запуска `bash deploy-web.sh`, **411 мс** изолированно.
При наблюдённом множителе 5,8 это дало бы ~2,4 с, то есть запас к дефолтным 5000 мс пока
есть, и падений на ней не наблюдалось. Явный таймаут ей не задан сознательно: подгонять
число под ненаблюдённый случай значило бы выдавать оценку за измерение. Если она когда-нибудь
покраснеет по таймауту — числа для решения уже здесь.

**Общий вывод, повторный для проекта.** Дефолтный таймаут — не запас, а ловушка, и запас,
зависящий от того, кто прогрел кэш, запасом не является. Тот же класс уже встречался на
проверке, обходящей весь `dist` (215 мс на прогретом кэше против 9,3 с на холодном).

**Третий случай, 2026-08-12 — тот же класс, но обнаружен CI, а не подозрением.**
`web/tests/browser-test-gating.test.ts`, проверка «под каждую объявленную метку в собранном
наборе есть выполняемый тест». В CI она упала по дефолтным 5000 мс на голове
`da91dc9b61565ad03a6ffa896a17affd892427d3`, тогда как на предыдущей голове
`c4198e9` тот же джоб был зелёным — то есть предмет появился вместе со сбором набора
подпроцессами `playwright --list`. Изолированно файл проходит за **3,55 с** (6 проверок),
и это ровно тот обманчивый запас, о котором абзац выше: в обязательном прогоне он
соревнуется за машину с семнадцатью другими файлами. Лечение то же — явный таймаут; число
взято **не** от наблюдённого времени, а от бюджета самого подпроцесса
(`COLLECT_SPAWN_TIMEOUT_MS * 2 + 30 с`), чтобы при зависшем `playwright` первым срабатывал
таймаут подпроцесса: он говорит, ЧТО зависло, а таймаут теста сказал бы только «не
уложилась».

Отдельно проверено, что лечение не превратило проверку в декоративную: у vitest форма
`it(name, { timeout }, fn)` могла бы при ошибке в сигнатуре принять объект за тело. Мутация
— добавить в `EXPECTED_MONTH_TAGS` несуществующую метку `month-negcheck-fake`
(объявленных меток 18 → 19): проверка покраснела по имени, «@month-negcheck-fake — теста
нет в собранном наборе», 6 passed → **5 passed, 1 failed**. Тело исполняется, таймаут
применён.



## TD-19. Визуальные эталоны разошлись со страницей расписания, и одно расхождение необъяснено

**Дата записи:** 2026-08-12. **Файлы:**
`web/tests/visual-baseline.spec.ts-snapshots/schedule-{desktop-desktop,mobile-mobile}-darwin.png`,
`web/tests/visual-baseline.spec.ts`, `web/package.json` (`test:e2e:visual`).
**Метки в коде:** нет. Предписание не перегенерировать эталоны живёт в задаче 5.2
`openspec/changes/schedule-month-filter/tasks.md`; после архива change оно уедет в
`openspec/specs/`, поэтому запись здесь — единственный след, который останется в списке долгов.

**Проблема.** Два эталона страницы расписания больше не совпадают с собранной страницей, и
внутри расхождения **два разных изменения, которые нельзя сливать в «облик изменился»**:

1. **Ожидаемое** — контрол фильтра по месяцу делает панель фильтров двухрядной на 1280, из-за
   чего всё ниже панели сдвигается вниз: **+78 px** по отчёту автора реализации (замеры
   мокапов давали близкую оценку — панель 145 px против 65 px). Снимок полностраничный
   (`fullPage: true`) при пороге `maxDiffPixelRatio: 0.001`, поэтому сдвиг заведомо превышает
   порог. Это следствие утверждённой работы, а не дефект.
2. **Постороннее** — расхождение **в области переключателя темы**: строки 21–50, x 1092–1143
   по отчёту автора реализации (PR #98). **Его причина НЕ установлена.** Ходовое объяснение
   «это переключатель темы, изменённый чужой работой» причиной записать нельзя — оно
   опровергается собственными данными (см. ниже); но и «расхождения нет» утверждать нельзя,
   оно наблюдалось.

**Почему расхождение не поймали раньше.** `visual-baseline.spec.ts` не запускает ни один
workflow: его вызывают только npm-скрипты `test:e2e:visual` и `test:e2e:visual:update`, а
`grep -rn 'e2e:visual\|visual-baseline' .github/` не находит ни одного вхождения. Снимки к
тому же платформенные (`-darwin`, снимаются на машине разработчика). То есть эти PNG **гейтом
не являются**, и их устаревание ничего не останавливает и никому не видно. Неисполняемость
самого файла — предмет **TD-16**; здесь она названа как причина, по которой дрейф остался
незамеченным, но предмет этой записи другой: **содержимое** эталонов разошлось со страницей.

**Что установлено измерением (проверяемо кем угодно).**

- все 12 эталонов последний раз записаны **одним** коммитом `981477e` (2026-08-06):
  `git log -1 --format=%h -- web/tests/visual-baseline.spec.ts-snapshots/<файл>` для каждого;
- после `981477e` область шапки, темы и корневых стилей менялась дважды:
  `f2e621b` (PR #35, контраст дорожки тумблера) и `80a10fe` (PR #32, корневой `font-size`);
  эталоны с тех пор не перегенерировались ни разу;
- эталоны расписания в ветке реализации фильтра месяца совпадают с `main` — посторонних PNG
  в PR #98 нет.

**Почему «виновата чужая правка шапки» не проходит.** Два факта против:

- **исходник шапки и все снимки синхронны на одном коммите.**
  `web/src/components/topnav/HeaderTools.astro` последний раз менялся в том же `981477e`, что
  записал все 12 эталонов, — то есть на нём снимок с шапкой совпадал. Версия «шапка ушла
  вперёд, а снимок остался» на этих данных не работает;
- **шапка есть на всех шести страницах набора**, поэтому её изменение сдвинуло бы **все**
  эталоны. Между тем остальные пять проходят (измерено автором PR #98).

**Уточнение, без которого предыдущий пункт неполон:** разметка шапки после `981477e` не
менялась, а **оформление тумблера менялось** — `f2e621b` (PR #35) правил его цвета в
`web/src/styles/base.css`, и только для **тёмной** темы (его собственный комментарий: «касалась
ТОЛЬКО тёмной темы»). Само по себе это в светлый снимок попасть не может, но вместе со
следующим пунктом перестаёт быть безразличным.

**Отдельная хрупкость обвязки, найденная по ходу: снятие эталона НЕ закрепляет тему.** Причиной
наблюдённого расхождения она, по результатам эксперимента ниже, **не является** — но остаётся
реальным риском на будущее, поэтому записана. Тема — не свойство разметки, а **состояние**:

- `web/src/components/topnav/HeaderTools.astro:228` —
  `applyTheme(resolveTheme(readStoredTheme() || document.documentElement.dataset.theme || 'light'))`,
  где `readStoredTheme()` читает `localStorage` (`ikpk.theme`); `HeadMeta.astro:34` ставит тему из
  того же ключа ещё до отрисовки;
- в `web/playwright.config.ts` **`colorScheme` не задан**, а `visual-baseline.spec.ts` тему не
  пинует, `localStorage` не чистит и `addInitScript` не использует — по `theme`, `colorScheme`,
  `localStorage`, `addInitScript` в нём совпадений нет;
- сверх того, **состояние самого тумблера зависит от гонки**: разметка приезжает с
  `aria-checked="false"` (`HeaderTools.astro:23`), а проставляет его скрипт (`:222`). Комментарий
  в `base.css:193-197` документирует этот момент прямо — «между применением темы и его
  выполнением существует момент „тёмная тема, тумблер выключен“. **В CI он ловится устойчиво,
  локально — нет**».

Следствие: облик тумблера в снимке — **не чистая функция кода страницы**, он зависит и от
состояния браузера. Измерением ниже показано, что в текущей обвязке это состояние
детерминированно (свежий контекст ⇒ светлая тема), поэтому наблюдённое расхождение объясняется не
им; но гарантии на это нет никакой — она держится на том, что штатная фикстура даёт свежий
контекст, а не на явном закреплении.

**Проведённый эксперимент, его метод и числа.** Гипотезу «расхождение даёт промежуток „тёмная
тема, тумблер выключен“ при незакреплённой теме» проверили измерением 2026-08-12. Метод: страница
расписания снималась **по 10 раз** в трёх сериях в одном окружении, и сравнивался **разброс внутри
серии**, а не совпадение с закоммиченными эталонами — те платформенные и снимались в другом
окружении, поэтому расхождение с ними о теме не говорит ничего. Серии: **A** — как сейчас
(свежий контекст, `colorScheme` не задан); **B** — закреплённая светлая (`colorScheme: 'light'`
плюс `localStorage['ikpk.theme'] = 'light'` через `addInitScript`); **C** — закреплённая тёмная.
Хешировались полная страница и вырез области тумблера. Условия: локальная сборка `main`,
раздача статикой, `chromium` из `playwright-core` 1.61.1, macOS.

Результат:

| серия | различных снимков страницы | различных снимков тумблера | состояние |
|---|---|---|---|
| A: без закрепления | 1 из 10 | 1 из 10 | `theme=light`, `aria-checked=false` |
| B: закреплена светлая | 1 из 10 | 1 из 10 | `theme=light`, `aria-checked=false` |
| C: закреплена тёмная | 1 из 10 | 1 из 10 | `theme=dark`, `aria-checked=true` |

**Разброса внутри серий нет ни в одной — то есть недетерминированность локально не
воспроизвелась.** Это **не** опровержение гипотезы: `base.css:193-197` прямо пишет, что промежуток
«в CI ловится устойчиво, локально — нет». Ответ здесь — «не смогла проверить», а не «причина не в
этом»; сильный ответ дал бы прогон серии там, где гонка ловится.

**Зато измерено другое, и это меняет список кандидатов.** Сравнение **между** сериями: A и B дают
**побайтово одинаковый** снимок (и страницы, и тумблера), а C отличается от них **и по странице, и
по вырезу тумблера**. Отсюда два следствия:

- **область тумблера действительно чувствительна к теме** — если прогон попадает в тёмную,
  расхождение видно ровно там, где автор реализации его и наблюдал (строки 21–50, x 1092–1143);
- **но закрепление светлой темы ничего не меняет для свежего контекста:** A == B побайтово. У
  свежего контекста `localStorage` пуст, поэтому тема детерминированно светлая, а
  `visual-baseline.spec.ts` работает штатной фикстурой `page`, то есть **каждый тест получает
  свежий контекст**. Значит объяснение «эталон снимался в другой теме» само требует механизма, по
  которому ключ `ikpk.theme` в прогоне оказался бы непустым, — а в этой обвязке такого механизма
  нет.

**Кандидаты после эксперимента (ни один не подтверждён), в порядке убывания правдоподобия:**

1. **среда снятия** — версия браузера, шрифты, ОС. Эксперимент её не трогал, а закоммиченные
   эталоны снимались на другой машине и другой сборке браузера, чем любой сегодняшний прогон;
2. **гонка «тема применена — `aria-checked` не выставлен»** (`base.css:193-197`) — локально не
   воспроизводится по описанию, поэтому и не воспроизвелась; проверяема только там, где ловится;
3. **состояние темы при снятии** — **ослаблено** измерением: свежий контекст даёт светлую тему
   детерминированно (A == B, 10 прогонов, нулевой разброс). Остаётся возможной, только если тот
   конкретный прогон шёл не в свежем контексте.

Это ровно то же различие, что мы требуем от гейтов: «дефектов нет» и «я не смогла проверить» —
разные ответы. Здесь верен второй, и теперь он подкреплён числами, а не рассуждением.

**Решение.** Перед перегенерацией эталонов расписания **сначала определить, что именно
составляет постороннюю часть расхождения** — вплоть до вывода «её нет, наблюдение объяснялось
средой снятия». Проверку темы повторять не нужно, она проведена (см. эксперимент выше);
следующий шаг по правдоподобию — сверить среду снятия (версия браузера и шрифты в момент
записи эталонов против текущей) либо прогнать серию там, где ловится гонка. Только после этого
обновлять PNG, и обновлять осознанно, с осмотром diff.
Порядок именно такой: перегенерация до объяснения превращает неразобранное изменение облика в
утверждённый эталон, то есть в «доказательство», что так и задумано.

**Что из этого следует практически — с поправкой по результатам эксперимента.** Первая редакция
этой записи утверждала, что из-за незакреплённой темы эталон воспроизводим только на машине
автора. Измерение показало, что **тема — не та причина**: свежий контекст даёт светлую
детерминированно (A == B побайтово). Невоспроизводимость остаётся, но её носитель другой —
**окружение снятия**, и об этом говорит сам суффикс `-darwin` в именах файлов.

Закрепление темы и цветовой схемы при снятии всё равно нужно, но как **страховка, а не как
исправление наблюдённого расхождения**: оно убирает зависимость от состояния, которое сегодня
случайно пусто, но ничем не гарантировано, — а серия C показывает, что при попадании в тёмную тему
расхождение появляется именно в области тумблера. Это требование к **процессу снятия**, а не к этим
PNG: «снятие эталона фиксирует тему и цветовую схему явно» — законный вход для change
`visual-regression-gate`, который проектирует снятие эталонов в CI. Передаётся туда именно
требование, а не само расхождение.

Отдельно, чтобы два соседних утверждения не читались как противоречие: **сам долг change
`visual-regression-gate` не покрывает**, а требование к процессу снятия (предыдущий абзац) —
покрывает и должно уйти туда. Разница в предмете: расхождение этих двенадцати PNG остаётся здесь,
потому что он прямо исключает этот набор из своей области
(`openspec/changes/visual-regression-gate/proposal.md:41-43`: «существующий
`visual-baseline.spec.ts` **не меняется и в CI не переносится**: у него другая задача —
сравнение облика при редизайне, — и другие эталоны (12 файлов `*-darwin.png`, 12 МБ).
Смешивать инструмент редизайна и гейт нельзя»). Он проектирует **новый** визуальный гейт со
своим набором. Описание PR #98 предлагает обратное («желательно уже механизмом
`visual-regression-gate`») — это предложение противоречит области того change, и настоящая
запись его отменяет.

**Когда актуально:** до любой перегенерации эталонов расписания, и до того, как кто-нибудь
сошлётся на эти PNG как на подтверждение облика. Найдено при `/opsx:update` задачи 5.2
change `schedule-month-filter` (PR #102).

---

## TD-20. После 2026-10-08 заплатки расписания выходят из окна актуальности

**Дата записи:** 2026-08-12. **Файлы:** `web/src/lib/schedule-supplements.ts` (даты),
`web/tests/schedule-month.spec.ts` (проверки `@month-supplement`,
`@month-supplement-synthetic`), `discovery/entities/schedule_entries.json` (снапшот
расписания). Каталога `web/src/data/`, значившегося здесь до правки, в репозитории нет вовсе —
по такой записи лечение было не найти.

**Номер.** Взят `TD-20`, а не `TD-19`: девятнадцатую занимает PR #102. На момент выбора
номера тот PR ещё не был слит, и сверка по файлу давала старшим номером `TD-18`; после
rebase на слитый #102 запись `TD-19` пришла из базы и лежит выше в этом же файле. Прежняя
редакция абзаца утверждала «в нём старший номер `TD-18`» и осталась несверенной после
rebase — на голове ветки это уже ложно, поэтому исправлено ревью, а не оставлено как след
хронологии. Коллизии номеров нет: `TD-19` в базе и `TD-19` из #102 — одна и та же запись.

**Что произойдёт и когда.** Обе записи-заплатки однодневные: `2026-10-01` и `2026-10-08`
(`startAt` = `endAt`). Окно актуальности держит запись до её последнего дня включительно
(`isCurrentOrFuture`), поэтому **начиная с 2026-10-09** обе исчезают со страницы расписания
навсегда. Дата точная, а не оценочная: она вычисляется из данных, а не из наблюдения.

**Почему это долг, а не дефект.** Заплатки закрывают пробел в снапшоте данных — записи,
которых в выгрузке нет. Их выход из окна законен: событие прошло. Но вместе с ними исчезает и
предмет проверки «заплатка достижима выбором своего месяца», а проверка лежит **внутри
`test:e2e:schedule`**, то есть внутри единственного workflow, который держит публикацию.

**Что сделано, чтобы гейт публикации не встал.** Проверка получила две ветки, и ветка
выбирается **по странице**, а не по дате в тексте теста:

- заплатка на странице есть → прежняя проверка целиком (достижима выбором своего месяца,
  рядом с ней есть обычные записи);
- заплатки нет → утверждается обратное наблюдаемое, и оно **не пустое**: ни одна заплатка не
  должна оставаться в окне на текущую дату. Если хотя бы одна в окне, а на странице её нет —
  значит запись потеряли, и ветка краснеет.

Обе ветки пройдены, и вторая проверена на невакуумность. Три состояния воспроизводимо, с
командой мутации и двумя числами; прогон каждый раз — весь файл целиком
(`playwright test tests/schedule-month.spec.ts --project=desktop`), и судится он по **имени**
покрасневшего теста, а не по цвету. Оговорка про прогон, чтобы команда была
воспроизводимой дословно: порт 4322 занимал посторонний фоновый preview осиротевшего
worktree, поэтому прогоны шли с временной конфигурацией, отличавшейся от штатной **только**
номером порта (4323); на свободном 4322 та же команда работает как есть. Опорный SHA
восстановления —
`change/schedule-month-filter-impl@6504eb615d28ad2d058bb5a43769e7ac01e0ef42`; после каждой
мутации `git status --porcelain` пуст и diff относительно него пуст:

| Состояние | Мутация (из `web/`) | Заплаток на странице | Результат |
|---|---|---|---|
| предмет есть | — | 2 из 2 (карточек 64) | ветка 1, зелено: 18 из 18 |
| предмет исчез законно | `perl -pi -e "s/2026-10-01T00:00:00\.000Z/2026-07-01T00:00:00.000Z/g; s/2026-10-08T00:00:00\.000Z/2026-07-08T00:00:00.000Z/g" src/lib/schedule-supplements.ts` + `npm run build` | 2 → **0** (карточек 64 → 62) | ветка 2, зелено: 18 из 18 |
| предмет потерян | даты настоящие, заплатки исключены со страницы: `perl -pi -e 's/\Q...scheduleSupplements.filter((entry) => !scheduleIndex.has\E/...scheduleSupplements.filter((entry) => false \&\& !scheduleIndex.has/' src/pages/raspisanie-i-tseny.astro` + `npm run build` | 2 → **0** (карточек 64 → 62) | **краснеет ровно `@month-supplement`** (17 passed, 1 failed): «на странице заплаток 0, а по данным и окну актуальности на 2026-08-12 обязано быть 2 — запись потеряна» |

Полнота третьей мутации проверена измерением, а не глазами: обе заплатки исчезли из
собранной страницы (`grep -c` по их заголовкам в `dist/raspisanie-i-tseny/index.html`: 1 и 1 →
0 и 0). Зелёная ветка при таком измерении означала бы декоративность ветки — она покраснела.
Ни одна из мутаций не трогала `web/tests/`: ветка выбирается данными, а не текстом проверки.

**Почему не `skip` и не «падать».** Падать нельзя: предмет исчезает от хода времени, а не от
дефекта, и ложное красное в гейте публикации дороже ложного зелёного. `skip` нельзя тоже, и
причина техническая: помеченный тест виден в собранном наборе, но выполняемым не считается,
поэтому мета-гейт `browser-test-gating.test.ts` покраснеет по метке `@month-supplement` — два
гейта поймали бы друг друга.

**Настоящее лечение — обновить снапшот расписания реальными данными, а не двигать даты
заплаток.** Сдвиг дат вперёд выглядит починкой, но он подделывает расписание: на сайте
появится событие, которого нет. Смягчение проверки — «не требовать заплатки вовсе» — лечением
тоже не является: оно снимает симптом и уносит с собой сам предмет. После 2026-10-08 устаревает
по существу не только проверка, но и сам снапшот — заплатки для него и написаны.

**Разрешает ветвление требование спеки, а не эта запись.** Право на ветку даёт требование
«Предмет, исчезнувший по календарю, отличается от предмета, потерянного дефектом»
(`openspec/changes/schedule-month-filter/specs/schedule-month-filter/spec.md`, внесено PR #106,
`main@1debf3609c4aa1efee846fa56784b1a85ba35534`); запись фиксирует **срок** — 2026-10-09 — и
способ лечения. Предъявлять её как разрешение нельзя: иначе контракт проверок менялся бы
дописыванием в список долгов.

## TD-21. С 2026-11-24 живых записей расписания станет не больше страницы, и проверка пагинации теряет предмет

**Дата записи:** 2026-08-12. **Файлы:** `web/tests/schedule-month.spec.ts` (проверка
`@month-pagination`, helper `openSchedule`), `discovery/entities/schedule_entries.json`
(снапшот расписания).

**Номер.** Взят `TD-21`: старший номер в этом файле — `TD-20`, и он же старший по всем
локальным и удалённым ветками репозитория (сверено перебором `refs/heads` и
`refs/remotes`, а не по памяти). `TD-19` в этой ветке уже есть — приехал ребейзом на
`main@8b4d13b`, поэтому оговорка записи `TD-20` про «девятнадцатую занимает PR #102»
относится к состоянию до ребейза.

**Что произойдёт и когда.** Проверка `@month-pagination` требовала, чтобы без фильтров на
странице было **больше 25** записей — иначе страница одна и скрывать нечего. Число живых
записей убывает монотонно от хода времени: окно актуальности (`isCurrentOrFuture`) убирает
запись на следующий день после её последнего дня. Числа вычислены из снапшота и заплаток, а
не оценены:

| Дата | Живых записей | Что происходит |
|---|---|---|
| 2026-08-12 | 64 | требование «больше 25» выполняется |
| 2026-11-23 | 26 | последний день, когда выполняется |
| **2026-11-24** | **25** | прежняя редакция проверки покраснела бы на исправном коде |
| 2027-09-18 | 0 | `openSchedule` требует хотя бы одну запись — краснеет весь файл |

Проверка лежит в `test:e2e:schedule`, то есть внутри **единственного** workflow, который
держит публикацию. Красное здесь останавливает выкладку исправных коммитов.

**Что сделано, чтобы гейт публикации не встал.** Тот же приём, что у `TD-20`: ветка
выбирается **по странице**, и обе ветки — настоящие проверки.

- записей больше страницы → прежняя проверка целиком (пагинация видна, месяц, влезающий в
  страницу, её скрывает, сброс возвращает);
- записей не больше страницы → утверждается обратное наблюдаемое, и оно **не пустое**:
  столько записей и должно быть по данным (если по снапшоту и окну живых больше страницы, а
  на странице меньше — записи потеряны, и ветка краснеет), а пагинации при одной странице не
  должно быть ни до выбора месяца, ни после него, ни после сброса.

Три состояния пройдены, обратная ветка проверена на невакуумность. Записаны команда мутации и
два числа; прогон каждый раз — весь файл (`playwright test tests/schedule-month.spec.ts
--project=desktop`), судится по **имени** покрасневшего теста. Та же оговорка, что в `TD-20`:
прогоны шли с временной конфигурацией, отличавшейся от штатной только номером порта (4323
вместо занятого 4322). Опорный SHA восстановления —
`change/schedule-month-filter-impl@6504eb615d28ad2d058bb5a43769e7ac01e0ef42`; после каждой
мутации `git status --porcelain` пуст и diff относительно него пуст:

| Состояние | Мутация (из `web/`) | Карточек на странице | Результат |
|---|---|---|---|
| предмет есть | — | 64 (живых по данным тоже 64) | ветка 1, зелено: 18 из 18 |
| предмет исчез законно | опорная дата сдвинута: `perl -pi -e "s/\Qexport const calendarToday = (): string => new Date().toISOString().slice(0, 10);\E/export const calendarToday = (): string => '2026-12-01';/" src/lib/schedule-window.ts` + `npm run build` | 64 → **24** | ветка 2, зелено: 18 из 18 |
| предмет потерян | дата настоящая, страница обрезана: `perl -pi -e 's/\Q.sort((a, b) => new Date(a.startAt).getTime() - new Date(b.startAt).getTime());\E/.sort((a, b) => new Date(a.startAt).getTime() - new Date(b.startAt).getTime()).slice(0, 20);/' src/pages/raspisanie-i-tseny.astro` + `npm run build` | 64 → **20** | **краснеет ровно `@month-pagination`** (17 passed, 1 failed): «на странице записей 20, не больше страницы, а по снапшоту и окну актуальности на 2026-08-12 живых 64 — записи потеряны» |

Полнота третьей мутации измерена, а не осмотрена: карточек в собранной странице
(`grep -o 'data-months="' dist/raspisanie-i-tseny/index.html | wc -l`) стало 20 против 64 — то
есть предмет действительно убран, и зелёное в этом состоянии означало бы декоративность ветки.
Считать по подстроке `data-schedule-item` нельзя: она встречается ещё и в селекторе внутри
скрипта страницы, и счёт завышается ровно на единицу (65 при 64 карточках) — та же ошибка, что
дала бы «мутация неполная» на полной мутации. Ни одна мутация не трогала `web/tests/`.

**Вторая дата: 2027-09-18, снапшот истекает целиком.** Последний день последнего события —
`2027-09-17`, поэтому с 2027-09-18 живых записей ноль, и падает не одна проверка, а сразу
несколько разных гейтов: `openSchedule` (браузерный файл целиком — требует хотя бы одной
записи), `scheduleItems()` и `monthOptions()` в `web/tests/schedule-month-dist.test.ts`
(пустая выборка карточек и пустой список месяцев там намеренно трактуются как провал). Той же
шкале подчиняются `offeredMonths` и подбор пары «месяц + город» в браузерном файле.

Ветвить их не стали, и это осознанно: ноль карточек на странице расписания — не «предмет
сценария исчез», а пустая страница расписания, то есть настоящий дефект продукта, и
молчаливое зелёное тут было бы хуже красного. Но дата известна и должна лежать здесь, а не в
чьей-то памяти: она отстоит на тринадцать месяцев от даты записи, то есть настоящее лечение
(обновление снапшота) наступит раньше — либо не наступит, и тогда встанет выкладка.

Требования при этом от снапшота не зависят: с 2026-08-12 «пагинация исчезает и возвращается»
закрыта синтетическим сценарием `@month-pagination-return`, а «заплатка достижима выбором
своего месяца» — `@month-supplement-synthetic`. Обе живые проверки остались характеризацией
текущего состояния.

**Расхождения со спекой больше нет, и разрешает ветвление она, а не эта запись.** Прежняя
редакция записи фиксировала расхождение: спека предписывала падать с «проверять было нечего»
всегда, а обе заплатки — `TD-20` и эта — вместо падения брали обратную ветку. Расхождение
закрыто `/opsx:update` (PR #106, `main@1debf3609c4aa1efee846fa56784b1a85ba35534`): в
`openspec/changes/schedule-month-filter/specs/schedule-month-filter/spec.md` появилось
требование «Предмет, исчезнувший по календарю, отличается от предмета, потерянного дефектом»,
и оно называет условия права на ветвление — вычислимость даты исчезновения, сверку отсутствия
со счётом по данным, независимую от календаря проверку требования, три негативных состояния и
обязательность вот этой самой долговой записи. Отсюда порядок предъявления: **ветвиться
разрешает требование спеки, а запись фиксирует срок и способ лечения.** Обратный порядок
означал бы, что контракт проверок меняют дописыванием в список долгов.

**Настоящее лечение — обновить снапшот расписания реальными данными.** Двигать даты записей
вперёд нельзя: это подделывает расписание. Правка самой проверки («требовать больше 10
записей вместо 25») лечит симптом и переносит ту же мину на более позднюю дату.

## TD-22 — Текст `bootstrap-vps.sh` и живой vhost на стенде расходятся после ручной правки

**Дата записи:** 2026-08-12. **Файлы:** `scripts/bootstrap-vps.sh` (heredoc `NGINX`),
живой `/etc/nginx/sites-available/ikpk.conf` на стенде `193.124.115.99`.
**Метки в коде:** нет. Ограничение названо требованием «Действие политики на живой
раздаче подтверждается свидетельством»
(`openspec/changes/serving-cache-headers/specs/static-serving/spec.md`; после архива —
`openspec/specs/static-serving/`).

**Проблема.** `scripts/bootstrap-vps.sh` пишет vhost только при первичной настройке —
если файл `/etc/nginx/sites-available/<site>.conf` уже существует, скрипт отказывается
его перезаписывать (код выхода 3), а обход `FORCE_VHOST=1` снёс бы правки certbot
(443-блок, редирект на https). Поэтому любое изменение политики раздачи на уже
развёрнутом сервере вносится **руками** по процедуре `docs/deploy-vps.md`, раздел
«Одноразовая правка на уже развёрнутом сервере», а не повторным запуском скрипта.

Отсюда дрейф: текст `scripts/bootstrap-vps.sh` в репозитории и файл на живом хосте
расходятся сразу после первой такой ручной правки, и **между выкладками это не проверяется
ничем**. Гейт `web/tests/serving-config.test.ts` читает только текст скрипта — содержимое
файла на сервере он не видит по построению. То есть после каждой ручной правки живого vhost
факт «политика соответствует спеке» доказан только на момент снятия свидетельства
(`curl -sS -D-` до/после, `openspec/changes/serving-cache-headers/tasks.md`, раздел 5) — не
постоянно.

**Два обоснования в прежней редакции этой записи были неверны, и после их исправления долг
сузился, а не исчез** (находки F4 и F8 ревью 5.5 PR #109; проверено на
`change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab`):

- было «выкладка конфигурацию nginx не переписывает вовсе» — **неверно**.
  `scripts/deploy-web.sh:114-121` каждым деплоем затирает
  `${WEB_ROOT}/shared/nginx-redirects.conf` содержимым `deploy/nginx-redirects.conf`. То
  есть часть конфигурации nginx **уже** является выкладываемым артефактом, и вариант 1 ниже
  — не greenfield, а распространение существующего механизма на второй файл. Не переписывает
  выкладка именно **vhost**, и предмет долга — он, а не «конфигурация nginx»;
- было «второй вариант требует SSH-доступа из CI, которого сейчас нет» — **неверно дважды**.
  Сверка живого конфига по SSH уже делается: `scripts/deploy-web.sh:195-210` тянет
  `nginx -T` с хоста по ssh и падает до переключения релиза, если активный vhost не
  подключает файл редиректов; утверждение вынесено функцией `redirects_include_active`, у
  которой есть собственные юнит-тесты (`web/tests/deploy-checks.test.ts`, включая случаи
  закомментированного `include` и упоминания в комментарии). Доступ нужен не из CI — деплой
  идёт с машины оператора, и там ключ есть.

**Что после этих исправлений остаётся долгом.** Не «нечем проверить», а «проверяется одно
свойство из многих, и только в момент деплоя»: утверждение сегодня одно — присутствует ли
`include` редиректов, — а директивы политики раздачи (`add_header` по шести классам,
`gzip_types`) не проверяются на живом хосте вовсе; между деплоями не проверяется ничего.

**Решение (не входит в объём `serving-cache-headers` — названо там ограничением, не
задачей):**
1. Перенести vhost в выкладываемый артефакт, по образцу уже работающего
   `shared/nginx-redirects.conf`: тогда `bootstrap-vps.sh` перестаёт быть единственным
   носителем. Цена — процедура первичной настройки и сосуществование с правками certbot,
   **или**
2. Расширить существующий preflight `deploy-web.sh` с одного утверждения до сверки текста:
   ожидаемый vhost порождается из heredoc'а той же подстановкой, которой его порождает
   bootstrap (готовый пайплайн — в `docs/deploy-vps.md`, раздел «Одноразовая правка»), и
   сравнивается с блоком `server` из `nginx -T`. Цена — расхождения, законно внесённые
   certbot, надо отличать от дрейфа, иначе гейт станет ложно-красным на каждом деплое.

Оба варианта — отдельная работа, но ни один не требует новой инфраструктуры: механизмы для
обоих в репозитории уже есть и применяются к соседнему файлу.

**Когда актуально:** при каждой ручной правке живого vhost (кеш-политика, будущие
security-заголовки и CSP из находки B4 `docs/security-audit-2026-08-08.md`, TLS/HSTS) —
до тех пор, пока конфигурация раздачи не станет выкладываемым артефактом или не появится
сверка. Найдено при реализации change `serving-cache-headers`.

---

## TD-23. Носитель требования про заплатку покраснеет от лечения TD-20

**Дата записи:** 2026-08-12. **Файлы:** `web/tests/schedule-month.spec.ts:857-903` (проверка
`@month-supplement-synthetic`), `web/src/lib/schedule-supplements.ts` (источник заплаток).

**Номер.** Взят `TD-23`, а не `TD-22`: двадцать вторую занимает `TD-22` на ветке
`change/serving-cache-headers-impl` (сверено перебором `refs/heads` и `refs/remotes` по
содержимому `docs/tech-debt.md`, а не по памяти; старший номер в этой ветке — `TD-21`).

**В чём латентный дефект.** Синтетический сценарий — единственный носитель требования
«заплатка достижима выбором своего месяца»: живая проверка `@month-supplement` после
2026-10-08 законно уходит в обратную ветку (`TD-20`). Но носитель начинается со строки
`expect(scheduleSupplements.length, 'заплаток в источнике нет — проверять было нечего')
.toBeGreaterThan(0)` (`:867`), а предписанное `TD-20` лечение — обновить снапшот реальными
данными, после чего заплатки **удаляются как ненужные**. То есть починка долга красит гейт
публикации: тест падает не потому, что поведение сломано, а потому, что предмета в источнике
больше нет по замыслу.

Второе, из того же места: чужой месяц выбирается из захардкоженной пары —
`const other = own === '2027-01' ? '2027-02' : '2027-01'` (`:741`). Обе заплатки сегодня
однодневные (`startAt` = `endAt`, 2026-10-01 и 2026-10-08), поэтому ключей месяца у каждой
ровно один, и `other` заведомо чужой. Многомесячная заплатка получила бы `other` из своих же
ключей — и вторая половина сценария («в чужом месяце заплатки нет») дала бы **ложное красное**
на исправном коде.

**Почему сейчас это латентно, а не дефект.** Обе заплатки существуют и обе однодневные:
`scheduleSupplements.length` равен 2, ключей месяца у каждой один. Ни одно из двух условий не
наступило.

**Правка, которую делать не сейчас.** Требование должно держаться на заплатке, которую
сценарий приносит **сам**, а не на содержимом `schedule-supplements.ts`: фикстура вида «запись
вне снапшота, склеенная тем же правилом» проверяет то же требование и не зависит ни от числа
заплаток, ни от их длительности. Чужой месяц при этом берётся как ключ, отсутствующий среди
ключей предмета, а не из пары констант. До тех пор действует условие: **лечение `TD-20`
обязано быть выполнено вместе с этой правкой, а не раньше неё** — иначе гейт публикации
покраснеет от закрытия долга.

## TD-24. Правило склейки расписания скопировано в проверку и может разойтись с продуктом

**Дата записи:** 2026-08-12. **Файлы:** `web/tests/schedule-month.spec.ts:117-129`
(`liveSnapshotEntries` и `liveEntryCount`), `web/src/pages/raspisanie-i-tseny.astro:59-63`
(склейка на странице).

**Номер.** См. оговорку про номер в `TD-23`: там же перебор ветвей.

**В чём латентный дефект.** `liveSnapshotEntries` считает, какие записи обязаны быть на
странице, и этим счётом ветка `@month-pagination` отличает «записей мало законно» от «записи
потеряли» (`TD-21`). Вывод о **времени** он берёт у продукта корректно — теми же
`isCurrentOrFuture` и `monthKeys`, что и страница. Но правило **склейки** он повторяет своими
словами: ключ дедупликации `slug:startAt`, отбрасывание заплатки с уже известным ключом и
условие `status === 'active'` записаны и в проверке (`:122-125`), и на странице (`:59-63`).
Копия у проверки и оригинал у продукта не связаны ничем, кроме внимания читателя.

**Потребителей копии стало двое.** `strictPairInData` (`:146`) считает по той же склейке,
есть ли строгая пара «месяц + город» — это счёт для ветки `@month-city` (`TD-25`). Общая
функция у них одна (`liveSnapshotEntries`), поэтому копия по-прежнему одна, но цена
расхождения выросла: ложное красное в гейте публикации теперь дают две ветки, а не одна.
Заодно `strictPairInData` группирует города по **имени**, тогда как страница — по слагу
(`slugify` живёт прямо в `raspisanie-i-tseny.astro` и не вынесен в модуль). Это второй
экземпляр той же болезни, и он закрыт не выносом, а сверкой: ветка `@month-city` сравнивает
число групп на странице с числом имён в данных и краснеет при расхождении. Выносить `slugify`
в `web/src/lib/` стоит тем же изменением, что и склейку.

Расхождение здесь даёт **ложное красное внутри гейта публикации**: если правило склейки на
странице изменится осмысленно (другой ключ дедупликации, другой набор статусов), проверка
посчитает по старому правилу, увидит на странице другое число и объявит «записи потеряны».
Обратное тоже возможно — совпавшее по величине расхождение промолчит.

**Почему сейчас это латентно.** Оба правила совпадают дословно, и комментарий в проверке
(`:110-113`) прямо называет склейку единственным местом возможного расхождения. То есть цена
уже названа, но не устранена.

**Правка, которую делать не сейчас:** вынести склейку чистой функцией в `web/src/lib/`
(рядом с `schedule-window.ts` и `schedule-supplements.ts` — «база плюс заплатки, дедуп по
`slug:startAt`, фильтр статуса»), и вызывать её и со страницы, и из проверки. Тогда копии не
станет вовсе, и вопрос о расхождении исчезнет вместе с ней. Почему не сделано здесь: страница
расписания — не предмет этой работы, а вынос затрагивает её сборку, то есть требует своих
прогонов и своего ревью. Проверять правку нужно негативно тем же способом, каким сегодня
проверена ветка `TD-21`: обрезать страницу и убедиться, что краснеет ровно
`@month-pagination`.

## TD-25. С 2027-05-24 на странице не остаётся строгой пары «месяц + город», и проверка пересечения теряет предмет

**Дата записи:** 2026-08-12. **Файлы:** `web/tests/schedule-month.spec.ts` (проверки
`@month-city` и `@month-city-synthetic`, helpers `liveSnapshotEntries`, `strictPairInData`,
`liveCityNames`), `discovery/entities/schedule_entries.json` (снапшот расписания),
`web/src/lib/schedule-supplements.ts` (заплатки).

**Номер.** Взят `TD-25`: перебраны `docs/tech-debt.md` на **всех** локальных и удалённых
ветках (`git show <ref>:docs/tech-debt.md` по всему `git branch -r` и `git branch`, а не по
памяти и не по одному `main`). Использованы номера 1–24 без пропусков: `TD-22` занимает
ветка `change/serving-cache-headers-impl`, `TD-20`, `TD-21`, `TD-23` и `TD-24` — эта ветка,
остальные — `main`. За эту итерацию ошибка с номером случалась дважды, поэтому перебор
записан, а не подразумевается.

**Что произойдёт и когда.** Проверка `@month-city` берёт пару «ключ месяца + город», у
которой пересечение **строго меньше** и множества месяца, и множества города. Строгость тут
не украшение: на вырожденной паре (`both === inMonth(key).length` и `both === byCity`)
ожидание совпадает с выдачей одного лишь месяца, и оба утверждения проверки зелены при
**полностью выключенном фильтре города** — ровно тот дефект, ради которого строгий отбор и
вводился. Число живых записей убывает от хода времени, и с некоторой даты строгой пары не
остаётся вовсе. Числа вычислены из данных, а не оценены:

| Дата | Живых записей | Месяцы и города | Строгая пара | Обратная ветка |
|---|---|---|---|---|
| 2026-08-12 | 64 | 11 месяцев, 6 городов | есть | не исполняется (идёт первая) |
| 2027-05-23 | 6 | 2027-05: СПб 2 + Москва 1; 2027-06: СПб 1; 2027-09: Москва 2 | последний день, когда есть | не исполняется |
| **2027-05-24** | **4** | 2027-05: СПб 1; 2027-06: СПб 1; 2027-09: Москва 2 | **нет: в каждом месяце ровно один город** | исполняется и **не пуста** |
| 2027-05-31 | 3 | 2027-06: СПб 1; 2027-09: Москва 2 | нет | не пуста |
| **2027-06-07** | **2** | 2027-09: Москва 2 | нет | **вакуумна: один месяц, один город** |
| 2027-09-18 | 0 | — | краснеет весь файл, см. `TD-21` | — |

**Две даты, а не одна.** Первая — 2027-05-24, когда исчезает предмет ПРЯМОЙ ветки
(строгая пара). Вторая — **2027-06-07**, когда исчезает предмет ОБРАТНОЙ: живых записей
остаётся 2, обе в месяце `2027-09` и обе в Москве, то есть один месяц и один город на всю
страницу. С этого дня обе половины обратной ветки зелены при **полностью выключенных**
фильтрах: выдача пары «единственный месяц + единственный город» совпадает со всей выдачей,
и выдача одного города — тоже. Окно полной вакуумности — **2027-06-07 … 2027-09-17**; с
2027-09-18 живых записей ноль и краснеет весь файл (`TD-21`), то есть молчания уже не
будет. Дат две по той же причине, по какой их две у `TD-21` (2026-11-24 и 2027-09-18):
запись, называющая только первую, утверждала бы про вторую ветку «оно не пустое» без срока
— а срок у этого утверждения есть.

**Как посчитано.** Перебором календарных дат: склейка снапшота (63 записи) с
`scheduleSupplements` (2 записи, обе новые по ключу — вместе 65) по ключу `slug:startAt`,
затем `status === 'active'` и `isCurrentOrFuture`, затем `monthKeys` — те же модули
`schedule-window` и `schedule-months`,
которыми о времени судит сборка, — и на каждую дату тот же отбор кандидатов и то же условие
строгости, что в проверке. Вакуумность обратной ветки считана тем же перебором и по её
собственному условию: `candidates[0]` (самый ранний месяц, город его первой записи), затем
«пересечение пары равно всей выдаче» И «записей этого города столько же, сколько всех» —
то есть ровно те два наблюдения, которые ветка утверждает. Города группированы по имени; на
сегодняшних данных группировка
совпадает с той, что на странице (6 имён → 6 различных слагов, записей без города 0), и
проверка на это совпадение не полагается, а сверяет его (см. ниже). Третья дата,
2027-09-18, — не предмет этой записи: там истекает снапшот целиком, и это `TD-21`.

Проверка лежит в `test:e2e:schedule`, то есть внутри **единственного** workflow, который
держит публикацию. Красное здесь останавливает выкладку исправных коммитов.

**Что сделано, чтобы гейт публикации не встал.** Тот же приём, что у `TD-20` и `TD-21`:
ветка выбирается **по странице**, и обе ветки — настоящие проверки.

- строгая пара на странице есть → прежняя проверка целиком (выдача есть в точности
  пересечение месяца и города);
- строгой пары нет → утверждается обратное наблюдаемое, и оно **не пустое до 2027-06-07**
  (с этого дня — вакуумно, см. «Две даты» выше). Три части:
  строгой пары не должно быть и по данным (если по снапшоту и окну она есть, значит записи
  потеряны или испорчен признак города — ветка краснеет); число групп городов на странице
  совпадает с числом имён городов в данных (иначе счёт выше считает не тот предмет, и
  «пары нет по данным» ничего не значит); остаток требования — пересечение на вырожденной
  паре (слабее, но ложным быть способно: выключенный фильтр месяца показал бы чужие месяцы)
  и то, что город сужает выдачу и без выбранного месяца.

**Требование от календаря больше не зависит.** Носитель требования «выбор месяца
складывается с городом» — синтетический сценарий `@month-city-synthetic`
(`web/tests/schedule-month.spec.ts:814`): 5 записей, в ноябре 3, в Москве 3, на пересечении
2. Такой набор различает четыре поведения, из которых на вырожденных данных неразличимы три:
фильтров нет вовсе (5), работает только месяц (3), только город (3), оба (2). Живая проверка
осталась характеризацией текущего состояния.

Три состояния пройдены, обратная ветка проверена на невакуумность **на дату мутации
(2027-05-24)** — на 2027-06-07 и позже она вакуумна, и мутация «предмет потерян» её уже не
покраснит. Записаны команды мутации
и два числа; прогон каждый раз — весь файл (`npm run test:e2e:schedule` из `web/`, он же
`playwright test tests/schedule-month.spec.ts --project=desktop` плюс `check:month-run`), и
судится он по **имени** покрасневшего теста, а не по цвету прогона. Порт 4322 в этот раз был
свободен, поэтому оговорка `TD-20` и `TD-21` про временный 4323 здесь не нужна. Опорный SHA
восстановления — `change/schedule-month-filter-impl@3900d31e96557bf3659b960db2e843f65d4416b4`;
после каждой мутации `git status --porcelain` пуст и diff относительно него пуст:

| Состояние | Мутация (из `web/`) | Что стало со страницей | Результат |
|---|---|---|---|
| предмет есть | — | 64 карточки, 6 слагов города | ветка 1, зелено: 19 из 19 |
| предмет исчез законно | опорная дата сдвинута на вычисленную: `perl -pi -e "s/\Qexport const calendarToday = (): string => new Date().toISOString().slice(0, 10);\E/export const calendarToday = (): string => '2027-05-24';/" src/lib/schedule-window.ts` + `npm run build` | карточек 64 → **4**, города `санкт-петербург` 2 и `москва` 2 — ровно распределение из таблицы выше, то есть строгой пары на странице нет | ветка 2, зелено: 19 из 19 |
| предмет потерян | дата настоящая, признак города схлопнут: `perl -pi -e "s/\QcitySlug: slugify(entry.city?.name \|\| 'unknown'),\E/citySlug: 'moskva',/" src/pages/raspisanie-i-tseny.astro` + `npm run build` | карточек 64, слагов города 6 → **1** | **краснеет `@month-city`** (17 passed, 2 failed): «на странице нет пары „месяц + город“ с пересечением строго меньше каждого из двух множеств, а по снапшоту и окну актуальности на 2026-08-12 такая пара есть — записи потеряны» |

Полнота мутаций измерена, а не осмотрена: во второй карточек в собранной странице
(`grep -o 'data-months="' dist/raspisanie-i-tseny/index.html | wc -l`) стало 4 против 64, а
`grep -o 'data-city="[^"]*"' … | sort | uniq -c` дал ровно два города; в третьей тот же счёт
дал 64 карточки при **одном** слаге города — то есть предмет действительно убран, и зелёное
означало бы декоративность ветки. Ни одна мутация не трогала `web/tests/`: ветка выбирается
данными, а не текстом проверки.

Вторым красным в третьем состоянии идёт `@month-empty-state` («каждое сочетание месяца и
города непусто — проверять было нечего»), и это ожидаемо: с одним городом на всю страницу
пустого сочетания «месяц + город» не существует. Названо, чтобы следующий исполнитель не
принял два красных за разлад: судить надо по имени, и нужное имя в списке есть.

**Негативная проверка носителя требования.** `@month-city-synthetic` проверен отдельной
мутацией — убит сам фильтр города в клиентском скрипте:
`perl -pi -e "s/\Qconst matchesCity = !city.value || item.dataset.city === city.value;\E/const matchesCity = true;/" src/components/schedule/ScheduleControlsScript.astro` + `npm run build`
(полнота измерена: `grep -c "matchesCity = true" dist/raspisanie-i-tseny/index.html` → 1).
Прогон 14 passed, 5 failed; **`@month-city-synthetic` краснеет по имени** с сообщением
«выдача не является пересечением месяца и города», и два числа видны прямо в нём: показанных
записей стало **3** вместо **2** — к «Ноябрь Москва два» и «Ноябрь Москва раз» добавилась
«Ноябрь Онлайн». Остальные четыре красных — `@month-city`, `@month-empty-state`,
`@month-empty-list`, `@month-missing-control` — тоже опираются на фильтр города, и это
ожидаемо от мутации такого охвата; более узкой мутации, убивающей пересечение и не задевающей
их, в скрипте нет: фильтр города там один.

**Настоящее лечение — обновить снапшот расписания реальными данными.** Двигать даты записей
вперёд нельзя: это подделывает расписание. Ослабить отбор пары («брать любую, где есть хоть
одна запись») лечением тоже не является — это возврат ровно к той редакции, которая была
зелёной при выключенном фильтре города.

**Разрешает ветвление требование спеки, а не эта запись.** Право на ветку даёт требование
«Предмет, исчезнувший по календарю, отличается от предмета, потерянного дефектом»
(`openspec/changes/schedule-month-filter/specs/schedule-month-filter/spec.md`); запись
фиксирует срок — 2027-05-24 — и способ лечения. Обратный порядок означал бы, что контракт
проверок меняют дописыванием в список долгов.

## TD-26. Адреса классов раздачи зашиты в проверке литералами одной сборки

**Дата записи:** 2026-08-13. **Файлы:** `web/tests/serving-config.test.ts:432-467`
(`OWN_TTL_CLASSES`), `:475-490` (`DEFAULT_CLASSES`). **Метки в коде:** нет.

**Проблема.** Проверка кеш-политики классифицирует адреса-примеры по правилам конфигурации,
и адреса заданы литералами, снятыми с **одной** сборки: `/_astro/CtaBand.CeDd4XWy.css`,
`/_astro/global.BgxQ8uu7.css`, `/pagefind/fragment/ru_129b2ce.pf_fragment`,
`/pagefind/index/ru_22aa6c8.pf_index`, `/pagefind/pagefind.ru_206ebec66e.pf_meta`. Хеш в
имени порождается из содержимого — то есть первая же правка соответствующего CSS или
переиндексация поиска делает эти адреса несуществующими.

Проверка от этого **не покраснеет**: она сопоставляет строку адреса с регулярными
выражениями конфигурации и файловой системы не касается. То есть тест продолжает утверждать
верное про правила и одновременно перестаёт утверждать что-либо про сборку — про неё он и не
утверждал, но читается как утверждающий, потому что литералы выглядят как реальные адреса.
Практический риск не в ложном зелёном самой проверки, а в чтении: следующий исполнитель
возьмёт эти адреса как актуальные — например для ручной приёмки раздела 5 — и получит 404 на
исправной раздаче.

**Чем подтверждён.** Литералы с хешами в перечисленных строках на
`change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab`; ни одна из 37
проверок файла не обращается к `web/dist`.

**Чем лечится.** Либо строить перечень адресов из фактического `web/dist` (тогда проверка
получает второй предмет — соответствие классов реальной сборке — и обязана падать на пустом
списке, а не проходить вакуумно), либо оставить литералы, но пометить их в коде как
синтетические и убрать из них хеши, заменив на явно поддельные (`/_astro/x.HASH.css`), чтобы
их нельзя было принять за адрес для приёмки. Второй путь дешевле и закрывает риск чтения,
первый закрывает ещё и дрейф классов.

**Когда актуально:** до первой правки стилей после этого change (хеши сменятся) и при любом
использовании этих адресов вне теста. Найдено ревью 5.5 PR #109 (находка F6).

---

## TD-27. Запрос каталога под `/_astro/` и `/fonts/` отдаёт встроенную страницу nginx

**Дата записи:** 2026-08-13. **Файлы:** `scripts/bootstrap-vps.sh` (heredoc `NGINX`,
`location ^~ /_astro/`, `location ^~ /fonts/`). **Метки в коде:** нет.

**Проблема.** Оба правила — префиксные `location` без `try_files`: `try_files` живёт в
`location /` и на них не распространяется. Запрос самого каталога (`/fonts/`, `/_astro/`)
разрешается директивой `index`, файла `index.html` там нет, `autoindex` выключен — nginx
отвечает **403** и отдаёт **свою встроенную страницу**, а не страницу ошибки сайта:
`error_page` задан только для 404. Посетитель видит страницу с подписью nginx вместо
оформления сайта, и заодно этот ответ приходит без `Cache-Control` — правило объявляет
заголовок без `always`.

Границу надо назвать точно, иначе долг прочитают шире, чем он есть: **404 под этими
правилами страницу сайта отдаёт** — `error_page 404 /404.html` наследуется location'ами, и
это проверено сценарием спеки. Речь только про 403 (и 5xx). Отсутствие `Cache-Control` на
таких ответах спека принимает явно и с причиной (требование «Заголовок кеширования не
теряется и не удваивается», абзац про сужение области); про **страницу** она не говорит
ничего — это и есть незакрытая часть.

**Чем подтверждён.** Текстом конфигурации на
`change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab`: в блоках
`^~ /_astro/` и `^~ /fonts/` нет ни `try_files`, ни `autoindex`, ни `error_page`; `error_page`
в блоке `server` перечисляет только 404. На живой раздаче не наблюдалось — стенд правится
руками, и приёмка раздела 5 шага для 403 не содержит.

**Чем лечится.** `error_page 403 /404.html;` в блоке `server` (наследуется всеми правилами),
либо `location = /fonts/ { return 404; }` и аналогично для `/_astro/`. Первое дешевле, но
меняет наблюдаемый код ответа на каталог, если писать `error_page 403 =404 /404.html` —
поэтому решение о коде должно быть принято явно, а не выбрано по удобству. Проверять надо
живым запросом: текст конфигурации не отличает «страница сайта» от «встроенная nginx».

**Когда актуально:** при следующей правке политики раздачи или при первой жалобе на
нефирменную страницу ошибки. Найдено ревью 5.5 PR #109 (находка F7).

---

## TD-28. Регулярное выражение класса карт сайта шире, чем класс в спецификации

**Дата записи:** 2026-08-13. **Файлы:** `scripts/bootstrap-vps.sh` (heredoc `NGINX`,
`location ~ ^/sitemap.*\.xml$`). **Метки в коде:** нет.

**Проблема.** Спека задаёт класс как «имя файла начинается с `sitemap` и оканчивается на
`.xml` **в корне сайта**». Реализация — `^/sitemap.*\.xml$`, и `.*` совпадает и с `/`:
правило накрывает `/sitemap-foo/bar.xml`, `/sitemapXX/YY/ZZ.xml` — любые вложенные пути,
начинающиеся на `/sitemap`. Сегодня таких адресов в сборке нет, поэтому наблюдаемого
расхождения нет тоже; появятся — и они получат `max-age=3600` вместо политики страниц, молча.

Это расхождение «реализация шире спеки», а не дефект поведения, и записывается именно так:
поведение сегодня верное, но верно оно по свойству сборки, а не по конфигурации. Проверка
этого не ловит, потому что сама проверяет класс теми же двумя адресами
(`/sitemap-index.xml`, `/sitemap-0.xml`), которые под оба прочтения попадают одинаково.

**Чем подтверждён.** Текстом правила на
`change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab` (строка 155) и
семантикой PCRE: `.` совпадает с `/`, ограничения на сегмент пути в выражении нет.

**Чем лечится.** Сузить до одного сегмента — `^/sitemap[^/]*\.xml$` — и добавить в проверку
отрицательный адрес (`/sitemap-foo/bar.xml` **не** относится к классу карт сайта). Без
отрицательного адреса правка не гейтится: оба прочтения дают один результат на нынешних
примерах. Ту же форму стоит применить к правилу корневых `.svg`, которое уже записано
одним сегментом (`^/[^/]+\.svg$`) — расхождение между двумя соседними правилами одного
файла само по себе повод привести их к одной форме.

**Когда актуально:** до появления любого адреса, начинающегося на `/sitemap` и содержащего
`/`. Найдено ревью 5.5 PR #109 (находка F9).

---

## TD-29. Диагностические ветви проверки раздачи не закреплены ни одним утверждением

**Дата записи:** 2026-08-13. **Файлы:** `web/tests/serving-config.test.ts` (14 мест
`throw new Error('ПРОВЕРИТЬ НЕ УДАЛОСЬ: …')`). **Метки в коде:** нет.

**Проблема.** Разбор конфигурации в проверке защищён четырнадцатью стражами, которые
отличают «нарушений нет» от «проверить не удалось»: незакрытый блок, лишняя `}`, директива
без `;`, неизвестный модификатор `location`, некомпилируемое регулярное выражение,
неразбираемая директива, отсутствие открытия и закрытия heredoc'а, `gzip_types` объявленный
дважды или пустой. Ни одну из этих ветвей **не утверждает ни один тест**: в файле 1031 строка
и **ноль** вхождений `toThrow` (`grep -c toThrow` → 0).

Часть ветвей всё же была пройдена — но однократно и вне гейта, в разовой кампании негативных
мутаций раздела 4 (4.7 переименовала разделитель heredoc'а и получила страж «не найдено
открытие heredoc'а»; 4.4 удалила `gzip_types` и получила страж пустого перечня). Кампания
закончилась, worktree удалён, повторяемости у неё нет. Ветви разбора — несбалансированные
скобки, пропущенная `;`, неизвестный модификатор, некомпилируемый regex, неразбираемая
директива — не пройдены **никогда**.

Почему это долг, а не придирка: страж существует ровно для того, чтобы отсутствие сигнала не
выдавалось за отсутствие проблемы. Непройденный страж — обещание того же рода, что
непроверенный гейт: если он сломан (например, условие никогда не истинно из-за опечатки в
разборе), проверка молча вернётся к тому поведению, от которого страж и заводили, — пройдёт
на пустом множестве.

**Чем подтверждён.** `grep -c 'throw new Error' web/tests/serving-config.test.ts` → 14;
`grep -c toThrow` → 0; прогон 37/37 зелёный, то есть ни один страж на текущем входе не
срабатывает. Всё на `change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab`.

**Чем лечится.** Юнит-тесты на сам разборщик с испорченными входами-строками (не мутацией
файла в дереве, а фикстурами): по фикстуре на каждый страж, `expect(...).toThrow(/ПРОВЕРИТЬ
НЕ УДАЛОСЬ/)`. Это дешевле мутаций и делает ветви пройденными постоянно, а не один раз.
Образец подхода уже есть в репозитории — `web/tests/deploy-checks.test.ts` проверяет
shell-функцию `redirects_include_active` набором подготовленных входов.

**Когда актуально:** при следующей правке разборщика конфигурации; до тех пор расхождение
между «страж есть» и «страж работает» остаётся непроверенным. Найдено ревью 5.5 PR #109
(находка F11).

---

## TD-30. Комментарий внутри heredoc'а vhost содержит подстановку команды

**Дата записи:** 2026-08-13. **Файлы:** `scripts/bootstrap-vps.sh:115`. **Метки в коде:**
нет.

**Проблема.** Разделитель heredoc'а `NGINX` не закавычен — это необходимо, им подставляются
`${DOMAIN}` и `${WEB_ROOT}`. Побочное следствие: bash выполняет и **подстановку команд**, а в
комментарии на строке 115 стоит `` `nginx -t` `` в обратных кавычках. То есть при каждом
запуске `bootstrap-vps.sh` на хосте `nginx -t` выполняется лишний раз — до того, как vhost
записан, — а в готовый конфиг попадает не текст `nginx -t`, а **пустой** stdout этой команды:
комментарий в живом файле короче, чем в скрипте.

Ущерб сегодня нулевой: `nginx -t` только читает конфигурацию, а испорчен лишь комментарий.
Записывается это по двум другим причинам. Первая: подстановка команды в шаблоне конфигурации
— готовый механизм исполнения произвольной команды из текста, который читается как
комментарий; следующая правка может внести туда что угодно, и `nginx -t` в порождённом
конфиге ничего не заметит. Вторая: это ещё один канал расхождения «текст скрипта ↔ файл на
хосте» (TD-22), и при сверке текстов он даст ложное расхождение, если о нём не знать.

**Чем подтверждён.** Поиск обратной кавычки по `scripts/bootstrap-vps.sh` даёт четыре
вхождения, и единственное внутри тела heredoc'а — строка 115 (остальные три — в обычных
комментариях скрипта, вне heredoc'а, где подстановка не выполняется). Порождение блока подстановкой (пайплайн из
`docs/deploy-vps.md`, раздел «Одноразовая правка на уже развёрнутом сервере») печатает
`nginx: command not found` на машине без nginx и даёт комментарий без текста команды.
Проверено на `change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab`.

**Воспроизведено второй раз, уже не в разборе, а в деле:** при исполнении шага приёмки 5.2
(порождение блока для стенда, голова `1c7e3da`) подстановка снова выполнила команду —
`nginx: command not found` в stderr на машине без nginx, — и в порождённом файле комментарий
пришёл в виде «`# Файл создаётся пустым ниже, до : include одного отсутствующего`», то есть
без текста команды. Именно этот файл ушёл на стенд, поэтому расхождение «текст скрипта ↔
файл на хосте» теперь не гипотеза, а факт с адресом: живой vhost на 193.124.115.99 содержит
укороченный комментарий. Побайтовая сверка живого конфига со порождённым текстом это
учитывает (расхождение нулевое, потому что сверяется именно порождённый текст), а сверка со
**скриптом** дала бы одну строку расхождения — и она законна.

**Чем лечится.** Экранировать обратные кавычки в комментарии (`` \` ``) или убрать их вовсе.
Правка однострочная, но это правка кода: по правилам проекта дефект начинается с падающего
теста, поэтому сначала проверка «в теле heredoc'а нет неэкранированных обратных кавычек и
`$(`» — она общая по признаку, а не по списку известных мест, — и только потом фикс.

**Когда актуально:** при следующей правке heredoc'а `NGINX` и при реализации любого из
вариантов закрытия TD-22 (сверка текстов). Найдено при исправлении находки F3 ревью 5.5
PR #109.

## TD-31. Два мета-гейта дают разный ответ на одном и том же дереве

**Дата записи:** 2026-08-13. **Файлы:** `web/tests/demo-gate.test.ts`,
`web/tests/repo-hygiene.test.ts`. **Метки в коде:** нет.

**Проблема.** На **неизменном** дереве полный прогон `npx vitest run` в `web/` дал разные
итоги: один раз `2 failed | 257 passed (259)`, затем четыре раза подряд `259 passed`.
Упавшие — `demo-gate.test.ts > материал для проверки на месте` и
`repo-hygiene.test.ts > тесты с особым предметом вписаны и в исключения основного прогона, и
в свой конфиг`. По отдельности эта пара проходит (21/21). Между прогонами не менялось ничего:
ни файлы, ни индекс, ни сборка — измерено пятью прогонами подряд, частота 1 из 5.

**Почему это долг, а не мелочь.** Оба падавших теста — **мета-гейты**: они утверждают, что
обязательный прогон `Tests` содержит нужные шаги, что набор проверок не пуст и что тесты с
особым предметом вписаны и в исключения основного конфига, и в свой. Предмет у них —
целостность самого гейта публикации. Гейт, который на одном дереве отвечает то «цел», то
«сломан», нельзя читать **ни в одну сторону**: зелёный не доказывает целостность, красный не
доказывает поломку. Это ровно то смешение «дефектов нет» и «не смогла проверить», против
которого написан раздел `AGENTS.md` про декоративные гейты, — только здесь оно возникает не от
формы проверки, а от невоспроизводимости.

Практическое следствие уже наступило: при разборе конфликта с `main` первый же прогон показал
два падения, и без повторных прогонов вывод был бы «merge сломал мета-гейты» — неверный.

**Чем подтверждён.** Пять последовательных `npx vitest run` в `web/` на дереве слияния
`origin/main` (`1363684`) в `change/serving-cache-headers-impl` (`79cbc6d`): 2 failed, затем
259/259 четырежды. Оба теста читают только статику — `.github/workflows/*.yml`,
`web/package.json`, список файлов в `tests/` — то есть от вывода сборки не зависят, и причина
лежит в самом прогоне (порядок, параллельные воркеры, состояние между файлами), а не в
предмете проверки.

**Чем лечится.** Разобраться, откуда невоспроизводимость: чтение общего состояния из
параллельных воркеров либо зависимость от порядка файлов. До разбора **нельзя** делать вывод
о целостности гейта публикации по одному прогону — только по нескольким. Не лечится
перезапуском до зелёного: это и есть способ спрятать долг.

**Чего эта запись НЕ утверждает.** Что флак внесён merge'ом. До слияния полный прогон на
голове ветки был зелёным (222/222), но **один** зелёный прогон отсутствия флака не
доказывает — частота 1 из 5 в него укладывается свободно.

## TD-32. Следствия наследования `add_header` не имеют адресата: change по security-заголовкам не заведён, а отчёт аудита вне git

**Дата записи:** 2026-08-13. **Файлы:** `scripts/bootstrap-vps.sh` (heredoc `NGINX`),
`openspec/changes/serving-cache-headers/specs/static-serving/spec.md`. **Метки в коде:** нет.

**Проблема.** Задача 7.3 change `serving-cache-headers` предписывает передать обе стороны
Решения 3 из его `design.md` «в change по security-заголовкам и CSP (находка B4)». Такого
change в `openspec/changes/` нет — проверено перечислением каталога на
`change/serving-cache-headers-impl@6aa1a4f0a15b1adf76aab1f8f9a2a53fda1e4d7c`: десять активных
changes, ни один не про заголовки безопасности. Отчёт, где находка B4 живёт,
`docs/security-audit-2026-08-08.md`, **не отслеживается git** (`git ls-files` пуст), то есть
адресом внутри репозитория он не является тоже: запись в него исчезнет вместе с рабочим
деревом и на другой машине её не увидит никто.

**Почему это долг, а не выполненный пункт.** Передача следствия в несуществующий адресат
читается как сделанная работа: пункт отмечен, а следствие не лежит нигде, где его найдёт
автор будущей правки конфигурации. Само следствие при этом дорогое и не наблюдаемо в тексте
той правки, которая его нарушит: набор заголовков, объявленный только в блоке `server`, не
доедет до шести правил со своим `add_header` (`/_astro/`, данные поиска, `/fonts/`,
`favicon.`, корневые `.svg`, карты сайта), и снаружи это видно только запросом адреса каждого
класса — по шести классам, а не по одному. Второе следствие обратное: новое правило
`location`, промолчавшее о заголовках, наследует серверное умолчание целиком, включая
`error_page 404 /404.html`. Третье: `^~` прекращает фазу регулярных выражений только когда он
же самый длинный совпавший префикс, поэтому защитой класса он не является.

**Чем закрыто на сегодня и что осталось.** Следствия перенесены в спеку самого change —
оговорка в требовании «Заголовок кеширования не теряется и не удваивается», — и при
архивировании они переезжают в `openspec/specs/static-serving/`, то есть получают постоянный
адрес, версионируемый вместе с кодом. Это закрывает **место**, но не **проверку**: оговорка
намеренно не оформлена требованиями, и предмет есть не у всех трёх следствий. Серверных
заголовков, кроме кеширующего, сегодня ноль, и префиксного `location` с более длинным
префиксом внутри покрытого поддерева тоже ноль — у первого и третьего следствий предмета нет.
У второго он есть: `location /` и **264** включённых правила перенаправления своих заголовков
не объявляют и наследуют умолчание **прямо от блока `server`**. Правила перенаправления при
этом уже под проверкой (`web/tests/serving-config.test.ts`, `[GREEN-BY-DESIGN] правила
перенаправления не объявляют своих add_header`, мутация 4.17), так что непокрытым остаётся
только будущее правило, которого ещё нет.

**Прежняя редакция этой записи называла `location /` «несущим механизмом 264 правил
перенаправления». Это неверно, и ошибка та же, которую задача 7.3 нашла в чужом артефакте:**
`location /` — соседний блок, а не внешний уровень, наследование идёт от `server`. Формулировка
исправлена здесь, в спеке и в 7.3. Остаётся: работа, которая
вносит в раздачу заголовки безопасности или новое правило `location`, обязана завести
проверку на своё правило сама — на общий признак («каждое правило со своим `add_header`
объявляет полный набор»), а не на список известных правил.

**Чем подтверждён.** `ls openspec/changes/` — security-headers/CSP среди них нет;
`git ls-files docs/security-audit-2026-08-08.md` — пусто при существующем файле в рабочем
дереве. Оба измерения на `6aa1a4f0a15b1adf76aab1f8f9a2a53fda1e4d7c`.

**Чего эта запись НЕ утверждает.** Что аудит безопасности не проводился или что находка B4
неверна: отчёт существует как файл, вопрос только в том, что он вне git и потому адресом для
передачи следствия быть не может. И не утверждает, что change по заголовкам безопасности
нужно заводить сейчас — заводить его или нет, решает владелец; долг фиксирует то, что до его
появления следствия держатся на спеке раздачи и ни на чём больше.

## TD-33. Бюджет Total Blocking Time краснеет от загрузки раннера, а не от изменения

**Дата записи:** 2026-08-13. **Файлы:** `lighthouserc.*` (бюджеты LHCI), workflow
«Lighthouse CI». **Метки в коде:** нет.

**Проблема.** Прогон `Lighthouse budgets (4 templates, median of 5)` упал на голове
`2980f89` ветки `change/serving-cache-headers-impl`: `total-blocking-time` на главной
ожидалось `<= 200`, получено **1052.8845** (медиана пяти прогонов, одно значение в выборке).
Изменение вызвать это не могло, и это установлено измерением, а не рассуждением:
`git diff --name-only 485a27d 2980f89 -- web/` даёт **ноль файлов** — собираемое дерево
побайтово идентично голове `485a27d`, на которой тот же прогон прошёл **зелёным** 28 минутами
раньше. На этой ветке LHCI зелёный на семи предыдущих головах подряд (`95f7e64`, `1c7e3da`,
`cb81518`, `d04f6eb`, `484fe6e`, `6aa1a4f`, `485a27d`).

Из последних 30 прогонов LHCI красных **2** — примерно 7%, то есть это повторяется, а не
единичный случай.

**Почему это долг, а не «перезапустить и забыть».** `Lighthouse budgets` — **required status
check** в защите ветки `main` (`gh api repos/.../branches/main/protection`), то есть он
блокирует **merge**. При этом в гейт публикации он не входит: выкладку останавливает только
`Tests` (см. `AGENTS.md`). Значит его краснота стоит ровно одного — времени человека на
разбор и рестарт, — и стоит его **регулярно**. Молчаливый рестарт эту цену скрывает: следующий
исполнитель пройдёт тот же путь с нуля, потому что в репозитории не останется следа, что
падение известно и чем оно объясняется.

TBT — самая чувствительная к загрузке процессора метрика Lighthouse: она измеряет время, на
которое главный поток заблокирован, и на общем раннере пятикратный выброс даёт не сайт, а
сосед по машине. Медиана пяти прогонов от этого не спасает, если сосед занимал процессор все
пять.

**Чем лечится.** Варианты, ни один не выбран (нужно решение владельца): поднять порог TBT до
величины, устойчивой на этом раннере, и записать причину; либо перевести TBT из
`maxNumericValue` в предупреждение, оставив жёсткими метрики, не зависящие от процессора
(размеры, число запросов, LCP по сети); либо убрать `Lighthouse budgets` из required checks,
оставив информативным. **Чего делать нельзя** — рестартовать до зелёного, не оставляя записи:
именно так расход становится невидимым.

**Чем подтверждён.** Прогон `31690846174`, шаг «Run Lighthouse CI»: `✘ total-blocking-time
failure for maxNumericValue assertion`, `expected: <=200`, `found: 1052.8845`, далее
`Assertion failed. Exiting with status code 1.` Остальные 11 проверок на том же SHA зелёные,
включая `Unit and build tests`.

## TD-34. Job зависает в `in_progress`, а прогон при этом объявляет успех — required check не отчитывается никогда

**Что наблюдалось.** PR #146 стоял `BLOCKED` при всех остальных зелёных проверках. Причина
не в проверке: прогон `Security` `32366339189` числился `status: completed,
conclusion: success` (обновлён 2026-08-20T11:58:03Z), а его job `Secret scan` остался
`in_progress` без вердикта. `Secret scan` входит в 11 обязательных, поэтому required check
не мог отчитаться **никогда**, и PR был заблокирован бессрочно.

Перезапуск того же прогона (`attempt 2`) дал `Secret scan: completed/success`, PR перешёл в
`UNSTABLE` и был слит. То есть падения не было вовсе — был потерянный вердикт.

**Почему это долг, а не разовая неприятность.** Это ровно тот класс, против которого написан
весь раздел про гейты в `AGENTS.md`, только со стороны площадки: отсутствие сигнала
неотличимо от «проверка ещё идёт», и ждать можно бесконечно. Отличить «job висит» от «job
работает» по `gh pr checks` **нельзя** — оба показываются как `pending`. Признак, который
различает: у прогона `status: completed`, а у его job — `in_progress`; такое сочетание
невозможно при нормальной работе и потому является надёжным индикатором.

В тот же день наблюдался родственный симптом: на #143 job `Unit and build tests` крутился
`6h0m15s` и упал по таймауту. Оба случая — про job, потерявший связь с раннером, поэтому
считать их единичными нельзя, а частоту никто не измеряет.

**Чем лечится.** Ни один вариант не выбран, нужно решение владельца: (а) добавить в
диагностику проверку сочетания «прогон завершён, job не завершён» и явно перезапускать такие
прогоны, чтобы ожидание не было бесконечным; (б) снизить таймаут job'ов до величины, при
которой зависание видно быстро, а не через шесть часов; (в) оставить как есть, но знать
признак. **Чего делать нельзя** — молчаливо перезапускать до зелёного: расход времени тогда
невидим, и следующий исполнитель разбирается с нуля.

**Чем подтверждён.** `gh api repos/pgorbachev/ikpk/actions/runs/32366339189` против
`…/jobs`: первый — `completed/success`, второй — `Secret scan: in_progress` без
`conclusion`. После `gh run rerun 32366339189`: `attempt=2`, `Secret scan:
completed/success`, слияние #146 merge-коммитом `fc8195b0b31f5accdc5ec169933d8597f760a795`.

## TD-35. Vitest роняет прогон по unhandled `EPIPE` при 52 зелёных файлах из 52

**Что наблюдалось.** На PR #147, содержащем **только два markdown-файла**, job `Unit and
build tests` упал. При этом в логе: `Test Files 52 passed (52)` — то есть не упал ни один
тест. Упал прогон:

```
⎯⎯ Unhandled Errors ⎯⎯
Vitest caught 1 unhandled error during the test run.
⎯⎯ Uncaught Exception ⎯⎯
Error: write EPIPE
 ❯ afterWriteDispatched node:internal/stream_base_commons:159:15
 ❯ writeGeneric node:internal/stream_base_commons:150:3
Serialized Error: { errno: -32, code: 'EPIPE', syscall: 'write' }
This error originated in "tests/deploy-checks.test.ts" … latest test
"каталога сборки нет — отказ"
```

Vitest при unhandled error завершается ненулевым кодом даже когда все тесты зелёные —
поэтому падение выглядит как провал проверки, хотя проверять было нечего.

**Почему это флак, а не регресс.** Дерево отличалось от `main` двумя markdown-файлами, и тот
же файл тестов прошёл на `main` (прогон PR #146) за несколько минут до этого. Одинаковое
дерево, разный исход — значит гонка. **Частота не измерена**, и судить по одному прогону
нельзя: замер требует нескольких прогонов на неизменном дереве.

**Предполагаемый механизм — гипотеза, не факт.** Тест зовёт
`execFileAsync('bash', ['-c', …])` (`web/tests/deploy-checks.test.ts:35`), а проверяемая
функция при отсутствующем каталоге (`/nonexistent-dist-ikpk`) отказывает немедленно. Похоже,
родитель пишет в поток уже завершившегося дочернего процесса, и ошибка всплывает
необработанной. Прямого доказательства этой цепочки нет: воспроизвести гонку локально не
удалось, поэтому механизм назван гипотезой, а не причиной.

**Второе, независимое наблюдение того же механизма (2026-08-20, PR #148, job `Unit and
build tests` на коммите `44e816c`).** `Test Files 57 passed (57)`, `Tests 983 passed
(983)`, `Errors 1 error` — снова ни один тест не упал, упал только прогон. Стек на этот раз
из `tests/deploy-checks-payment-role.test.ts:59:22` (`runFn`), а не из
`deploy-checks.test.ts` — но код идентичен: `const child = execFileAsync('bash', [...])`
сразу за которым `child.child.stdin?.end('')`, тот же `EPIPE` в
`Socket.Writable.end`/`Socket.end`. Файл — копия того же хелпера `runFn`/`requireFn`
(`web/tests/deploy-checks-payment-role.test.ts:57-59`), не переиспользование модуля, то
есть гонка живёт в паттерне, а не в одном файле. Это превращает «гипотезу» из первого
наблюдения в подтверждённый повторяющийся паттерн: два разных файла, тот же код записи в
stdin сразу после запуска, тот же необработанный `EPIPE`. Частота всё ещё не измерена
(двух прогонов недостаточно для оценки), но принадлежность к одному классу — уже факт, а
не предположение.

**Чем лечится.** Было предложено три варианта: (а) найти и закрыть источник записи в
завершившийся процесс — в обоих файлах, раз паттерн скопирован; (б) обработать `EPIPE` на
потоках дочернего процесса в общем тестовом помощнике (вынести `runFn` туда, а не
копировать); (в) измерить частоту серией прогонов на неизменном дереве и только потом
решать. **Чего делать нельзя** — перезапускать до зелёного, не оставляя записи: это ровно
тот способ сделать расход невидимым, против которого написан раздел про гейты в
`AGENTS.md`.

**Третье наблюдение (2026-08-22, PR #150, job `Unit and build tests`, коммит
`e5322be15f8bc768ae68427e2fd4a93a917b8fe1`, прогон `32558888916`).** `Test Files 57 passed (57)`,
`Tests 984 passed (984)`, `Errors 1 error` — снова ни один тест не упал. Стек тот же, что во
втором наблюдении: `runFn tests/deploy-checks-payment-role.test.ts:59:22`,
`child.child.stdin?.end('')`, `{ errno: -32, code: 'EPIPE', syscall: 'write' }`. Дельта PR
относительно базы слияния (`f03e61cb…`) — **четыре файла в `docs/`**, из них два `.md` и два
`.png`: ни одного исходника и ни одного теста. Относительно головы `main` на тот момент
(`63a69f7ac0…`) дерево отличалось семью файлами, потому что ветка от `main` отставала, — это
разные величины, и путать их нельзя.

Соседний факт, усиливающий вывод: на **предыдущем** коммите той же ветки
(`856972fa4831abb89b0af5d74c78a004a6857cec`, прогон `32558630017`) тот же job прошёл
**успешно**.

**Что добавляет третий случай.** После перебазирования той же ветки на свежий `main` прогон
прошёл зелёным (`32559062215`, коммит `8c750ada3b…`). **Дельта PR при этом не менялась** —
те же четыре файла в `docs/`; корпус юнит-тестов совпал по числу файлов и тестов
(`57 passed (57)` и `984 passed (984)` в обоих прогонах). Менялась база: перебазирование
затянуло в дерево изменения PR #149, из-за чего деревья двух прогонов отличаются тремя
файлами — `.github/workflows/nightly.yml`, `docs/tech-debt.md` и
`web/tests/parity-compare.test.ts`; последний в job `Unit and build tests` не участвует.

Отсюда точная формулировка вывода: снята альтернатива **«причина в дельте PR»**, а не
«в дереве вообще». Одинаковая дельта и одинаковый корпус тестов дали разный исход в двух
прогонах подряд — этого для вывода о гонке достаточно, но утверждать побайтовое совпадение
деревьев нельзя, а первая редакция этого абзаца утверждала именно его.

Частота по-прежнему **не измерена**: три падения на трёх разных PR дают нижнюю границу, а не
оценку — для неё нужна серия прогонов на неизменном дереве, и она так и не сделана.

**Что это уже стоило.** Красный обязательный контекст на PR, не содержащем ни одного
исходника и ни одного теста, и задержка merge. Трижды подряд. Способ выхода каждый раз был
свой, и это стоит различать: в третьем случае **перезапуска не было вовсе** — прогон
`32558888916` остался `attempt=1` и `failure` навсегда, а зелёным стал новый прогон после
перебазирования. Пока источник не закрыт, каждый PR платит эту цену с вероятностью, которую
никто не знает.

**Оговорка к предыдущему абзацу.** Его последняя фраза («пока источник не закрыт, каждый PR
платит эту цену») верна на момент написания и отменяется следующим абзацем: источник закрыт
в этой же поставке. Сам текст наблюдения не правлю — это запись о том, что наблюдалось тогда,
а не описание текущего состояния.

**Четвёртое наблюдение и выбор варианта (2026-08-22, PR #152, коммит `90142a1`, прогон
32559835666).** Четвёртое, а не третье: наблюдение по PR #150 выше легло в `main` раньше
(#154). Случаи разные — разные PR, коммиты и прогоны, — поэтому оба сохранены; сливать их
в один значило бы потерять один из двух замеров. `Test Files 59 passed (59)`, `Tests 1027 passed (1027)`, `Errors 1 error`,
`write EPIPE`, шаг с кодом 1 — четвёртый раз тот же класс. Источник на этот раз отнесён
vitest'ом к `tests/deploy-checks-payment-role.test.ts`. Новый файл этой поставки
(`deploy-form-links.test.ts`, 32 проверки, каждая поднимает `bash`) добавил нагрузки и,
по-видимому, поднял вероятность гонки: те же файлы прошли без `EPIPE` на `42fe094` и
`b9c2dda`, а локально три прогона подряд дали 1027/1027 без единой ошибки.

**Выбран вариант (а):** в обоих запускателях (`deploy-checks.test.ts`,
`deploy-checks-payment-role.test.ts`) перед записью в `stdin` ставится
`child.child.stdin?.on('error', () => {})`. Гасится именно ошибка записи в дочерний
процесс; код выхода проверяемой функции по-прежнему возвращается и ни на что не
подменяется. Механизм закрыт по построению: запись в закрытый поток больше не может стать
необработанной ошибкой процесса.

**Что НЕ сделано и почему долг не снимается целиком.** Вариант (б) — вынести `runFn` в
общий помощник — не выполнен: сейчас исправлены две копии, и третья копия, если её кто-то
заведёт, приедет без обработчика. Частота (вариант в) по-прежнему не измерена, и один
зелёный прогон её не измеряет: он показывает, что при этой правке гонка не сработала, а не
что её нет. Аргумент за устранение здесь механический, а не статистический.

**Отношение к TD-34.** Разные причины: там вердикт job'а терялся на стороне площадки, здесь
прогон падает по собственной необработанной ошибке. Общее — оба делают красный цвет
бессмысленным: в одном случае проверка не отчитывается никогда, в другом краснеет при
полностью зелёных тестах.

## TD-36. Три сценария `payment-release-modes.test.ts` ждут `200` там, где контракт требует `201`

**Номер исправлен при слиянии с `main` 2026-08-20: был TD-34, но `main` независимо занял
этот номер под другую находку — см. TD-34 и TD-35 выше. Содержание записи не менялось,
только номер и заголовок; во всех остальных файлах (`tasks.md`,
`web/tests/payment-release-modes.test.ts`) ссылки на TD-34 в этом смысле обновлены до
TD-36 тем же коммитом.**

**ЗАКРЫТ 2026-08-20 решением владельца — первый вариант лечения:** `toBe(200)` → `toBe(201)`
в трёх местах, отдельной test-only поставкой (`60b53cc40777f4bfbd220b26fe4d1f2dc1ed967f`),
а не правкой в ходе реализации. Негативная проверка выполнена мутацией реализации:
`payments/src/app.ts:746` `status: 201` → `status: 200` при закоммиченном фиксе — прогон
`npx vitest run tests/payment-release-modes.test.ts` даёт `4 failed | 30 passed (34)`,
павшие поимённо — все три сценария этого TD плюс «хвостовой слэш канонизируется» (тоже
утверждает 201); восстановление подтверждено пустым `git status --porcelain` и нулевым
diff к HEAD. Запись ниже сохранена как история.

**Дата записи:** 2026-08-20. **Файлы:** `web/tests/payment-release-modes.test.ts` (строки
227, 239, 266). **Метки в коде:** нет — тест из красной базы change `online-payment-flow`
(задачи 4.10/5.10e), редактировать который в ходе реализации запрещено (`AGENTS.md`,
«красные тесты ослаблять и переписывать нельзя»).

**Проблема.** Три сценария в describe-блоках «3.16(2)/5.10e стенд создаёт платёж в 1440249…»
и «3.16(6) одинаковый requestId в двух контурах…» отправляют `POST /payments` с ПЕРВЫМ
обращением по свежему `requestId` (через `validPayload()`, который генерирует новый
`randomUUID()`) и утверждают `expect(res.status).toBe(200)`. Контракт этого эндпоинта
(`openspec/changes/archive/2026-08-21-online-payment-flow/design.md`, «Решение 2», таблица HTTP-кодов) прямо
разводит два случая: `201` — платёж создан впервые; `200` — повтор с уже известным
`requestId`, платёж которого ещё не оплачен. Реализация (`payments/src/app.ts`, ветка
первого создания) отдаёт `201`, что совпадает с уже принятым и зелёным контрактом
(`web/tests/payment-post.test.ts:26` того же обязательного набора проверяет именно это:
первое создание — `201`, второй запрос тем же `requestId` — `200`). Расхождение
воспроизведено дважды, независимо, на этой же сессии реализации и на независимом ревью
(Codex, той же датой) — оба пришли к выводу, что дефект в тексте теста, а не в коде.

**Почему это долг, а не «поправить код под тест».** Изменить код, чтобы первое создание
тоже отдавало `200`, значило бы разойтись с уже принятым `design.md` и сломать
`payment-post.test.ts` — то есть заменить один дефект на подтверждённый. Изменить сам
тест нельзя без решения владельца — это красный тест из отдельной сессии, предшествующей
реализации, и правка «на месте» превратила бы находку в тихо исчезнувшую.

**Чем это стоит.** `payment-release-modes.test.ts` входит в обязательный `vitest run` пакета
`web` и не исключён ни в одном конфиге (`web/vitest.config.ts`), то есть три красных здесь
красят единственный гейт публикации `Tests` (см. `AGENTS.md`, «Гейт публикации») — до
исправления или явного решения владельца PR с этой веткой не может слиться зелёным по
единственному required-check.

**Чем лечится.** Решение владельца: либо `toBe(200)` → `toBe(201)` в трёх местах (тогда это
не дефект теста, а опечатка, которую можно править как обычную правку теста, а не как
находку), либо подтверждение, что первое создание в этом сценарии обязано быть `200` (тогда
нужна отдельная правка `design.md`/`payments/src/app.ts` и негативная проверка на неё). До
решения — тест остаётся красным осознанно, а не забытым.

**Чем подтверждён.** Локальный прогон `npx vitest run tests/payment-release-modes.test.ts`
на `web/`: `3 failed | 25 passed (28)`, все три — `AssertionError: expected 201 to be 200`.
Сверено построчно с `design.md` («Решение 2») и `payment-post.test.ts:24-27` двумя
независимыми проходами (эта сессия и Codex-ревью, 2026-08-20).

## TD-37. `compat-safari-desktop` и `compat-ios-iphone-se` упали по таймауту в 10000 мс на первом ночном прогоне после #148, реран того же коммита прошёл

**Дата записи:** 2026-08-21. **Файлы:** `web/tests/compat.spec.ts:12` (тест `/ loads with
core content`), `.github/workflows/nightly.yml` (job `compat`), `web/playwright.config.ts`
(проекты `compat-safari-desktop`, `compat-ios-iphone-se`). **Метки в коде:** нет.

**Проблема.** Прогон `32443410006` (nightly, коммит `f03e61c` — merge PR #148) упал в job
«Cross-browser compat (7 projects)»: `tests/compat.spec.ts:12` — `Test timeout of 10000ms
exceeded` — ровно на двух проектах из семи, `compat-safari-desktop` и
`compat-ios-iphone-se`. Оба используют движок WebKit.

**Почему это флак, а не регресс.** Немедленный рерун того же прогона на том же коммите
(`gh run rerun 32443410006 --failed`, attempt 2) дал job «Cross-browser compat (7
projects)»: `completed/success` — все 7 проектов зелёные без единой правки дерева. Это и не
«WebKit ломается»: два других WebKit-движковых профиля того же прогона,
`compat-ios-iphone-14` и `compat-ios-ipad`, прошли на обеих попытках. Одинаковое дерево,
разный исход у двух конкретных профилей при третьем и четвёртом WebKit-профиле стабильно
зелёном — сигнатура гонки за раннер/холодный старт, а не сигнатура кода или конфигурации.
Причина не установлена и не выбирается наугад: конкретный механизм (порядок загрузки,
аналитика, холодный кеш браузера, конкуренция за раннер) не подтверждён ни одним
наблюдением, поэтому в фикс не переносится — данных одного флака и одного чистого рерана
недостаточно, чтобы отличить гипотезы друг от друга.

**Почему это долг, а не разовая неприятность.** `nightly.yml` — cron-only и не входит в
гейт публикации (`AGENTS.md`, «Гейт публикации»: единственная блокирующая проверка —
`Tests`), поэтому это падение никого не заблокировало и осталось бы незамеченным без
разбора. Но `tests/compat.spec.ts:12` — тест на 10-секундный глобальный таймаут
(`web/playwright.config.ts`, `timeout: 10000`, без переопределения по проекту) на холодном
раннере под нагрузкой семи параллельных браузерных профилей; при повторении на других
WebKit-профилях или без снижения нагрузки на раннере тот же класс флака воспроизведётся
на следующих ночных прогонах, и без записи каждый такой случай разбирался бы с нуля.

**Чем лечится.** Не выбрано и не применяется в этой поставке: правка таймаута
`compat.spec.ts` или `playwright.config.ts` под единственный наблюдённый флак была бы
подгонкой под непроверенную причину. Один флак и один чистый реран — недостаточно данных,
чтобы отличить «глобальный таймаут слишком тесен для холодного WebKit на общем раннере» от
«разовая перегрузка раннера»; лечение выбирается после накопления частоты серией прогонов
на неизменном дереве (тот же принцип, что в TD-35), а не по одному наблюдению.

**Чем подтверждён.** `gh run view 32443410006` и `gh api
repos/pgorbachev/ikpk/actions/runs/32443410006/jobs`: job `compat`, шаг с тестами,
`compat-safari-desktop` и `compat-ios-iphone-se` — `Test timeout of 10000ms exceeded` на
`tests/compat.spec.ts:12`; `compat-ios-iphone-14` и `compat-ios-ipad` — `success` в том же
прогоне. `gh run rerun 32443410006 --failed` (attempt 2), тот же коммит `f03e61c`: job
`compat` — `completed/success`, все 7 проектов зелёные. Дополнительно: сканирование истории
`gh run list --workflow=nightly.yml` за 15 прогонов — `parity-remote` красный на каждом из
них (см. отдельный фикс в этой же поставке), `compat` красный только на этом одном прогоне
до рерана.

## TD-38. Гейт ссылок на CRM построчный: значение атрибута, разорванное переводом строки, не извлекается

**Где.** `scripts/lib/deploy-checks.sh`, `form_links_match_mode` (извлечение носителей
ссылки), и симметричный признак в `web/tests/demo-output.test.ts`.

**Что не закрыто.** Извлечение опирается на `grep`, то есть работает построчно. Значение
вида

```html
<a href="
  https://b24-cbqwqo.bitrix24site.ru/news/">заявка</a>
```

браузером обрабатывается как обычная ссылка — ведущие пробелы и перевод строки в значении
атрибута он игнорирует, — а `grep` такой атрибут не видит: на первой строке нет закрывающей
кавычки, на второй нет имени атрибута. Ссылка остаётся кликабельной и в набор кандидатов не
попадает; при наличии рядом хотя бы одной правильной заглушки гейт зелёный.

**Почему сейчас не блокирует.** В собранном выводе таких значений нет: проверено на
`web/dist` и `web/dist-demo` поставки PR #152. Все ссылки форм эмитируются компонентами и
данными в одну строку.

**Чем лечится.** Разбором HTML парсером вместо `grep`. Инструмент в репозитории уже есть —
проверки `rich-content` читают собранный вывод браузером, — то есть это перенос предмета в
существующий механизм, а не новая зависимость. Точечное расширение регулярки не годится
принципиально: это ровно то «перечисление частных случаев того, что проверяешь», от которого
уже пострадал этот же гейт (см. историю в самом файле — признак по форме пути прятал 268
страниц со ссылкой в CRM заказчика).

**Отношение к остальному.** Родственно записи о том, что гейтам нужен парсер, а не
приблизительный разбор: каждый следующий обход находится именно там, где стоит регулярка.
Соседние щели того же извлечения — регистр имени атрибута и пробелы внутри значения — в
PR #152 **закрыты** по находке владельца; открытым остаётся только разрыв строкой.
## TD-39. Тесты, поднимающие платёжный процесс, краснеют от нагрузки машины, а не от изменения

**Номер исправлен при слиянии с `main` 2026-08-22: запись создавалась как TD-36, затем как
TD-37, и оба номера `main` независимо занял под другие находки. Содержание не менялось.**

**Связь с TD-37 (выше).** Там ночной прогон упал по таймауту 10 000 мс на двух тестах
совместимости, а реран того же коммита прошёл. Это то же явление, что описано здесь, только
в CI и на другом наборе: предел ожидания, рассчитанный на незагруженную машину. Две
независимые записи об одном классе — довод в пользу того, что предел надо задавать явно и с
запасом, а не оставлять по умолчанию.

**Файлы:** `web/tests/payment-startup.test.ts`, `web/tests/payment-secrets-rate.test.ts`
**Метки в коде:** нет

**Что наблюдалось.** Три полных локальных прогона одного и того же дерева
(`docs/rule-branch-from-fresh-main`, ветка гейта ссылок; изменения в прогоне не участвуют —
затронуты `bin/check-spec-refs`, его тест, `AGENTS.md`, `security.yml`) дали **разные** наборы
падений:

| прогон | упавшие тесты |
|---|---|
| 1 | `payment-startup` ×1, `repo-hygiene` ×1, `demo-gate` ×1 |
| 2 | `payment-secrets-rate` ×1, `payment-startup` ×2 |
| 3 | `payment-secrets-rate` ×3, `payment-startup` ×1 |

Те же два файла в изоляции — **37 из 37 зелёных**. На предыдущей голове той же ветки полный
прогон был зелёным (923 из 923), и в CI эти тесты прошли.

**Характер падений.** Утверждение ждёт причину отказа в `stderr`, а получает **пустую строку**:

```
AssertionError: expected '' to match /PAYMENT_GET_RATE_LIMIT/i
+ Received:
```

То есть процесс не успел ни стартовать, ни написать причину, а тест уже прочитал вывод. Предмет
проверки — fail-closed до открытия порта — при этом исправен: в изоляции он подтверждается.

**Фон, измеренный в тот же момент.** Диск заполнен на 98 % (12 GiB свободно из 460), из них
`/private/tmp` занимает 14 GiB в 116 каталогах `ikpk-*` — оставленные worktree прошлых задач со
своими `node_modules` и `dist`. Параллельно шло до трёх прогонов Vitest.

**Почему это флак, а не регресс.** Одинаковое дерево — разный исход, причём падают разные тесты
одного семейства (те, что поднимают процесс и читают его `stderr`). **Частота не измерена**:
три прогона на загруженной машине — это не замер, а наблюдение. Замер требует прогонов при
свободном диске и без параллельной нагрузки.

**Цена.** Прогон полного набора приходится повторять, а на каждый уходит около трёх минут; при
этом покраснение выглядит как дефект и требует разбора. Отдельно опасно то, что такое падение
**маскирует настоящие**: в прогонах 1 и 2 вместе с платёжными падал файл гейта ссылок, и его
причина (лимит хука `beforeAll` в 10 с) была настоящим дефектом теста — её легко было списать на
тот же шум.

**Что делать (не сделано).** Поднять устойчивость к нагрузке в самих тестах: ждать признак
готовности процесса, а не фиксированный интервал; либо сериализовать файлы, поднимающие процессы,
через `poolOptions`/`fileParallelism`. Плюс отдельная задача — уборка `/private/tmp`: 116
worktree это не «мусор в темпе», а причина падений на диске в 98 %.

**Третье наблюдение, 24.08.2026, ветка `chore/td-dark-theme-role-tokens` (TD-42, правка не
касается платежей вовсе).** При `uptime` load average 15,6–17,96 и 12 параллельных
пользовательских сессий на машине (`who | wc -l`) все **четыре** файла семейства упали: не
только `payment-startup.test.ts` и `payment-secrets-rate.test.ts`, но и
`payment-persistence.test.ts`, `payment-release-modes.test.ts` — 40 из 40 тестов, поднимающих
процесс и читающих его `stderr`/факт прослушивания порта. Полный прогон дал 4 failed из 60
файлов (1094 passed из 1134). Повтор тех же четырёх файлов в изоляции секунды спустя, при
той же нагрузке, дал тот же результат (40 failed) — то есть при активной нагрузке 15+
«изоляция от других vitest» перестаёт помогать: предмет флака не файл-соседи по прогону, а
сама машина. Отношение к правке проверено прямо: ни один из четырёх файлов не ссылается на
`tokens.css`, `global.css` ни `a11y` (`grep` по всем четырём — пусто).


## TD-40. Визуальные эталоны отстали от кода на тринадцать коммитов и не пересняты ни разу с 06.08

**Файлы:** `web/tests/visual-baseline.spec.ts-snapshots/*.png` (12 снимков: 6 страниц × 2
вьюпорта), `web/tests/visual-baseline.spec.ts`
**Смежное:** TD-16 — тот же набор не исполняется гейтом публикации. Здесь предмет другой:
не «проверка не запускается», а «эталон, с которым она сравнивает, устарел».

**Проблема.** Снимки сняты один раз, коммитом `981477e` (06.08.2026, PR #27), и с тех пор
не обновлялись ни разу: `git log --oneline -- web/tests/visual-baseline.spec.ts-snapshots`
даёт ровно одну строку. За это время в `web/src/{components,pages,styles}` легло **13**
коммитов, среди которых редизайн формы оплаты (#151, `f25b193`) — а `/oplata` входит в
шесть эталонных страниц, — фильтр расписания по месяцу (#98), sanitizer и RichContent
(#114), матрица контуров оплаты (`a8a4bba`).

**Почему это не находка про конкретный PR.** Ревью PR #153 предъявило измеренное
расхождение (`Expected an image 1280px by 6026px, received 1341px by 6079px`, 12 %
пикселей) и отнесло его к этому PR. Отнесение неверно: эталоны разошлись с кодом **до**
него, и сколько из этих 12 % принадлежит какому коммиту, по снимкам не разделить. Именно
поэтому пересъёмка внутри тематического PR вредна: она сложила бы трёхмесячный дрейф
чужих правок в один коммит и объявила бы его «эталоном» без ревью того, что в нём
закрепляется.

**Что делать.** Пересъёмка отдельной работой, где предмет — сами эталоны: снять, глазами
разобрать diff по каждой из шести страниц, назвать, какое изменение чем вызвано. Плюс
решить, из какой среды снимать: закоммиченные снимки помечены `-darwin`, то есть сняты на
macOS, и на linux-раннере набор был бы другим — см. TD-16 про то, что в CI этот файл не
исполняется вовсе.

**Пока долг не закрыт**, зелёный `test:e2e:visual` локально невозможен, а красный ничего
не доказывает: он краснеет от накопленного дрейфа, а не от правки, которую проверяют.

**Дополнение 23.08.2026: шаблон семинара переверстан целиком** — расписание переехало из
таблицы под описанием в липкую боковую колонку (D3, PR #166). То есть у двух его эталонов
(`seminar-desktop-desktop-darwin.png`, `seminar-mobile-mobile-darwin.png`) к прежнему дрейфу
добавилось изменение самой раскладки, и разбирать их diff придётся уже по двум причинам
сразу.

Записано потому, что в ходе D3 они были пересняты и **откат сделан по этому же запрету**:
пересъёмка внутри тематического PR запекла бы трёхмесячный дрейф чужих правок в коммит про
расписание. Оправдание «шапка `visual-baseline.spec.ts` прямо называет раскатку варианта
поводом обновить эталоны» не работает: та фраза предполагает эталоны, совпадающие с кодом, а
здесь они не совпадают с 06.08.

**Дополнение 24.08.2026: change `social-accounts` (PR #168).** Подвал на всех 12 эталонах
меняет состав и подачу колонки «Подписывайтесь» (четыре аккаунта, официальные марки).
Эталоны **не переснимаются** в этом PR — Решение 10 того change; пересъёмка остаётся
отдельной работой TD-40.

## TD-41. Бюджет веса 404 поднимается подряд вслед за фактом

**Файлы:** `web/tests/seo-package.test.ts` (тест «404.html is lighter than 34KB»)

**Проблема.** Лимит прошёл 26 → 27 → 28 → 32 → 34 КБ. Каждый раз причиной был site-wide
элемент в шапке или подвале: навигация на 404, второй телефон (D12), официальные марки
соцсетей, затем Telegram как марка + две копии Rutube в DOM (`social-accounts`, замерено
33491 Б при прежнем пороге 32768). Каждый подъём назван и измерен — гейт не декоративен, —
но храповик, который всегда догоняет факт, перестаёт быть бюджетом: он фиксирует
достигнутое, а не удерживает границу.

**Что делать.** Не поднимать лимит снова молча. Либо назначить бюджет от требования
(какой вес страницы ошибки допустим в gzip на медленной сети) и держать его, либо
признать, что предмет гейта — не вес 404, а «шапка и подвал не растут незаметно», и мерить
именно это на всех страницах, а не на одной. Сокращение веса: одна тема Rutube в HTML
вместо двух (сейчас обе всегда в DOM, переключение CSS).

## TD-43. Шапка переполняется по горизонтали в зоне 901–973 px

**Про номер:** 42 занят открытым PR #157 (роли тёмной темы); взят следующий свободный,
чтобы не перенумеровывать чужую ветку.

**Файлы:** `web/src/components/topnav/TopNav.astro` (медиазапросы `max-width: 1440px`
и `max-width: 900px`)
**Смежное:** TD-4 — переполнение той же шапки при увеличенном корневом кегле. Здесь
предмет другой: базовый кегль, обычный вьюпорт, конкретная зона ширин.

**Проблема.** Между 901 и 973 px горизонтальное меню уже показано, а места ему всё равно
не хватает. Замерено на собранном `dist` страницы `/raspisanie-i-tseny`, chromium, обе
сборки на одной машине (переполнение в CSS-px):

| вьюпорт | `main` (`c128288`) | ветка `fix/demo-followups-red-green` |
|---|---|---|
| ≤ 900 | 0 | 0 |
| 901 | 209 | 73 |
| 940 | — | 34 |
| 974 и выше | 0 при ≥1180, но 110 при 1000 и 10 при 1100 | 0 |

**Дефект старше работы по списку заказчика, и эта работа его СОКРАЩАЕТ.** Ревью PR #153
сначала отнесло его к этой работе целиком; отнесение снято измерением контроля на `main`.
Промежуточная редакция ветки зону углубляла (272/173/73 при 901/1000/1100 — длиннее подпись
CTA), но ужатие шапки в полосе ≤1440, сделанное ради сохранения телефона на 1280 (решение
владельца 2026-08-23), вылечило её почти целиком: верхняя граница сдвинулась с 1172 до 973,
а на всех ширинах ветка теперь не хуже `main`.

**Что осталось.** Полоса 901–973 px: меню, кнопка и инструменты не помещаются даже ужатыми.
Ужимать дальше нечего без потери элементов — дескриптор под логотипом в этой полосе уже
скрыт, отступы меню и кнопки уже сокращены.

**Корень, найденный при разборе падения CI 23.08 и не видимый по вьюпорту.** У
`.topnav-bar` стоит `max-width: 1240px`, то есть выше 1240 шапка не получает больше места —
растут только поля по краям. Измерено на собранном `dist` (контент против контейнера):

| состояние | контент | контейнер | переполнение контейнера |
|---|---|---|---|
| ужата, 1 номер | 1240 | 1240 | 0 |
| ужата, 2 номера | 1240 | 1240 | 0 |
| не ужата, 2 номера | 1321 | 1240 | **81** |

Последняя строка держится на **всех** ширинах от 1601 и выше, включая 2560. За вьюпорт это
не выходит только потому, что контейнер отцентрован и излишек умещается в боковое поле —
то есть логотип и кнопка перестают быть выровнены с содержимым страницы, а гейт по
переполнению вьюпорта этого не видит по построению. Гейт на переполнение КОНТЕЙНЕРА здесь
не заводится сознательно: он родился бы красным, а красный гейт — это не гейт.

Отсюда же цена ошибки в порогах: у ужатого состояния запас в контейнере ровно 0. Пока порог
ужатия и порог второго номера стояли на одном значении 1440, шапка на 1441 одновременно
теряла ужатие и получала второй номер; на macOS это проходило впритык, на более широких
шрифтах Linux уезжало на 40 px, и поймал это собственный гейт в CI. Пороги разведены
(1440 и 1600), но запас остаётся нулевым — следующее добавление в шапку выльется снова.

**Что делать.** Мокап шапки, затем реализация: либо перенос строки меню, либо сокращение
состава меню, либо иная группировка действий. Отдельно решить судьбу дескриптора под
логотипом: вместе с двумя номерами и длинной подписью CTA он в 1240 px не помещается.

## TD-44. Два расходящихся исполнения одного приёма «содержимое плюс липкий сайдбар»

**Файлы:** `web/src/styles/layout.css` (`.content-sidebar-layout`,
`.content-sidebar-aside--sticky`), `web/src/pages/statyi/[slug].astro` (потребитель),
`web/src/pages/[institute]/[courseGroup]/[seminar].astro` (своя раскладка)

**Проблема.** Приём один, реализаций две, и константы у них разные:

| | статья | семинар |
|---|---|---|
| перелом | `min-width: 900px` | `max-width: 1024px` |
| ширина колонки | `--layout-aside-width: 280px` | `21rem` (336 px) |
| отступ липкости | `+1.5rem` | `+1rem` |
| предел высоты | нет | `100vh − header − 2rem`, своя прокрутка |

Замерено на собранном сайте: при 950, 1000 и 1024 px статья идёт в две колонки, а семинар в
одну; расходятся и отступы липкости (84 против 76).

**Почему это не исправлено в PR #166.** Обе константы семинара взяты из утверждённого
владельцем мокапа варианта C, а перелом общего примитива (900) в кадрах не показывался
вовсе; сведение к общему примитиву изменило бы поведение в полосе 900–1024, которую владелец
не видел. Плюс общий примитив не умеет предела высоты с внутренней прокруткой, а он —
принятое решение варианта C.

**Что делать.** Свести отдельной работой: либо параметризовать примитив (перелом и отступ
как переменные, предел высоты — необязательный модификатор) и перевести на него страницу
семинара, либо назвать два приёма разными и развести имена, чтобы следующий читатель не
считал одно другим. Первое лучше: сейчас правка примитива не доедет до семинара молча.

## TD-42. Роли `--text-primary`, `--text-inverse` и `--surface-inverse` в тёмной теме перевёрнуты дважды

**ЗАКРЫТ 2026-08-23 red/green, PR #157 (`fd63cf9`).** Убраны три строки повторного
переобъявления в `:root[data-theme='dark']` (`web/src/styles/tokens.css`) — роли,
объявленные в `:root`, сами резолвятся в верные для тёмной темы значения через каскад, раз
базовые токены `--color-dark-700`/`--color-light-100` в этом же блоке уже инвертированы.
`--surface-subtle` не тронут: у него свой per-тема литерал, а не алиас на инвертированный
токен.

Красная проверка (вычисленный контраст, не текст токена) — `web/tests/a11y.spec.ts`, describe
«Роли текста и поверхностей не инвертируются дважды в тёмной теме»: до фикса 2 failed из 3
(`--text-primary` на `--surface-page` и `--surface-inverse` на `--surface-page` — по 1.00:1
при требуемых 4.5:1 и 3:1), после — 3 passed. Негативная проверка: `git revert --no-commit`
фикс-коммита → пересборка → те же два теста красны по имени, третий зелёный; восстановление
подтверждено `git diff <fix-sha> --name-only` (пусто) — не голым `git checkout --`, который в
момент мутации взял версию из индекса, а не из HEAD (пойманная в той же сессии ошибка).

**Уточнение охвата, найденное независимым ревью при закрытии.** Формулировка ниже («на боевых
страницах роль не используется») устарела уже к моменту открытия PR #157: между записью долга
и им слился PR #166 (D3), чей `SeminarSchedulePanel.astro` использует `--text-primary` на
боевой странице семинара без `DEMO_FORMS`-гейта. До фикса дата и цена семинара в тёмной теме
на реальной странице были фактически невидимы (1.00:1) — дефект был не только демо-стендовым.
Полная история red/green, мутации и это уточнение — в описании PR #157.

Запись ниже сохранена как история.

**Где.** `web/src/styles/tokens.css`, блок `:root[data-theme='dark']` — строки 167–168 и 166
на `origin/main@e0c4d1d8b94647f018dec6bd518855cfd2553fcf`.

**Что не закрыто.** В `:root` роль объявлена как `--text-primary: var(--color-dark-700)`. Тот
же блок тёмной темы уже переворачивает базовые токены: `--color-dark-700: #eceeec` (светлый),
`--color-light-100: #171a17` (тёмный). Этого достаточно — роль разрешилась бы в светлый
`#eceeec` сама. Но блок переобъявляет её ещё раз, `--text-primary: var(--color-light-100)`, и
получается вторая инверсия: «основной текст» в тёмной теме становится почти чёрным.

Измерено в браузере на сборке этого SHA: текст `rgb(23, 26, 23)` на фоне `rgb(31, 36, 31)`,
**контраст 1,11:1** при требуемых 4,5:1 — надпись не видна. Тем же механизмом задеты
`--text-inverse` (становится светлым там, где должен быть тёмным) и `--surface-inverse`.

**Почему сейчас не блокирует (утверждение устарело к моменту фикса — см. ниже).** На боевых
страницах роль не используется: все девять секций главной (`HeroHybrid`, `Advantages`,
`Approach`, `Programs`, `Segments`, `UpcomingSeminars`, `Teachers`, `News`, `CtaBand`) берут
`--color-dark-700` напрямую — ноль вхождений `var(--text-primary)`, проверено поимённо. Все
44 вхождения роли живут в секциях-прототипах каркаса и в `SeminarArchitectureHeader`, которые
импортируются только страницами `/preview/*`, а те собираются лишь при `DEMO_FORMS`. То есть
на момент этой записи (`origin/main@e0c4d1d8b94647f018dec6bd518855cfd2553fcf`) дефект виден
на демо-стенде и не виден на проде.

**Уточнение 24.08.2026, найдено независимым ревью при закрытии долга.** Между записью долга и
началом его исправления в `main` слился PR #166 (D3, расписание семинара в липкой боковой
колонке) — и его `SeminarSchedulePanel.astro` использует `--text-primary` для `.schedule-date`
и `.schedule-price` (строки 240, 292), без какого-либо `DEMO_FORMS`-гейта, на боевой странице
`web/src/pages/[institute]/[courseGroup]/[seminar].astro`. Утверждение «дефект виден только на
демо» **устарело уже к моменту создания ветки-фикса**, и это не было перепроверено при
открытии PR #157. Практически: до фикса ниже дата и цена семинара при включённой тёмной теме
на реальной странице семинара давали 1,00:1 — были фактически невидимы для живого посетителя,
а не только на `/preview/*`. Фикс (см. «Чем лечится») закрывает и этот случай тем же
механизмом каскада, так что отдельного повторного лечения не требуется — исправлена
формулировка, а не код.

**Почему это всё равно долг, а не «не наша забота».** Смысл этих токенов объявлен в самом
файле: «фундамент под master template», значения названы по роли, чтобы вариант оформления
менялся сменой значений, а не правкой десятков файлов. Любой новый компонент, который примет
роли, в тёмной теме исчезнет. Это не гипотеза: первый же компонент, написанный на ролях —
секция отзывов в мокапах доделок после демо (PR #155) — исчез с первой попытки, и дефект
нашёлся именно так. Демо-стенд к тому же часть продукта: его проверки входят в обязательный
прогон `Tests`.

**Чем лечится.** Убрать переобъявление ролей в блоке тёмной темы там, где базовый токен уже
перевёрнут: `--text-primary`, `--text-inverse` и `--surface-inverse` должны разрешаться через
уже инвертированные `--color-dark-700` / `--color-light-100`, а не назначаться повторно.
Порядок — red/green: сначала падающая проверка контраста роли в тёмной теме, потом правка.
Проверку ставить на **вычисленный** контраст, а не на присутствие строки в CSS: правило,
сверяющее текст файла, не заметит следующей такой инверсии через другой токен.

**Отношение к остальному.** Родственно классу «проверка ничего не измеряет»: тёмную тему у
нас не снимают визуальные эталоны и не проверяет ни один гейт, поэтому дефект такого рода
может жить неограниченно долго. Найден он не проверкой, а тем, что мокап впервые нарисовали в
тёмной теме.

## TD-45. `dependabot-auto-merge-policy.yml` зовёт метод слияния статическим литералом

**Что наблюдалось.** Живой дефект (`openspec/changes/dependabot-auto-merge/evidence/live-rollout.md`,
§15): джоб «Policy / Enable native auto-merge» вызывал `gh pr merge --auto --merge
--match-head-commit`, а репозиторий разрешает только squash (`allow_merge_commit: false`,
`allow_squash_merge: true`). Все 6 фактических исполнений джоба за всё время (24.08.2026)
падали одной ошибкой `GraphQL: Merge method merge commits are not allowed on this
repository`. Исправлено в PR #188 (`12814d0`) заменой литерала на `--squash`.

**Почему это всё равно долг.** Фикс заменил один жёстко зашитый литерал на другой — тот же
класс регрессии повторится, если политика репозитория снова изменится (снова разрешат
merge-commit, отключат squash), и обнаружится это опять только по живому падению джоба, а не
заранее. Независимое ревью на PR #188 назвало это прямо: «тот же класс регрессии, только с
другим значением».

**Чем лечится.** Перед вызовом `gh pr merge --auto` запрашивать текущую политику репозитория
(`gh api repos/$GITHUB_REPOSITORY --jq '{allow_merge_commit,allow_squash_merge,allow_rebase_merge}'`)
и выбирать первый разрешённый метод из списка приоритета, а не полагаться на литерал,
зафиксированный на момент написания workflow. Не сделано в PR #188 намеренно: это расширение
поведения сверх минимального red/green-фикса найденного дефекта, а не часть самого фикса.

**Ответ на этот вопрос уже был в репозитории до дефекта.** Прероквизиты дельты
(`openspec/changes/dependabot-auto-merge/evidence/repository-prerequisites.md:45`,
`"mergeMethod": "SQUASH"`) зафиксировали правильный метод ещё 2026-08-18 — раньше, чем в коде
появился жёсткий `--merge`. Сверка реализации с этим файлом на этапе кода предотвратила бы
все шесть падений.

## TD-46. Хеш официальной марки сверяет ассет, а страница рендерит копию в `.astro`

**ЗАКРЫТ 2026-08-25** в PR #168: `web/src/lib/social-mark-sync.ts` —
`assertMarkAstroBodiesMatchAssets()` (нормализованное тело `<svg>` ассета =
`*Mark.astro`, допустимы XML-prolog и `aria-hidden="true"` на корне). Вызов из
`assertMarkFilesMatchRegistry()` при сборке подвала. Спека/design Decision 17
обновлены: исключение `rich-content-safety` + обязательный гейт равенства копий.

**Файлы:** `web/src/assets/social-marks/*.svg`, `web/src/lib/social-mark-files.ts`
(`assertMarkFilesMatchRegistry`), `web/src/lib/social-mark-sync.ts`,
`web/src/components/social-marks/*Mark.astro`,
`web/src/lib/social-marks-registry.ts`

**Проблема (историческая).** Задача 3.2 / Decision 17 change `social-accounts` требует, чтобы показанная
марка совпадала с лицензионно проверенным первоисточником. Гейт хешировал файлы в
`web/src/assets/social-marks/`. На страницу попадала независимая разметка в
`*Mark.astro`. Негативная мутация §5.6
(ветка `test/social-accounts@3ecd0ec468c08ff787416f0fa657b67d8101de18`): смена
цвета только в `.astro` прошла сборку без ошибки хеша.

## TD-47. Измерение бюджетов Lighthouse грузит внешние виджеты (external-widgets) живыми

**Файлы:** `web/lighthouserc.cjs` (`staticDistDir: './dist'`)

**Проблема.** Спека change `external-widgets` требует, чтобы браузерный прогон, чей
предмет не является сторонним сервисом, не зависел от его доступности — обеспечивается
перехватом стороннего трафика (`web/tests/helpers/third-party-guard.ts`). У измерения
бюджетов Lighthouse хука для такой подмены нет вовсе: оно запускает настоящий браузер по
уже собранному боевому `dist`, где виджет отзывов и загрузчик чата обращаются к настоящим
хостам напрямую. Требование о перехвате к этому измерению неприменимо технически, а не по
недосмотру, но это цена, а не закрытая забота — без записи следующий читатель принял бы её
за отсутствующую.

**Что делать.** Если бюджет начнёт флакать из-за недоступности стороннего сервиса —
считать это подтверждением записи, а не новым дефектом. Закрывать зависимость можно только
переносом измерения на артефакт без живых сторонних встраиваний (например, сборка с
объявленным отсутствием чата) либо появлением у `lighthouserc.cjs` собственного хука
перехвата — оба варианта вне рамок текущего change.

## TD-48. Недоступность сторонних сервисов у внешних виджетов (external-widgets) не охраняется в CI — прогон эталонов вне гейта

**Файлы:** `web/tests/visual-baseline.spec.ts`, `web/tests/browser-test-gating.test.ts`
(`ACKNOWLEDGED_DEBT`, запись `'visual-baseline.spec.ts'`)

**Проблема.** Сценарий change `external-widgets` «недоступность стороннего сервиса не
меняет результат сравнения облика» логически проверяется в `visual-baseline.spec.ts`, но
этот файл целиком числится в списке признанного долга браузерных проверок — ни один
гейтующий workflow его не исполняет. Значит требование существует в тексте, но не
охраняется автоматически: расхождение может появиться и остаться незамеченным сколь угодно
долго.

**Что делать.** Требование останется неохраняемым до тех пор, пока `visual-baseline.spec.ts`
не выйдет из `ACKNOWLEDGED_DEBT` и не встанет в обязательный прогон — эта работа
самостоятельная и не входит в рамки текущего change.

## TD-49. Доступность внутри стороннего виджета чата (external-widgets) не гарантируется

**Файлы:** `web/tests/helpers/third-party-guard.ts` (подставной интерфейс чата),
компоненты фасада чата возможности `external-widgets`

**Проблема.** Наш контракт — доступный триггер, фокус-ловушка и закрытие фасада; markup
ВНУТРИ загруженного стороннего виджета чата нам не принадлежит и не проверяется теми же
гейтами, что наш собственный markup (`a11y.spec.ts` исключает сторонние iframe явно по той
же причине). Доступность интерфейса, который рисует сторонний загрузчик после ответа,
остаётся известным отклонением, а не выполненным требованием.

**Что делать.** Проверять можно только то, что контролируем: наш триггер, наш `aria-*` и
наш фокус до открытия чата. Аудит доступности самого стороннего интерфейса — задача
поставщика виджета, не этого репозитория.

## TD-50. Часы работы недоступны на обычной странице, если загрузчик чата (external-widgets) не ответил

**Файлы:** компоненты фасада чата возможности `external-widgets`
(`MANAGER_HOURS_ATTR`/`MANAGER_HOURS_VALUE`, `web/tests/helpers/external-widgets.ts`)

**Проблема.** Прежняя редакция контракта обещала часы работы и телефон без условия открытия
чата. Текущая спека сужает это: часы работы читаются только из ответа стороннего загрузчика
после нажатия кнопки чата, поэтому если загрузчик не ответил (сторонний сервис недоступен
или медленный) либо пользователь чат не открывал, часы работы вне страниц контактов и
оплаты узнать нельзя. Формулировка «наша гарантия — телефоны в подвале» тихо роняет
половину прежнего обещания — умолчание об этом читалось бы как выполненное требование.

**Что делать.** Если понадобится гарантия часов работы без открытия чата — часы должны
идти статическим содержимым страницы (как телефон), а не полем ответа стороннего
загрузчика; это отдельная работа за рамками текущего change.

## TD-51. Политика конфиденциальности не называет чат и счётчик аналитики внутри виджета отзывов

**Файлы:** `web/public/terms/*.pdf` (исходника в репозитории нет),
`web/tests/external-widgets-privacy-doc.test.ts` (три `it.skip`)

**Проблема.** Спека change `external-widgets` требует, чтобы документ о персональных
данных называл: (1) сбор данных через чат на сайте, (2) получателя этих данных —
оператора портала Bitrix24, (3) стороннего счётчика аналитики (Яндекс.Метрика),
работающего внутри виджета отзывов. Действующий PDF ничего из этого не называет —
исходник, из которого он собирается, в репозиторий не передан (`docs/client-requests.md`,
пункт 17). Без исходника вносить правки нечем: единственный законный путь — либо
получить редактируемый исходник, либо явное согласие заказчика, что текст правим мы и
присылаем на утверждение (это и есть формулировка пункта 17, дедлайн +5 раб. дней,
мягкий).

Пункт 17 отправлен заказчику отдельным вопросом и НЕ решён на момент записи. Три
проверки временно переведены в `it.skip` с явной причиной — это не легализация дефекта:
требование спеки не переписано, оно по-прежнему `SHALL`, а тесты остаются в файле и
покраснеют обратно, как только `.skip` снят.

**Что делать.** Как только придёт ответ по пункту 17: получить исходник либо составить
текст самостоятельно и отправить на утверждение → обновить `web/public/terms/*.pdf` →
снять `.skip` с трёх тестов в `external-widgets-privacy-doc.test.ts` → закрыть TD-51.
Change `external-widgets` не архивируется, пока это не сделано.

## TD-52. Кнопка чата перекрывает нижний контент на мобильном (375px) на семи шаблонах

**Файлы:** `web/src/components/chat/ChatFacade.astro`, `web/src/styles/widgets.css`,
`web/tests/external-widgets.spec.ts` (`KNOWN_MOBILE_OVERLAP`, mobile-проект, семь `test.skip`)

**Проблема.** На ширине 375px кнопка вызова чата (32×32px + отступ) реально перекрывает
нижний контент семи шаблонов: `home` (карточка «Ближайший семинар»), `course` (карточка
модуля программы), `seminar`/`seminar-dated`, `kontakty` (ссылка `tel:`), `statyi`
(карточки статей), `raspisanie` (`<select>` выбора города). Измерено `getBoundingClientRect`
— не тестовый дефект и не сторонний баг (`.topnav-drawer` за счёт `position: absolute`
даёт похожий, но ОТДЕЛЬНЫЙ класс ложных перекрытий, устранённый ранее той же сессией
ревью — см. коммит SHA c1937b5 и комментарий над `insideClosedDetails`).

Список — по факту двух воспроизводимых прогонов CI (`Playwright smoke`,
`--project=mobile`), а не по прежней таблице в `docs/handoff/external-widgets-impl-
surface.md` §6: та писалась раньше, при более коротком `TEMPLATES`, и называла 5 из
тогдашних 11 шаблонов, а не семь из текущих. Таблицу стоит обновить при следующей
правке §6, здесь не тронута намеренно — не предмет этого TD.

Точечного CSS-фикса нет: у нескольких шаблонов контент занимает почти всю высоту
вьюпорта у нижнего края при скролле 0 (например, карточка семинара — 196px из 204px
свободных), и никакой разумный размер/позиция кнопки не даёт безопасного угла без
одного из двух вмешательств за пределами компонента чата — правки чужой вёрстки
(редизайн) или нового поведения кнопки на скролле (не описано спекой). По правилу
`AGENTS.md` «Изменения облика» это не точечный фикс позиции, а решение о поведении —
принято владельцем (2026-08-25) как **известное отклонение**, а не решается кодом. Семь
проверок переведены в `test.skip` на mobile-проекте (desktop эта же проверка по-прежнему
покрывает) с явной причиной и ссылкой на TD-52 — требование спеки не переписано.

**Что делать.** Один из двух путей: (а) мокап поведения кнопки на мобильном (например,
скрывать/сдвигать до прокрутки мимо героя) → `/opsx:update` спеки под новое поведение →
снять `.skip`; (б) `/opsx:update` спеки с явным исключением для перечисленных шаблонов на
375px и обоснованием → снять `.skip` без изменения кода. Ни один путь не выбран на
момент записи. Change `external-widgets` не архивируется, пока TD-52 не закрыт.

**Отложено явно (владелец, 2026-08-29):** оформлено отдельной задачей —
`openspec/changes/external-widgets/tasks.md`, задача 1.5a — сейчас не делаем, код и `.skip`
не трогаются.

## TD-53. Позитивный контроль датозависимого фрагмента (знаки наград) не построить — `DECLARED_AWARD_BADGES` пуст

**ЗАКРЫТ 2026-08-29, решением владельца, НЕ закрытием задачи 1.3.** Владелец решил считать
условия использования марки «Хорошее место 2026» соблюдёнными без отдельной проверки правил
сервиса (задача 1.3 `tasks.md` формально не закрыта) и разместить знак на этом основании —
`markUsageEvidence` называет это допущением прямо, а не выдаёт за проверенный факт
(`web/src/lib/award-badges.ts`). Знак «Здесь хорошо!» (2ГИС) в данные не добавлен: у ИКПК нет
подтверждённой карточки в 2ГИС (`docs/client-requests.md`, пункт 13, не решён) — отдельное,
фактическое основание, которое решение о марке не снимает. `describe.skip` в
`external-widgets-build-year.spec.ts` снят, три теста зелёные; попутно найден и исправлен
независимый дефект — `test.beforeAll` в этом файле ни разу не запускался, пока `describe`
был скрыт, и его собственный таймаут (10с, умолчание `playwright.config.ts:16`) не
покрывался `describe.configure({ timeout: 15 * 60 * 1000 })` — тот governит таймаут тестов, а
не хуков; добавлен `testInfo.setTimeout(15 * 60 * 1000)` внутри `beforeAll`. Две новые записи
свидетельств — `docs/handoff/external-widgets-acceptance-evidence.md`, «Право использования
знака…» и «Подтверждение награждения…» — понадобились сразу: `declaredBadgeCount()` считает
длину массива, и появление одной записи сделало применимыми оба приёмочных пункта.

**Файлы (исходная проблема, до закрытия):** `web/src/lib/award-badges.ts`
(`DECLARED_AWARD_BADGES: [] `), `web/tests/external-widgets-build-year.spec.ts` (весь
`describe.skip`)

**Проблема.** `external-widgets-build-year.spec.ts` проверяет, что строка знаков наград
корректно реагирует на смену `BUILD_YEAR`, и первым шагом строит ОБЯЗАТЕЛЬНЫЙ
позитивный контроль: без хотя бы одного знака с объявленным `year` в
`DECLARED_AWARD_BADGES` состав знаков не может измениться ни от какого `BUILD_YEAR`, и
проверять зависимость не на чем (`declaredBadgeYear()`, `web/tests/external-widgets-
build-year.spec.ts:78`, fail-closed throw). Массив пуст: задача 1.3 tasks.md
(«Проверить условия использования марок «Хорошее место» и «Здесь хорошо!» на сайте») не
закрыта, а без подтверждённого `markUsageEvidence` знак не может законно объявиться
(комментарий над `DECLARED_AWARD_BADGES`).

Добавить знак БЕЗ `markUsageEvidence` ради одного этого теста нельзя: `declaredBadgeCount()`
(`web/tests/external-widgets-acceptance.test.ts:138`) считает длину массива, а не
видимые знаки, и появление хотя бы одной записи сделало бы применимыми два пункта
приёмки («Право использования знака…», «Подтверждение награждения…»), под которые нет
свидетельства — красное просто переехало бы в другой файл.

Ни один из трёх тестов файла не запускается независимо: `test.beforeAll` (общий на
`describe.configure({ mode: 'serial' })`) зовёт `declaredBadgeYear()` первым, и его throw
останавливает файл целиком раньше первого теста — поэтому `.skip` стоит на всём
`describe`, а не на одном тесте.

**Что сделано вместо этого.** Задача 1.3 не закрыта — вместо неё владелец 2026-08-29 принял
риск и решил не проверять условия использования марки отдельно (см. «ЗАКРЫТ» выше).
`DECLARED_AWARD_BADGES` несёт одну запись («Хорошее место 2026»), `.skip` снят, приёмочные
свидетельства записаны. TD-53 закрыт этим решением, а не выполнением исходного плана —
задача 1.3 в `tasks.md` остаётся невыполненной, и это named known deviation, а не забытый
пункт.

## TD-54. `Deploy to GitHub Pages` гарантированно красный — Pages не включён на уровне репозитория

**Файлы:** нет — чисто наблюдаемое поведение, код и тесты не менялись.

**Проблема.** GitHub Pages не включён на уровне настроек репозитория
(`gh api repos/pgorbachev/ikpk/pages` → 404: `Not Found`, «Ensure GitHub Pages has been
enabled»), а фактическая публикация идёт на VPS (`docs/deploy-vps.md`,
`scripts/deploy-web.sh`) — Pages была вторым, интерим-путём без `CNAME`. Из-за этого
каждый успешный прогон `Tests` на `main` порождает гарантированно красный запуск
`Deploy to GitHub Pages` (`HttpError: Not Found`, `Failed to create deployment
(status: 404)`). Публикацию это не блокирует — `Deploy to GitHub Pages` не входит ни в
обязательные контексты branch protection, ни в гейт публикации (`Tests` — единственный
workflow, событие об успехе которого что-либо запускает), это постоянно красный, но
некритичный прогон в ленте Actions.

**Рассмотренный и отклонённый путь.** Отключение триггера `workflow_run` в
`.github/workflows/deploy.yml` (оставить только `workflow_dispatch`) устраняет красноту,
но ломает 14 тестов в `demo-gate.test.ts`/`deploy-gating.test.ts` и, при дальнейшей
проверке, ещё 6 в `browser-test-gating.test.ts`/`social-accounts.test.ts` — ≈20 тестов в
4 файлах, а не 2, как показалось сначала. Все они намеренно выводят «обязательный
прогон» не по имени, а из структуры гейта (`workflow_run.workflows` у публикующего
workflow, `workflowRunTrigger`, `web/tests/helpers/workflows.ts`), и без триггера теряют
предмет. Правка отменена целиком (`.github/workflows/deploy.yml`,
`web/tests/demo-gate.test.ts`, `web/tests/deploy-gating.test.ts` возвращены к
зафиксированному состоянию); владелец предпочёл известный красный прогон правке такого
объёма ради косметики.

Отдельно: `deploy-gating` сам по себе не трогается — `openspec/changes/cms-content-publication`
(активный, ещё не смерженный change) содержит собственную `MODIFIED`-дельту на него,
переработанную под сценарий внешнего CMS-контента, и любая правка `deploy-gating` сейчас
означает переписывать код, на который эта дельта уже опирается.

**Что делать.** Один из двух путей, решает владелец: (а) включить Pages на уровне
репозитория (`gh api --method PUT repos/pgorbachev/ikpk/pages …` или через UI) — тогда
`Deploy to GitHub Pages` начнёт проходить зелёным без изменения кода вовсе; (б) решить,
что Pages не возвращается никогда → отдельная задача, дожидающаяся разрешения конфликта
с `cms-content-publication`, на снос `workflow_run`-триггера и адаптацию всех ≈20
тестов в 4 файлах под новый контракт (не просто `.skip`, а переписывание — они
проверяют реальный инвариант, который в этом случае исчезнет). Ни один путь не выбран
на момент записи.

## TD-55. Знак награды на главной не соответствует утверждённому мокапу — нет иконки и подписи источника

**ЗАКРЫТ 2026-08-30; визуальная сверка усилена 2026-09-04.** `BadgeDeclaration` получил
типизированное поле `provider`: его стабильный идентификатор выбирает собственную SVG-иконку
Яндекс Карт или 2ГИС, а `provider.label` даёт отдельную подпись источника без разбора `id`
или URL.
Компонент `AwardBadge.astro` теперь воспроизводит именно утверждённый variant E: у Яндекса
красная метка 18×18 на серой круглой подложке 26×26; у 2ГИС — оранжево-зелёная плитка с
синим кругом и белой звездой; у белого pill-чипа сохранены тонкая граница и лёгкая тень.
Строка знаков резервирует высоту чипа и сохраняет инвариант смены `BUILD_YEAR`.

Регресс закрыт тремя уровнями: `external-widgets-dist.test.ts` fail-closed проверяет
непустой набор знаков и наличие ровно одной иконки, отдельного названия и источника;
`award-badge-render.test.ts` рендерит отдельные фикстуры обоих провайдеров и фиксирует
их SVG, размеры подложки и тень; `external-widgets-build-year.spec.ts` в тёмной теме
утверждённого кадра измеряет размеры 26×26 и 18×18, белую поверхность чипа, серую
подложку, тень и геометрию двух строк вместе с прежними рамками строки. RED на исходном `d180779`:
`у знака нет ровно одной сервисной иконки`, 0 вместо 1; GREEN после исправления — 1 passed.
Негативная мутация после фикса — удалить `data-award-icon` у Яндекс SVG — красит тот же
поимённый тест; после восстановления tracked-дерево чистое, checksum `dist` до и после
совпал (`bf7aaefd2928fcb67573cc8c50ab216ac6413c21`). Реестры исполняемого вывода
перегенерированы штатно в порядке `generate-baseline` → `generate-occurrences` по двум
деревьям (`dist`, `dist-demo`).

**Файлы:** `web/src/components/home/AwardBadge.astro:11`, `class="award-badge"`
(разметка знака), `web/src/components/home/AwardBadge.astro:46`,
`.award-badge {` (стили), `web/src/lib/award-badges.ts:41`,
`export interface BadgeDeclaration {`

**Проблема.** Утверждённый мокап (`docs/demo-2026-08-23-mockup-choice.md`, выбор
владельца 23.08.2026, «вариант E» — раскладка D + иконки в трактовке C) показывает знак
награды как чип с **цветной иконкой** (метка для Яндекса, лента/медаль для 2ГИС) и
**двумя строками текста** — жирное название («Хорошее место 2026») и серым источник
(«Яндекс Карты») под ним. Референс — `docs/design/mockups/demo-followups/variant-e/
home-1280d-02-reviews-block.png`.

Реализация до исправления на `d180779` (`Reviews.astro`) рисовала знак как один голый текст
(`{badge.label ?? 'Знак награды'}`) в рамке — без иконки и без второй строки с
источником. Разрыв стал видимым только сейчас, при закрытии TD-53: до неё
`DECLARED_AWARD_BADGES` был пуст, рисовать было нечего, и расхождение с мокапом ничем
не проявлялось. Сама разметка/стили знака не менялись с исходной реализации (PR #187,
change `external-widgets`, шаг 4) — это её изначальный пробел, не новый регресс.

**Что НЕ является этой находкой.** Широкая лента из четырёх карточек отзывов на том же
кадре мокапа — это ЗАЯВЛЕННАЯ заглушка («Такой раскладки официальный виджет Яндекса не
даёт — форму подсмотрели у конкурента», подпись на кадре), и её замена на компактную
карточку официального виджета — принятое, документированное отклонение
(`openspec/changes/external-widgets/proposal.md`, «Оба встраивания... официальный
виджет Яндекс Карт»). Пустое серое поле под карточкой рейтинга на реальном стенде —
следствие ЭТОГО принятого отклонения, а не дефект. TD-55 — только про сам чип знака,
который мокап не объявлял заглушкой.

**Что требовало решения до исправления и как решено:**
- официальных изображений знаков не выдаёт ни один из двух сервисов (`docs/design/
  mockups/demo-followups/README.md:134`, «официальных марок для сайта ни один из двух
  сервисов не выдаёт (finding 22)») — иконки в самом мокапе тоже нарисованы вручную
  (SVG), не взяты официально. Поэтому и здесь иконка рисуется, а не скачивается;
- в `BadgeDeclaration` (`web/src/lib/award-badges.ts`) добавлен объект `provider`: его
  `label` даёт имя источника («Яндекс Карты» / «2ГИС») отдельной строкой без разбора
  `id` или `sourceUrl`;
- иконка выбирается по `provider.id` для обоих сервисов (Яндекс и 2ГИС), хотя сейчас
  данными заполнен только Яндекс (TD-53). Поэтому добавление 2ГИС после решения
  `docs/client-requests.md`, пункта 13, не потребует переделывать компонент;
- по правилу `AGENTS.md` «Изменения облика: мокапы до внедрения» новых мокапов здесь
  НЕ требуется — целевой облик уже утверждён и приложен (кадр variant-e выше), это
  доработка существующей реализации до уже принятого решения, а не новое решение об
  облике.

**Контекст для передачи.** Это change `external-widgets` (уже частично смержен:
спека — PR #163, тесты — PR #171/#182, реализация — PR #187, данные знака — PR #201).
Change ещё НЕ заархивирован — держат TD-51 (документ о ПДн, ждёт заказчика) и TD-52
(перекрытие кнопки чата на мобильном, отложено владельцем 2026-08-29). Закрытие TD-55
не разблокировало архивирование само по себе, но убрало реальное визуальное расхождение
с утверждённым обликом.

**Что сделано.** `.award-badge` в `AwardBadge.astro` получил иконку (свой SVG, как в
мокапе — не официальный ассет, его нет) и вторую строку с именем источника, выбираемые
по сервису знака (сейчас в данных — только Яндекс). Целевой вид сверяется с
`docs/design/mockups/demo-followups/variant-e/home-1280d-02-reviews-block.png`.
Регресс-тесты проверяют, что это не ломает `web/tests/external-widgets-build-year.spec.ts`
(явное исключение содержимого `[data-award-row]` через внедрённый `visibility: hidden`,
сохраняющее рамку строки) и `external-widgets-dist.test.ts` (запрет
посторонних `<img>` в секции отзывов вне знака — новая иконка внутри знака сама по себе
не «посторонняя фотография»; признак `subtreeHasBadgeAncestor` остаётся зелёным).

## TD-56. Журнал происхождения не пишется на событие правки — только читается публикующим прогоном

**Файлы:** `openspec/specs/cms-content-source/spec.md:105`, `` **Известное отклонение
(TD-56).** `` (сам текст требования и оговорки); `web/scripts/lib/provenance-ledger.ts:122`,
`async recordEvent(event: { fingerprint: string; marker: EventMarker | null }): Promise<LedgerEntry> {`
(писатель существует, но не вызывается извне модуля); `web/scripts/capture-content-snapshot.ts:40`,
`copyPinned();` (снимок берётся из фикстуры независимо от `CMS_URL`).

**Проблема.** Требование «Возраст состояния контента определяется журналом происхождения»
(`openspec/specs/cms-content-source/spec.md`) описывает журнал, в который запись добавляется
**на событие изменения контента**. По факту сегодня ни один workflow не вызывает
`recordEvent`/`acceptState` — `grep` по `.github/workflows/*.yml` и `web/scripts/*.ts` (кроме
самого модуля и тестов) даёт ноль совпадений. Публикующий прогон (`publication-cli.ts`) только
**читает** журнал через `observe()`, сверяя текущий снимок с уже существующими записями; новых
записей на живую правку контента взяться неоткуда, потому что живого источника правок
(REST/вебхук CMS) у снимка тоже нет — `capture-content-snapshot.ts` безусловно копирует
закреплённую фикстуру.

Найдено при попытке заархивировать `cms-content-publication` (PR #199 смержен 2026-08-30,
несмотря на предупреждение в PR #203, поднятое накануне) — архивирование перенесло это `SHALL`
в основную спеку без оговорки, и `openspec/specs/` начало описывать поведение, которого в
проде нет. Эта запись и правка `spec.md:105` — устранение того конкретного расхождения; TD-56
остаётся открытым до появления живого писателя.

**Что НЕ является этой находкой.** Свойства журнала, которые проверяются для уже существующих
записей (неизменяемость, возрастающие номера, невозрастающая высшая отметка, различение
устаревания и регресса) — реализованы и покрыты тестами (`web/tests/cms-provenance-ledger.test.ts`,
`web/tests/cms-publish-gate.test.ts`), это не они под вопросом. Под вопросом только источник
**новых** записей на живую правку.

**Что требует решения.** Закрывается тем же change/шагом, что подключит живой REST к CMS
(снимет отложенность задач 3.4–3.9a бывшего `cms-content-publication`, теперь в
`openspec/specs/cms-content-source/spec.md`): нужен вебхук или polling от системы управления,
вызывающий `recordEvent` с верным маркером (`edit`/`restore`/`initial-migration`) на каждое
изменение опубликованной записи. До тех пор гейт публикации работает в режиме
observe + pinned-bootstrap, как и было явно решено при реализации (7.5a в архиве
`openspec/changes/archive/2026-08-29-cms-content-publication/tasks.md`).
