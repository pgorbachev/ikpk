# Архитектура нового решения

## Обзор

Новый сайт — **Astro (статический генератор) + Strapi CMS**.
Фронтенд и бэкенд полностью разделены. Каждый компонент можно заменить независимо.

## Стек технологий

| Компонент | Технология | Назначение |
|-----------|-----------|------------|
| Фронтенд | Astro (SSG) | Статические HTML-страницы, 0 KB JS по умолчанию |
| CMS | Strapi | Админ-панель с веб-интерфейсом + REST API |
| Поиск | Pagefind | Полнотекстовый поиск в браузере, без сервера |
| Интерактив | React (islands) | Только для форм и фильтров |
| Хостинг фронта | CDN (Netlify/Vercel) | Статика на edge-серверах по всему миру |
| Хостинг CMS | VPS / Cloud | Отдельный сервер для Strapi |
| Хранилище медиа | Yandex Cloud Storage | Без изменений, совместимо |

## Архитектура

```
┌─ CDN (Netlify / Vercel) ─────────────────────────┐
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │              Astro (SSG)                     │  │
│  │                                              │  │
│  │  256 статических HTML-страниц                │  │
│  │  0 KB JavaScript (по умолчанию)              │  │
│  │                                              │  │
│  │  React islands (по необходимости):           │  │
│  │  • Поиск по сайту (Pagefind)                 │  │
│  │  • Фильтр расписания                        │  │
│  │  • Форма записи на семинар                   │  │
│  │  • Форма подписки на новости                 │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  Статика: HTML + CSS + шрифты + SVG               │
│  Раздаётся с ближайшего edge-сервера              │
└────────────────────────┬───────────────────────────┘
                         │
              (данные при сборке)
                         │
┌────────────────────────▼───────────────────────────┐
│                  VPS / Cloud                        │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │            Strapi CMS                        │  │
│  │                                              │  │
│  │  Админ-панель:  https://cms.ikpk.su/admin    │  │
│  │                                              │  │
│  │  • Веб-интерфейс для контент-менеджера       │  │
│  │  • WYSIWYG-редактор                          │  │
│  │  • Загрузка изображений                      │  │
│  │  • Роли: администратор / редактор            │  │
│  │                                              │  │
│  │  REST API:                                   │  │
│  │  • /api/institutes                           │  │
│  │  • /api/seminars                             │  │
│  │  • /api/articles                             │  │
│  │  • /api/teachers                             │  │
│  │  • /api/schedule-entries                     │  │
│  │  • ...                                       │  │
│  │                                              │  │
│  │  ┌────────────────────────────────────────┐  │  │
│  │  │          База данных (PostgreSQL)      │  │  │
│  │  └────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────┘  │
│                                                     │
│  ┌──────────────────────────────────────────────┐  │
│  │        Yandex Cloud Storage (медиа)          │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

## Поток обновления контента

```
Контент-менеджер
       │
       ▼
┌──────────────────┐
│  Strapi          │
│  админ-панель    │  cms.ikpk.su/admin
│  (в браузере)    │
└────────┬─────────┘
         │
         │ сохранение
         ▼
┌──────────────────┐     webhook      ┌──────────────┐
│  Strapi API      │────────────────▶│  Netlify /    │
│  (база данных)   │                  │  Vercel CI   │
└──────────────────┘                  └──────┬───────┘
                                             │
                                      astro build
                                      (2–5 минут)
                                             │
                                             ▼
                                      ┌──────────────┐
                                      │  CDN         │
                                      │  (новые      │
                                      │  страницы)   │
                                      └──────┬───────┘
                                             │
                                             ▼
                                      ┌──────────────┐
                                      │  Посетители  │
                                      │  видят       │
                                      │  обновления  │
                                      └──────────────┘

Время от сохранения до публикации: 2–5 минут
```

## Целевые показатели производительности

| Метрика | Текущий сайт | Новый сайт | Улучшение |
|---------|-------------|------------|-----------|
| Lighthouse Performance (mobile) | 36–56 | ≥ 85 | **+50–130%** |
| LCP (mobile) | 8.6–9.8 с | ≤ 2.5 с | **в 3–4 раза** |
| TBT (mobile) | 290–2070 мс | ≤ 200 мс | **в 1.5–10 раз** |
| TTFB | 360–1130 мс | ≤ 300 мс | **CDN edge** |
| JS-бандл | ~500 KB | ~0 KB* | **~500 KB экономии** |

\* JavaScript загружается только для интерактивных компонентов (формы, фильтры)

## Преимущества нового решения

### Для посетителей
- Страницы загружаются в 3–4 раза быстрее
- Работает даже на слабом мобильном интернете
- Лучше индексируется поисковиками (SEO)

### Для контент-менеджера
- Стандартная CMS с понятным интерфейсом (не кастомный код)
- WYSIWYG-редактор для статей
- Управление семинарами, расписанием, преподавателями через формы
- Роли доступа (кто может что редактировать)

### Для владельца
- Независимость от конкретного разработчика (стандартный стек)
- Фронтенд и CMS можно менять независимо
- Низкая стоимость хостинга (~$10–20/мес за CMS, фронт бесплатно на CDN)
- Автоматические бэкапы базы данных

## Поиск по сайту (Pagefind)

```
astro build
     │
     ▼
┌──────────────────┐
│  256 HTML-файлов │
└────────┬─────────┘
         │
   pagefind (индексация)
         │
         ▼
┌──────────────────┐     ┌──────────────────┐
│  Поисковый       │     │  Посетитель      │
│  индекс (~50 KB) │────▶│  вводит запрос   │
│  (на CDN)        │     │  в браузере      │
└──────────────────┘     └──────────────────┘

Всё работает в браузере. Сервер не нужен.
```

**Возможности:**
- Полнотекстовый поиск по всем 256 страницам
- Толерантность к опечаткам (fuzzy matching, 1–2 символа)
- Поддержка русского языка
- Размер: ~20 KB JS + ~50 KB индекс (подгружается по требованию)
- Скорость: результаты за ~10 мс
- Стоимость: бесплатно

**При необходимости** можно заменить на Algolia (облачный, более мощный) —
компонент поиска на фронте один, меняется только движок.

## Модель данных (10 сущностей)

```
Institute (3)
  └── CourseGroup (26)
        └── Seminar (115)
              ├── Teacher (26)  [M:N]
              └── ScheduleEntry (расписание)

Article (68)
VideoPlaylist (6)
News (новости)
Promotion (акции)
Page (статические страницы: оплата, контакты, сведения...)
```

Каждая сущность с публичной страницей имеет SEO-поля:
- `seo_title`, `seo_description`, `og_image`, `noindex`
