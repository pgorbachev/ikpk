# Свидетельства приёмки — change `external-widgets`

Файл содержит по одной записи на каждый **применимый** приёмочный пункт спеки
(`openspec/changes/external-widgets/specs/external-widgets/spec.md`). Применимость
вычисляется из репозитория (`web/tests/external-widgets-acceptance.test.ts`,
`acceptancePoints()`), а не решается на глаз: пункт, чьё условие не наступило, записи не
требует. Ниже — три записи; оставшиеся три сейчас неприменимы (боевая сборка идёт с
`CHAT_LOADER_SRC=none`, байтовый предел 404 не поднимался).

По мере того как условия остальных пунктов наступают (появится реальный адрес
загрузчика чата, поднимется предел 404), сюда добавляются новые записи — старые не
переписываются.

## Тексты и имена авторов проверены приёмкой

- **Что проверено:** секция отзывов на главной (`/`, боевая сборка, `CHAT_LOADER_SRC=none`) —
  собранный статический HTML и архитектура ленивой загрузки виджета.
- **Кем:** Claude Code (техническая проверка сборки; владельцу стоит подтвердить визуально
  на демо/стенде отдельно, если нужна приёмка именно им — эта запись её не заменяет
  автоматически).
- **Дата:** 2026-08-25
- **Наблюдение:** в собранном `dist/index.html` секция `[data-reviews-section]` несёт
  только: заголовок «Отзывы на Яндекс.Картах», подпись, ряд знаков наград (пуст),
  плейсхолдер «Загрузка отзывов…», `noscript`-ссылку на страницу отзывов Яндекс.Карт и
  атрибут `data-reviews-embed` с адресом виджета. Ни одного `<iframe>` в статическом
  выводе нет вовсе — он создаётся клиентским скриптом только по появлению секции в
  зоне видимости (`web/src/components/home/sections/Reviews.astro`), и ни одного текста
  отзыва или имени автора нигде в собранном HTML нет.
- **Непустота предмета:** секция существует на странице, `data-reviews-embed` несёт
  реальный адрес официального виджета (`https://yandex.ru/maps-reviews-widget/112883331290?comments`),
  плейсхолдер и `noscript`-ссылка на месте — предмет не пуст и не удалён.
- **Источник:** `cd web && CHAT_LOADER_SRC=none npm run build && grep -oE '<div class="reviews-embed"[^>]*>.{0,400}' dist/index.html` —
  вывод команды воспроизводим на любой сборке той же ревизии. Дополнительная структурная
  гарантия: сам виджет — `<iframe>` на чужом происхождении (`yandex.ru`), поэтому даже
  после появления на странице содержимое отзывов кросс-origin и нашему коду недоступно
  ни при каком состоянии — то есть текст отзывов не может попасть в наш DOM ни статически,
  ни во время выполнения.

> **Знак награды: два приёмочных пункта сняты 2026-09-05 вместе с предметом.** Здесь стояли
> «Право использования знака подтверждено свидетельством приёмки» и «Подтверждение награждения
> записано свидетельством приёмки». Оба существовали, потому что марку рисовали МЫ: копия чужой
> награды требовала и подтверждения награждения, и подтверждения права её размещать.
>
> Решением владельца знак выводит официальное встраивание Яндекса
> (`https://yandex.ru/sprav/widget/rating-badge/<id>?type=award`, замерено — 200, живой рейтинг).
> Подтверждать нечего: утверждает сервис, выдавший награду, и он же снимает знак с карточки, когда
> награда кончается. Сценарии удалены из спеки, поэтому и пункты сняты — множество пунктов закрыто
> и сверяется со сценариями, а пункт без сценария красил бы сверку.
>
> Заметка оформлена цитатой, а не разделом: заголовок `##` здесь читается как приёмочный пункт.


> **Повторное наблюдение на облике 2026-09-05 — к пункту «Тексты и имена авторов проверены
> приёмкой» выше.** Оформлено цитатой, а не разделом, по той же причине, что заметка о знаке:
> заголовок `##` здесь читается как приёмочный пункт, а множество пунктов закрыто и сверяется
> со сценариями спеки. Запись от 25.08 не переписана — она описывает другую секцию: заголовок
> «Отзывы на Яндекс.Картах», подпись под ним, знак отдельным рядом. За 5 сентября облик менялся
> трижды, поэтому наблюдение с прежнего на нынешний не переносится.
>
>
> - **Зачем повторно.** Запись от 25.08 остаётся выше и не переписывается, но она описывает
>   другую секцию: там был заголовок «Отзывы на Яндекс.Картах» и подпись под ним, а знак
>   наград стоял отдельным рядом. За 5 сентября облик изменился трижды — заголовок сокращён
>   до «Отзывы», подпись снята, высота области поднята с 384 px до 1359, знак вынесен
>   отдельной полосой под отзывами и увеличен втрое. Наблюдение, снятое на прежнем облике,
>   на нынешний не переносится.
> - **Что проверено:** секция отзывов на главной (`/`, боевая сборка, `CHAT_LOADER_SRC=none`) —
>   собранный статический HTML и поведение в браузере.
> - **Кем:** Claude Code (техническая проверка). Владельческая приёмка облика прошла отдельно:
>   вариант B выбран по мокапам, результат осмотрен на стенде и локально.
> - **Дата:** 2026-09-05
> - **Наблюдение:** в собранном `dist/index.html` секция `[data-reviews-section]` несёт
>   заголовок «Отзывы», контейнер `[data-reviews-embed]` с адресом виджета, плейсхолдер
>   «Загрузка отзывов…», `noscript`-ссылку и полосу знака с `[data-award-embed]`. Ни одного
>   `<iframe>` в статическом выводе нет — оба создаются клиентским скриптом по появлении
>   секции в зоне видимости. Ни одного текста отзыва и ни одного имени автора в собранном
>   HTML нет.
> - **Непустота предмета:** секция существует, оба адреса встраиваний реальны
>   (`https://yandex.ru/maps-reviews-widget/112883331290?comments` и
>   `https://yandex.ru/sprav/widget/rating-badge/112883331290?type=award`), плейсхолдер и
>   `noscript`-ссылка на месте — предмет не пуст и не удалён.
> - **Источник:** `cd web && CHAT_LOADER_SRC=none npm run build`, затем поиск по
>   `dist/index.html`. Структурная гарантия прежняя и от облика не зависит: содержимое обоих
>   виджетов приходит в cross-origin `<iframe>` на `yandex.ru`, поэтому тексты отзывов
>   недоступны нашему коду ни статически, ни во время выполнения.
