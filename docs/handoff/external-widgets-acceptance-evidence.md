# Свидетельства приёмки — change `external-widgets`

Файл содержит по одной записи на каждый **применимый** приёмочный пункт спеки
(`openspec/changes/external-widgets/specs/external-widgets/spec.md`). Применимость
вычисляется из репозитория (`web/tests/external-widgets-acceptance.test.ts`,
`acceptancePoints()`), а не решается на глаз: пункт, чьё условие не наступило, записи не
требует. Ниже — три записи; оставшиеся три сейчас неприменимы (боевая сборка идёт с
`CHAT_LOADER_SRC=none`, байтовый предел 404 не поднимался).

По мере того как условия остальных пунктов наступают (появится реальный адрес
загрузчика чата, поднимется предел 404), сюда добавляются новые записи — старые не
переписываются.

## Тексты и имена авторов проверены приёмкой

- **Что проверено:** секция отзывов на главной (`/`, боевая сборка, `CHAT_LOADER_SRC=none`) —
  собранный статический HTML и архитектура ленивой загрузки виджета.
- **Кем:** Claude Code (техническая проверка сборки; владельцу стоит подтвердить визуально
  на демо/стенде отдельно, если нужна приёмка именно им — эта запись её не заменяет
  автоматически).
- **Дата:** 2026-08-25
- **Наблюдение:** в собранном `dist/index.html` секция `[data-reviews-section]` несёт
  только: заголовок «Отзывы на Яндекс.Картах», подпись, ряд знаков наград (пуст),
  плейсхолдер «Загрузка отзывов…», `noscript`-ссылку на страницу отзывов Яндекс.Карт и
  атрибут `data-reviews-embed` с адресом виджета. Ни одного `<iframe>` в статическом
  выводе нет вовсе — он создаётся клиентским скриптом только по появлению секции в
  зоне видимости (`web/src/components/home/sections/Reviews.astro`), и ни одного текста
  отзыва или имени автора нигде в собранном HTML нет.
- **Непустота предмета:** секция существует на странице, `data-reviews-embed` несёт
  реальный адрес официального виджета (`https://yandex.ru/maps-reviews-widget/112883331290?comments`),
  плейсхолдер и `noscript`-ссылка на месте — предмет не пуст и не удалён.
- **Источник:** `cd web && CHAT_LOADER_SRC=none npm run build && grep -oE '<div class="reviews-embed"[^>]*>.{0,400}' dist/index.html` —
  вывод команды воспроизводим на любой сборке той же ревизии. Дополнительная структурная
  гарантия: сам виджет — `<iframe>` на чужом происхождении (`yandex.ru`), поэтому даже
  после появления на странице содержимое отзывов кросс-origin и нашему коду недоступно
  ни при каком состоянии — то есть текст отзывов не может попасть в наш DOM ни статически,
  ни во время выполнения.

## Право использования знака подтверждено свидетельством приёмки

- **Что проверено:** право размещать знак «Хорошее место 2026» (Яндекс Карты) на сайте
  (`web/src/lib/award-badges.ts`, `DECLARED_AWARD_BADGES`, поле `markUsageEvidence`).
- **Кем:** владелец.
- **Дата:** 2026-08-29
- **Наблюдение:** правила использования марки отдельно не запрошены и не изучены —
  задача 1.3 `tasks.md` не закрыта. Владелец принял решение считать условия
  использования соблюдёнными **без проверки** и разместить знак на этом основании; это
  осознанное допущение, а не результат сверки с правилами сервиса, и запись называет
  это прямо, а не маскирует под «проверено».
- **Непустота предмета:** решение принято явно и зафиксировано дважды — в поле
  `markUsageEvidence` модуля данных и в этой записи; это не пропущенный шаг, а
  сознательный выбор с известной ценой (риск претензии сервиса при несоблюдении
  неизученных правил остаётся на владельце).
- **Источник:** этот разговор с исполнителем, 2026-08-29 (решение владельца
  зафиксировано только здесь и в коде — отдельного документа с правилами использования
  марки нет).

## Подтверждение награждения записано свидетельством приёмки

- **Что проверено:** сам факт награждения «Хорошее место 2026» (Яндекс Карты) — отдельно
  от права использования марки (см. запись выше).
- **Кем:** владелец.
- **Дата:** 2026-08-23
- **Наблюдение:** факт награждения подтверждён фотографией наклейки на двери центра,
  сделанной владельцем; официальных электронных знаков для сайта сервис не выдаёт,
  поэтому наклейка — единственный доступный источник
  (`docs/design/mockups/demo-followups/README.md:134`,
  `Сам факт наград взят с фотографии наклеек на двери центра (владелец, 23.08.2026), а не из открытых источников`).
- **Непустота предмета:** наклейка физически присутствует на фотографии, год на ней
  совпадает с годом сборки (2026) — предмет не выдуман и не устарел.
- **Источник:** `docs/design/mockups/demo-followups/README.md:134` (фотография сама не
  передана в репозиторий — только описание факта и даты).
