# Ненормативный снимок доделок после демо заказчику (intake). Требования живут в OpenSpec

**Дата демо-сессии:** 2026-08-19
**Дата приёма списка:** 2026-08-20
**Стенд, который показывали:** `http://193.124.115.99/` (demo-VPS)
**Статус:** ненормативный. Этот файл **не является источником требований** и не
задаёт объём работ.

## Зачем этот файл

Заказчик прошёл по демо-стенду и надиктовал список доделок — хаотично, в
порядке появления мыслей, вперемешку с вопросами и ссылками на конкретные
страницы стенда. Чтобы объём был проверяемым — чтобы через месяц можно было
спросить «а это вообще просили?» и «не просочилось ли лишнее?» — список записан
дословно, одним снимком, на дату приёма.

Дальше это **не источник истины**. Порядок сверки — из
[AGENTS.md](../AGENTS.md#source-of-truth): требования живут в активных changes в
`openspec/changes/`, принятое поведение — в `openspec/specs/`. При любом
расхождении между этим файлом и спецификацией **побеждает спецификация**, без
обсуждения: снимок сделан один раз и не переживает уточнений, а change
версионируется вместе с кодом.

Практическое следствие: этот файл не обновляется по ходу работ. Если задание
изменилось — оно меняется в change, а не здесь.

Тот же жанр, что [`backlog-2026-08-11-intake.md`](backlog-2026-08-11-intake.md),
и та же оговорка про ненормативность. Разница в источнике: там задание владельца,
здесь — реакция заказчика на показанный стенд.

## Правила этого снимка

1. **Формулировки не приглажены.** Обрывочность, разнобой в нумерации,
   опечатки («Категаризация», «Инстаграмм»), незаконченные фразы сохранены как
   есть. Хаотичность списка — свойство снимка, а не дефект: приглаженный текст
   уже нельзя отличить от нашей интерпретации.
2. **Единственная внесённая правка** — четыре вложения-изображения не
   передались при вставке (в исходнике на их месте стоит символ-заполнитель
   `U+FFFC`). Они отмечены маркером `[ВЛОЖЕНИЕ НЕ ПЕРЕДАЛОСЬ]` и перечислены в
   разделе «Пропущенные вложения».
3. **Идентификаторы `D1`–`D27` присвоены при приёме** и в исходнике
   отсутствуют. Они нужны только для ссылок из разбора; порядок в списке
   приоритетом **не является**.
4. Авторская нумерация (`1`–`11`, затем маркированные пункты, затем вложенные)
   показана рядом с ID и тоже не является приоритетом.

## Список

### Нумерованная часть

| ID | Как записано |
|---|---|
| D1 | **1.** Обучение прикладной кинезиологии для врачей и специалистов по движению<br>Сделать более общее название |
| D2 | **2.** Ближайший семинар с названием |
| D3 | **3.** `http://193.124.115.99/institut-apledzhera/primenenie-principov-akupunktury/ispolzovanie-meridianov-v-kranio-sakralnoj-terapii-1`<br>Расписание поднять вверх. `http://193.124.115.99/institut-apledzhera/primenenie-principov-akupunktury` — добавить время семинара (если нет в плане — «уточняйте у менеджера (номер тел)») |
| D4 | **4.** Преподаватели отдельно, Авторы методик отдельно (иностранные специалисты) |
| D5 | **5.** перефразировать |
| D6 | **6.** `[ВЛОЖЕНИЕ НЕ ПЕРЕДАЛОСЬ]` |
| D7 | **7.** Организация -> программа (направление) -> семинар<br>Семинар м.б. Привязан к нескольким программам |
| D8 | **8.** Отзывы — из Яндекс карты ИКПК (там есть и фото дополнительные) |
| D9 | **9.** Все программа адаптированы как для врачей, так и без мед образования |
| D10 | **10.** Записаться -> Выбрать программу (на главной странице) |
| D11 | **11.** `http://193.124.115.99/raspisanie-i-tseny` зарегистрироваться -> Записаться на семинар |

### Маркированная часть

| ID | Как записано |
|---|---|
| D12 | +7 (812) 646-54-50 — 2 номера телефона +79810387797 (и в контакты тоже) (это телефон Веры). |
| D13 | В контакты медцентра добавить второй мобильный номер |
| D14 | понедельник — пятница: 10:00 - 18:00, суббота, воскресенье — выходной — у менеджера (но семинары проводятся) |
| D15 | Магазин -> есть ли лучшее название? |
| D16 | Убрать Инстаграмм и фейсбук |
| D17 | `[ВЛОЖЕНИЕ НЕ ПЕРЕДАЛОСЬ]` |
| D18 | *(пустой пункт списка — в исходнике маркер без текста)* |
| D19 | Должно быть внизу |
| D20 | `[ВЛОЖЕНИЕ НЕ ПЕРЕДАЛОСЬ]` |

### Вложенная часть

Записана в исходнике как подпункты последнего маркированного пункта. Относится
ли она к нему по смыслу — при приёме не установлено.

| ID | Как записано |
|---|---|
| D21 | Новости -> предложения |
| D22 | Статьи — убрать даты выкладки. |
| D23 | Категаризация статей (категории статей взять из программ. многое ко многому) |
| D24 | Статьи добавить ссылки — вам может заинтересовать обучение\статьи. |
| D25 | Убрать сортировку по алфавиту |
| D26 | Заменить картинку приведи друга (лучше рекомендуй нас). Уточнить состав акции. |
| D27 | `[ВЛОЖЕНИЕ НЕ ПЕРЕДАЛОСЬ]` |

## Пропущенные вложения

Четыре пункта — **D6, D17, D20, D27** — состояли из одного изображения
(скриншот стенда или пример). При передаче списка изображения не пришли:
в тексте на их месте стоит `U+FFFC` (object replacement character), то есть
содержимое утрачено, а не сокращено.

Это не косметическая потеря. **Три пункта не читаются без них:**

- **D5** («перефразировать») — не сказано, что именно перефразировать. Ближайшее
  вложение — D6, и вероятно D5 относится к нему, но это догадка;
- **D19** («Должно быть внизу») — не сказано, что должно быть внизу и на какой
  странице. Пункт стоит между D17 и D20, то есть между двумя вложениями;
- **D18** — пустой пункт, скорее всего подпись к соседнему изображению.

Пока вложения не восстановлены, D5, D18 и D19 **не подлежат разбору**: любая их
трактовка была бы придумана нами. Помечены как неопределённые, а не отброшены и
не угаданы.

## Что дальше

Разбор (классификация пунктов и маршрутизация в changes, дефекты, вопросы
заказчику) — **отдельным шагом и отдельным документом**. Этот файл фиксирует
только сырьё, до интерпретации, и после коммита не редактируется.
