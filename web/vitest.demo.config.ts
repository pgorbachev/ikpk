import { defineConfig } from 'vitest/config';

/**
 * Проверки по выводу ДЕМО-сборки (`dist-demo`) — отдельный набор, потому что у них
 * отдельный предмет.
 *
 * Своя конфигурация здесь не косметика: `vitest.build.config.ts` выбирает проверки,
 * которые читают боевой `dist`, и запускать их по демо-выводу нельзя — «счётчики
 * присутствуют» прошло бы мимо предмета. Развод предметов требует change
 * `deploy-gated-on-tests` (решение 6).
 *
 * Конфигурация выбирает ФАЙЛЫ и не переопределяет корень вывода: корень объявлен в
 * `tests/helpers/demo-dist.ts`. Нацелить существующие боевые гейты на демо-каталог
 * одной конфигурацией нельзя — они читают `dist`, объявленный в их собственном
 * модуле.
 */
export default defineConfig({
  test: {
    /**
     * ЯВНЫЙ предел теста. Без него действует умолчание vitest — 5000 мс, и это не величина
     * по существу, а ложное красное: самая долгая проверка этого набора измерена в 5260 мс
     * (`rich-content contract: whole-document hazard scan` — разбор dist-demo браузером) на
     * незагруженной машине, то есть умолчание она перерастает сама по себе, а в обязательном
     * прогоне борется за машину с остальными файлами набора.
     *
     * Значение выбрано от ИЗМЕРЕНИЯ, а не на глаз: 5260 мс × запас на состязание за
     * процессор ≈ 15 с, и 15 с — рекомендованный верхний предел (решение владельца
     * 2026-08-19). Больше брать нельзя: предел, поднятый «с запасом на всё», превращает
     * зависшую проверку в долгую.
     *
     * Предел НЕ является исходом: ни одна проверка этого набора не выводит отказ из того, что
     * что-то не уложилось во время. Незавершившийся процесс — предмет обвязки
     * `tests/helpers/payment-service.ts`, и там он отдельный исход (`timeout`), который
     * поднимает исключение, а не читается как подтверждённый fail-closed.
     */
    testTimeout: 15_000,
    include: [
      'tests/demo-output.test.ts',
      'tests/demo-prototypes.test.ts',
      'tests/demo-payment-form.test.ts',
      'tests/preview-role-dist.test.ts',
      'tests/social-accounts-preview-dist.test.ts',
      'tests/rich-content-canary.demo.test.ts',
      'tests/rich-content-hazard.demo.test.ts',
    ],
  },
});
