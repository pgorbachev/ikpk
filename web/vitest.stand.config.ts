import { defineConfig } from 'vitest/config';

/**
 * Проверки по выводу сборки РОЛИ `stand` (`dist-stand`) — отдельный набор, потому что у них
 * отдельный предмет.
 *
 * Своя конфигурация здесь не косметика, и довод тот же, каким заведена `vitest.demo.config.ts`:
 * `vitest.build.config.ts` выбирает проверки, читающие боевой `dist`, и запускать их по
 * выводу другой роли нельзя — «состав присутствует» прошло бы мимо предмета. Принятая спека
 * `openspec/specs/deploy-gating/spec.md` требует, чтобы проверка получала на вход ровно тот
 * вывод, про который написана.
 *
 * Почему роль `stand` понадобилась отдельно: до change `social-accounts` вывод `dist-stand`
 * проверялся только браузерными наборами оплаты (`playwright.stand.config.ts`), а проверок по
 * разметке у него не было ни одной. Между тем это артефакт, который выкладывается на
 * публичный стенд и который смотрит заказчик, — парой «боевой и демо» он не покрыт вовсе
 * (`openspec/changes/social-accounts/design.md`, Решение 11).
 *
 * Конфигурация выбирает ФАЙЛЫ и не переопределяет корень вывода: корень объявляет сам файл
 * проверки. Нацелить существующие боевые гейты на этот каталог одной конфигурацией нельзя —
 * они читают `dist`, объявленный в их собственном модуле.
 */
export default defineConfig({
  test: {
    /**
     * ЯВНЫЙ предел теста — по той же причине, что в двух соседних конфигурациях: умолчание
     * vitest (5000 мс) не является величиной по существу, а обход 270 страниц вывода
     * парсером его перерастает. Значение взято тем же, что у наборов `dist` и `dist-demo`,
     * чтобы три предмета одного рода не отвечали по-разному на одинаковую нагрузку.
     */
    testTimeout: 15_000,
    include: ['tests/social-accounts-stand-dist.test.ts'],
  },
});
