/**
 * Сегменты первого уровня, о которых Strapi может знать САМ, без обращения к дереву
 * `web/src/pages`: сегменты каталогов (сами являются маршрутами сборки — списочные страницы)
 * плюс идентификаторы уже существующих статических страниц системы управления (запрошены из
 * БД вызывающей стороной, а не здесь).
 *
 * НАХОДКА, не полное решение: полный список маршрутов сборки Astro (произвольные файлы
 * `web/src/pages/**`) Strapi в реальном времени не видит — это два разных процесса без общего
 * рантайма. Здесь проверяются только шесть известных каталогов; конфликт с остальными
 * страницами сборки (например, `/oplata`, `/o-nas`) ловит только вторая линия — сборка `web`
 * (design.md: "проверка на стороне сборки — вторая линия"). Синхронизировать оба списка
 * автоматически можно было бы через артефакт, который `web` генерирует, а Strapi читает при
 * старте, — это отдельная работа, не часть текущей задачи (см. отчёт по задаче).
 */
export const KNOWN_CATALOG_SEGMENTS = ['instituty', 'programmy', 'seminary', 'specialisty', 'statyi', 'video'] as const;
