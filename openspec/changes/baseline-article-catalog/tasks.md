## 1. Фиксация исследуемого состояния

- [x] 1.1 Зафиксировать revision кода (`feat/demo-mode-and-hero-photo@542151b`) и способ, которым он соединён с процессными коммитами `main` — в `proposal.md` и `design.md`
- [x] 1.2 Снять состояние production GET-запросами (`/statyi`, `/statyi/`, статья со слэшем и без, `/statyi/page/2`, `/statyi?q=`, `sitemap.xml`) и записать результаты в `proposal.md`
- [x] 1.3 Собрать сайт на этом revision и зафиксировать измерения: число страниц, вес `dist/statyi/index.html`, число `<template>`, число ссылок вне `<template>`

## 2. Разбор найденного на четыре части

- [x] 2.1 Выделить принятое поведение и описать его через `SHALL` — только его
- [x] 2.2 Вынести известные дефекты в раздел known deviations с измерением и адресом исправления (KD-1…KD-8)
- [x] 2.3 Перечислить детали реализации, не являющиеся контрактом
- [x] 2.4 Вынести неопределённые места вопросами владельцу, не домысливая их в требования

## 3. Characterization-тесты

- [x] 3.1 Написать `web/tests/article-catalog.test.ts` — проверки по собранному `dist`: данные, адреса, состав страницы списка, индексные инварианты, разметка статьи, связь между статьями
- [x] 3.2 Написать `web/tests/article-catalog.spec.ts` — браузерные проверки: охват поиска, глубина поиска, охват сортировки, возврат к списку, разделяемая ссылка `?q=`, `canonical` при параметре
- [x] 3.3 Включить новый dist-зависимый файл в `web/vitest.build.config.ts` и исключить из `web/vitest.config.ts`
- [x] 3.4 Предъявить зелёный прогон обоих наборов с точной командой
- [x] 3.5 Проверить каждый новый гейт негативно: нарушить поведение, показать красный прогон, восстановить и подтвердить восстановление контрольной суммой
- [x] 3.6 Проверить детерминированность сборки двумя полными сборками и сравнением контрольной суммы страниц статей
- [x] 3.7 Подключить браузерный набор к запуску: скрипт `test:e2e:catalog` в `web/package.json` и шаг в job `e2e-smoke` workflow `Tests`. Найдено при переносе на `main`: файл не запускался ни скриптом, ни CI — семь проверок ничего не сторожили (`design.md`, Решение 3а)

## 4. Валидация и локальные гейты

- [x] 4.1 `./bin/openspec validate baseline-article-catalog --type change --strict --no-interactive`
- [x] 4.2 `npm run lint` и `npm run typecheck` в `web/` — новые тестовые файлы не должны ронять статические проверки
- [x] 4.3 Прогнать полный набор build-гейтов и убедиться, что baseline ничего не сломал

## 5. Ревью (не автором baseline)

- [ ] 5.1 Независимая сверка «спека ↔ код ↔ production» — по каждому требованию проверить утверждение по репозиторию и запросом к живому сайту
- [ ] 5.2 Ревью с уклоном в полноту последствий: не пропущено ли принятое поведение и не превратилось ли отклонение в требование
- [ ] 5.3 Ответы владельца на шесть вопросов раздела «Неопределённые места»; расхождения с production (`?q=`, `meta description` на `/statyi`, число вариантов сортировки) подтвердить или отклонить с причиной

## 6. Перенос на `main` и архив (один PR)

- [x] 6.0 Перенести baseline на актуальный `origin/main`. Обычный rebase не годится: ветка стояла на неразжатой истории `542151b`, а `main` содержит её squash'ем `587e523` — см. `design.md`, Решение 1а. Перепроверить характеризацию на новой базе: 22/22 (vitest) и 7/7 (Playwright) зелёные
- [ ] 6.1 Локально заархивировать baseline: `./bin/openspec archive baseline-article-catalog`
- [ ] 6.2 Переписать `article-list-pagination` относительно появившейся основной спеки: требования об `canonical`, `sitemap`, полнокорпусном поиске и уникальных `title` перестают быть `ADDED`
- [ ] 6.3 Строгая валидация обеих сущностей плюс проверка **применимости** дельты применением в одноразовом worktree — зелёной валидации недостаточно
- [ ] 6.4 Слить одним PR архив baseline и обновлённую дельту, без промежуточного состояния, где основная спека есть, а дельта ей противоречит — merge выполняет владелец
