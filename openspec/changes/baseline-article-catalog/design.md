## Context

Мотивация — в `proposal.md`. Здесь только решения, которые baseline вынужден принять
из-за трёх обстоятельств.

1. **Код и процесс лежат в разошедшихся ветках.** `openspec/` и `./bin/openspec` появились
   в `main` (`291c6b2`, `4e8ab6a`, `2420a6d`), а описываемый код живёт в
   `feat/demo-mode-and-hero-photo@542151b`, который от `main` отошёл раньше
   (merge-base `9c4b446`). Ни в одной из веток нет обоих.
2. **Baseline описывает поведение, часть которого держится на дефекте.** Полнокорпусный
   поиск существует ровно потому, что весь каталог дублируется в разметку. Требование
   нельзя написать так, чтобы оно оказалось истинным только благодаря отклонению.
3. **По той же области есть активная дельта** `article-list-pagination`, и она пересекается
   с baseline. Правило `AGENTS.md` запрещает сливать overlapping baseline, пока дельта не
   обновлена, — но обновляется она в PR с архивом, а не здесь.

## Goals / Non-Goals

**Goals:**

- зафиксировать принятый контракт каталога так, чтобы дельту можно было переписать
  относительно него;
- дать каждому принятому требованию воспроизводимую проверку, зелёную на существующем коде
  и падающую при нарушении поведения;
- оставить отклонения отклонениями: с измерением, адресом исправления и без `SHALL`.

**Non-Goals:**

- менять продуктовый код или его поведение;
- переписывать `article-list-pagination`;
- описывать облик страницы списка и статьи (это отдельная область, закреплённая
  визуальными эталонами и `parity-audit.spec.ts`);
- закрывать TD-1, TD-2, TD-3.

## Decisions

### Решение 1. Ветка baseline = код `542151b` + процессные коммиты `main`

**Это решение исчерпано: обстоятельство, вызвавшее его, снято.** Оно оставлено здесь
целиком, потому что описывает состояние, в котором baseline снимался, и без него
непонятно, почему исследуемый revision — не `main`.

Взято было: `git reset --hard 542151b` в собственном worktree, затем `git merge 2420a6d`.
Конфликтов нет — процессные коммиты и код не пересекаются ни одним файлом (проверено
`git diff --name-only` по обеим сторонам merge-base).

Альтернативы:

- **описать baseline на `main`** — отвергнуто: там нет описываемого кода. Спека без кода
  проверяется только на слово, а characterization-тесты запустить негде;
- **merge `542151b` в процессную ветку** (обратный порядок) — отвергнуто: PR к `main` тогда
  содержит все 26 коммитов кода, и ревью baseline утонет в чужой диффе;
- **cherry-pick openspec-коммитов** — отвергнуто: три merge-коммита с 30 файлами, история
  расходится, а слияние потом даёт дубли.

Тогда из этого следовало: baseline **не готов к слиянию в `main` сам по себе** — сначала
владелец решает судьбу `feat/demo-mode-and-hero-photo`.

### Решение 1а. Ветка кода опубликована — baseline перенесён на `main`

Владелец опубликовал `feat/demo-mode-and-hero-photo` squash-коммитом
`587e5232f72d843530901fd5bf268a915e0387ac` («feat(web): прототипы трёх каркасов подачи для
демо выбора (#36)»), и описываемый код теперь есть в `main`. Условие Решения 1 снято, а
вместе с ним и оговорка «не готов к слиянию».

Перенос сделан не обычным rebase: ветка baseline стояла на **неразжатой** истории
`542151b`, а `main` содержит её же squash'ем, поэтому rebase заново проиграл бы все 26
коммитов кода поверх их собственной копии. Взято:

```
git rebase --onto e26b4fee15d94c5fa4072714d055459cb37edfa8 409b7a7 change/baseline-article-catalog
```

то есть перенесены только пять коммитов самого baseline, а процессный коммит `409b7a7`
отброшен как уже содержащийся в `main` (проверено: `openspec/`, `bin/openspec`,
`bin/check-openspec-integration`, job «OpenSpec integration» и раздел процесса в `AGENTS.md`
в `main` есть, причём `main` ушёл дальше — там ещё четыре change'а, которых в `409b7a7` не
было). Конфликтов нет: пять перенесённых коммитов трогают только
`openspec/changes/baseline-article-catalog/**`, два тестовых файла и две строки конфигураций
vitest.

Что это меняет в существе baseline: **ничего в требованиях и ничего в измерениях**.
Исследуемым revision остаётся `542151b` — с него снималось поведение, и подменять его
задним числом нельзя. Но characterization-тесты перепроверены на новой базе и остались
зелёными: 22 из 22 (vitest, `dist`) и 7 из 7 (Playwright) на
`change/baseline-article-catalog@<голова PR>`, собранной из `e26b4fee`. Это и есть довод,
что baseline описывает не особенность одной ветки: код за 27 коммитов `main` сдвинулся, а
поведение каталога — нет.

### Решение 2. Требование о достижимости — обход от корня с зафиксированным составом

Записано: «при обходе от корня по отрендеренным ссылкам, минуя страницы с `nofollow`,
недостижим ровно один адрес». Не записано ни «есть хоть одна входящая ссылка» (слабая
форма), ни «существует индексируемый путь по страницам списка без исполнения скрипта»
(сильная).

Почему не слабая. Первая редакция записала именно её — и ревью показало, что проверка тогда
измеряет не то, что заявлено: «на страницу хоть кто-то ссылается» считает достижимой
страницу, на которую ведёт только другая недостижимая. Сегодня оба замера дают 1, но
совпадение — не модель, а сценарий спеки при этом говорил «обходится от корня», то есть
текст и проверка расходились.

Почему не сильная. При обходе, исключающем содержимое `<template>` и страницы с `nofollow`,
недостижим ровно один адрес каталога
(`/statyi/kislotno-shelochnoj-balans-i-lechenie-pacienta`), а 61 статья достигается только
через блок «Другие статьи» соседних статей. Сильная форма была бы **ложной**, а её проверка
— красной: baseline перестал бы быть baseline и превратился в задачу на исправление. Оно
принадлежит `article-list-pagination`, где сильная форма уже записана.

Признак исключения — `nofollow` в метатеге robots, а не префикс пути `/sitemap`. Сегодня
это одна и та же страница, но хардкод пути совпал бы с общим признаком по случайности, а
`AGENTS.md` прямо запрещает перечислять частные случаи предмета проверки.

Потребитель графа назван в требовании: это поисковый обход. Для человека без JavaScript
недостижимых страниц нет вовсе — карта сайта отдаёт обычные `<a>` на все 68 статей без
`rel`, и человек ими пользуется; обход же останавливает `nofollow` на самой карте. Без
указания потребителя цифра «одна недостижимая» ничего не значит.

### Решение 3. Две среды проверки, а не одна

`web/tests/article-catalog.test.ts` работает по собранному `dist` (vitest, набор
`vitest.build.config.ts`), `web/tests/article-catalog.spec.ts` — в браузере (Playwright).

Разделение вынужденное: охват поиска и сортировки в статическом HTML не наблюдаем вообще —
там лежат данные, а не результат. Обратное тоже верно: 68 страниц статей быстрее и надёжнее
проверять чтением файлов, чем 68 переходами в браузере. Правило проекта («дефекты раскладки
ловятся только Playwright») здесь не нарушено: раскладка в этой области не описана.

Новый dist-зависимый файл добавлен в `include` набора `vitest.build.config.ts` и в
`exclude` обычного `vitest.config.ts` — иначе он либо не запустится, либо упадёт в наборе
без сборки.

### Решение 3а. Браузерный набор подключён к запуску — иначе он был гейтом только на словах

Найдено при переносе на `main`: `web/tests/article-catalog.spec.ts` **не запускался ничем**.
Файл в репозитории есть, зелёный прогон предъявлялся вручную, но ни один скрипт `web/` его
не называл, а `playwright.config.ts` собирает `**/*.spec.ts` только при вызове без списка
файлов — все три вызова в CI и в `package.json` идут с явным файлом. То есть семь проверок
существовали, ничего не сторожа: сломай поведение — и никто бы не узнал. Это тот самый
случай, который `AGENTS.md` называет «отсутствие сигнала выдано за отсутствие проблемы»,
и для baseline он особенно тяжёлый: DoD требует зелёных characterization-тестов, а не
зелёных **однажды**.

Подключено: скрипт `test:e2e:catalog` в `web/package.json` и отдельный шаг в job
`e2e-smoke` workflow `Tests`. Отдельным шагом, а не добавлением файла в `test:e2e:smoke`:
у smoke свой предмет, и объединение скрыло бы, что именно упало. Только `--project=desktop`
— предмет проверок (охват поиска и сортировки, `?q=`) от ширины окна не зависит, а
раскладка в области каталога не описана вовсе.

Побочное следствие, названное сознательно: `Tests` — единственная проверка в гейте
публикации (`AGENTS.md`, «Гейт публикации»), поэтому теперь падение характеризации каталога
останавливает выкладку сайта. Это и есть намерение: гейт, который ничего не останавливает,
гейтом не является.

**Та же болезнь шире области, и это не наша находка на исправление.** Ничем не запускаются
ещё пять браузерных наборов: `parity-audit.spec.ts`, `institute-parity.spec.ts`,
`seminar-parity.spec.ts`, `schedule-parity.spec.ts`, `course-group-parity.spec.ts`; у
`visual-baseline.spec.ts` и `compare.spec.ts` скрипт есть, но в CI их не вызывает ни один
workflow. Проверено на `e26b4fee`: в `.github/workflows/` есть ровно три вызова Playwright —
`test:e2e:smoke`, `test:e2e:a11y` (`test.yml`) и `test:e2e:compat` (`nightly.yml`).
Здесь это только зафиксировано: подключать пять чужих наборов к гейту публикации в PR
baseline нельзя — их состояние неизвестно, и решение об этом не входит в область каталога.

Прямое следствие для самой спеки: KD-3 и KD-7 ссылались на `parity-audit.spec.ts` как на
существующий гейт, держащий часть контракта. Формулировки исправлены — гейт, который не
запускается, ничего не держит.

### Решение 4. Числа из данных, а не из константы

Ожидаемое число страниц списка вычисляется в проверке как `ceil(N / 6)` по
`discovery/entities/articles.json`, а не сравнивается с записанным «12». Иначе рост каталога
красит гейт на исправном поведении, а `AGENTS.md` прямо запрещает проверки, отстающие от
предмета молча.

По той же причине проверки не перечисляют частные случаи: страницы каталога берутся из
данных, а не из списка известных адресов.

### Решение 5. «Самые новые» проверяются формулировкой, устойчивой к тай-брейкам

У 68 статей всего 6 различных `published_at` (KD-8), поэтому «шесть самых новых» — не
однозначный набор. Проверка утверждает то, что от разрешения тай-брейков не зависит: ни одна
показанная статья не старее любой непоказанной.

Альтернатива — сравнивать со списком slug, вычисленным той же сортировкой, — отвергнута: это
переписывание реализации в тесте, и тест стал бы зелёным при любой ошибке, повторённой в
обоих местах.

### Решение 6. Детерминированность сборки проверяется разово, а не гейтом

Сценарий «повторная сборка даёт тот же результат» проверен двумя полными сборками и
сравнением контрольной суммы страниц статей — свидетельство приложено к отчёту. Постоянным
гейтом это не делается: второй `npm run build` в каждом прогоне удваивает самую дорогую
операцию конвейера ради свойства, которое ломается только правкой алгоритма подбора.

Вместо гейта в наборе остаётся наблюдаемое следствие детерминированности: наборы «других
статей» различаются между статьями (68 различных наборов на 68 статей). Случайный подбор
это тоже прошёл бы — поэтому свойство и проверено отдельно, а не выдано за проверенное.

### Решение 7. `?q=` записан как принятое поведение, хотя паритетом не является

Живой сайт `?q=` не обслуживает: `https://ikpk.su/statyi?q=<запрос>` отдаёт тот же список
из шести статей и пустое поле поиска. У нас параметр читается клиентом и фильтрует.

Это записано в требования, потому что активная дельта прямо запрещает его терять, и потому
что ссылка на результаты — наблюдаемое обещание страницы. Но обоснование в спеке названо
честно: это наше поведение, не паритет. Вопрос владельцу оставлен открытым (пункт 3 раздела
«Неопределённые места»).

## Risks / Trade-offs

- **Baseline легализует дефект формулировкой, а не строкой.** → Каждое отклонение вынесено
  в отдельный раздел с измерением и адресом исправления; требование о связности намеренно
  слабое (Решение 2), и его слабость объяснена в самой спеке, а не только здесь.
- **Тесты зелёные с рождения, поэтому неизвестно, что они ловят.** → Все 29 проверок прошли
  негативную проверку: поведение временно нарушалось (22 мутации собранного каталога, данных
  и конфигурации редиректов), тест краснел, состояние восстанавливалось. Восстановление
  подтверждено побайтово — контрольная сумма страниц статей совпала с исходной.
- **Мутации применялись к `dist`, а не к исходникам.** → Это осознанно: правка исходника
  требует полной пересборки на каждую из 22 мутаций. Риск — мутация может оказаться
  недостижимой в реальном коде; принимается, потому что проверяется не код, а способность
  гейта различать состояния. Две мутации (данные каталога, конфигурация редиректов)
  затрагивают именно источники.
- **Спека расходится с production в двух местах** (`?q=` живой сайт не обслуживает;
  вариантов сортировки там два, а не четыре). → Расхождения измерены, названы в спеке и
  вынесены вопросами, а не сглажены. Оба — в нашу пользу, поэтому в требования они попали;
  обоснование «паритет» к ним не применяется.

  Третье расхождение, заявленное в первой редакции («на production `/statyi` нет
  `meta description`»), **снято ревью как фактическая ошибка**: описание там есть —
  `content="Все материалы от преподавателей и экспертов ИКПК на сайте института"
  name="description"`. Ошибка родилась из поиска по порядку атрибутов `name` перед
  `content`, а в живой разметке порядок обратный. Требование в спеке от этого не
  меняется — оно верно само по себе, — но обоснование ссылалось на отличие, которого нет.
- **Негативная проверка гейта достижимости: мутация записана неверно в первой редакции.**
  → Блоков «Другие статьи» на странице статьи **два**, и оба питаются одним источником:
  `AsideCard tone="muted" title="Другие статьи"` внутри `<aside>`
  (`web/src/pages/statyi/[slug].astro:119`) и полноширинный
  `<section class="related-section section">` (`:141`). Снятие любого ОДНОГО оставляет
  ссылки живыми, и гейт остаётся зелёным на 1 — то есть по описанию «снять любой
  `<aside …>`» мутация не воспроизводится, а следующий исполнитель заключил бы, что гейт
  декоративен. Воспроизводимая мутация — снять **оба** блока на всех 68 страницах статей;
  тогда недостижимых 62 вместо 1 и гейт красный. Замер ревью, подтверждённый чтением
  шаблона:

  ```
  без мутации                                    → 1   зелёный
  снят только <aside …> (AsideCard)               → 1   зелёный
  снят только <section class="related-section">   → 1   зелёный
  сняты ОБА блока                                 → 62  красный
  ```

  Ловушка, из-за которой неполная мутация выглядит удавшейся: при снятии одного `<aside>`
  прогон **всё равно краснеет** — падают два других теста, «до четырёх ссылок на
  существующие статьи» и «наборы других статей различаются». Наблюдатель видит красное и
  заключает, что мутация сработала, хотя проверяемый гейт при этом зелёный. Отсюда
  практическое следствие: смотреть не на цвет прогона, а на **имя** покрасневшего теста.

- **Открытое наблюдение: одиночное падение в первом прогоне после правки файла.** Дважды
  первый запуск `vitest run --config vitest.build.config.ts tests/article-catalog.test.ts`
  сразу после перезаписи файла дал `1 failed | 21 passed` при длительности 8.6 с против
  0.7 с в последующих прогонах; какой именно тест упал, оба раза не сохранилось (вывод
  отфильтрован при чтении). Воспроизвести прицельно не удалось: `touch` файла, повторная
  перезапись через тот же скрипт, полный набор (101 из 101) — всё зелёное; `globalSetup` и
  хуков сборки в конфигурации нет, то есть объяснение через пересборку не годится.
  Записано как наблюдение, а не как «показалось»: если повторится, первым делом сохранять
  полный вывод целиком.

- **Дельта `article-list-pagination` останется несовместимой до PR с архивом.** → Это
  зафиксировано в `tasks.md` как обязательный шаг, и baseline нельзя сливать раньше.

## Migration Plan

Нечего разворачивать: продуктовый код не меняется, поведение сайта не меняется. Порядок
дальнейших шагов — в `tasks.md`; критично, что архив baseline и обновление дельты идут
**одним** PR.

Откат: удаление каталога change и двух тестовых файлов плюс возврат двух строк в
конфигурациях vitest. Ни одного следа в продукте.

## Open Questions

Отложить можно только то, что не меняет ни спеку, ни подход, ни разбивку задач. Здесь
таких — один:

- Порог «различных наборов не меньше 75% числа статей» в проверке блока «Другие статьи»
  выбран как заведомо недостижимый для вырожденного случая (сейчас 68 из 68). Точное
  значение можно уточнить после первого пополнения каталога — на состав требований и
  задач это не влияет.

Остальные неизвестные — не отложенные, а адресованные владельцу: они перечислены в спеке
(«Неопределённые места») и по каждому названо, чего именно не хватает. Ни один из них не
меняет требований: спорные места записаны так, как система ведёт себя сегодня.
