## 0. Порядок работы и обозначения

Порядок из `AGENTS.md` переносится сюда целиком, потому что нарушить его легко, а заметить
нарушение потом нечем:

```
спека → 2 независимых ревью (разные уклоны, только чтение) → approve владельца
      → МОКАПЫ контрола + выбор владельца
      → тесты ОТДЕЛЬНОЙ сессией с чистым контекстом (красный прогон предъявлен)
      → реализация (другой исполнитель)
      → негативные проверки каждого нового гейта
      → ревью кода (два независимых исполнителя) → приёмка → архив
```

Реализация не начинается ни до approve, ни до выбора мокапа: облик панели фильтров
меняется (пятый элемент в сетку на четыре колонки), и правило «мокапы до внедрения»
применяется в полном объёме — точечным исправлением дефекта это изменение не является.

**Обозначения у проверок:**

- **[RED]** — проверка обязана падать на текущем коде. Она и есть предъявляемое
  свидетельство: красный прогон по одному из двух способов из `AGENTS.md` — отдельный push
  коммита только с тестами (упавший check-run на этом SHA) либо приложенный к PR лог с
  точной командой и SHA. «Тесты лежат коммитом раньше» доказательством не является.
- **[GREEN-BY-DESIGN]** — проверка защищает **уже существующее** поведение и на текущем
  коде зелена по определению; красного прогона у неё быть не может. Для неё
  предъявляется **негативная мутация**: поведение временно нарушается, проверка обязана
  покраснеть. Записывается воспроизводимо — точная команда или патч плюс два числа, до и
  после; судить по **имени** покрасневшего теста, а не по цвету прогона.

Каждая мутация выполняется в своём worktree, фикс к моменту мутации закоммичен,
восстановление подтверждается постусловием, подобранным под предмет мутации: для
отслеживаемых файлов — пустой `git status --porcelain` и отсутствие diff относительно SHA
фикса; для мутаций внутри `web/dist/` — контрольная сумма дерева до и после
(`find . -type f -exec shasum {} + | sort | shasum`, из каталога, по относительным путям)
либо пересборка. `git status` внутри `dist` слеп: каталог в `.gitignore`.

## 1. Мокапы и выбор владельца (до разметки)

- [x] 1.1 Собрать **не менее двух полноценных мокапов страницы** `/raspisanie-i-tseny` с
      контролом месяца — целые страницы, а не вырезанный элемент: предмет выбора —
      раскладка панели из пяти элементов, а её видно только на странице. Сейчас сетка
      четырёхколоночная (`web/src/components/schedule/ScheduleFilters.astro:67-72`), с
      переходом на две колонки при ≤1100px и на одну при ≤700px (`:128-138`)
- [x] 1.2 В каждом варианте показать оба состояния контрола: выключенное (до исполнения
      скрипта) и рабочее — оформление `:disabled` в проекте уже задано (`:110-113` и
      `:151-154` для тёмной темы), и выключенный вид посетитель увидит на каждой загрузке.
      **Выключенный рисовать без стрелки:** сокращённая запись `background` в
      `.schedule-select:disabled` сбрасывает `background-image`, которым стрелка и
      нарисована (`:115-126`), а специфичность у правила с `:disabled` выше — так уже
      выглядит список программ до выбора института
- [x] 1.3 Показать оба разрешения: 1280 и 375 CSS-пикселей. Панель на узком экране
      становится одноколоночной, то есть пятый элемент удлиняет её на целую строку —
      именно это и решает владелец
- [x] 1.4 Вынести в мокапы два вопроса из `design.md`: показывать ли число записей в
      подписи месяца и отделять ли визуально следующий год (в данных после декабря 2026
      идёт февраль 2027, затем сентябрь 2027). На требования ответ не влияет; на разметку
      влияет
- [x] 1.5 Получить выбор владельца и зафиксировать его свидетельством (какой вариант,
      дата). Без этого пункта задачи раздела 3 не начинаются

**РЕШЕНИЕ ВЛАДЕЛЬЦА, 2026-08-11 (свидетельство пункта 1.5).** Мокапы сняты с реальной
сборки на `origin/main@873fa75da6c0ebeeeb37a66c14e405f6293921ee`, ветка
`design/month-filter-mockups`, опубликованы PR #92 (в `main` НЕ сливаются сознательно:
26 файлов, ~14 МБ изображений — история репозитория этого не стоит; PR закрыт, ветка
сохранена как артефакт, картинки доступны в его diff).

Выбрано:

- **вариант B** — контрол месяца вторым элементом панели, сразу после поиска; на 1280
  панель становится двухрядной (145px против 65px) и сдвигает список событий на 80px вниз;
- **число записей в подписи — показывать** («Сентябрь 2026 (15)»);
- **месяцы группировать по году** (после декабря 2026 — февраль 2027, затем сентябрь 2027).

Почему B, а не A — измерено на мокапах, а не выбрано на глаз: в варианте A под текст
пятой колонки остаётся 132px, а самой длинной подписи с числом нужно 144px, то есть «A +
число» обрезает подпись; в B под текст 265px. Кроме того A сужает три существующих фильтра
с 173px до 132px и начинает обрезать «Набережные Челны» (150px), которое сегодня влезает.
На 375 в варианте A нижняя кромка контрола оказывается на 814px при экране 812px (виден не
целиком), в B — на 583px.

Следствия для реализации, вытекающие из решения: число в подписи обязано совпадать с
фактической выдачей месяца (иначе подпись — второй источник правды, см. Risks в design);
группировка по году — визуальная, на состав списка месяцев и на поведение фильтра не
влияет; двухрядная панель на 1280 меняет поток страницы, поэтому визуальные эталоны
`schedule-*` разойдутся с собранной страницей — но **перегенерации в этом change не будет**:
решением владельца от 2026-08-12 расхождение уходит долгом, см. задачу 5.2. Здесь это
сказано, потому что прежняя редакция абзаца обещала обратное.

## 2. Тесты по спеке — отдельной сессией, до реализации

Вход сессии: утверждённая спека и код репозитория. Обсуждение, которым спека рождалась, в
контекст не переносится. Пишутся только тесты; реализация не трогается.

**Где тесты обязаны оказаться.** Публикацию останавливает единственный workflow `Tests`, и
в нём исполняются: `web/tests/**/*.test.ts` через `npm test`, файлы из include-списка
`web/vitest.build.config.ts` через `npm run test:build`, `web/tests/site.spec.ts` через
`test:e2e:smoke` и `web/tests/a11y.spec.ts` через `test:e2e:a11y`
(`.github/workflows/test.yml:104,107`). **`web/tests/schedule-parity.spec.ts` не запускается
ни одним workflow** — ни `Tests`, ни `Nightly` (там только `test:e2e:compat`). Проверка,
положенная туда, будет зелёной потому, что её никто не выполняет.

Перечень снят на `aa7678cf` и **меняется независимо от этой работы**: change
`deploy-gated-on-tests` (раздел 6) добавляет в тот же job демо-сборку и отдельный
vitest-конфиг по её выводу. Поэтому перед началом сессии тестов состав перечитывается по
`.github/workflows/`, а не берётся отсюда на веру. В спецификации перечня нет намеренно —
там записано свойство, а не список.

**Общее правило для всех проверок ниже.** Предмет проверки (месяц, число записей, карточка)
берётся из фактического содержимого страницы, а не записывается в тест константой:
захардкоженный «сентябрь 2026» превратится в ложное падение при первом обновлении данных.
Если подходящего предмета на странице нет — проверка падает с «проверять было нечего», а не
проходит.

Две ловушки при заведении нового файла проверок:

- новый `tests/*.test.ts` подхватывается `vitest.config.ts` автоматически (`tests/**/*.test.ts`);
- но новый гейт **по собранному `dist`** надо и добавить в include-список
  `vitest.build.config.ts`, и внести в exclude `vitest.config.ts` — иначе он либо не
  запускается вовсе, либо запускается до сборки и падает на отсутствующем `dist`.

- [x] 2.1 [RED] Юниты чистого модуля месяцев на фикстурах: ключи записи внутри одного
      месяца (один ключ), через границу месяца (два ключа), через две границы (три
      ключа — событие длиннее двух месяцев в данных отсутствует, но формула обязана
      работать и на нём), при `endAt` раньше `startAt` (последний день — `startAt`,
      правило уже реализовано в `lastDay()`)
- [x] 2.2 [RED] Юнит: для идущего события, начавшегося в прошлом месяце, прошлый месяц
      отсутствует **и в ключах карточки, и в списке** — отсечение применено при выводе
      ключей, а не только к списку. Только фикстурами: в данных на опорную дату идущих
      событий нет ни одного, и молчание данных подтверждением поведения не является.
      Именно это место было дефектом первой редакции спеки: отсечение только в списке
      делало гейт 2.8 красным на исправном коде в три календарных дня из ближайших
      четырёх месяцев
- [x] 2.3 [RED] Юнит: у каждой записи, прошедшей окно актуальности, после отсечения
      остаётся **хотя бы один** ключ (инвариант, на котором держится отсечение)
- [x] 2.4 [RED] Юнит: список месяцев — объединение ключей записей, по возрастанию, без
      пустых месяцев внутри диапазона (фикстура с разрывом: декабрь, затем февраль).
      Фикстуру подавать **не отсортированной по дате**: на отсортированном входе проверка
      порядка зелена по совпадению и не измеряет сортировку вовсе
- [x] 2.5 [RED] Юнит подписи: ключ `2026-09` → «Сентябрь 2026»; заглавная буква,
      четырёхзначный год, приписки «г.» нет; результат не зависит от текущего времени
      (проверить при подменённом системном времени либо доказать отсутствием обращения к
      нему) **и не зависит от часового пояса окружения** — прогнать при `TZ`, сдвинутом в
      обе стороны от UTC: подпись, собранная через объект даты, разойдётся с ключом ровно
      на границе месяца
- [x] 2.6 [RED] Гейт по `dist`: у каждой карточки расписания непустой список ключей
      месяцев формата `YYYY-MM`; карточек без признака ноль. Пустая выборка карточек —
      падение с текстом «проверять нечего», а не успех
- [x] 2.7 [RED] Гейт по `dist`: сумма чисел записей по всем предложенным месяцам равна
      числу карточек **плюс суммарное число пересечённых границ** — то есть Σ(ключей − 1)
      по всем карточкам. Обе неверные формулировки назвать в комментарии, чтобы их не
      написали заново: «сумма = число карточек» красна на исправном коде уже сейчас, а
      «сумма = карточки + число карточек с несколькими ключами» верна только пока событие
      не длиннее двух месяцев (сегодня самое длинное — 4 дня, но это свойство данных).
      На опорную дату: 64 карточки, 2 пересечения, сумма 66
- [x] 2.8 [RED] Гейт по `dist`: множество предложенных месяцев совпадает с объединением
      ключей карточек в обе стороны — ни месяца без записей, ни ключа без месяца в списке
- [x] 2.9 [RED] Гейт по `dist`: контрол месяца приезжает выключенным, и ни одна карточка
      расписания не приезжает скрытой. Для **этого** сценария достаточно разметки отданного
      документа, отдельный прогон с отключённым JavaScript ему не нужен
- [x] 2.9a [RED] Сценарий «включение не сдвигает раскладку» — **прогон с отключённым
      JavaScript обязателен**, и это новая для проекта инфраструктура (`javaScriptEnabled`
      до сих пор не встречался нигде). Решение принято и записано в `design.md`: обычный
      браузерный прогон состояние «до включения» получить не может вовсе — включение идёт по
      ходу разбора документа, измерение застаёт конечное состояние, и проверка была бы
      зелёной при любом дефекте. Метрики снимаются во втором контексте
      (`javaScriptEnabled: false`) и сравниваются с метриками после включения; страница в
      этом контексте обязана отдаться с кодом 200, иначе измерять нечего. Область
      применения узкая: только этот сценарий
- [x] 2.10 [RED] Гейт по `dist`: блок скрипта управления расписанием опознаётся в
      документе однозначно (по признаку, а не по совпадению текста), и внутри него нет ни
      получения текущего времени, ни сравнения дат. Отсутствие опознаваемого блока —
      падение: иначе проверка ищет не там и молчит
- [x] 2.11 [RED] Браузерная проверка в файле, который запускается workflow `Tests`: выбор месяца
      сужает выдачу; месяц вместе с городом даёт пересечение; выбор института после
      выбора месяца ограничивает список программ и **не меняет** список месяцев. Месяц и
      город брать из фактических опций страницы, а не константами
- [x] 2.12 [RED] Браузерная: пагинация скрывается, когда после выбора месяца остаётся не
      больше 25 записей, и возвращается при сбросе месяца. Месяц выбирать по фактическому
      числу карточек; если на странице не нашлось ни одного месяца, дающего одну страницу
      при более чем одной без фильтров, — падать с «проверять было нечего», а не проходить
      (на опорную дату условие выполняется: без фильтров 64 записи — 3 страницы, самый
      населённый месяц — 15).
      **Как сделано на самом деле, и почему иначе:** требование закрывает синтетический
      сценарий `@month-pagination-return` (набор «30 записей в 2026-11 плюс 3 в 2026-12»),
      а проверка по живым данным (`@month-pagination`) осталась характеризацией и от хода
      времени не краснеет. «Падать, когда предмета нет» здесь неприменимо: предмет —
      «страниц больше одной» — исчезает законно, живых записей на 2026-11-24 останется ровно
      25 (TD-21), и падение остановило бы выкладку исправных коммитов. Ревью подтвердило,
      что это выбор реализации, а не предписание спеки: сценарий «пагинация исчезает и
      возвращается» реальных данных не требует
- [x] 2.12a [RED] Сброс на первую страницу — **отдельной проверкой на синтетическом
      наборе**, где у одного месяца больше 25 записей. На реальных данных сценарий
      ненаблюдаем: самый населённый месяц — 15 записей при 25 на странице, и зажим
      `currentPage = Math.min(...)` (`ScheduleControlsScript.astro:152`) приводит к первой
      странице даже без сброса, то есть браузерный тест по данным зелен и при сломанном
      поведении. Способ по `design.md`: поднять скрипт из собранной страницы по его
      признаку (задача 3.8) и исполнить над синтетическими карточками. Если сессия тестов
      сочтёт синтетический набор слишком хрупким — **явная отметка «сценарий не проверен»
      с этой причиной**; браузерный тест по реальным данным как проверка этого сценария не
      предъявляется
- [x] 2.13 [RED] Браузерная: сочетание месяца и других фильтров без записей показывает
      `[data-schedule-empty]` и ноль карточек
- [x] 2.13a [RED] Браузерная: месяц и поиск дают один и тот же набор в обоих порядках —
      сначала запрос затем месяц, и наоборот
- [x] 2.14 [RED] Браузерная: выбор месяца не меняет адрес страницы
- [x] 2.15 [RED] Браузерная: запись-заплатка (id 900001+) присутствует в выдаче при выборе
      своего месяца, наряду с обычными записями. Формулировка «месяц только с заплаткой»
      невыполнима — обе заплатки в октябре 2026, где ещё 12 обычных записей; проверять
      «по разметке заплатка не отличима» тоже нечем: идентификатора записи в разметке
      карточки нет, такая проверка проходила бы всегда.
      **Носитель требования — синтетический сценарий** `@month-supplement-synthetic`: имя и
      даты заплатки берутся из `scheduleSupplements`, ключи месяцев — из того же `monthKeys`,
      опорная дата — первый день самой заплатки, поэтому от календаря вывод не зависит.
      Проверка по живому набору (`@month-supplement`) осталась характеризацией: после
      2026-10-08 окно актуальности законно уводит обе заплатки, и она подтверждает лишь их
      отсутствие (TD-20). Способ верификации внесён в спеку отдельной правкой — она
      разрешена владельцем узко, только для этого сценария
- [x] 2.15a [RED] Юнит/гейт: пустой список месяцев оставляет контрол в разметке
      выключенным, и остальные фильтры продолжают работать. Проверяется фикстурами —
      в данных пустого расписания нет. Ветка опасна: ранний выход скрипта
      (`ScheduleControlsScript.astro:14-22`) при отсутствии одного контрола гасит все
      остальные.
      **Дополнено по находке ревью:** браузерная `@month-empty-list` монтирует СВОЮ разметку
      и измеряет скрипт, то есть про шаблон не говорит ничего, а dist-гейт видит только
      непустой список. Присутствие контрола в самом шаблоне при `months: []` проверяет
      `web/tests/schedule-filters.render.test.ts` — рендер компонента через Astro Container
      API (новая для проекта инфраструктура, объявлена в конфигурации)
- [x] 2.16 [RED] Браузерная a11y: у контрола месяца есть доступное имя и **видимый
      индикатор фокуса**. Существующая проверка фокуса берёт `select` через `.first()`
      (`web/tests/a11y.spec.ts:130-133`), то есть смотрит только на институт — новый
      контрол ею не покрыт.
      **Предпосылка устарела и названа ложной:** с появлением контрола месяца первым `select`
      в DOM стал месяц (`ScheduleFilters.astro:62` против `:76`), то есть старый гейт молча
      начал измерять именно его, а институт остался без покрытия. Исправлено отдельным
      коммитом: старый гейт закреплён именными селекторами (институт, место, поиск), месяц и
      программа исключены с причиной — оба законно бывают `disabled`
- [x] 2.17 [RED] Браузерная в мобильном проекте (375px): контрол виден, месяц выбирается,
      горизонтального переполнения нет. При **обычном** кегле: проверка переполнения при
      удвоенном кегле на этой странице в мобильном проекте помечена `fixme` по TD-5
      (`web/tests/a11y.spec.ts:633-635`) и на новом дефекте не покраснеет — опираться на неё
      нельзя
- [x] 2.18 [GREEN-BY-DESIGN] Существующий гейт `web/tests/schedule-window.test.ts:82-212`
      остаётся зелёным **содержательно**: сравнение `startAt` с датой по-прежнему только в
      `web/src/lib/schedule-window.ts`. Негативная мутация — внести сравнение в модуль
      месяцев и убедиться, что покраснел именно этот тест
- [x] 2.19 [GREEN-BY-DESIGN] Мета-гейт из трёх частей. Во всех трёх «исполняется» означает
      **«запускается workflow, успех которого требуется для публикации»**; запуск любым
      другим workflow условия не удовлетворяет. **(а) По файлам:** каждый файл браузерных
      проверок либо исполняется в этом смысле, либо **поимённо назван** в списке признанного
      долга. Файлов там **восемь**: `compare`, **`compat`**, `course-group-parity`,
      `institute-parity`, `parity-audit`, `schedule-parity`, `seminar-parity`,
      `visual-baseline`. `compat.spec.ts` запускается — но workflow `Nightly full checks`,
      который для публикации не требуется, поэтому исполняемым он не является и остаётся
      сиротой; пропустить его значит сделать гейт красным на исправном коде.
      **(б) По живости списка:** имя файла, которого в репозитории нет, и имя файла, который
      **стал** запускаться workflow, требуемым для публикации, роняют проверку.
      **(в) По СОБРАННОМУ НАБОРУ ТЕСТОВ, а не по тексту файлов.** Сверка идёт с перечнем,
      который даёт `playwright test <те же аргументы, что у гейта публикации> --list
      --reporter=json`: сбор импортирует файлы и исполняет регистрации `test()`, поэтому
      закомментированный тест в перечень не попадает. Каждый браузерный сценарий несёт
      **устойчивую метку** в имени; набор ожидаемых меток объявлен явно; объявленная метка без
      теста и тест с необъявленной меткой роняют гейт одинаково; тест с `skip`/`fixme`
      присутствующим не считается (в перечне он виден, но не выполняется — различимо по
      `annotations` и `expectedStatus` в JSON-отчёте); пустой перечень — провал.
      Дополнительно остаётся текстовое условие **про место**: признаки
      (`data-schedule-filter="month"`, `data-months`) не встречаются в файлах из списка долга.
      **Почему (в) переписана:** её первая редакция требовала, чтобы признак встречался в
      тексте исполняемого файла, и удовлетворялась комментарием — можно было удалить все
      сценарии, оставив строку `// data-months`, и гейт оставался зелёным. Условие против
      декоративности само было декоративным. Из наличия строки наличие проверки не выводится;
      обратное направление (строка в файле из долга роняет гейт) остаётся законным — оно про
      место, а не про выполнение.
      Workflow разбирать через `web/tests/helpers/workflows.ts` (готовый YAML-разбор, пустой
      результат — провал), а не построчным поиском
- [x] 2.19a [RED] Каждая браузерная проверка из этого раздела получает **устойчивую метку** в
      имени теста, и набор ожидаемых меток объявляется явно рядом с мета-гейтом. Метка — не
      название теста: названия переписывают при правке формулировок, и сверка по ним краснела бы
      от переименования, то есть от работы, которая ничего не ломает. Точных названий тестов
      спецификация не закрепляет намеренно
- [x] 2.19b Проверить, поднимает ли режим сбора (`--list`) `webServer` из
      `playwright.config.ts`. Если поднимает — листинг выносится отдельным шагом workflow, а не
      подпроцессом из vitest, иначе мета-гейт начнёт собирать сайт. Проверено чтением, что флаг
      `--list` в закреплённой версии есть и что пропуски различимы по `annotations` /
      `expectedStatus`; **форма вывода и поведение `webServer` прогоном не проверялись** — это
      работа автора проверок, а не принятый факт.
      **Закрыто прогоном.** `--list` `webServer` не поднимает (ни одного слушающего порта до
      и после, `dist` не собирается) и установленных браузеров не требует — проверено в
      окружении с пустым `PLAYWRIGHT_BROWSERS_PATH`, где настоящий прогон падает с
      «Executable doesn't exist», а сбор даёт 6 passed; подтверждается и зелёным `Unit and
      build tests` на PR, где `playwright install` не выполняется вовсе. Отдельный шаг
      workflow не понадобился.
      **Но различимость пропусков по `annotations` / `expectedStatus` подтвердилась только
      для СТАТИЧЕСКОЙ формы** (`test.skip` в заголовке). Рантайм-форма (`test.skip(true, …)`
      в теле) в перечне выглядит как `expectedStatus: "passed"` с пустыми `annotations` —
      сбор её не видит вовсе. Отсюда вторая сверка, с прогоном: `scripts/check-month-run.ts`
- [x] 2.20 [GREEN-BY-DESIGN] Каждая браузерная проверка страницы расписания утверждает код
      ответа `200`. Негативная мутация — направить проверку на несуществующий адрес,
      убедиться в падении: разбор страницы 404 иначе проходит всегда
- [x] 2.21 Сценарий «число в подписи месяца совпадает с выдачей» — проверка пишется, если
      мокап выбрал подпись с числом; если нет, поставить **явную отметку**, что сценарий
      не применим по решению владельца, с указанием выбранного варианта. Молчаливого
      пропуска сценария быть не должно
- [x] 2.22 Предъявить красный прогон: отдельный push коммита **только с тестами** (упавший
      check-run на этом SHA) либо лог с точной командой и полным SHA. Перечислить, какие
      тесты падали и **с каким текстом** — по имени падения, а не по цвету прогона
- [x] 2.23 Проверить полноту покрытия: каждый сценарий спеки имеет проверку либо явную
      отметку, почему её нет. Отметка называет причину, а не констатирует отсутствие

## 3. Реализация (после approve, выбора мокапа и красного прогона)

- [x] 3.1 Новый чистый модуль вывода месяцев (например `web/src/lib/schedule-months.ts`):
      ключи месяцев записи (от месяца `startAt` до месяца `lastDay(entry)` включительно,
      **с отсечением месяцев ранее месяца опорной даты прямо здесь**), список месяцев как
      объединение уже отсечённых ключей в возрастающем порядке, подпись месяца. Опорная
      дата — аргументом; `new Date()` внутри нет. Отсечение именно в выводе ключей, а не в
      построении списка: иначе у карточки остаётся ключ, которого нет в списке
- [x] 3.2 Последний день события брать из `lastDay()` (`web/src/lib/schedule-window.ts:25`),
      не вычислять заново: правило про `endAt` раньше `startAt` уже реализовано и покрыто
      тестами, второе его место — тот самый дефект, против которого стоит гейт
- [x] 3.3 Подпись — таблицей из 12 названий в именительном падеже, а не выводом `Intl`:
      `Intl` с русской локалью даёт «сентябрь 2026 г.» (строчная буква и приписка), а его
      вывод к тому же зависит от версии ICU в сборочном окружении. Номер месяца и год
      брать **из самого ключа**; объект даты для этого не собирать — он вносит часовой
      пояс, а ключ выведен по UTC, и на границе месяца подпись разошлась бы с ключом
- [x] 3.4 `web/src/pages/raspisanie-i-tseny.astro`: вычислить ключи месяцев и список
      месяцев **после** существующего фильтра окна (`:57-63`), передав уже готовое
      `todayDate` (`:57`) — второй вычислитель «сегодня» не заводить
- [x] 3.5 `ScheduleListing.astro`: признак карточки со списком ключей, рядом с
      существующими `data-institute` / `data-program` / `data-city` / `data-search`
      (`:38-41`)
- [x] 3.6 `SchedulePage.astro` и `ScheduleFilters.astro`: проброс списка месяцев и контрол
      по выбранному мокапу. Контрол приезжает `disabled` и включается скриптом — образец
      рядом, список программ (`ScheduleFilters.astro:46` + `ScheduleControlsScript.astro:60`);
      пустой список месяцев оставляет контрол выключенным, но **не убирает его из
      разметки**: ранний выход скрипта (`ScheduleControlsScript.astro:14-22`) гасит все
      фильтры, если хотя бы один элемент не найден, поэтому исчезнувший контрол месяца
      увёл бы с собой поиск, институт, программу и город. Пустое значение называется
      «Не выбрано», как у трёх соседних фильтров
- [x] 3.7 `ScheduleControlsScript.astro`: условие месяца в `getFilteredItems` (`:53-62`)
      **сравнением по целому токену**, а не `includes` — `includes` здесь верен лишь
      случайно, из-за фиксированной ширины ключа (обоснование в `design.md`). Обработчик
      смены — по образцу города (`:159-162`), со сбросом `currentPage = 1`
- [x] 3.8 Признак опознаваемости блока скрипта управления (атрибут вида
      `data-schedule-controls`) — без него проверка из 2.10 неприменима: в документе есть
      другие inline-скрипты
- [x] 3.9 Ни одного `new Date` / `Date.now` / `Date.parse` в клиентском скрипте
      расписания; окно актуальности не дублируется ни в модуле месяцев, ни в скрипте
- [x] 3.10 Раскладка панели фильтров по выбранному мокапу, включая ≤1100px и ≤700px;
      включение контрола скриптом не сдвигает раскладку (место занято заранее)

## 4. Негативные проверки новых гейтов

Каждый пункт: точная команда или патч, число до и число после, суждение по **имени**
покрасневшего теста. Мутация — в своём worktree, фикс закоммичен до мутации.

**Где свидетельства по отмеченным пунктам.** Патчи, числа «до/после» и имена покрасневших
тестов — в описании PR #98. База измерения на опорную дату: 64 записи, сумма по месяцам 66,
пересечённых границ 2, предложено 11 месяцев. Полнота каждой мутации проверялась
измерением, а не на глаз. Фикс был закоммичен до мутаций, после каждой — постусловие:
пустой `git status --porcelain` и пустой diff к SHA фикса.

Два результата, которых в этом плане не было:

- **4.2 не ловится dist-гейтом суммы, и это свойство формулы, а не дефект.** При разбиении
  по месяцу начала обе части равенства меняются согласованно (66/64/2 → 64/64/0), поэтому
  сверка остаётся зелёной; дефект ловят только юниты на фикстурах. Ожидание «падает 2.7 и
  сценарий про границу» подтвердилось для юнитов и **не** подтвердилось для dist.
- **4.11 не наблюдаема ничем, кроме синтетического набора** — при возвращённом дефекте 15
  браузерных проверок по реальным данным остались зелёными, покраснела ровно синтетика.
  Подтверждает оговорку самой задачи 4.11 измерением.

**Мутации мета-гейта прогнаны на итоговой голове** (`98b058d`), своим прогоном, а не
зачётом чужого. База — 6 passed, 16 меток, 4 текстовых признака в исполняемом файле, 0 в
файлах долга.

- **4.8c — главная.** Регистрация теста убрана комментированием тела, признаки оставлены:
  `data-schedule-filter="month"` 2 → 2 и `data-months` 2 → 2, то есть **всего 4 → 4**, а
  живых помеченных тестов 16 → 15. Прежняя текстовая редакция на этой мутации была бы
  зелёной; новая краснеет по имени «под каждую объявленную метку в собранном наборе есть
  выполняемый тест» с текстом `@month-narrow — теста нет в собранном наборе`. Это и
  отличает гейт от декорации.
- **4.8d.** `test.skip` при неизменном тексте: меток в файле 16 → 16, гейт краснеет с
  `@month-narrow — skip: «…»`. Собранное от выполняемого отличает.
- **4.8e.** Метка переименована в `@month-narrowed`, объявление не тронуто → краснеют **обе**
  проверки: объявленной метки нет в наборе и в наборе есть необъявленная. «Переименовали» не
  выглядит как «всё в порядке».
- **4.8a.** Имя убрано из списка долга → краснеет часть (а), названа `compat.spec.ts`.
- **4.8b.** В список долга добавлено несуществующее имя → краснеет часть (б), названо оно же.
- **4.8.** Проверка перенесена в `schedule-parity.spec.ts`: метки в исполняемом файле
  16 → 15, признаки в файле долга 0 → 2. Краснеют часть (в) и проверка «признаки не
  встречаются в файлах из списка долга»; часть (а) остаётся зелёной, потому что состав файлов
  не изменился — ровно как предсказывала задача.

После каждой мутации постусловие: пустой `git status --porcelain` и пустой diff к `98b058d`;
мета-гейт восстановлен до 6 passed, меток 16, признаков в файлах долга 0.

**Две ветки мета-гейта, не задетые первыми шестью мутациями.** Находка ревью: ни одна из шести
мутаций выше не проходит через ветки `untagged` и `resolved` — то есть они оставались
обещанием, как непроверенный гейт. Прогнаны своим прогоном на голове
`cb50a5b3ed2de34a1d719322dbcc84adce215d27` (фикс закоммичен до мутаций), база — 6 passed.

- **4.8f — ветка `untagged`.** Раньше она была пуста по построению: комментирование тела,
  `skip` и переименование метки список не наполняют (переименованный тег всё равно начинается
  с `month-`). Мутация — дописать в конец `web/tests/schedule-month.spec.ts` сценарий **без
  метки**:

  ```ts
  test('контрол месяца отдаётся страницей', async ({ page }) => {
    await openSchedule(page);
    await expect(page.locator(MONTH)).toBeVisible();
  });
  ```

  Непомеченных сценариев в файлах про месяц 0 → 1, мета-гейт 6 passed → 5 passed, 1 failed.
  Краснеет по имени «у каждого сценария месяца есть метка — непомеченный гейту невидим» с
  текстом `«контрол месяца отдаётся страницей» (schedule-month.spec.ts)`. Метки при этом
  16 → 16, то есть обе сверки объявления с набором молчат — ветку ловит только эта проверка.
- **4.8g — ветка `resolved`.** Мутации по списку долга (4.8a, 4.8b) роняют `orphans` и
  `stale`, но не её. Мутация — вписать файл из списка долга в аргументы гейтующего скрипта
  `test:e2e:schedule` в `web/package.json`:

  ```
  playwright test tests/schedule-month.spec.ts → playwright test tests/schedule-month.spec.ts tests/compare.spec.ts
  ```

  Имён в списке долга, которые уже исполняются, 0 → 1, мета-гейт 6 passed → 5 passed,
  1 failed. Краснеет по имени «список долга не содержит имён, которые уже исполняются или
  которых нет», и **по нужному из двух утверждений** — текст «файлы уже исполняются — их пора
  убрать из списка долга: compare.spec.ts», а не про отсутствующий файл: `stale` остаётся 0.
  Состав файлов не изменился, поэтому часть (а) зелена — ровно как в 4.8.

После каждой из двух — постусловие: пустой `git status --porcelain`, пустой diff к
`cb50a5b3ed2de34a1d719322dbcc84adce215d27`, мета-гейт восстановлен до 6 passed.

**Заплатка от хода времени в `@month-pagination` (TD-21), три состояния.** Проверка требовала
больше 25 живых записей, то есть утверждала про весь набор данных; на 2026-11-24 живых
останется ровно 25, и она покраснела бы на исправном коде внутри прогона, который держит
публикацию. Приём — ветвление по странице, как в `TD-20`. Мутации и числа:

- данные как есть → 64 живых, 64 карточки в разметке → ветка 1, зелено;
- `web/src/lib/schedule-window.ts`: `calendarToday` возвращает `'2026-12-01'` вместо
  `new Date()…` → 24 живых, 24 карточки → ветка 2, зелено;
- дата настоящая, но `web/src/pages/raspisanie-i-tseny.astro` обрезает список —
  `.slice(0, 20)` после сортировки → 20 карточек при 64 живых по данным → ветка 2
  **краснеет** по имени `@month-pagination`: «на странице записей 20, не больше страницы, а по
  снапшоту и окну актуальности на 2026-08-12 живых 64 — записи потеряны», 64 против ожидаемого
  ≤ 25.

Постусловие после каждой: пустой `git status --porcelain`, пустой diff к
`cb50a5b3ed2de34a1d719322dbcc84adce215d27`, пересборка возвращает 64 карточки.

**Второе ревью: рантайм-пропуск обходил ОБА гейта, и это исправлено (B2).** Мутация — первой
строкой тела `@month-narrow` добавлено `test.skip(true, '…')`. Числа на голове
`19fdc1a1c8657404af5ac8f0d02eb0c0d7c52f5f`: мета-гейт 6 passed → **6 passed** (сбор `--list`
отдаёт такой тест как `expectedStatus: "passed"` с пустыми `annotations` — отличить нечем),
прогон playwright 18 passed → 17 passed + **1 skipped** при коде выхода **0** (проверено
отдельным вызовом). То есть весь объём сценариев месяца выключался одной строкой при двух
зелёных гейтах, а условие, добавленное против декоративности, обходилось дешевле прежней
текстовой редакции.

Сверка с ПРОГОНОМ добавлена там, где прогон и происходит: `web/scripts/check-month-run.ts`
вызывается из `test:e2e:schedule`, то есть внутри шага `Run schedule month filter tests` в job
`e2e-smoke`. В юнит-конфигурацию это не положено намеренно: иначе юнит-прогон стал бы зависеть
от браузерного. Объявление меток вынесено в `web/tests/helpers/month-tags.ts` — с ним сверяются
оба гейта, и две копии разошлись бы. Та же мутация после правки: `test:e2e:schedule` **код
выхода 1**, красное по имени `@month-narrow` с текстом «тест есть, но не выполнился: … →
skipped». Восстановление: пустой `git status --porcelain`, 18 passed, все 18 объявленных
сценариев выполнены.

**Негативные проверки двух новых синтетических сценариев.**

- Инверсия условия месяца в скрипте управления (`indexOf(...) !== -1` → `=== -1`): браузерные
  18 passed → **6 passed, 12 failed**, среди упавших `@month-supplement-synthetic` с текстом
  «заплатка „Что такое КСТ…“ не попала в выдачу своего месяца 2026-10». То есть сценарий ловит
  ровно то, что обещает.
- Пагинация: первая мутация (`pagination.hidden = false`) гейт **не покраснила**, и это не
  декоративность, а неудавшийся эксперимент — при одной странице `innerHTML` всё равно
  очищается, пустой `nav` невидим и без атрибута. Полная мутация — считать одну страницу
  многими (`pageCount <= 1` → `<= 0` в обоих местах `renderPagination`): 2 passed → **2
  failed**, `@month-pagination-return` с текстом «три записи, а пагинация осталась».
- Независимость от календаря предъявлена измерением: `calendarToday` → `'2027-06-01'` (обе
  заплатки давно вне окна, на странице 3 карточки) — оба синтетических сценария зелёные, а
  оба живых берут обратные ветки и тоже зелёные.

**Ветки мета-гейта, пройденные РЕВЬЮЕРОМ (атрибуция важна: своим прогоном я их не
повторяла).** База 6 passed:

- `'site.spec.ts'` дописан в список долга → 6 passed → **1 failed** («файлы уже исполняются —
  их пора убрать из списка долга: site.spec.ts»);
- удаление шага `Run schedule month filter tests` из workflow → 6 passed → **4 failed**;
- **снятие** метки `@month-narrow` из заголовка → 6 passed → **2 failed** («под каждую
  объявленную метку…» и «у каждого сценария месяца есть метка»). Наполняет `untagged` именно
  снятие метки, а не переименование: 4.8e предъявляет лишь одну половину союза «либо».

**Гейт окна актуальности ловил одно начертание из пяти (находка второго ревьюера).** Признак
был списком двух литеральных начертаний. Проверено пробником `web/src/lib/probe-new-window.ts`
с пятью начертаниями одного дефекта: прежний гейт — **1 из 5** (`e.startAt.slice(0, 10) >= t`),
новый, на разборе синтаксического дерева, — **5 из 5**, каждое со своей строкой. Мимо прежнего
проходило и то начертание, которым правило записано в самом `schedule-window.ts`
(`(e.startAt ?? '').slice(0, 10) >= t`), и вынос сравнения в переменную. Постусловие: пробник
удалён, пустой `git status --porcelain`, 9 passed.

**Пробел «контрол месяца присутствует в разметке всегда» (M5).** Мутация — обернуть блок
месяца в `ScheduleFilters.astro` в `{months.length > 0 && (…)}`. До правки её не ловил ни один
гейт; после — рендер-гейт 2 passed → **1 passed, 1 failed** («при пустом списке месяцев контрол
исчез из разметки»), при этом на той же мутации остались зелёными юниты 216, dist 112 и
браузерные месяца 18. Это и есть измеренная величина пробела.

**Гейт видимого фокуса (M4).** Мутация — снять индикатор у `.schedule-select` временным блоком
стилей: 2 passed → **1 passed, 1 failed**, названы ОБА поля (`institute`, `city`). До правки
та же мутация дала бы одну строку, и та про месяц.

**Гейт видимого фокуса, вырожденные входы (M1a, M1b).** M4 доказывает, что гейт видит
настоящий дефект; эти две — что он не молчит там, где проверять нечего. Обе выполнены на
голове `da91dc9b61565ad03a6ffa896a17affd892427d3` в отдельном worktree, при закоммиченном
фиксе, прогоном
`node tests/helpers/preview-reset.mjs && npx playwright test tests/a11y.spec.ts
--project=desktop -g "видимый фокус" --reporter=list` (базовый прогон без мутаций — 2/2
зелёные, 5 селекторов, 0 ненайденных).

- **M1a, «нечего проверять».** В списке целей `/statyi` селектор
  `.article-filter-bar input` заменён на `.article-filter-bar input-m1a-nonexistent`.
  Ненайденных селекторов **0 → 1**. Покраснел именно `/statyi: поля управления получают
  видимый фокус`, соседний `/raspisanie-i-tseny` остался зелёным; сообщение — «эти селекторы
  не нашли ни одного поля — проверять было нечего», `Received: ["/statyi
  .article-filter-bar input-m1a-nonexistent"]`. До правки этот вход давал `continue`, то
  есть зелёное.
- **M1b, «страница отдала не 200».** Адрес цели заменён на `/statyi-m1b-nonexistent-page`.
  Код ответа **200 → 404** (число из самого сообщения теста — доказательство, что предмет
  мутации исчез, а не испортилась строка сравнения). Падение на утверждении статуса, то
  есть **до** цикла по селекторам: анализ доступности не доходит до страницы 404, где
  нарушений нет по построению.

Судили по ИМЕНИ покрасневшего теста, а не по цвету прогона: в обоих случаях соседний
сценарий остался зелёным. Восстановление после каждой мутации подтверждено постусловием по
предмету — мутировался отслеживаемый файл, поэтому `git status --porcelain` пуст и
`git diff da91dc9b61565ad03a6ffa896a17affd892427d3` пуст.

Побочная находка прогона, к гейту не относящаяся, но стоящая записи: порт держал сиротский
`astro preview` с `ppid 1` из каталога `/private/tmp/wt-pr98-rebase`, которого в
`git worktree list` уже нет — демон переживает удаление своего worktree. Ищется он только
широким шаблоном (`pgrep -fl "astro.*preview"`): привычное `pgrep -fl "astro preview"` его
не находит, потому что в командной строке идёт `node …/astro.mjs preview --port 4322` и
слова не соседствуют. То есть привычная проверка отвечала «процессов нет» там, где процесс
был.

**Расхождение с требованием было названо вслух и с тех пор закрыто правкой требования.** Спека
предписывает падать с «проверять было нечего», когда предмета нет (`spec.md`, требование про
«дефектов нет» против «проверять было нечего» и сценарий «предмет проверки берётся со
страницы»), и то же слово стоит в задаче 2.12; обе заплатки от хода времени — `TD-20` и
`TD-21` — вместо падения берут обратную ветку. Отдельный шаг `/opsx:update` сделан — PR #106,
`main@1debf3609c4aa1efee846fa56784b1a85ba35534`: в спеке появилось требование «Предмет,
исчезнувший по календарю, отличается от предмета, потерянного дефектом» (`spec.md:574`) и
названо ровно одно исключение из падения (`spec.md:512`). То есть расхождения больше нет, и
обратная ветвь обеих заплаток разрешена требованием, а не терпится. Запись в
`docs/tech-debt.md` (TD-20, TD-21) остаётся про сам долг данных, а не про расхождение со
спекой.

**Снятая dist-проверка «фильтрация опирается на признак записи».** Она утверждала поведение, а
измеряла текст скрипта, и фильтр комментариев её не спасал: из наличия строки наличие
фильтрации не следует вовсе. Измерено на голове
`7b18284bd69fa1badb77b521713d78dc99722b81` мутацией
`web/src/components/schedule/ScheduleControlsScript.astro`: вместо сравнения по токену —
`const matchesMonth = true;`, признак `data-months` оставлен только в комментарии (в `dist` он
доезжает, скрипт `is:inline`). Результат: `schedule-month-dist.test.ts` 13 passed → **13
passed** (снятая проверка зелёная вместе с убранной фильтрацией), браузерный прогон
`npm run test:e2e:schedule` 16 passed → **9 passed, 7 failed**: `@month-narrow`, `@month-city`,
`@month-empty-state`, `@month-pagination`, `@month-mobile`, `@month-whole-key`,
`@month-three-months`. Предмет стережёт выдача, а не текст, поэтому проверка снята, а не
починена. Постусловие: пустой `git status --porcelain`, пустой diff к
`7b18284bd69fa1badb77b521713d78dc99722b81`.

- [x] 4.1 Убрать признак месяца у карточек → падают 2.6, 2.8 и браузерные проверки выбора.
      Проверить, что мутация **полная**: признак ставится в одном месте, но убедиться
      измерением, а не глазами — в проекте уже был случай, когда неполная мутация
      оставляла предмет живым и гейт зелёным
- [x] 4.2 Разбить события по месяцу **начала** (снять принадлежность к последующим
      месяцам) → падает 2.7 и сценарий «через границу месяца в обоих месяцах». Числа до и
      после: на опорную дату сумма по месяцам 66 при 64 карточках → станет 64
- [x] 4.2a Перенести отсечение прошлых месяцев из вывода ключей в построение списка (то
      есть вернуть дефект первой редакции спеки) → падает 2.2 и гейт сверки 2.8. Мутация
      обязательна отдельно от 4.2: на реальных данных её видно только в три календарных
      дня, и без фикстуры она не обнаруживается вовсе
- [x] 4.3 Заменить сравнение по токену на `includes` → должна падать 2.6/сценарий «по
      целому ключу». **Ожидаемая ловушка:** на текущем формате ключа `includes` даёт
      верный ответ, и мутация может не покраснеть. Это значит не «гейт декоративен», а
      «мутация не убрала предмет»: тогда мутировать формат ключа (например, добавить
      суффикс) и повторить. Различать два случая обязательно
- [x] 4.4 Снять `disabled` с контрола месяца в разметке → падает 2.9
- [x] 4.5 Внести сравнение `startAt` с датой в модуль месяцев → падает 2.18 (существующий
      гейт окна), покраснеть должен именно он.

      **Формулировка задачи устарела внутри самой этой работы — исправлено по находке
      ревью 5.5 (P1-2).** Прежняя редакция предписывала вносить мутацию «в форме, которую
      гейт способен поймать», объясняла это узостью признаков
      (`new Date(<что-то>.startAt).getTime() <` и `<что-то>.startAt.slice(0, 10) <` в одной
      строке) и требовала назвать разложенное на две строки сравнение
      (`const t = new Date(entry.startAt).getTime();` и ниже `if (t < now)`) **известной
      узостью существующего гейта**. На голове ветки этой узости больше нет: коммит
      `1a7e5e2` переписал гейт с перечня начертаний на разбор AST — первый проход помечает
      переменные, чей инициализатор упоминает `startAt`
      (`web/tests/schedule-window.test.ts:126-141`), второй ищет сравнение с помеченной
      переменной (`:150-159`), и одноуровневый вынос ловится наравне с однострочной формой.
      Разметочные признаки (`:86-89`) остались только как первый, более дешёвый проход.

      Почему это не косметика: следующий исполнитель, читающий задачу буквально, внёс бы
      разложенную мутацию, **ожидая зелёного**, получил бы красное и заключил, что сломал
      что-то постороннее. Устаревшее описание гейта опаснее отсутствующего — оно
      предписывает неверное ожидание. Тот же довод продублирован в комментарии
      `web/tests/schedule-months.test.ts:302-304` и выправлен там же.
- [x] 4.6 Внести `new Date()` в клиентский скрипт → падает 2.10
- [x] 4.7 Снять признак опознаваемости блока скрипта → 2.10 падает с текстом «блок не
      найден», а не молча зеленеет
- [x] 4.8 Перенести одну проверку в `web/tests/schedule-parity.spec.ts` → падает мета-гейт
      2.19 **по части (в), а не по (а)**: файл уже назван в списке долга, поэтому состав
      файлов не меняется и файловый критерий молчит. Если краснеет по (а) — значит список
      долга разошёлся с репозиторием, и это отдельный дефект
- [x] 4.8a Убрать один файл из списка долга (не меняя того, что его не запускает ни один
      workflow, требуемый для публикации) → падает 2.19 по части (а)
- [x] 4.8b Оставить в списке долга имя файла, которого нет, либо имя файла, который
      запускается workflow, требуемым для публикации → падает 2.19 по части (б)
- [x] 4.8c **Закомментировать тело браузерной проверки, оставив строку с её признаком** (или
      оставить признак мёртвым кодом) → падает 2.19 по части (в) с указанием отсутствующей
      метки. Это главная мутация всего мета-гейта: прежняя редакция (в) на ней была **зелёной**,
      потому что сверяла текст файла. Формулировка «удалить проверки месяца целиком» для
      негативной проверки не годилась — вместе с проверками исчезали и строки, текстовое
      условие краснело, и из этого делался неверный вывод, что гейт ловит удаление сценария
- [x] 4.8d Пометить браузерную проверку `test.skip` (или `fixme`), не меняя её текста →
      падает 2.19: в собранном перечне тест виден, но не выполняется. Без этой мутации
      неизвестно, отличает ли гейт собранное от выполняемого — а пометки `fixme` в проекте
      уже используются, то есть путь реальный
- [x] 4.8f Дописать в файл про месяц сценарий **без метки** `@month-…` → падает 2.19 по ветке
      «у каждого сценария месяца есть метка». Без этой мутации ветка оставалась непройденной:
      комментирование тела, `skip` и переименование метки её не наполняют — список
      непомеченных пуст по построению
- [x] 4.8g Вписать файл из списка долга в аргументы гейтующего скрипта в `package.json` →
      падает 2.19 по ветке «файлы уже исполняются — их пора убрать из списка долга». Мутации
      4.8a и 4.8b роняют соседние ветки (`orphans`, `stale`), а эту — нет
- [x] 4.8e Переименовать метку сценария, не меняя объявленный набор → падает 2.19 в обе
      стороны (объявленной метки нет в перечне; в перечне есть необъявленная). Проверяет, что
      сверка симметрична и «переименовали» не выглядит как «всё в порядке»
- [x] 4.9 Направить браузерную проверку на несуществующий адрес → падает 2.20 на коде
      ответа
- [x] 4.10 Подать в подпись месяца текущее время вместо аргумента → падает 2.5
- [x] 4.10a Собрать подпись через объект даты вместо номера месяца из ключа и прогнать при
      сдвинутом `TZ` → падает 2.5 на несовпадении подписи с ключом
- [x] 4.10b Убрать признак месяца у одной карточки из середины списка (не у всех) →
      падает 2.6. Проверка на полноту мутации из 4.1 в обратную сторону: гейт обязан
      ловить единичный пропуск, а не только поголовное отсутствие
- [x] 4.11 Убрать сброс на первую страницу при смене месяца → падает **2.12a**, и только
      она. Браузерная проверка по реальным данным на эту мутацию **не отреагирует** —
      максимум 15 записей в месяце при 25 на странице, зажим номера страницы даёт тот же
      наблюдаемый результат. Зелёный прогон браузерных проверок здесь означает не
      «мутация не сработала», а «этот предмет ими и не проверялся»; судить по имени
      покрасневшего теста. Если 2.12a оформлена отметкой «не проверено» — записать, что
      мутация не обнаруживается ничем, и не выдавать это за пройденную негативную проверку
- [x] 4.12 Восстановление после **каждой** мутации подтвердить постусловием по предмету:
      отслеживаемые файлы — пустой `git status --porcelain` плюс отсутствие diff к SHA
      фикса; мутации внутри `web/dist/` — контрольная сумма дерева до и после либо
      пересборка (`git status` в `dist` слеп, каталог в `.gitignore`)

## 5. Приёмка и архив

- [x] 5.0 **Предусловие приёмки: мета-гейт переделан под сверку с собранным набором тестов.**
      Реализация, сверяющая текст файлов (`text.includes` по признакам), требованию не
      удовлетворяет — она зелёная при удалённых сценариях. Пока это не исправлено,
      свидетельство прогона проверок принимать нельзя и change не закрывается: остальные гейты
      могут быть зелёными, а доказательства, что браузерные проверки месяца вообще
      выполняются, нет. Мутации 4.8c, 4.8d и 4.8e — предъявляемое свидетельство переделки
- [x] 5.1 Локальные гейты в `web/`: `npm run lint`, `npm run typecheck`, `npm run build`,
      `npm test`, `npm run test:build`, `npm run test:e2e:smoke`, `npm run test:e2e:a11y`.
      Перечислить в отчёте, что запускалось и что прошло
- [x] 5.2 Визуальные эталоны `schedule-desktop` и `schedule-mobile` в этом change
      **не перегенерируются**, и их расхождение с собранной страницей фиксируется как долг
      (решение владельца 2026-08-12). Из PR реализации посторонние PNG **выносятся**.

      Основание — два проверяемых факта: снимки платформенные (`-darwin`, снимаются на
      машине разработчика) и **ни один workflow их не проверяет** — `visual-baseline.spec.ts`
      запускают только npm-скрипты `test:e2e:visual` / `test:e2e:visual:update`, которых нет
      ни в одном workflow. То есть эти PNG гейтом не являются, их устаревание ничего не
      останавливает, а перегенерация в этом PR **поглотила бы необъяснённое изменение** —
      в снимок вошёл переключатель темы в шапке, к фильтру месяца отношения не имеющий.
      Обновлённый эталон утверждал бы «так и надо» про изменение, которое никто не разбирал.

      **Поправка к прежней редакции этой задачи.** Здесь стояло, что постороннее расхождение —
      переключатель темы, пришедший с `981477e` (PR #27), то есть дрейф старше фильтра месяца.
      При проверке для записи TD-19 это **не подтвердилось**: эталоны записаны **тем же**
      коммитом `981477e`, что и последняя правка `HeaderTools.astro`, то есть на нём снимок с
      шапкой совпадал; а изменение шапки сдвинуло бы все шесть страниц набора, тогда как
      остальные пять проходят.

      Причина постороннего расхождения **не установлена**, и версию «виновата незакреплённая тема»
      проверили измерением: 3 серии по 10 снимков (без закрепления / светлая / тёмная) дали
      **нулевой разброс внутри каждой**, а без закрепления и со светлой — побайтово одинаковый
      снимок. То есть тема в этой обвязке детерминированна (свежий контекст, пустой
      `localStorage`), и причина не в ней; при этом серия с тёмной темой отличается именно в области
      тумблера, поэтому закрепление темы всё равно нужно — как страховка. Задокументированная
      гонка с `aria-checked` (`base.css:193-197`) локально не воспроизводится по своему же
      описанию. Ведущий кандидат теперь — **среда снятия**. Числа, метод и порядок дальнейших
      действий — в TD-19; повторять проверку темы не нужно.

      Перегенерировать эталоны до объяснения нельзя: обновлённый эталон утверждал бы «так и надо»
      про изменение, которого никто не разобрал

      Куда уходит расхождение: **записью `docs/tech-debt.md` → TD-19**, а не в change
      `visual-regression-gate`. Запись заведена тем же изменением, что и эта задача: иначе долг
      предписан, но не записан, и после архива change предписание уехало бы в
      `openspec/specs/`, не оставив следа в списке долгов. Проверено, а не предположено: тот change прямо исключает этот
      набор из своей области — `openspec/changes/visual-regression-gate/proposal.md:41-43`,
      «существующий `visual-baseline.spec.ts` **не меняется и в CI не переносится**: у него
      другая задача — сравнение облика при редизайне, — и другие эталоны (12 файлов
      `*-darwin.png`, 12 МБ)». Он проектирует **новый** визуальный гейт со своим набором, а не
      берёт на себя этот.

      TD-16 тоже не годится как место: она про **неисполняемость** восьми файлов, а здесь
      предмет другой — содержимое эталонов разошлось с собранной страницей, и внутри него два
      разных расхождения (ожидаемое от контрола месяца и неразобранное от переключателя темы).
      Запись обязана называть оба и не сливать их в одно «облик изменился»
- [x] 5.2a Убедиться, что в PR реализации нет ни одного изменённого файла
      `web/tests/visual-baseline.spec.ts-snapshots/*.png`. Проверка предметная, а не на
      доверии: `git diff --name-only <база>..HEAD -- 'web/tests/**/*.png'` обязан быть пуст
- [x] 5.3 Проверить, что число новых `skipIf` равно нулю. Если появился — запись в
      `docs/tech-debt.md` обязательна
- [x] 5.4 Убедиться, что состояние TD-5 не изменилось: `fixme` мобильной проверки
      переполнения при удвоенном кегле не снимается и не расширяется этим изменением
- [x] 5.5 Ревью кода двумя независимыми исполнителями (не автором реализации), с разными
      уклонами; подтверждённые находки исправлены, отклонённые названы с причиной

      **Закрыто тремя проходами, а не двумя, и третий понадобился по существу.** Проходы 1
      («соответствие фактам») и 2 («полнота последствий») — находки внесены. Проход 3
      (13.08.2026) взял уклон «дефекты, внесённые самими исправлениями»: по опыту проекта
      часть дефектов вносится в ходе правок по находкам, и правки по проходам 1–2 сами
      отревьюированы не были. Он дал пять P2 при отсутствии P0/P1, и материальная причина
      отсутствия названа: в неотревьюированной дельте нет ни строки продуктового кода —
      `web/src/`, `web/scripts/` и `web/package.json` побайтово идентичны между
      `da91dc9b61565ad03a6ffa896a17affd892427d3` и головой прохода 3.

      Все пять P2 закрыты (`e79665b`, `4ef59e7`, `a784f27`): дефект гейта
      `check-month-run` через red/green, вторая дата истечения в TD-25, `THEN` вырожденной
      пары в спеке, два неверных числа в теле PR.

      Дельту этих правок отревьюировал **не их автор** — сессия-оркестратор, с независимой
      перепроверкой по репозиторию, а не по отчёту: подтверждено, что на коммите теста
      `e79665b` guard'а ещё нет (то есть красное было настоящим), и негативная проверка
      нового гейта выполнена заново своей мутацией (условие guard'а сделано недостижимым,
      `=== 0` → `=== -1`): **2 passed → 1 failed | 1 passed**, покрасневший тест назван по
      имени — «на пустом объявлении падает, а не сообщает об успехе», в сообщении
      воспроизведён сам дефект («выполнены все 0 объявленных сценариев месяца», код 0);
      контрольная проверка осталась зелёной, то есть мутация попала в свой предмет.
      Постусловие по предмету мутации: `git status --porcelain` пуст и `git diff a784f27`
      пуст, контрольный прогон снова 2 passed.

      Отдельным пунктом задания обоим ревьюерам (внесено задачей 6.5): убедиться, что
      **календарное ветвление применено только там, где дата исчезновения предмета
      вычислима**, и что **ни одна ветка не прикрывает потерянный предмет** — то есть в
      каждой ветке «предмета нет» есть достижимое падение, сверяющее отсутствие со счётом
      по данным. Пункт обязателен, а не желателен: автоматически это не проверяется ничем,
      риск назван остаточным в `design.md`. Проверять по репозиторию: условие ветвления —
      наблюдаемый вывод и счёт по данным, а не календарная дата литералом (обратная мутация
      «записать дату в условие» автоматически не ловится, задача 6.12)

      **Кем исполнен этот пункт — названо, а не подразумевается.** Он исполнен проходом 3;
      про проходы 1 и 2 такого свидетельства в теле PR нет, и выдавать их за исполнителей
      пункта нельзя. Что проход 3 действительно его исполнил, доказывается не заявлением, а
      результатом: он нашёл ровно дефект названного класса — обратная ветка `@month-city`
      прикрывала потерянный предмет, потому что у утверждения «оно не пустое» не было срока
      (P2-1, закрыто в TD-25 второй датой 2027-06-07 и окном вакуумности
      2027-06-07 … 2027-09-17). Дополнительно проверено, что условие ветвления — счёт по
      данным, а не календарная дата литералом, и что даты TD-20/21/25 воспроизводятся
      независимым счётом по `discovery/entities/schedule_entries.json`.
- [x] 5.6 Строгая валидация `./bin/openspec validate schedule-month-filter --strict
      --no-interactive` зелёная перед приёмкой
- [x] 5.7 Архивировать change; требования переезжают в `openspec/specs/`. Если к этому
      моменту появился baseline области расписания — сначала сверить дельту с основной
      спекой и проверить **применимость** (применением в одноразовом worktree), а не
      только строгую валидацию: она не проверяет существование требования в основной спеке

      **Условие про baseline проверено и не наступило.** На момент архивирования
      (13.08.2026) в `openspec/specs/` лежат две capability — `article-catalog` и
      `deploy-gating`, — baseline области расписания среди них нет, поэтому сверять дельту
      с основной спекой не с чем и проверка применимости к этому change неприменима.
      Проверено списком, а не по памяти: `./bin/openspec validate --all --strict
      --no-interactive` печатает состав, и `spec/` в нём ровно два.

      Оговорка о порядке относительно merge: архив лежит в ветке, поэтому требования
      попадают в `openspec/specs/` в момент merge, одним изменением вместе с самим change —
      без промежуточного состояния, где основная спека есть, а дельта ей противоречит.

## 6. Календарное исчезновение предмета проверки

Раздел добавлен правкой спеки от 2026-08-12 и относится к требованию «Предмет, исчезнувший по
календарю, отличается от предмета, потерянного дефектом». Существующие пункты разделов 2–5 он
не отменяет и не переномеровывает: общее правило «нет предмета — падать» продолжает
действовать, а здесь описано **единственное названное исключение** из него.

Нумерация начата с 6 намеренно — чтобы правка не переставляла номера, на которые уже ссылаются
мутации и отметки в разделах 2 и 4.

- [x] 6.1 Для каждой проверки, у которой предмет исчезает по календарю, **вычислить дату
      исчезновения** из источника данных и того же вывода о времени, что у продукта
      (`web/src/lib/schedule-window.ts`), и записать её в комментарий проверки. Вычислить, а не
      оценить: дата — критерий права на ветвление, и без неё случай считается первым (предмет
      потерян) и проверка падает. На сегодня таких проверок две — заплатки (`2026-10-09`) и
      число живых записей (`2026-11-24`), — но задача сформулирована по признаку, а не по
      перечню: третья такая проверка попадает под неё автоматически

      Даты **пересчитаны самостоятельно**, а не взяты из отчёта: склейка снапшота
      `discovery/entities/schedule_entries.json` (63 записи, все `active`) с
      `scheduleSupplements` по тому же ключу `slug:startAt`, что у страницы, затем
      `isCurrentOrFuture` из `web/src/lib/schedule-window.ts` перебором календарных дат. Счёт
      совпал с известными числами: заплатки исчезают **2026-10-09** (в окне 1 → 0), порог
      пагинации нарушается **2026-11-24** (живых 26 → 25), снапшот истекает **2027-09-18**
      (живых 1 → 0); живых на 2026-08-12 — 64, что сходится с числом карточек в собранной
      странице (64). Даты со способом счёта записаны в комментарии обеих ветвящихся проверок
      (`web/tests/schedule-month.spec.ts`, `@month-pagination` и `@month-supplement`)

      **Отметка «таких проверок две» оказалась неполной**: третья, `@month-city`, ветвилась
      по календарю уже тогда и в счёт не попала. Найдена ревью 5.5, дата вычислена
      (2027-05-24) — см. раздел 6г. Задача сформулирована по признаку, и признак сработал;
      подвёл именно перечень в отметке, поэтому он здесь исправлен, а не оставлен как след
- [x] 6.2 Условие ветвления построить **на наблюдаемом выводе и счёте по данным**, а не на
      дате-литерале в тексте проверки. Счёт по данным брать из тех же модулей, что использует
      страница (окно актуальности, склейка с заплатками), и опорную дату — из того же источника,
      что у сборки. Свой счёт срока жизни записи или своя «сегодня» — второй носитель правды о
      времени, запрещённый требованием «Клиентский код не выводит время»

      Проверено по репозиторию: условие `@month-pagination` — `entries.length <= PAGE_SIZE`
      (число карточек, прочитанных со страницы), условие `@month-supplement` —
      `present === undefined` (заплатка, найденная в разметке и в списке месяцев). Календарной
      даты ни в одном условии нет: даты в файле встречаются только в комментариях и в
      синтетических наборах. Счёт по данным — `liveEntryCount` и `isCurrentOrFuture` из
      `schedule-window`, склейка с заплатками тем же ключом `slug:startAt`, что на странице
      (`web/src/pages/raspisanie-i-tseny.astro:60-63`); опорная дата — `calendarToday()` из
      того же модуля, которым её берёт сборка (`raspisanie-i-tseny.astro:58`). Второго
      носителя правды о времени в проверке нет: своей арифметики дат она не делает вовсе — в
      файле нет ни одного `new Date()` (grep без совпадений), «сегодня» приходит только из
      `calendarToday()`
- [x] 6.3 Ветку «предмета нет» написать так, чтобы она **утверждала наблюдаемое**: сверка
      отсутствия предмета со счётом по данным (расхождение → падение с числами: сколько
      наблюдается и сколько обязано быть) плюс остаток требования, наблюдаемый и без предмета.
      Утверждение, которое невозможно сделать ложным, не годится — это `skip` под другим именем

      Обе ветки падают с двумя числами, и оба падения предъявлены (см. 6.10):
      `@month-pagination` — «на странице записей 20, не больше страницы, а по снапшоту и окну
      актуальности на 2026-08-12 живых 64 — записи потеряны»; `@month-supplement` — «на
      странице заплаток 0, а по данным и окну актуальности на 2026-08-12 обязано быть 2 —
      запись потеряна». Второе сообщение доработано этой задачей: прежняя редакция
      перечисляла имена заплаток, но не называла наблюдаемое число. Остаток требования,
      наблюдаемый без предмета: у пагинации — её отсутствие до выбора месяца, после и после
      сброса; у заплаток — заплатка, попавшая на страницу, но недостижимая выбором своего
      месяца
- [x] 6.4 Убедиться, что **каждое требование, чья проверка использует календарное ветвление**,
      закрыто сценарием на собственных данных — фикстурах чистой функции либо синтетическом
      наборе, где предмет существует по построению. На сегодня это ровно два сценария:
      «заплатка достижима выбором своего месяца» — нужна синтетическая запись-заплатка вместе
      с обычной записью того же месяца, подтверждающая достижимость заплатки выбором своего
      месяца независимо от окна актуальности; и «пагинация исчезает и возвращается» — нужен
      синтетический набор из трёх состояний: больше 25 записей без фильтра → навигация видна,
      после выбора малочисленного месяца → скрыта, после сброса фильтра → видна снова.
      В `#98` (`change/schedule-month-filter-impl@369fea9eab96c447a33d9dda089f6a03ae82e86c`,
      `web/tests/schedule-month.spec.ts`) оба сценария уже присутствуют: `пагинация скрывается
      выбором месяца и возвращается сбросом @month-pagination-return` (строка 677) и `заплатка
      достижима выбором своего месяца независимо от календаря @month-supplement-synthetic`
      (строка 703); проверки по живому набору (`@month-pagination`, `@month-supplement`, строки
      270 и 334) сами ссылаются на них как на носителя требования. Задача этим не закрыта —
      предстоит не написать, а **подтвердить**: убедиться, что для обоих сценариев прогон
      зелёный на названном SHA, что каждое требование с календарным ветвлением закрыто именно
      такой проверкой без пробелов, и предъявить сам прогон. Требование в этом объёме, у
      которого такой проверки нет или прогон не предъявлен, — **дефект спеки**: возвращается
      через `/opsx:update`, а не переформулируется на месте под то, что удобно проверить

      **Подтверждено, прогон предъявлен.** На
      `change/schedule-month-filter-impl@6504eb615d28ad2d058bb5a43769e7ac01e0ef42` прогон
      `playwright test tests/schedule-month.spec.ts --project=desktop` дал 18 из 18, среди них
      №17 `@month-pagination-return` (строка 695) и №18 `@month-supplement-synthetic`
      (строка 721) — оба зелёные; `check-month-run` на том же прогоне: «выполнены все 18
      объявленных сценариев месяца, пропущено 0». Пробелов нет: календарное ветвление
      применено ровно в двух проверках, и требование каждой из них закрыто синтетическим
      сценарием на собственных данных — что подтверждено ещё и мутациями M1 и M3, где живой
      предмет исчезал, а оба синтетических сценария оставались зелёными

      **«Пробелов нет» было сказано преждевременно.** Пробел был ровно один и именно того
      рода, против которого стоит задача: третья ветвящаяся проверка (`@month-city`) в счёт
      не попала, и её требование носителя не имело. Закрыто разделом 6г — носитель
      `@month-city-synthetic`, прогон 19 из 19 на
      `change/schedule-month-filter-impl@3900d31e96557bf3659b960db2e843f65d4416b4`,
      `check-month-run` — все 19 объявленных выполнены. Отметка сохранена вместе с
      поправкой: подтверждение, сделанное по перечню вместо признака, — это тот самый
      способ получить зелёное там, где не смотрели
- [x] 6.5 Ревью кода (раздел 5.5) отдельным пунктом задания: убедиться, что ветвление применено
      только там, где дата исчезновения предмета **вычислима**, и что ни одна ветка не
      прикрывает потерянный предмет. Автоматически это не проверяется ничем — риск назван в
      `design.md` как остаточный, поэтому пункт ревью обязателен, а не желателен

      Пункт внесён в задание 5.5 (см. раздел 5) — само ревью ещё не проведено, закрыт именно
      внос пункта
- [x] 6.6 Убедиться, что для этого случая не появилось ни одного `skip`/`fixme`. Запрет
      технический: помеченный тест в собранном наборе виден, но выполняемым не считается, и
      мета-гейт 2.19 краснеет по его метке. Отдельная мутация не нужна — её роль исполняет 4.8d,
      и повторять её здесь незачем; проверяется фактом отсутствия пометок

      Факт: `grep -nE "(test|describe)\.(skip|fixme)|\.(skip|fixme)\(|annotation"
      web/tests/schedule-month.spec.ts` — совпадений нет (код выхода 1, то есть «нет
      совпадений», а не ошибка чтения). Слова `skip` и `fixme` в файле встречаются трижды и
      все три раза в комментариях — про то, почему пометка не используется, и про `fixme`
      **чужой** проверки по TD-5. Независимое подтверждение того же факта — `check-month-run`
      на прогоне 6.4: «пропущено по данным прогона 0»
- [x] 6.7 Для каждой ветвящейся проверки — запись в `docs/tech-debt.md` с вычисленной датой
      исчезновения предмета, названным прогоном и настоящим лечением (обновить данные реальным
      содержимым). Сдвиг дат записей вперёд и смягчение порога проверки лечением не считаются и
      в записи называются негодными. На сегодня записи есть — TD-20 и TD-21; задача требует, чтобы
      их **не** предъявляли как разрешение ветвиться: разрешает требование спеки, запись фиксирует
      срок

      Обе записи сверены с этим требованием и дополнены: даты исчезновения (2026-10-09,
      2026-11-24, плюс 2027-09-18 у TD-21) стояли и раньше — теперь при каждой лежит **команда
      мутации и два числа**, имя покрасневшего теста и опорный SHA восстановления; названо
      негодным не только смещение дат, но и смягчение проверки; в обеих прямо сказано, что
      ветвление разрешает требование спеки, а запись фиксирует срок и лечение. Из TD-21 убрано
      устаревшее «расхождение со спекой, в коде сделано, в спеке нет»: расхождение закрыто PR
      #106 (`main@1debf3609c4aa1efee846fa56784b1a85ba35534`), и оставленное «нет» читалось бы
      как разрешение, выданное списком долгов

      Записей стало три: к TD-20 и TD-21 добавилась **TD-25** на ветку `@month-city`
      (раздел 6г), в том же формате и с той же оговоркой про порядок предъявления

### 6н. Негативные проверки ветвления (три состояния, не два)

Каждая ветвящаяся проверка предъявляет **три** записанных состояния. Ветка выбирается сдвигом
опорной даты или данных — **не правкой текста проверки**: правка текста доказывает, что тест
можно сломать, а не что ветка достижима в жизни. Мутация — в своём worktree, фикс закоммичен
до мутации, восстановление подтверждается постусловием по предмету (раздел 0). Судить по
**имени** покрасневшего теста.

Все четыре мутации сделаны в своём worktree (`/private/tmp/wt-month6`, ветка
`change/schedule-month-filter-impl`) на закоммиченном состоянии: опорный SHA —
`6504eb615d28ad2d058bb5a43769e7ac01e0ef42`. После каждой — восстановление
`git checkout -- <файл>`, постусловие «`git status --porcelain` пуст **и** diff относительно
опорного SHA пуст» выполнено все четыре раза. Прогон каждый раз — весь файл
(`playwright test tests/schedule-month.spec.ts --config=<временный, порт 4323> --project=desktop`),
и вывод судится по **имени** покрасневшего теста. Порт 4322 занимал посторонний фоновый preview
осиротевшего worktree (`/private/tmp/wt-pr98-rebase`, каталог удалён, PID 55240), чужие
процессы трогать запрещено — поэтому конфигурация прогона отличалась от штатной **только**
номером порта, а после прогонов удалена.

- [x] 6.8 Состояние «предмет есть»: данные как есть → ветка первая, зелено. Записать число
      предметов, а не только цвет

      Заплатки: **2 из 2** на странице (`grep -c` по заголовкам в
      `dist/raspisanie-i-tseny/index.html` — 1 и 1) → `@month-supplement` ветка 1, зелено.
      Пагинация: **64** карточки (`grep -o 'data-months="' … | wc -l`) при пороге 25, живых по
      данным тоже 64 — два счёта над одним предметом сошлись → `@month-pagination` ветка 1,
      зелено. Прогон целиком: 18 из 18, `check-month-run` — 18 сценариев, пропущено 0
- [x] 6.9 Состояние «предмет исчез законно»: сдвинуть опорную дату (или даты записей) так,
      чтобы предмет вышел из окна → ветка вторая, зелено. Записать два числа — до и после

      Обе санкционированные формы использованы по разу. **M1, сдвиг данных** (заплатки):
      `perl -pi -e "s/2026-10-01T00:00:00\.000Z/2026-07-01T00:00:00.000Z/g;
      s/2026-10-08T00:00:00\.000Z/2026-07-08T00:00:00.000Z/g" src/lib/schedule-supplements.ts`
      + `npm run build` → заплаток на странице **2 → 0**, карточек 64 → 62; прогон 18 из 18,
      `@month-supplement` зелёный во второй ветке. **M3, сдвиг опорной даты** (пагинация):
      `calendarToday` заменён на `() => '2026-12-01'` в `src/lib/schedule-window.ts`
      + `npm run build` → карточек **64 → 24**; прогон 18 из 18, `@month-pagination` зелёный во
      второй ветке. В обоих состояниях первая ветка недостижима по построению условия, поэтому
      зелёное здесь — это зелёное именно второй ветки
- [x] 6.10 Состояние «предмет потерян»: **дата настоящая**, но предмет убран из наблюдаемого
      вывода → ветка вторая **краснеет**, и текст падения называет расхождение со счётом по
      данным. Это главная мутация раздела: без неё неизвестно, отличает ли ветка «предмет
      кончился» от «предмет потеряли», то есть является ли она проверкой вообще

      **M2** (заплатки): даты настоящие, склейка на странице обезврежена —
      `perl -pi -e 's/\Q...scheduleSupplements.filter((entry) => !scheduleIndex.has\E/
      ...scheduleSupplements.filter((entry) => false \&\& !scheduleIndex.has/'
      src/pages/raspisanie-i-tseny.astro` + `npm run build`. Заплаток на странице 2 → 0, при
      этом по данным в окне на 2026-08-12 их 2. Результат: 17 passed, **1 failed —
      `@month-supplement`**, «на странице заплаток 0, а по данным и окну актуальности на
      2026-08-12 обязано быть 2 — запись потеряна».

      **M4** (пагинация): дата настоящая, выдача обрезана — `.slice(0, 20)` добавлен к
      сортировке записей в `src/pages/raspisanie-i-tseny.astro` + `npm run build`. Карточек
      64 → 20 при 64 живых по данным. Результат: 17 passed, **1 failed —
      `@month-pagination`**, «на странице записей 20, не больше страницы, а по снапшоту и окну
      актуальности на 2026-08-12 живых 64 — записи потеряны».

      В обоих случаях краснел ровно проверяемый тест — судить пришлось по имени, потому что
      прогон был бы красным и от чужого падения
- [x] 6.11 Проверить **полноту** мутации 6.10 измерением, а не глазами: зелёная ветка после
      мутации значит одно из двух — ветка декоративна **или** предмет не убран. Различать
      обязательно; в проекте уже был случай, когда неполная мутация объявлялась доказательством
      декоративности гейта

      Измерено по собранной странице до и после каждой мутации: M2 — заголовки обеих заплаток
      1 и 1 → 0 и 0 (карточек 64 → 62), M4 — карточек 64 → 20. То есть предмет убран, и
      покраснение — свойство ветки, а не следствие неполной мутации.

      Счётчик пришлось подобрать под предмет: `grep -o 'data-schedule-item'` даёт **65** при 64
      карточках, потому что подстрока встречается ещё и в селекторе внутри скрипта страницы.
      На полной мутации такой счёт показал бы «предмет не убран целиком» и увёл бы вывод в
      сторону — поэтому карточки считаются по `data-months="`, которого в скрипте нет
- [x] 6.12 Проверить, что ветка выбирается **по данным, а не по литералу**: сдвинуть данные,
      не меняя ни одной даты в тексте проверки, и убедиться, что ветка сменилась. Обратная
      мутация — записать дату в условие ветвления — обязана быть замечена ревью (6.5), потому
      что автоматически она не ловится

      Ни одна из четырёх мутаций не трогала `web/tests/` — менялись только `src/lib` и
      `src/pages` (видно по путям в командах выше), то есть ветка сменилась от данных и опорной
      даты, а не от правки проверки. Смена ветки подтверждается измерением, а не догадкой: в M1
      заплаток на странице 0, значит `present === undefined`, значит исполнилась вторая ветка —
      первая при этом условии недостижима, это `if … return`; в M3 карточек 24 при пороге 25,
      значит `entries.length <= PAGE_SIZE`. Обратная мутация (записать дату литералом в условие)
      автоматически не ловится — она вынесена пунктом в задание ревью 5.5

### 6г. Третья ветвящаяся проверка: «месяц вместе с городом» (находка P1-1 ревью 5.5)

Раздел 6 был написан по признаку, но его отметки называли **два** сценария — и это оказалось
не описанием признака, а перечнем, который отстал. Ревью нашло третью календарно ветвящуюся
проверку, `@month-city`: она выбирала пару «месяц + город» со строгим сужением, а при её
отсутствии молча брала любую. Ни одного из трёх условий раздела 6 для неё выполнено не было
— ни вычисленной даты, ни долговой записи, ни трёх состояний, — и вырожденная ветка была
декоративной: при `both === inMonth(key).length` и `both === byCity` оба утверждения зелены
при полностью выключенном фильтре города.

- [x] 6г.1 Вычислить дату исчезновения предмета (условие 6.1). **2027-05-24.** Счёт: склейка
      снапшота с `scheduleSupplements` по ключу `slug:startAt`, отбор `status === 'active'` и
      `isCurrentOrFuture`, ключи из `monthKeys` — те же модули, которыми о времени судит
      сборка, — перебором календарных дат, и на каждую дату тот же отбор кандидатов и то же
      условие строгости, что в проверке. По 2027-05-23 живых 6 (2027-05: СПб 2 + Москва 1) и
      строгая пара есть; с 2027-05-24 живых 4, и в каждом живом месяце ровно один город
- [x] 6г.2 Написать носителя требования, независимого от календаря (условие 6.4):
      `@month-city-synthetic` (`web/tests/schedule-month.spec.ts:814`) — 5 записей, в ноябре 3,
      в Москве 3, на пересечении 2. Набор различает четыре поведения, из которых на
      вырожденных данных неразличимы три: фильтров нет (5), только месяц (3), только город
      (3), оба (2); проверяется ещё и независимость от порядка задания условий. Метка внесена
      в `EXPECTED_MONTH_TAGS` — объявленных стало 19 вместо 18, и `check:month-run`
      подтверждает, что выполнены все 19
- [x] 6г.3 Переписать ветку «предмета нет» так, чтобы она утверждала наблюдаемое (условие
      6.3): сверка отсутствия строгой пары со счётом по данным (`strictPairInData`), сверка
      группировки городов на странице с данными (счёт по имени, страница по слагу — без этого
      счёт был бы не о том предмете) и остаток требования: пересечение на вырожденной паре
      плюс то, что город сужает выдачу и без месяца
- [x] 6г.4 Долговая запись (условие 6.7): **TD-25**, с датой 2027-05-24, тремя состояниями,
      командами мутаций, двумя числами в каждом и опорным SHA восстановления. Номер перебран
      по всем локальным и удалённым веткам: 1–24 заняты, TD-22 — на
      `change/serving-cache-headers-impl`
- [x] 6г.5 Три состояния (раздел 6н). Опорный SHA восстановления —
      `3900d31e96557bf3659b960db2e843f65d4416b4`, прогон каждый раз весь файл
      (`npm run test:e2e:schedule` из `web/`), судится по имени; после каждой мутации
      `git status --porcelain` пуст и diff относительно опорного SHA пуст. Порт 4322 был
      свободен, временная конфигурация не понадобилась.

      **Предмет есть:** данные как есть, 64 карточки и 6 слагов города → ветка 1, 19 из 19.
      **Предмет исчез законно:** `calendarToday` заменён на `() => '2027-05-24'` в
      `src/lib/schedule-window.ts` + `npm run build` → карточек **64 → 4**, города
      `санкт-петербург` 2 и `москва` 2, то есть ровно распределение из счёта 6г.1 и строгой
      пары на странице нет → ветка 2, 19 из 19.
      **Предмет потерян:** дата настоящая, признак города схлопнут (`citySlug: 'moskva'` в
      `src/pages/raspisanie-i-tseny.astro`) + `npm run build` → карточек 64, слагов города
      **6 → 1** → 17 passed, 2 failed, **краснеет `@month-city`**: «на странице нет пары
      „месяц + город“ с пересечением строго меньше каждого из двух множеств, а по снапшоту и
      окну актуальности на 2026-08-12 такая пара есть — записи потеряны». Второе красное —
      `@month-empty-state`: с одним городом пустого сочетания «месяц + город» не существует,
      это ожидаемое следствие мутации, а не разлад
- [x] 6г.6 Негативная проверка самого носителя: убит фильтр города в клиентском скрипте
      (`const matchesCity = true;` в `ScheduleControlsScript.astro`) + `npm run build`;
      полнота измерена — `grep -c "matchesCity = true" dist/raspisanie-i-tseny/index.html` → 1.
      Прогон 14 passed, 5 failed, **`@month-city-synthetic` краснеет по имени**: показанных
      записей **2 → 3**, к «Ноябрь Москва два» и «Ноябрь Москва раз» добавилась «Ноябрь
      Онлайн». Остальные четыре красных опираются на тот же фильтр; более узкой мутации в
      скрипте нет — фильтр города там один
- [x] 6г.7 Спека обновлена через `/opsx:update`: число ветвящихся сценариев в требовании о
      календарном исчезновении предмета стало три, третий назван поимённо вместе с тем, чем
      закрыта его независимость от календаря; в требование «Выбор месяца складывается…»
      внесено, почему пара обязана быть строгой; сама фраза про «сегодня» переписана так,
      чтобы число в ней читалось как отстающее наблюдение, а не как признак
- [x] 6г.8 Ратифицированы девять строк спеки, дописанных коммитом реализации
      `0c86c3febfaad3896f6c03d760d9db2b5190aabe`. Одна строка (`AND при выборе другого месяца
      её карточки в выдаче нет`) вводила новую обязанность и оставлена в нормативной форме
      решением этого update. Восемь строк абзаца «Способ проверки этого сценария» переписаны
      как изложение следствия, а единственная содержавшаяся в них новая обязанность — брать
      имя, даты и опорную дату из источника заплатки — поднята в общее требование, где
      действует на все три ветвящихся сценария. Разбор и обоснование — в `design.md`

## 6д. Находки третьего прохода ревью

Раздел добавлен 2026-08-13 по трём подтверждённым находкам ревью PR #98. Пункты разделов 2–6г
он не отменяет; нумерация продолжена буквой по той же причине, что у 6г, — чтобы не
переставлять номера, на которые ссылаются мутации и отметки.

- [x] 6д.1 Сценарий «месяц вместе с городом» переписан через `/opsx:update`: `WHEN` больше не
      сужен до строгой пары, `THEN` утверждает наблюдаемое при **любой** паре (включая
      вырожденную), а механика — отбор пары и поведение ветки «предмета нет» — перенесена в
      `design.md`. Обязанность продукта при этом не менялась: она стояла и стоит в теле
      требования; менялось то, что для вырожденной пары сценарий не утверждал никакого
      `THEN`, и на календарной ветке привязать к нему проверку было нечем. Тот же update внёс
      в тело требования абзац о том, что строгость — правило отбора предмета, а не условие
      обязанности
- [x] 6д.2 В TD-25 названа **вторая** дата — `2027-06-07`, с которой обратная ветка
      `@month-city` вакуумна: живых записей 2, обе в `2027-09` и обе в Москве, то есть один
      месяц и один город, и оба наблюдения ветки зелены при выключенных фильтрах. Окно
      вакуумности `2027-06-07 … 2027-09-17`; с `2027-09-18` живых ноль и краснеет весь файл
      (TD-21). Пересчитано независимо — тот же перебор дат, те же модули времени и тот же
      предикат живости, что в тестах, плюс собственное условие ветки; первая дата
      (`2027-05-24`) подтвердилась. Прежняя редакция утверждала про обратную ветку «оно не
      пустое» без срока — теперь срок назван, как у TD-21 с её двумя датами. Вторая дата
      внесена и в комментарий проверки
- [x] 6д.3 Гейт `web/scripts/check-month-run.ts` получил guard на пустое объявление. Без него
      цикл по `EXPECTED_MONTH_TAGS` на пустом списке не делал ни шага, скрипт печатал
      «выполнены все 0 объявленных сценариев месяца» и выходил с кодом 0 — вакуумность,
      выданная за успех; симметрия `undeclared` этого не ловила, потому что прогон без меток
      `month-*` вместе с пустым объявлением зелен. Red/green: `web/tests/check-month-run.test.ts`
      запускает **подлинный** скрипт (`copyFileSync`) с подменённым модулем объявления — до
      guard'а тест «на пустом объявлении падает, а не сообщает об успехе» падал (код 0 вместо
      1, в выводе «выполнены все 0»), после guard'а оба теста зелены. В том же файле —
      контроль невакуумности самого теста: непустое объявление с выполненным тестом даёт код
      0. Guard тот же и по той же причине, что у сестринской сверки
      `web/tests/browser-test-gating.test.ts`
