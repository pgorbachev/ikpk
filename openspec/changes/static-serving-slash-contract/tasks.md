## 1. Проверка соответствия кода уже принятому поведению

- [x] 1.1 Сверить действующий текст `scripts/bootstrap-vps.sh` (правило
      `location ~ ^(/.*[^/])/$ { return 301 $1$is_args$args; }`) с требованием в
      `specs/static-serving/spec.md` этого change: форма адреса, ровно три класса
      исключения СРЕДИ ПРАВИЛ КЕШ-ПОЛИТИКИ по порядку выбора `location` (`^~ /_astro/`,
      `^~ /fonts/`, `favicon.` без якоря конца строки), сохранение строки запроса —
      совпадает ли поведение дословно. Не путать с двумя другими множествами: адресными
      классами кеш-политики, заякоренными на конец строки (`pf_*`, `.svg`, карта сайта) —
      спека прямо называет их НЕ исключением; и 257 точными совпадениями легаси-адресов из
      `deploy/nginx-redirects.conf` — они тоже не доходят до правила нормализации, но по
      другой причине (точное совпадение выигрывает независимо от порядка) и не считаются
      среди «ровно трёх»
- [x] 1.2 Сверить `deploy/nginx-redirects.conf`: у каждого легаси-адреса без слэша (кроме
      адреса со строкой запроса — он правилом по пути не обслуживается вовсе) есть
      собственная запись с вариантом со слэшем, ведущим на тот же канон, а не на
      промежуточный адрес
- [x] 1.3 Убедиться, что `web/tests/serving-config.test.ts` (блок `единственность адреса:
      форма со завершающим слэшем`) зелёный на текущей голове `main` и что каждый из трёх
      новых сценариев спеки покрыт конкретным тестом этого блока — назвать соответствие
      явно (сценарий → имя теста)
- [ ] 1.4 Снять и приложить к архивированию свидетельство для абзаца про `merge_slashes`
      (ручная приёмка, причина названа в самой спеке — предмет не выразим текстом
      конфигурации, потому что директива нигде не объявлена явно, а поведение зависит от
      умолчания nginx): `curl -sS -D- -o /dev/null 'http://<host-стенда>/kontakty//'` без
      `-L` — ожидается `301` с `Location: .../kontakty`. Предыдущий прогон этой команды был
      сделан в сессии, готовившей PR #200, но нигде не сохранён отдельно от разговора —
      этот шаг переснимает его как воспроизводимое свидетельство, а не ссылается на
      несохранённое

## 2. Проверка отсутствия регресса у смежного требования

- [x] 2.1 Перечитать требование «Перенаправления схемы и хоста отдаются раздачей, а не
      правилами по пути» (`openspec/specs/static-serving/spec.md`) и убедиться, что его
      сценарий «Адрес с www и со слэшем» (цепочка в два шага: канонизация хоста, затем
      путевая нормализация) остаётся верным при новом тексте требования из этого change.
      **Не считать, что это следует из формального текста бюджета цепочки** — дельта прямо
      говорит, что бюджет «≤2 шага от объявленного в карте адреса» покрывает только адрес,
      дословно совпадающий со строкой карты, а не форму со слэшем. Проверить нужно другое:
      что для легаси-адреса (не только для канонического, как в существующем сценарии) та
      же комбинация схема/хост + слэш действительно укладывается в два шага благодаря
      второму пункту дельты (собственный вариант со слэшем, ведущий сразу на канон), а не
      предполагать это как данность

## 3. Верификация и архив

- [x] 3.1 `./bin/openspec validate --all --strict --no-interactive` зелёный
- [x] 3.2 `./bin/check-spec-refs` зелёный (код 0) — ссылка этого change на PR #200 и на
      существующие тесты корректна
- [x] 3.3 Независимая сверка: требование ↔ код ↔ тест — тот, кто архивирует, не тот, кто
      писал спеку (или явно назвать причину, если это не так)
- [ ] 3.4 `/opsx:archive`: требование переезжает в `openspec/specs/static-serving/spec.md`,
      change перемещается в `openspec/changes/archive/`
- [ ] 3.5 Обновить `openspec/change-flow.yaml`: записать переход этого change в конечное
      состояние по завершении (переход в `active` уже записан при заведении change —
      число, от которого он отталкивался, названо честно как непроверяемое из чистого
      клона, см. `proposal.md`)
