## Why

Сайт публикует **два разных номера образовательной лицензии одновременно**. На главной
захардкожен «№3471», а на `/svedeniya-ob-obrazovatelnoy-organizatsii` из восстановленных
панелей выводится «№ Л035-01271-78/00330375 от 13 июня 2018 г.» вместе со ссылкой на
реестр Рособрнадзора. Для образовательной организации это не опечатка: посетитель, который
пойдёт проверять лицензию по номеру с главной, в реестре её не найдёт.

Все факты сняты с `change/education-trust@aa7678cff3d7a63aeeac581ff36b901ca2ea9a70` и
**пересчитаны по собранному выводу** (`web/dist` после `npm run build`), а не по снимкам
данных. Полные свидетельства с командами — в `design.md`.

В собранном боевом выводе «3471» лежит в двух файлах: `dist/index.html` (из
`web/src/components/home/sections/Programs.astro:26`) и
`dist/sotrudnichestvo-s-nami/index.html` (перенесённый контент, форма «№ 3471» — с
пробелом). Третье вхождение, `web/src/components/home/sections/InstitutesStrips.astro:13`,
в боевой вывод **не попадает вовсе**: компонент используется только на
`web/src/pages/preview/[variant].astro`, а `/preview` в боевой сборке не собирается.
Ни один тест в `web/tests/` номера лицензии не упоминает.

Рядом — вторая проблема того же рода: **сайт обещает документы, но не говорит, кому какой
достанется**. Условие «при наличии медицинского образования — удостоверение о повышении
квалификации» повторяется в 69 панелях «Выдаваемые документы»; обозначения образца
несовместимы между собой («государственного» в 40 панелях, «установленного» в 10). При
этом на главной (`Advantages.astro:25`) это частное условие **уже обобщено на всех**:
«студенты с базовым медицинским образованием получают удостоверения … с внесением сведений
в ФИС ФРДО … возможность получить … баллы НМО», тогда как в данных ФИС ФРДО заявлена ровно
на одной странице из 69, а баллы НМО — на десяти, и обещаны они **за цикл целиком, а не за
отдельный семинар**.

Третье: ответы на вопросы поступающего **есть, но их не найти в одном месте**. 28 вопросов
уже выводятся как `<details>/<summary>` на трёх хабах институтов и `/oplata`. Заголовок
«Часто задаваемые вопросы» на хабах тоже уже выводится — и это меняет объём работы: она не
про появление секции, а про её надёжность и про `/oplata`, где заголовка нет. Состав
вопросов на хабах держится на том, что функция `extractAdditionalHtml`
(`web/src/pages/[institute].astro:61-72`) находит **самый ранний** из трёх известных
заголовков и режет HTML до конца строки.

Почему сейчас: блок доверия — предусловие go-live (`docs/client-requests.md`, пункт 6
прямо просит номер лицензии), а `openspec/specs/` эту область не описывает вовсе.

## What Changes

- **У номера лицензии появляется единственный источник данных**, и требование адресуется
  **собираемому коду**, а проверка публикации — **собранному выводу**. Разделение несущее:
  правило про исходники ловит превью, которое в боевой вывод не попадает; правило про
  вывод ловит перенесённый контент, которого в исходниках нет.
- **«3471» перестаёт публиковаться как действующий номер** — либо исчезает, либо подаётся
  как прежний, и только после подтверждения формулировки заказчиком.
- **Рядом с номером появляется ссылка на реестр.** Оговорка, выясненная при сборе
  свидетельств: сводный реестр **не даёт постоянного адреса отдельной записи** — поиск
  идёт по ИНН или номеру. Обещается ссылка на реестр плюс данные для поиска (ИНН
  7811351115).
- **Канонический номер не выносится на новые места до ручной приёмки записи реестра.**
  Иначе спека объявляет `SHALL` значение, не подтверждённое по её же критерию.
- **Обобщение про ФИС ФРДО и НМО на главной приводится в соответствие с данными** — это
  ровно то, что запрещает новая возможность, и оно живёт в боевом выводе сегодня.
- **Появляется сводное соответствие «исходное образование → документ → выдающее лицо»**,
  выведенное из данных панелей.
- **Появляется критерий «подтверждено» — в спецификации**, вместе с **льготой для
  перенесённого контента** (без неё требование запрещало бы миграцию контента заказчика) и
  с правилом, что переформулирование на том же месте — тоже новое утверждение.
- **Вопросы поступающего получают надёжную секцию**: название на `/oplata`, где его нет, и
  независимость состава от совпадения строки заголовка на хабах.
- **Разметка `FAQPage` в область не входит** — Google прекратил показ FAQ rich results.
  Это «не делаем сейчас», а не запрет навсегда: вечный тест-надзиратель не пишется.
- **Меняется облик**, поэтому работа начинается с полноценных мокапов в вариантах и выбора
  владельцем — до любой разметки.

## Известные отклонения (не легализуются, но и не закрываются здесь)

Названы явно, чтобы требования не архивировались уже нарушенными:

- **Числа на боевой главной** — «14000+ специалистов», «> 20 лет», «1500+ получили
  государственную аккредитацию» (`Approach.astro:23-36`). Комментарий файла сам говорит,
  что они сверяются с заказчиком, а `docs/client-requests.md` (пункт 6) не знает даже
  порядка: «5000+ или 14000+?». Это неподтверждённые утверждения в боевом выводе. В область
  **не входят** — вопрос владельцу №8; требования этого change на них не распространяются,
  и это сказано в самой спеке, а не подразумевается.
- **Перенесённый `/sotrudnichestvo-s-nami`** — «№ 3471» и «Программы Института
  аккредитованы на портале НМО». Под льготу перенесённого контента попадает, но льгота
  названа в требовании, а страница перечислена поимённо — вопрос владельцу №6.

## Capabilities

### New Capabilities

- `verifiable-public-claims`: что сайт вправе утверждать о себе — критерий
  подтверждённости, льгота для перенесённого контента, единственный источник для
  регулируемых идентификаторов.
- `organization-license-identity`: какой номер лицензии канонический, откуда берётся, чем
  сопровождается и что происходит с прежним номером.
- `issued-documents-clarity`: что сайт обещает выдать, от чьего имени и при каком условии.
- `admission-faq`: находимость и устойчивость ответов поступающему.

### Modified Capabilities

<!-- Нет: на момент написания openspec/specs/ пуст. К моменту слияния там могут появиться
     основные спеки (архивы deploy-gated-on-tests и baseline-article-catalog идут в той же
     итерации). Если baseline опишет подачу главной, хабов институтов, /oplata или
     /svedeniya-ob-obrazovatelnoy-organizatsii — часть требований ниже станет MODIFIED.
     Проверяется до слияния применением в одноразовом worktree: строгой валидации для
     этого недостаточно. -->

## Impact

- **Исходники с номером:** `Programs.astro:26` (боевая главная),
  `InstitutesStrips.astro:13` (**только превью** — в боевой вывод не попадает).
- **Собранный вывод с номером:** `dist/index.html`, `dist/sotrudnichestvo-s-nami/index.html`.
- **Обобщение ФРДО/НМО:** `Advantages.astro:25` → `dist/index.html`.
- **Новый носитель данных организации** — сегодня реквизиты живут только внутри
  легаси-HTML, машиночитаемого источника нет.
- **Страницы:** главная, `/svedeniya-ob-obrazovatelnoy-organizatsii`, три хаба институтов,
  `/oplata`. Место сводного соответствия выбирается мокапом.
- **Пересечение с `payment-request-flow`:** тот change владеет `/oplata` — панелью
  «Способы оплаты?», размещением формы и раскладкой страницы
  (`payment-request-flow/proposal.md:52,90,117,126`). Секция вопросов этого change и форма
  того делят одну страницу и один визуальный эталон. Владелец разводит области — вопрос №7.
- **Визуальные эталоны, которые сломаются:** `home-{desktop,mobile}`,
  `institute-{desktop,mobile}`, `oplata-{desktop,mobile}` в
  `web/tests/visual-baseline.spec.ts-snapshots/`. Эталон `oplata-*` — общий с
  `payment-request-flow`.
- **Существующий гейт, который таблица обязана учесть:** `content-quality.test.ts:474`
  требует обёртку `<div class="table-scroll" tabindex="0">` вокруг таблиц. Из этого **не
  следует**, что соответствие подаётся таблицей: форма выбирается мокапом, и гейт
  применяется, только если выбрана таблица.
- **Доступность:** на `/svedeniya-ob-obrazovatelnoy-organizatsii` один и тот же
  `id="clip0_516_29476"` повторяется **56 раз** — знать до того, как связывать элементы
  через `aria-labelledby`.
- **Данные ЗЕТ/НМО:** `docs/zet-nmo-data-audit.md` (ветка `origin/docs/zet-nmo-audit`,
  коммит `2ccee7b`; на базе этого change файла ещё нет) прямо передаёт вопрос сюда и
  фиксирует, что **баллы НМО обещаны за цикл, а не за семинар**. Расхождение чисел «12» и
  «10» — разные рамки счёта (семинары против панелей), а не ошибка.
- **Внешняя проверка:** `obrnadzor.gov.ru` из окружения разработки недоступен — живая
  доступность реестра не может быть автоматическим гейтом.
- **Зависимость от заказчика:** формулировка «ранее №3471», охват ФИС ФРДО и корректный
  термин для образца документа. `docs/client-requests.md` не отправлен.
- **Новых зависимостей не добавляется.**

## Вопросы владельцу и заказчику

Единый список; пункты 1–5 — от автора спеки, 6–8 добавлены ревью.

1. **Формулировка «ранее №3471»** — подтверждает ли заказчик, что 3471 был прежним номером
   этой же лицензии? Основание теперь документальное, а не только совпадение даты: панель
   «Уставные документы» на `/svedeniya-…` прямо пишет «Номер лицензии на осуществление
   образовательной деятельности был изменен. Смотрите файл „Выписка из реестра лицензий“».
   Подтверждение формулировки всё равно за заказчиком. Без ответа номер снимается.
2. **Охват ФИС ФРДО** — на все программы с удостоверением или только на ту одну, где
   заявлена? От ответа зависит и текст на главной, и строка соответствия.
3. **Обозначение образца** — «установленного» вместо «государственного» по 273-ФЗ.
   Юридическую квалификацию даёт заказчик, не исполнитель.
4. **Место сводного соответствия** — `/svedeniya-…`, отдельная страница или блок на хабах.
   Решается выбором мокапа.
5. **Ручная приёмка записи реестра** — кто и с какого адреса открывает реестр и сохраняет
   свидетельство, раз из окружения разработки хост недоступен. Теперь это **предусловие**
   выноса номера на новые места, а не финальная проверка.
6. **Перенесённый `/sotrudnichestvo-s-nami`** («№ 3471», «аккредитованы на портале НМО») —
   правим контент заказчика вопреки Non-Goal, или гейт исключает перенесённые страницы, и
   тогда требование архивируется с названным отклонением?
7. **Кому принадлежат панели `/oplata`** — этому change или `payment-request-flow`, и кто
   ведёт общий эталон `oplata-*`?
8. **Числа «14000+ / > 20 лет / 1500+»** на боевой главной — в область этого change или
   отдельным изменением? Сейчас записаны как известное отклонение.
