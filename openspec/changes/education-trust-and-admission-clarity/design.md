## Context

Мотивация — в `proposal.md`, раздел «Why». Здесь только то, что определяет подход.

Опорная ревизия: `change/education-trust@aa7678cff3d7a63aeeac581ff36b901ca2ea9a70`
(голова `origin/main` на момент работы; задание называло базой `e26b4fee15d94c5fa4072714d055459cb37edfa8`,
но по правилу плана база перенесена на фактическую голову — два коммита между ними
трогают `web/package.json`, `web/package-lock.json`, `web/playwright.config.ts` и два новых
файла в `web/tests/helpers/`, области этого change не касаясь).

**Свидетельства пересняты по собранному выводу.** Первая редакция считала по снимкам данных
(`discovery/entities/*.json`), и независимое ревью показало, чем это плохо: обобщение про
ФИС ФРДО живёт в компоненте главной, которого в панелях нет вовсе, а третье вхождение
«3471» — в перенесённой странице, которой нет в исходниках. Ниже всё, что относится к
публикации, посчитано по `web/dist` после `npm run build`.

Четыре обстоятельства формируют решения:

1. **Машиночитаемого источника реквизитов организации нет** — они существуют только внутри
   строки легаси-HTML.
2. **Легаси-панель — контент заказчика, и она уже содержит правильный номер.** «Единый
   источник» не может означать «источник вытесняет панель».
3. **Исходники и собранный вывод — разные множества.** Компонент превью в вывод не
   попадает; перенесённая страница в исходниках не существует. Одна проверка над одним из
   этих множеств правило не удержит.
4. **Реестр Рособрнадзора недоступен из окружения и не даёт адреса записи.**

## Goals / Non-Goals

**Goals:**

- Сделать номер лицензии проверяемым и одинаковым везде, где он публикуется.
- Дать критерий подтверждённости, применимый механически, вместе с границами его действия.
- Собрать обещание о документах в одном месте, различая исходное образование, выдающее
  лицо и основание для баллов НМО.
- Убрать обобщение про ФИС ФРДО с главной либо подтвердить его.
- Сделать секцию вопросов названной и устойчивой.

**Non-Goals:**

- Не переписываем легаси-контент панелей кодом (в том числе опечатку «удостовернеие» и
  разнобой формулировок).
- Не добавляем разметку `FAQPage`.
- Не сочиняем данные.
- Не проверяем живую доступность реестра автоматическим гейтом.
- Не решаем юридический вопрос «государственного» против «установленного» образца.
- Не трогаем числа «14000+ / > 20 лет / 1500+» — известное отклонение, вопрос владельцу №8.

## Свидетельства

Команды воспроизводимы из корня рабочего дерева на указанной ревизии. Там, где предмет —
публикация, счёт идёт по `web/dist` после `npm run build`.

### С1. Официальный адрес реестра лицензий

```
https://obrnadzor.gov.ru/gosudarstvennye-uslugi-i-funkczii/gosudarstvennye-uslugi/liczenzirovanie-obrazovatelnoj-deyatelnosti/svodnyj-reestr-liczenzij/
```

**Где найден:** `discovery/entities/collapsible_panels.json:200` — панель «Основные
сведения» страницы `/svedeniya-ob-obrazovatelnoy-organizatsii`, внутри
`<a href="…" target="_blank">реестре лицензий</a>`. Ссылку поставил сам заказчик на боевом
сайте.

**Подтверждение актуальности:** независимый веб-поиск возвращает первым результатом ровно
этот адрес, озаглавленный «Реестр лицензий | Федеральная служба по надзору в сфере
образования и науки». Дополнительно найден набор открытых данных того же реестра:
`https://obrnadzor.gov.ru/otkrytoe-pravitelstvo/opendata/7701537808-fbdrl/`.

**Честная оговорка — адрес не открыт мной:**

```
$ curl -sS -o /dev/null -w "code=%{http_code}\n" --max-time 25 \
    "https://obrnadzor.gov.ru/…/svodnyj-reestr-liczenzij/"
curl: (28) Connection timed out after 25006 milliseconds

$ curl -sS -o /dev/null -w "code=%{http_code}\n" --max-time 25 "https://islod.obrnadzor.gov.ru/rlic/"
curl: (28) Connection timed out after 25002 milliseconds
```

Через инструмент выборки — `ECONNREFUSED 80.250.169.2:443`: DNS разрешается, порт 443
соединение не принимает. Вероятная причина — ограничение доступа для не-российских адресов;
отличить от простоя отсюда нельзя. Статус С1: **адрес найден и подтверждён внешним поиском,
живая доступность не проверена.**

Отсюда решение Р8 (ниже): пока запись не открыта и не сохранена, канонический номер не
выносится на новые места.

### С2. Постоянного адреса отдельной записи реестра не существует

Поиск в реестре выполняется по ИНН, регистрационному номеру лицензии или названию — запись
достаётся формой, а не адресом. Ни в легаси-контенте, ни в результатах поиска нет примера
постоянной ссылки на конкретную запись.

**Следствие:** требование «живая ссылка на официальную запись реестра» из решения владельца
№1 в буквальном виде невыполнимо. Заменено на «ссылка на реестр **плюс** данные для поиска
записи». Подмена названа явно.

### С3. Реквизиты для поиска записи

| Поле | Значение |
|---|---|
| Полное наименование | Общество с ограниченной ответственностью «Институт клинической прикладной кинезиологии» |
| Номер лицензии | № Л035-01271-78/00330375 от 13 июня 2018 г. |
| ИНН / КПП | 7811351115 / 780601001 |
| ОГРН | 5067847182651 |
| Юридический адрес | 195112, Санкт-Петербург, Новочеркасский пр., д. 22/15, Лит А, помещение 4Н |

### С4. «3471»: три места, и они принадлежат разным множествам

**В исходниках** (`grep -rn "3471" web/src scripts cms/src web/tests`) — два:

- `web/src/components/home/sections/Programs.astro:26` — боевая главная;
- `web/src/components/home/sections/InstitutesStrips.astro:13` — **только превью**.

**В собранном боевом выводе** (`grep -rl "3471" web/dist`) — тоже два, но не те же:

- `dist/index.html` — из `Programs.astro`;
- `dist/sotrudnichestvo-s-nami/index.html` — перенесённый контент
  (`discovery/entities/static_pages.json`, слаг `sotrudnichestvo-s-nami`), форма записи
  «№ 3471» **с пробелом**.

`InstitutesStrips.astro` в боевом выводе отсутствует: он импортируется только в
`web/src/pages/preview/[variant].astro:31,70`, а `/preview` в боевую сборку не попадает
(проверено: каталога `preview` в `web/dist` нет).

**Три следствия, каждое меняет проверку:**

1. Проверка над выводом **не увидит** `InstitutesStrips` — правило единственного источника
   должно проверяться по **исходникам**.
2. Проверка над исходниками **не увидит** `/sotrudnichestvo-s-nami` — правило о публикации
   должно проверяться по **выводу**.
3. Мутация `InstitutesStrips` **не может** покраснить гейт над выводом. Первая редакция
   задач требовала именно этого — задача была невыполнима.

Дата «13.06.2018» совпадает с датой канонического номера. Сильнее того: панель «Уставные
документы» на `/svedeniya-…` прямо пишет — «Номер лицензии на осуществление образовательной
деятельности был изменен. Смотрите файл „Выписка из реестра лицензий“». Это документальное
основание вопроса №1, но **не подтверждение формулировки** «ранее №3471»: заказчик
подтверждает, как это назвать на сайте.

Тестов над номером нет: `grep -rn "лиценз\|3471\|Л035" web/tests` — ноль.

### С5. Канонический номер уже виден посетителю

Панель подставляется на сборке: `panelsFor` по пути страницы (`web/src/lib/data.ts:11-21`)
→ `cleanBodyHtml` → сборка `<details open><summary>…` в
`web/src/lib/html-cleaner.ts:343-353` (подстановка восстановленного контента — `:345-347`,
сборка элемента — `:353`). Вызов — `svedeniya-ob-obrazovatelnoy-organizatsii.astro:24`.

Существенная деталь для секции вопросов: панели раскрыты по умолчанию (`<details open>`).

### С6. Данные документов

69 панелей «Выдаваемые документы». **Число различных формулировок зависит от нормализации,
поэтому приводится с методом:** 26 различных строк без нормализации, **21** после
схлопывания пробелов (`re.sub(r'\s+',' ',…)` над текстом без тегов). Первая редакция
называла 21 без метода — воспроизвести это число не удавалось.

- **ФИС ФРДО — 1 панель из 69**
  (`/institut-klinicheskoy-prikladnoy-kineziologii/specializirovannye/gelmintozy-…`);
- **НМО — 10 панелей**;
- **выдающее лицо:** ЧУ ДПО «Институт кинезиологии» — 4 панели; институты Апледжера и
  Барраля за подписью преподавателя с внесением в IAHE — 33 панели;
- **1 панель:** «Выдача документов не предусмотрена»;
- **обозначение образца:** «государственного» — 40 панелей, «установленного» — 10;
- **опечатка «удостовернеие» — в 4 панелях** (первая редакция говорила «в 2»: считались
  различные тексты, а не панели).

### С6а. Что об этих данных говорит аудит ЗЕТ/НМО

`docs/zet-nmo-data-audit.md` живёт в ветке `origin/docs/zet-nmo-audit`, коммит `2ccee7b`;
на базе этого change файла ещё нет, поэтому он назван как внешняя зависимость, а не как
файл репозитория.

Оттуда существенное для соответствия:

- **баллы НМО обещаны за цикл целиком**, а не за отдельный семинар: «по итогам прохождения
  всех семинаров цикла „Прикладная Кинезиология“…», текст идентичен во всех восьми
  семинарах цикла. Бейдж «даёт баллы НМО» на карточке одного семинара исказил бы обещание —
  отсюда отдельное требование;
- расхождение «12» и «10» — **разные рамки счёта**: 12 семинаров с упоминанием НМО/ЗЕТ
  против 10 панелей «Выдаваемые документы» с упоминанием баллов. Не ошибка, но рамку надо
  называть;
- аудит опроверг разведочное «все 12 — ИКПК»: 11 ИКПК + 1 Апледжера.

### С7. Обобщение про ФИС ФРДО живёт на главной

`grep -rl "ФРДО" web/dist` — **два файла**:

- `dist/institut-klinicheskoy-prikladnoy-kineziologii/specializirovannye/gelmintozy-…/index.html`
  — перенесённый контент, льгота;
- **`dist/index.html`** — из `web/src/components/home/sections/Advantages.astro:25`:
  «Студенты с базовым медицинским образованием получают удостоверения о повышении
  квалификации с внесением сведений в ФИС ФРДО. Они также имеют возможность получить
  единицы трудоёмкости (ЗЕТ, баллы НМО).»

Это обобщение частного случая (1 программа из 69) на всех студентов с медицинским
образованием, сделанное **нами**, а не заказчиком, — то есть новое утверждение без
подтверждения. Гейт, поставленный над панелями, его не увидел бы.

### С8. FAQ: заголовок уже выводится, а зависимость имеет запасной путь

`grep -rl "Часто задаваемые вопросы" web/dist` — три файла: `institut-apledzhera/`,
`institut-barralya/`, `institut-klinicheskoy-prikladnoy-kineziologii/`.

Причина: `extractAdditionalHtml` (`web/src/pages/[institute].astro:61-72`) ищет **самое
раннее** совпадение альтернации из трёх заголовков и режет HTML до конца строки. Позиции
заголовков в `description_html`:

| Институт | Дополнительная информация | Почему выбирают | Часто задаваемые вопросы |
|---|---|---|---|
| ИКПК | 15240 | 16260 | 17670 |
| Апледжера | 214536 | — | 217953 |
| Барраля | 156694 | — | 160979 |

Срез начинается на «Дополнительной информации» и **включает** заголовок «Часто задаваемые
вопросы», лежащий дальше. Отсюда два вывода:

1. **Сценарий «секция названа» зелёный на хабах уже сегодня.** Красным его делает только
   `/oplata`, где вопросы есть, а названия нет. Первая редакция называла эту проверку RED
   для всех страниц — это была четвёртая ловушка декоративного гейта в проекте.
2. **У зависимости от заголовка есть запасной путь.** Убрав «Дополнительную информацию»,
   срез начнётся со следующего совпадения — у ИКПК с «Почему выбирают», у остальных с
   «Часто задаваемые вопросы», — и вопросы уцелеют. Мутация одного заголовка ничего не
   доказывает; красной проверка становится только при мутации **всех** совпадающих.

### С9. Не всякий `<details>` — вопрос

По собранному выводу (`grep -roh "<summary[^>]*>[^<]*" web/dist`): **269** раскрывающихся
элементов с **пустым** заголовком; самые частые непустые — «Для кого подойдет» (163),
«Учебный план» (155), «Образование» (49). Это разделы описания программ и служебная
разметка.

Первая редакция называла ловушкой панели «Обучение» и «Компания». Это было неверно: в
собранном выводе они не появляются вовсе (у них нет триггера-заголовка на хабах), то есть
предметом проверки они быть не могут. Ловушка настоящая, но состоит в другом — в
большинстве раскрывающихся блоков, которые вопросами не являются.

### С10. Доступность: 56 одинаковых идентификаторов

На `/svedeniya-ob-obrazovatelnoy-organizatsii` `id="clip0_516_29476"` встречается **56 раз**
(все 56 вхождений `id="clip0_*"` — одно и то же значение). Знать до того, как связывать
элементы через `aria-labelledby`: ссылка на повторяющийся идентификатор ведёт на первый.

## Decisions

### Р1. Источник данных — отдельный модуль реквизитов

Номер лицензии, дата, наименование, ИНН и адрес реестра выносятся в один модуль данных
организации. Отклонено: разбор легаси-HTML на сборке (строка контента становится
контрактом); заведение в Strapi (CMS ещё не источник сайта); «оставить копии и сверять их»
(согласованность копий можно получить и на неверном значении).

### Р2. Два правила над двумя разными множествами

Правило **единственного источника** проверяется по **исходникам** — иначе `InstitutesStrips`
(превью) остаётся с записанным строкой номером незамеченно. Правило **о публикации** («3471»
не выдаётся за действующий, обобщения не публикуются) проверяется по **собранному выводу** —
иначе `/sotrudnichestvo-s-nami` и главная с ФИС ФРДО невидимы.

Это прямое следствие С4 и С7 и главная механическая правка после ревью: одна проверка над
одним множеством правило не удерживает.

### Р3. Мутация гейта лицензии — по предмету каждого гейта

Для гейта над **исходниками**: вернуть номер строкой поочерёдно в `Programs.astro`, в
`InstitutesStrips.astro`, затем в оба — три прогона.

Для гейта над **выводом**: мутировать только то, что в вывод попадает. Мутация
`InstitutesStrips` здесь бессмысленна — и это записано, чтобы следующий исполнитель не
принял зелёный прогон за декоративность гейта.

Урок неполного отката сохранён, но применён к правильному предмету.

### Р4. Ссылка на реестр — не автоматический гейт

Проверяется текст и адрес ссылки в собранной странице; живая доступность — нет (С1).
Отклонено: проверка с ретраями и пропуском при недоступности — это «не смогла проверить»,
выданное за «дефектов нет».

### Р5. Три случая новизны, а не два

Новым считается утверждение на новом месте, утверждение с расширенным охватом **и смена
смысла на прежнем месте**. Третий случай добавлен после ревью: переобозначение
«№3471 → прежний номер» происходит на тех же страницах и первыми двумя не покрывалось,
хотя именно оно — флагманский случай этого change.

### Р6. Льгота перенесённого контента живёт при критерии, а не при одном требовании

В первой редакции граница «новое против перенесённого» существовала только прозой в
design, а требование «неподтверждённое не публикуется» стояло без оговорки — буквально оно
запрещало перенос контента заказчика и противоречило требованию сохранить упоминание ФИС
ФРДО на его странице. Теперь льгота — отдельное требование в `verifiable-public-claims`, и
оба других требования ссылаются на неё.

Страницы под льготой перечисляются **поимённо**, а не признаком «похоже на легаси»: признак
отстаёт от предмета молча, список виден в ревью.

### Р7. Соответствие — от исходного образования, с выдающим лицом и основанием для НМО

Запись соединяет исходное образование, документ и выдающее лицо (С6: без последнего
соответствие неверно для 37 панелей из 69). Баллы НМО подаются с основанием — за цикл, а не
за семинар (С6а). Форма подачи спекой не предписывается: таблица или список выбирается
мокапом. Из этого следует и то, что существующий гейт обёртки таблиц применяется
**условно** — только если выбрана таблица.

### Р8. Канонический номер не выносится на новые места до приёмки записи реестра

Спека объявляет номер `SHALL`-значением, но его единственный источник сегодня —
перенесённый контент заказчика, а запись реестра не открыта (С1). Вынос на главную —
новое утверждение по Р5, и без приёмки он нарушал бы собственный критерий этой спеки.
Поэтому ручная приёмка поднята из финальной проверки в **предусловие**.

Отклонено: объявить панель заказчика достаточным подтверждением — тогда критерий
подтверждённости перестаёт что-либо ограничивать, ведь «3471» тоже пришёл из контента
заказчика.

### Р9. Область не растягивается на числа главной

Числа «14000+ / > 20 лет / 1500+» тоже не подтверждены, но их правка меняет облик по
другому основанию и требует своих мокапов. Они названы известным отклонением и вопросом
владельцу №8, а область возможности ограничена явно — в самой спеке, а не умолчанием.
Иначе требование архивировалось бы уже нарушенным.

## Risks / Trade-offs

- **Вопрос владельца №6 может закрыть льготу** (правим `/sotrudnichestvo-s-nami`) → тогда
  Non-Goal «не переписываем легаси» перестаёт действовать для одной страницы; список льготы
  сокращается на одну строку, требования не меняются.
- **Заказчик не подтвердит «ранее №3471»** → номер исчезает с главной; выполнимо без
  заказчика.
- **Заказчик подтвердит юридически неверный термин** → спека не легализует термин: право
  публиковать даёт критерий, но задача включает возврат вопроса с указанием на 273-ФЗ.
- **Запись реестра не удастся открыть никому** → номер остаётся только на
  `/svedeniya-…`, подача на главной ограничивается ссылкой на реестр без номера. Это
  ухудшение против замысла, но честное.
- **Пересечение с `payment-request-flow` на `/oplata`** → без ответа на вопрос №7 два
  change правят одну страницу и один эталон; риск конфликта эталонов назван, разведение —
  за владельцем.
- **Эталоны `home-*` обновятся дважды** — при снятии «3471» и после выбора мокапа. Порядок
  задан в разделе задач, чтобы второе обновление не скрыло регресс первого.
- **56 одинаковых `id` на `/svedeniya-…`** → связывание через `aria-labelledby` там даст
  ссылку на первый элемент; учитывать при разметке.

## Migration Plan

Правок данных и адресов нет; миграция — это порядок внутри работы:

1. часть без изменения облика: источник данных, снятие «3471» из `Programs.astro` и
   `InstitutesStrips.astro`, сверка с панелью;
2. приведение текста про ФИС ФРДО на главной в соответствие с данными;
3. мокапы в вариантах → выбор владельцем;
4. разметка выбранного варианта, обновление эталонов `home-*`, `institute-*`, `oplata-*`;
5. утверждения, зависящие от заказчика и от приёмки реестра, — последними.

Откат: удаление секций и возврат текста; данных, которые нельзя вернуть, change не создаёт.

## Open Questions

Единый список из восьми вопросов — в `proposal.md`, раздел «Вопросы владельцу и заказчику».
Здесь не дублируется, чтобы два списка не разошлись.

Все восемь не меняют требований и подхода: они меняют только то, публикуется ли конкретное
утверждение и кому принадлежит спорная область.
