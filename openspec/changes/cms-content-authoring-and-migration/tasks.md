## 1. Предусловия утверждения

- [x] 1.1 СДЕЛАНО: пересечение с `education-trust-and-admission-clarity` закрыто по содержанию —
      уровень данных о документах приведён к семинару с агрегацией по программе, введена
      категория «сведения уточняются» вместо строки соответствия с выдуманными значениями
- [x] 1.2 СДЕЛАНО: дельта `rich-content-safety` переиспользует принятую границу; собственная
      политика санитизации удалена
- [x] 1.3 СДЕЛАНО: дельта `static-serving` добавлена — у админки и API класс с `no-store`
- [x] 1.4 СДЕЛАНО: снимок, ревизии, гейт публикации и триггеры вынесены в change
      `cms-content-publication` (PR #136); здесь остаётся наполнение системы управления. Термин
      «снимок» из требований этого change убран: опорная дата описана как приходящая аргументом
      сборки, источник контента — нейтрально
- [x] 1.5 СДЕЛАНО 24.08.2026: сверено с текущим текстом `cms-content-publication` (PR #136,
      OPEN, не заархивирован) — его `proposal.md` прямо строит своё Why на этом change
      («Контент переезжает в систему управления (change cms-content-authoring-and-migration), и
      в тот момент, когда сборка начнёт брать его оттуда…»), то есть порядок «сначала
      наполнение, потом переключение источника» согласован с обеих сторон одинаково.
      Расхождений не найдено; сверку придётся повторить, если любая из двух сторон изменит
      порядок в своём тексте до архивирования
- [x] 1.5a СДЕЛАНО 24.08.2026: `online-payment-flow` заархивирован раньше этого change, и
      измерение вопроса дало более точный ответ, чем сама постановка. Правка — не текст, а
      регекс-патч поверх легаси-текста во время рендера (`web/src/pages/oplata.astro`,
      `withPaymentCopy`); «исправленный текст» как данные не существует нигде. Перенос обязан
      применить патч один раз при импорте и сохранить результат — см. D13. Задача на перенос —
      8.1e
- [x] 1.5b СДЕЛАНО 24.08.2026: `prod-serving-on-nginx` заархивирован
      (`d75dc4461c859e728b59401be6d74d83ca8e1127`, PR #167), блок `MODIFIED` перенесён на новый
      текст `openspec/specs/static-serving/spec.md` — добавлена строка `no-store` для админки и
      её API рядом с двумя строками соседа, обоснование дополнено фразой про общий класс.
      Применимость проверена архивированием в одноразовом worktree (не в рабочем дереве, дважды):
      первая попытка поймала настоящую потерю — перенос по вручную извлечённому блоку не захватил
      сценарий соседа «Адрес состояния развёрнутой раздачи», инструмент отказал с
      `archive_spec_update_failed`. После точного повторного переноса и коммита архивирование в
      свежем одноразовом worktree прошло чисто: `specsUpdated: true`, 28 добавлено, 1 изменено
      (наше требование), 0 потеряно — и оба сценария, наш и соседа, присутствуют в результирующей
      основной спеке. Оба worktree удалены, ветка этого change не двигалась
- [x] 1.5c СДЕЛАНО 24.08.2026: проверила по тексту, что ни одно требование этого change не
      утверждает о площадке боевого сайта (`grep SHALL` по дельте `static-serving` — все
      предложения либо унаследованы дословно из основной спеки, либо про класс `no-store`
      админки). Нашла неточность в собственной границе: `proposal.md` списывал TLS целиком
      соседу, хотя у этого change есть своё требование «Админка и её API раздаются только по
      TLS» — не конфликт (разные имена и сертификаты), но формулировка вводила в заблуждение.
      Уточнено: «там — TLS боевого сайта», TLS админки остаётся предметом этого change
- [ ] 1.5d **Довести подачу категорий до change прежде, чем обязательность категории будет
      включена.** Здесь введены категория как данные и её обязательность, а подача (теги, фильтр
      с мультивыбором, адрес фильтра, `canonical`, `noindex`) решением Q31 отнесена к каталогу
      статей и в активные changes не внесена. Пока этого нет, обязательное поле не имеет
      потребителя: редактор заполняет то, чего посетитель не видит. Маршрут — `/opsx:update`
      каталога статей либо новый change; выбор зафиксировать
- [ ] 1.5e **Предупредить `visual-regression-gate` о переезде адреса.** Тот change снимает
      эталоны на живом адресе `/institut-apledzhera`; под плоской схемой этот адрес становится
      целью перенаправления, а страница института уезжает в `/instituty/institut-apledzhera`.
      Его правило «кто меняет страницу, тот обновляет эталоны» покрывает изменение содержимого,
      но не **переезд адреса** — эталон снимался бы с редиректа. Согласовать до выкладки новых
      адресов
- [x] 1.6 СДЕЛАНО 24.08.2026: три круга независимого ревью, разными уклонами и разными
      исполнителями (Opus + Codex), строго на чтение, по коммитам дельты. Круг 1 — факты
      (пересчёт чисел, верность пересказа прежней редакции) и последствия (пересечения с
      соседними changes, границы предмета): 12 находок, все внесены. Круг 2 — та же пара
      уклонов на дельте исправлений: 6 находок (обе стороны — Opus, Codex умер на лимите),
      все внесены; из них 5 — дефекты, внесённые ПРИ исправлении предыдущих находок,
      включая три захода подряд в один тай-брейк, прежде чем сменить формулировку на
      устройство (тип сравнения по схеме поля, а не словом). Круг 3 — узкий, только по двум
      финальным коммитам: 1 находка. Итого 19 находок за три круга, из них 6 внесены
      исправлениями предыдущих — независимость проходов подтверждена тем, что уклоны
      пересеклись только на одной находке из двенадцати в первом круге.
      Честно: из пяти запусков прохода «регресс в тексте» во втором и третьем круге
      независимую поставку дал только один (Opus в круге 1 и один из двух в круге 2);
      трое агентов погибли или ушли в простой без отчёта (лимит сессии, дважды сбой среды).
      Их список прошёл автор по своему тексту — слабее независимого, но не ноль: ещё 4
      находки, две из них открылись самими исправлениями. Полный разбор — в истории PR #132
      и в переписке сессии, здесь не дублируется
- [ ] 1.7 **ОСТАЁТСЯ ОТКРЫТОЙ — требует владельца, не автоматизируется.** Три вопроса
      заказчику: неотправленный запрос из пункта 11 `docs/client-requests.md:38`, `**Письменное согласование MVP DoD**` — расхождение с PRD DoD, который требует «все FR-01..13»;
      классификация 57 семинаров без сведений о документах; выбор между заморозкой
      редактирования старого сайта и атомарной выгрузкой. Ни один из трёх не решается текстом
      change и не изобретается за заказчика — это переписка с живым человеком. Реализация не
      начинается до утверждения change владельцем (см. таблицу конвейера в AGENTS.md), а
      утверждение здесь зависит от этих трёх ответов постольку, поскольку они меняют конкретные
      требования (обязательность полей о документах у части семинаров; способ финальной сверки
      переноса). **Решение владельца 30.08.2026:** локальную реализацию продолжать; эти три
      ответа остаются блокерами миграции и приёмки, но не локальной разработки. До ответа 57
      семинаров сохраняют состояние «сведения не подтверждены», предпочтительный способ
      переноса — заморозка, а MVP DoD не считается письменно согласованным

## 2. Красные тесты — отдельной сессией с чистым контекстом

- [x] 2.1 Написать тесты по сценариям всех spec-файлов change (состав считать по каталогу
      `specs/`). Выполнено отдельной чистой сессией в
      `e10823146e2d82d06624167d2e20dfe30e5d3f2c`: 135 сценариев четырёх spec-файлов, из них
      99 получили новые автоматические проверки, 20 уже покрыты существующими гейтами, 16
      требуют живого свидетельства
- [x] 2.2 Предъявить красный прогон: отдельный push коммита только с тестами либо лог с точной
      командой и SHA. Выбрана первая ветка: отдельный push-коммит только с тестами и двумя
      конфигурациями Vitest — `e10823146e2d82d06624167d2e20dfe30e5d3f2c`; он достижим из
      `feat/cms-content-authoring-2@e10823146e2d82d06624167d2e20dfe30e5d3f2c`
- [ ] 2.3 Для каждого сценария — автоматическая проверка либо явная отметка с причиной и
      сохранённым свидетельством (живая админка, ответ nginx — команда, адрес, вывод)
- [x] 2.4 Сценарии без объективного критерия вернуть в change через `/opsx:update`. Таких
      сценариев сессия тестов не нашла: 16 неавтоматизированных сценариев имеют объективный
      критерий, но требуют развёрнутого Strapi, живого ответа nginx или сборки из CMS; причины
      и требуемые свидетельства перечислены рядом с тестами. Их фактические свидетельства
      остаются открыты по 2.3

## 3. Схема CMS

- [x] 3.1 Поле порядка у **института**, семинара, программы, преподавателя; пустое — в конец,
      вторичная сортировка по идентификатору. У института оно уже есть в схеме и по нему уже
      сортирует сборка — задача в том, чтобы это перестало быть неописанным. Сверено и
      доведено 30.08.2026: удалён `default: 0` у всех четырёх схем, общий
      `byExplicitOrder` подключён к production data access для институтов, программ,
      семинаров и персон; пустое значение уходит в конец, равные значения сравниваются по
      `slug` лексикографически. Негативная проверка перед исправлением: 5 падений в
      `cms-schema-contract.test.ts`; после исправления релевантный прогон — 71/71, `web`
      typecheck — 0 ошибок. Импорт также переносит `order` всех четырёх типов, не подменяя
      пустое значение нулём; отдельный red/green — 4 падения → 36/36
- [x] 3.2 Удалить хранимое поле статуса из схемы семинара. Сверено 30.08.2026:
      `cms/src/api/seminar/content-types/seminar/schema.json` не содержит `status`, а
      вычисление по расписанию и опорной дате покрыто `planned-seminars.ts` и
      `seminar-dates.ts`; импорт удалённое поле не отправляет; релевантные прогоны — 71/71
      и 36/36
- [x] 3.3 Множественное медиа-поле у семинара, программы, института. Сверено 30.08.2026:
      у всех трёх схем `media.multiple: true`, а страницы используют первое изображение как
      основное; `cms-schema-contract.test.ts` зелёный
- [x] 3.4 Три поля секций семинара с именами по смыслу (учебный план, режим обучения,
      рекомендации); поля произвольного содержимого о документах не создавать
- [x] 3.5a Переход из «сведения не подтверждены» требует записанного основания: дата, источник,
      кто внёс; без основания переход отклоняется. Реализовано 30.08.2026: три поля основания
      хранятся в схеме семинара, document-service middleware проверяет переход при `update`.
      Красный прогон: 1 lifecycle + 1 schema failure; зелёный: CMS 40/40, schema 48/48,
      `cms` и `web` typecheck — 0 ошибок
- [x] 3.5 Документы: состояние сведений из трёх значений плюс повторяемый набор записей
      {документ, выдающее лицо, условие, основание}. Условие — контролируемый перечень по двум
      основаниям (исходное образование, результат обучения), не более одного значения по каждому,
      соединение — И; дизъюнкция выражается отдельными записями набора
- [ ] 3.5b Три формы представления охвата: строка с документом, строка «документы не выдаются»,
      список «сведения уточняются»; каждый семинар попадает ровно в одну
- [ ] 3.6 Связь персоны с институтом: источник состава института, **не** источник адреса и
      **не** обязательное поле для публикации (`docs/demo-2026-08-23-cms-decisions.md`, W2).
      Легаси-поле для
      определения института не используется. Частично реализовано 30.08.2026: схема персоны хранит
      необязательную many-to-many связь `institutes` и не содержит
      `institute_legacy_id`; перенос использует исходное значение только для разрешения
      `documentId` и записывает relation payload `{ set: [...] }`; runtime-состав выводится
      только из связи на уровне контрактной функции. Red/green импорта — 1 падение → 41/41,
      schema-contract — 54/54. Пункт остаётся открытым: текущая страница института фильтрует
      pinned snapshot по `institute_legacy_id`; заменить это связью можно после подключения
      live capture, общего с 3.5b и 4.7
- [x] 3.6a Тип «персона» один для преподавателей и авторов методик; признак одного значения
      (`преподаватель` | `автор методики`), обязательный для публикации, редактируемый
      администратором. Признак хранится, а не выводится из свойств записи
- [ ] 3.6b Состав педагогических работников на странице «Сведения об образовательной
      организации» — только персоны с признаком «преподаватель». В
      `education-trust-and-admission-clarity` это правило **не** дублировать: там состав
      остальных разделов и формулировки
- [x] 3.6c Признак «является категорией статей» у программы, по умолчанию поднятый; связь статьи
      с программами как категория; не менее одной категории для публикации статьи; снятие
      признака (а также снятие с публикации и удаление программы) отклоняется, пока программа —
      единственная категория опубликованной статьи, с указанием этих статей
- [x] 3.7 Схема события расписания под состав данных; институт и программа не дублируются.
      Импорт преподавателей события разрешает исходные numeric id в `documentId` и записывает
      many-to-many relation через `{ set: [...] }`, а не сырой JSON; red/green — 1 → 37/37
- [x] 3.7a Свериться и адаптировать `web/scripts/lib/planned-seminars.ts` к новой форме события
      расписания (связь на преподавателей вместо json, новые признаки) — вычисление статуса
      опирается на его форму
- [x] 3.8 Убрать цену и длительность из схемы семинара
- [x] 3.9 Легаси-поля необязательны во всех типах
- [ ] 3.10 HTML-поле тела с визуальным редактором вместо markdown; плагин подтвердить проверкой:
      текущая версия Strapi, без внешних CDN, сохраняет таблицы, разметка внутри принятого
      allowlist
- [x] 3.11 Грамматика идентификатора и области уникальности **по каталогам**: институт — в
      `/instituty`, программа — в `/programmy`, семинар — в `/seminary`, персона — в
      `/specialisty`, статья — в `/statyi`, видео-плейлист — в `/video`; статическая страница —
      среди всех сегментов первого уровня вместе с сегментами каталогов и маршрутами сборки.
      Совпадение идентификаторов у записей **разных** типов конфликтом не является
- [x] 3.12 История адресов: постоянное хранение, **переживание удаления владеющей записи**,
      недоступность прямому редактированию любой ролью, запрет занять адрес из истории любой
      записи — включая удалённую. Тип скрыт из content-manager и content-type-builder,
      публичный REST CRUD отключён пустым routes-модулем; пишет только внутренний lifecycle.
      Удаление владельца и запрет повторного занятия покрыты CMS lifecycle и адресными тестами.
      RED на оставшийся core router: 1/50; GREEN: schema-contract 50/50
- [x] 3.13 Обязательность полей по типам с отказом на публикации записи. Реализовано
      30.08.2026 для всех десяти типов из таблицы: проверка выполняется document-service
      middleware только на `publish`; schema-level `required` снят, поэтому неполный черновик
      сохраняется. Новость и акция получили поле даты, импорт сохраняет в нём исходный
      `createdAt`. RED: CMS 3 падения и schema-contract 9 падений; GREEN: CMS 43/43,
      schema-contract 49/49, scripts 78/78, все три typecheck и оба lint без ошибок
- [x] 3.14 Роли: воспроизводимо и идемпотентно сводить редактора к узкой матрице — статьи,
      семинары и расписание create/read/update/publish; категории статьи; у программы update
      только `is_article_category`, программы и преподаватели read для связей; необходимые
      действия медиатеки. Запретить delete, остальные типы, пользователей, роли, настройки и
      структуру типов. Проверить положительные действия над статьёй и расписанием, field-level
      отказ остальных полей программы, отказ административных действий и удаление вручную
      добавленного лишнего разрешения после повторного запуска. Реализовано 30.08.2026:
      bootstrap вызывает `admin::role.assignPermissions` с полным allowlist; CMS unit RED 1/46,
      GREEN 50/50. Живая ability роли подтвердила allow для article title, schedule startAt и
      `is_article_category`, deny для имени программы, delete статьи, update новости и ролей.
      Негативная мутация: вручную добавленный `news-item.update` — 1 до перезапуска, 0 после

## 4. Адреса и маршруты

**Ограничение порядка на всю секцию:** выкладка сайта с плоскими адресами — не раньше переезда
раздачи на nginx (`prod-serving-on-nginx`, PR #158). До него отдать 301 нечем, и новые адреса
означали бы не «редиректы позже», а молчание по проиндексированным адресам.

- [x] 4.1 Вычисление адреса записи из каталога (по типу) и идентификатора: `/instituty/<код>`,
      `/programmy/<код>`, `/seminary/<код>`, `/specialisty/<код>`. Каталог и путь целиком —
      не редактируемые поля
- [x] 4.1a Страницы-списки четырёх каталогов; `/programmy` обязан существовать раньше, чем
      **цель** кнопки «Выбрать программу» на главной переведут на него (пункт D10 списка с
      демонстрации, класс red/green). Именно цель, а не подпись: подпись уже стоит в трёх местах
      композиции главной, а ведёт на `/raspisanie-i-tseny` (`web/src/lib/navigation.ts` плюс
      литерал в двух компонентах). И зависимость транзитивная: вся эта секция ждёт переезда
      раздачи, значит перевод цели ждёт его тоже. Исполнитель того пункта обязанности читать этот
      change не имеет и без предупреждения переведёт цель в отсутствующую страницу
- [x] 4.1b Перегенерация карты адресов `discovery/url_map.csv` и правил
      `deploy/nginx-redirects.conf` под новые цели: измерено на голове ветки — 684 строки из 1023
      в карте и 172 правила из 264 целятся в адреса, начинающиеся с идентификатора института.
      Правил с несуществующей целью не остаётся. Выполнено 30.08.2026 воспроизводимой
      `npm run url-map:flatten`: 684 цели распределены по `page_type` в четыре плоских каталога,
      source-map и fixture снимка синхронизированы, повторный запуск даёт 0 изменений.
      `redirects:gen` перегенерировал 434 уникальных адреса; build — 458 страниц; оба прежних
      RED зелёные, build-конфигурация `seo-package` — 25/25
- [ ] 4.1c Иерархические ссылки в телах перенесённого контента, `canonical`, `og:url`, JSON-LD и
      карта сайта — на новые адреса. Проверять пересчётом, а не выборкой. Предмет измерен и он
      **не там, где казалось**: у 68 статей таких ссылок ноль (все их `href` — внутристраничные
      якоря). По мере «`href` во всех строковых полях всех файлов `discovery/entities/`» итог
      **539**: восстановленные панели 288, программы 96, статические страницы 66, семинары 58,
      институты 31. Панели входят потому, что их содержимое переносит этот же change (задача 8.4)
      и внутри полей сущностей эти ссылки станут ссылками тела
- [ ] 4.1c-bis Снять `noindex` с детальных плоских адресов (`/instituty/<код>`,
      `/programmy/<код>`, `/seminary/<код>`, `/specialisty/<код>`), когда они становятся
      каноническими — иначе 4.1b/4.1c бессмысленны: редирект или прямая ссылка приводят на
      страницу, которая прямо просит поисковик её не индексировать. Найдено ревью PR #186:
      ни `cms-content-publication` (Non-Goals её proposal.md — «адреса и история адресов»
      прямо исключены), ни этот change явно не называют момент снятия `noindex` — единственное
      упоминание `noindex` в этом change (задача 1.6, адрес фильтра категорий статей) о другом
      адресе и решено отдельно (Q31). Сейчас все четыре плоских детальных шаблона размечены
      `noindex` явно:
      `web/src/pages/instituty/[slug].astro:46`, `noindex`; тот же атрибут там же —
      `web/src/pages/programmy/[slug].astro:33`, `noindex`;
      `web/src/pages/seminary/[slug].astro:43`, `noindex`;
      `web/src/pages/specialisty/[slug].astro:31`, `noindex`.
      Условие снятия: источник данных сборки
      переключён (соседний change) — до этого страница по плоскому адресу отдаёт то же
      содержимое, что и старая, и индексация обеих параллельно дала бы дубли
- [x] 4.2 Порождаемый маршрут для новых статических страниц; существующие страницы продолжают
      раздаваться по своим адресам
- [x] 4.3 Смена идентификатора или связи не роняет сборку и порождает постоянное перенаправление.
      Lifecycle записывает прежний адрес только после успешного переименования; генератор
      `web/scripts/gen-redirects.ts` добавляет историю из content snapshot в nginx-конфиг.
      Смена одной лишь связи адреса не касается и истории не пишет. Проверки 30.08.2026:
      `cms/src/lifecycles/content-address.test.ts`, `scripts/lib/cms-content-address.test.ts`,
      `web/tests/history-redirects.test.ts`
- [x] 4.4 Перенаправление из истории ведёт на текущий адрес одним переходом; цепочки не
      накапливаются. Все прежние адреса одного владельца вычисляются заново против его текущей
      опубликованной версии, а не друг против друга; два последовательных переименования
      покрыты адресным контрактом и snapshot-тестом
- [x] 4.5 Переименование института или программы **не** меняет адресов других записей и не
      порождает для них перенаправлений; перенос семинара в другую программу адреса не меняет.
      Негативная проверка обязательна: переименовать программу с опубликованными семинарами и
      убедиться, что ни один их адрес не изменился и лишних правил не появилось. Негативная
      проверка есть в `web/tests/history-redirects.test.ts`: история программы создаёт правило
      только для программы, стабильный семинар правил не получает; перенос связи отдельно
      покрыт `cms-content-address.test.ts`
- [x] 4.6 Генерация правил перенаправления выводится из истории адресов; правил с несуществующей
      целью нет. `redirectsFromAddressHistory` принимает только владельца с текущей
      опубликованной версией в том же снимке; draft-only, снятая и удалённая запись правила не
      дают. `owner_type` сделан обязательным в схеме, чтобы цель вычислялась однозначно. RED:
      отсутствующий модуль и необязательный `owner_type`; GREEN: 55/55 адресных и schema-тестов,
      генератор на закреплённом снимке воспроизводит существующий конфиг без diff
- [ ] 4.7 Снятая с публикации запись исчезает из списков, расписания и карты сайта; ссылок и
      перенаправлений в никуда не остаётся
- [x] 4.8 Освободившиеся при переходе сегменты первого уровня (прежние адреса институтов)
      остаются занятыми историей адресов: статическая страница с таким идентификатором не
      сохраняется
- [x] 4.8a **Контактные данные называют номер менеджера** — поле или пометка у контактных
      данных, откуда фолбэк «уточняйте у менеджера» берёт номер. Отдельной задачей потому, что
      требование в спеке есть, а исполнителя у него не было: контакты сегодня не сущность
      системы управления, а захардкоженные пропсы
      (`web/src/components/contacts/ContactShell.astro` — четыре номера, по паре у института и
      медцентра). Решить и зафиксировать: пометка появляется в CMS вместе с переносом контактов
      или остаётся в источнике данных сборки. Решение 30.08.2026: до появления отдельной
      сущности контактов роль остаётся в общем источнике данных сайта — именованная константа
      `MANAGER_PHONE` рядом с `MANAGER_PHONE_HREF` в `web/src/lib/navigation.ts`; страницы
      контактов, оплаты, шапка и CTA уже читают этот источник. Проверка роли без комментариев —
      `cms-order-and-dates.test.ts`, 1/1
- [ ] 4.9 Даты семинара: на странице программы — ближайшая по **первому** дню среди событий,
      делающих семинар запланированным; на странице семинара — все такие события; при их
      отсутствии — «уточняйте у менеджера» с телефоном **из контактных данных сайта**, а не
      вписанным в шаблон. Фолбэк появляется ровно при отсутствии событий

## 5. Медиа

- [ ] 5.1 Файлы из системы управления передаются существующему шагу производных вместе с
      `media-originals/`; манифест по объединённому набору
- [ ] 5.2 Неуспех сборки при используемом изображении без производных или без известных размеров
- [ ] 5.3 Проверка, что медиа системы управления отдаётся со статических адресов, а внешние
      ссылки в содержимом сохраняются
- [x] 5.4 Ограничения загрузки: только растровые форматы, отказ векторной графике и документам,
      предел размера с указанием предела
- [ ] 5.5 Проверка неизменности оригиналов после сборки

## 6. Безопасность редакторского ввода

- [ ] 6.1 Контент из системы управления проходит существующий центральный sink принятой границы;
      второй конвейер и вторую политику не вводить
- [ ] 6.2 Черновик с непроходящей разметкой сохраняется с предупреждением; публикация такой
      записи отклоняется с указанием непринятого элемента или атрибута
- [ ] 6.3 Гейт: опубликованное содержимое совпадает с тем, что видел редактор, за вычетом только
      того, о чём он предупреждён

## 7. Развёртывание CMS

- [ ] 7.1 Развернуть Strapi и PostgreSQL на VPS, админка за nginx
- [ ] 7.1a **TLS для админки и её API:** имя **либо** сертификат на адрес (Let's Encrypt с 2026
      выдаёт и такие, Certbot поддерживает — домен рекомендуется, но безусловным предусловием не
      является), автоматическое обновление с учётом короткого срока действия, перенаправление с
      незащищённого протокола, признак «только по защищённому соединению» у cookie сессии.
      Отдельным пунктом, потому что нынешний bootstrap пишет vhost с одним `listen 80`
      (`scripts/bootstrap-vps.sh@d8131372071838cc94ad614af31861c541939e7f:72`, `listen 80;`), а HTTPS в инструкции развёртывания —
      необязательный шаг; панель в таком vhost отдаёт пароль администратора открытым текстом
- [ ] 7.2 Бэкапы базы и загруженных файлов, проверенные восстановлением — **инфраструктурная
      задача без спекового предмета**: требования о резервном копировании в спеке нет и не
      предполагается, поэтому сессия красных тестов её не покрывает; проверка ручная, со
      свидетельством
- [ ] 7.3 Адреса админки и API получают `no-store`; общее правило для адресов вне классов на них
      не распространяется — проверка по тексту конфигурации плюс свидетельство по живой раздаче
- [ ] 7.4 Закрыть или ограничить доступом advisory prod-дерева `cms` до вывода админки в
      интернет; измерить состав заново на момент развёртывания
- [ ] 7.5 Зафиксировать выбор по доступу заказчика: журнал происхождения (change
      `cms-content-publication`) введён до первой правки **либо** доступ к редактированию закрыт
      до его введения. Правки в промежутке ни в одну запись журнала не попадают, и восстановление
      раннего резервного копирования после них выглядит новым состоянием, а не откатом

## 8. Перенос контента

- [x] 8.1 Импорт: идемпотентность по легаси-идентификатору **и по содержимому медиа**, ненулевой
      код выхода при потерянной записи, оборванной связи и неудачной загрузке, счётчик неудачных
      загрузок, явный флаг обхода (закрывает TD-8)
- [ ] 8.1c Защита правок заказчика при повторном переносе — **непрерывный** запрет редактирования
      на весь период переноса **либо атомарное условное обновление**: запись применяется только
      если целевая ревизия равна той, что оставил предыдущий прогон (conditional update по
      ожидаемой ревизии, ETag/`If-Match` или эквивалент), иначе — блокировка, удерживаемая на всё
      окно «сравнение → запись». Отдельная проверка ревизии с последующей безусловной записью
      **не годится**: между ними редактор успевает сохранить правку, и она затирается. При
      расхождении перенос останавливается и называет запись
- [ ] 8.1d Негативная проверка к 8.1c: сохранить правку в окне между сравнением и записью
      (задержка в переносе на время правки) — перенос обязан отказаться записывать, содержимое
      остаётся тем, что ввёл редактор. Без этой проверки «сравнение перед записью» неотличимо от
      атомарного обновления
- [x] 8.1a Повторный запуск **доводит или заменяет** частично перенесённую запись, а не считает её
      готовой по факту существования легаси-идентификатора: иначе запись, на которой упал прогон,
      остаётся сломанной навсегда при «успешном» переносе
- [x] 8.1b Разделить существующие 445 оригиналов: те, на которые ссылается перенесённый контент,
      становятся записями медиатеки; остальные остаются материалом сборки в репозитории с явно
      названной ролью
- [ ] 8.2 Перенести контент; разовая сверка подтверждает, что прежний адрес каждой записи
      известен, попал в историю адресов и перенаправляет на новый одним переходом. **Прежних
      адресов у записи может быть два** — на старом сайте и наш нынешний; у персон они
      расходятся (у 28 из 29 легаси-адрес `/teachers/<идентификатор>`, наш маршрут —
      иерархический), и оба обязаны попасть в историю. Проверять
      **пересчётом**: число записей с прежним адресом в истории равно числу перенесённых.
      Равенства вычисленного и прежнего адреса под плоской схемой не бывает — прежнее требование
      о равенстве заменено, а не ослаблено
- [ ] 8.2a Первичное деление 29 персон на `преподаватель` / `автор методики` — один раз при
      переносе; разметка уходит заказчику на подтверждение. Повторный прогон не возвращает
      перенесённое значение поверх правки администратора (проверяется механизмом 8.7a)
- [ ] 8.1e Перенос текста `/oplata`: применить `withPaymentCopy`
      (`web/src/pages/oplata.astro`) один раз к телу страницы и к восстановленным панелям
      (`discovery/entities/collapsible_panels.json`, ключ `/oplata`) при импорте, сохранить
      результат как значение поля. Тот же приём — для любой другой перенесённой записи, если
      найдётся рендер-тайм патч поверх её текста: перенос сохраняет результат патча, а не его
      вход (D13)
- [ ] 8.2b Первичная разметка категорий у 68 статей — один раз при переносе, ИИ; повторный прогон
      не перезаписывает правку администратора. **Обязательность категории включается только
      после** того, как категория задана у всех перенесённых статей: включённая раньше, она
      отвергнет весь существующий контент сразу
- [ ] 8.3 Семинары без исходных сведений о документах получают состояние «сведения не
      подтверждены»; вывод «не выдаются» из отсутствия сведений запрещён
- [ ] 8.4 Исчерпывающая карта «адрес и заголовок панели → поле сущности» для 96 страниц и 39
      заголовков; перенос останавливается на заголовке вне карты
- [ ] 8.5 Перенести содержимое записей-заплаток расписания либо назвать отброшенные с причиной.
      **Само удаление модуля из сборки — не здесь:** он подмешивается в страницу расписания
      (`web/src/lib/schedule-supplements.ts`, удалён; заплатки в фикстуре снимка), а чтение
      из системы управления вводит change `cms-content-publication`. Снять модуль раньше значит
      удалить события у посетителя; проверку «удалённое в системе управления событие не
      возвращается, статус семинара пересчитан» выполняет соседний change после переключения
      источника
- [ ] 8.6 Сверка рендера тел до и после переноса; расхождение — дефект переноса
- [ ] 8.7 Зафиксировать выбор **одного из трёх** способов: заморозка редактирования старого
      сайта, атомарная выгрузка или обход со сверкой глобальной ревизии на его концах.
      Для выгрузки — **и таблицы, и медиа**: транзакционный дамп покрывает только таблицы,
      поэтому к нему нужен снимок хранилища, доказанная неизменяемость файлов или отметка по
      байтам медиа, сверяемая до и после обхода
- [ ] 8.7a Идемпотентность против правок редактора: прогон переноса сохраняет ревизию или
      контрольную сумму, которую сам записал, следующий прогон сравнивает с **ней**, а не с
      прочитанным в этом же прогоне; расхождение останавливает перенос и называет запись
- [ ] 8.8 Заключительная сверка перед передачей источника соседнему change: число записей по
      типам, значения полей, целостность связей, состав медиа, **обнаружение удалений**

## 9. Гейты и негативная проверка

- [ ] 9.1 Для каждого нового гейта негативная проверка в своём worktree: вернуть дефект,
      убедиться, что покраснел **именно** этот гейт (судить по имени теста, а не по цвету
      прогона)
- [ ] 9.2 Записать каждую мутацию воспроизводимо: точная команда или патч плюс два числа — до и
      после
- [ ] 9.3 Подтвердить восстановление постусловием под предмет мутации
- [ ] 9.4 Проверить, что ни один гейт не проходит вакуумно: пустой список, отсутствующий каталог
      и ноль элементов дают «не выполнено», а не успех
- [ ] 9.5 Сверить ответы гейтов над одним предметом: расхождение либо устранить, либо назвать

## 10. Приёмка

- [ ] 10.1 Обновить `docs/architecture/new-architecture.md` и `docs/migration-runbook.md`
- [ ] 10.2 Снять запись TD-8 из `docs/tech-debt.md`
- [ ] 10.3 Локальные гейты в затронутых пакетах: `web` — `lint`, `typecheck`, `build`, `test`,
      `test:build`; `cms` — `lint`, `typecheck`, `build`; `scripts` — `lint`, `typecheck`
- [ ] 10.4 `./bin/openspec validate --all --strict --no-interactive` зелёная
- [ ] 10.5 Проверить применимость дельт применением в одноразовом worktree
- [ ] 10.6 Заархивировать change, требования переехали в `openspec/specs/`
