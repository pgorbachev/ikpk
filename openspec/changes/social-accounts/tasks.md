# Tasks — состав и подача внешних аккаунтов

**Порядок разделов 2 → 3 обязателен**, и раздел 2 поставляется **независимо** от раздела 3.
Состав — требование заказчика и пункт go-live-гейта; подача зависит от внешней стороны
(правообладатели марок) и срока не имеет. Держать первое за второе нельзя (Решение 9).

## 1. Тесты по спеке — отдельной сессией, до реализации

- [x] 1.1 Начать **отдельную сессию с чистым контекстом**. На входе — только утверждённые
      `proposal.md`, `design.md`, `specs/social-accounts/spec.md` и код репозитория.
      Обсуждение, которым спека рождалась, не переносится
- [x] 1.2 Написать тесты по каждому сценарию спеки либо явно отметить сценарий как
      закрытый ручной приёмкой с названной причиной и способом сохранения свидетельства.
      Кандидаты на ручную приёмку названы заранее и их **три**: приёмка отличимости Youtube
      и Rutube по форме, выбор владельца при запрещённых или неоднозначных условиях
      использования марки, выбор владельца при недостижимом пороге контраста
- [x] 1.3 Предъявить **красный прогон** одним из двух способов, назвав его явно: отдельный
      push коммита только с тестами (упавший check-run на этом SHA) либо лог с точной
      командой и SHA. «Тесты лежат коммитом раньше» доказательством не является
- [x] 1.4 Дефект спеки — сценарий, для которого нельзя определить объективный критерий, —
      вернуть в change через `/opsx:update`, а не переформулировать на месте под то, что
      удобно проверить

## 2. Состав

- [x] 2.1 Привести состав в едином источнике к четырём аккаунтам: убрать Instagram и
      Facebook из `web/src/lib/social.ts:29`, `export const SOCIAL_LINKS: SocialLink[] = [`
- [x] 2.2 **Переписать обоснование в том же файле.** Старый комментарий объяснял присутствие
      двух сетей решением от 2026-07-27, отменённым решением D16; после снятия сетей (2.1) он
      стал бы ложным утверждением в единственном источнике правды. Новый текст —
      `web/src/lib/social.ts:15`, `Состав закрыт перечнем решения заказчика D16 с демо 2026-08-19: Instagram и`
      — называет действующее решение и его дату, чем оно отменяет прежнее, и почему сверка с
      разметкой внешнего сайта больше не является основанием вернуть сети
- [x] 2.3 **Свести вторые определения адресов к единому источнику. Их два.**
      (а) Rutube: был независимый литерал того же канала прямо в
      `web/src/lib/video-mirrors.ts`, шедший прямо в разметку:
      `web/src/pages/video/index.astro:26`, `RUTUBE</a>`. Теперь
      `web/src/lib/video-mirrors.ts:27`, `RUTUBE_CHANNEL_URL,` реэкспортирует запись перечня —
      `web/src/lib/social.ts:62`, `export const RUTUBE_CHANNEL_URL = hrefOf('Rutube');`.
      (б) ВКонтакте: был независимый литерал в `social.ts`, и существующий ре-экспорт
      `web/src/lib/video-mirrors.ts:26`, `VK_COMMUNITY_URL as VK_CHANNEL_URL,`
      переиспользовал именно его, а не запись перечня — «для ВКонтакте уже сделано» было
      неверно, единого источника там не было. Теперь литерала нет:
      `web/src/lib/social.ts:59`, `export const VK_COMMUNITY_URL = hrefOf('ВКонтакте');`.
      Первое требование спеки объявляет такие вторые определения нарушением, то есть без
      этой задачи спека принимается заведомо нарушенной. Это же и есть тот дефект, из-за
      которого модуль заводился: адреса стояли в разметке футера **и в отдельном модуле
      страниц видео**
- [x] 2.4 Сделать проверку состава симметричной. Сегодня
      `web/tests/content-quality.test.ts:498`, `it('принятый состав присутствует в подвале каждой страницы', () => {`
      утверждает только «ни одна не исчезла». Добавить вторую половину — «эти две не
      вернулись» — отдельным списком, не выводимым из первого (Решение 2)
- [x] 2.5 **Развести области двух половин.** «Присутствует» проверяется **внутри подвала**
      (роль `contentinfo`), «отсутствует» — по всей странице. Измерено, почему: вне подвала
      на `/video` лежат адреса сообщества ВКонтакте и канала Rutube, на `/video/<slug>` —
      адрес ВКонтакте. Если «присутствует» мерить по всей странице, исчезновение любого из
      этих двух аккаунтов из подвала пройдёт незамеченным. Прежняя редакция этой задачи
      утверждала сильнее — «проходит при полностью пустом подвале», — и это неверно:
      четырёх адресов вне подвала нет ни на одной странице, поэтому половина, требующая
      всех четырёх, покраснела бы. Дефект тоньше и относится к отдельному аккаунту
- [x] 2.6 Записать ожидания проверки независимо от `social.ts` (Решение 3). Проверить это
      мутацией: удалить запись из источника состава и убедиться, что проверка краснеет, а
      не «сдвигает ожидание вместе с ней»
- [x] 2.7 Расширить предмет проверки с одной страницы до всего вывода.
      `web/tests/content-quality.test.ts:500`, `for (const file of walkHtml()) {` читает только
      главную; в том же файле уже есть обход всех страниц вывода
- [x] 2.8 **Покрыть каждую роль артефакта, собираемую обязательным прогоном:** `ci`
      (`dist`), `preview` (`dist-demo`), `stand` (`dist-stand`). Роль `stand` — это то, что
      выкладывается на публичный стенд и что смотрит заказчик, и парой «боевой и демо» она
      не покрыта. Пустой перечень страниц роняет проверку, а не проходит тихо
- [x] 2.9 **Провести новые файлы проверок через три реестра — иначе покраснеет не предмет,
      а обвязка:**
      (а) проверка демо-вывода выбирается **поимённым перечнем файлов** в
      `web/vitest.demo.config.ts`, и нацелить существующий боевой гейт на `dist-demo`
      конфигурацией нельзя — он читает `dist`, объявленный в его собственном модуле;
      (б) новый файл обязан попасть **и** в `exclude` основного прогона: сверка идёт
      **точным равенством** множеств, расхождение роняет гейт —
      `web/tests/repo-hygiene.test.ts:308`, `it('тесты с особым предметом вписаны и в исключения основного прогона, и в свой конфиг'`;
      (в) новый браузерный `*.spec.ts` роняет мета-гейт `web/tests/browser-test-gating.test.ts`,
      если его не исполняет гейтующий workflow и он не внесён поимённо в `ACKNOWLEDGED_DEBT`
      с причиной. `test:e2e:smoke` и `test:e2e:a11y` запускают ровно по одному файлу каждый,
      то есть новые браузерные проверки надо либо вписать в них, либо завести свой скрипт и
      шаг обязательного прогона
- [x] 2.10 Проверить, что смежные проверки не начали лгать после снятия двух сетей:
      `web/tests/parity-compare.test.ts:206`, `it('homepage footer keeps contacts and social links', () => {`
      и `web/tests/compare.spec.ts:185`, `test('footer has social links (VK, YouTube, Telegram)'`
      написаны через «хоть что-нибудь из перечисленного» и останутся зелёными. Решить по
      каждой: ужесточить или оставить с названной причиной. **Знать до решения:**
      `compare.spec.ts` — сирота, он перечислен в `ACKNOWLEDGED_DEBT` мета-гейта, то есть
      его ужесточение в CI не исполняется и защиты не даёт
      Решение: `parity-compare.test.ts` ужесточён до всех четырёх принятых адресов; `compare.spec.ts` оставлен дизъюнкцией — сирота ACKNOWLEDGED_DEBT, в CI не исполняется

## 3. Подача

- [x] 3.1 Получить официальные марки четырёх сетей из первоисточников правообладателей и
      сохранить файлом в `web/src/assets/social-marks/` (Решение 17), по одному `.svg` на
      сеть
- [x] 3.2 **Проверить условия использования каждой марки** и записать по каждой сети
      запись в реестр применимости `web/src/lib/social-marks-registry.ts` (Решение 18):
      сеть, исход (`mark` / `text-link` — исход бинарный, третьего значения нет), дата,
      источник условий, вывод, и для исхода `mark` — **хеш применённого файла**,
      зафиксированный в момент проверки, отдельно от текущего состояния файла на диске
      (иначе сравнение «файл марки совпадает с первоисточником» сравнивало бы файл с самим
      собой). Поля названы требованием «Решение о применимости марки зафиксировано в
      машиночитаемом реестре». Происхождение марки проверяется сверкой с зафиксированным
      первоисточником, а не суждением о том, кем нарисован файл. Состояний три, и третье —
      «условия не опубликованы, требуют отдельного разрешения или неоднозначны» — самое
      вероятное для ВКонтакте и Rutube. Во втором и третьем случае исход выбирает
      **владелец**, не исполнитель: подача иконками — его решение, и заключение
      исполнителя об условиях его не отменяет. Запись без источника условий или без даты
      считается неполной, и неполная запись равнозначна отсутствующей
- [x] 3.3 **Предъявить владельцу кадр подвала с настоящими марками и получить выбор ДО
      правки компонента** (Решение 14). Мокапы снимались с нарисованными глифами, которые
      спека в продукт не пускает, то есть облик, который поедет, владелец пока не видел.
      Приёмка после внедрения правило «мокапы до внедрения» не выполняет
- [x] 3.4 Заменить текстовый список иконками в колонке «Подписывайтесь»:
      `web/src/components/Footer.astro:54`, `<ul class="footer-links footer-social">`.
      Марка каждой сети с исходом `mark` рендерится через `*Mark.astro` (элемент `<svg>` в
      AST — `set:html` запрещён `rich-content-safety`). Ассет в `web/src/assets/social-marks/`
      сверяется хешем реестра (`?raw` в `social-mark-files.ts`) **и** нормализованным телом
      с `*Mark.astro` (`assertMarkAstroBodiesMatchAssets`, Decision 17 / TD-45). Не
      публиковать файл в `web/public/` (Решение 12). Сеть с исходом `text-link` — текстовая
      ссылка. Сетку подвала не перестраивать
- [x] 3.5 Оправа — собственная форма каждой марки, единой оправы нет (Решение 5, выбор
      владельца 23.08.2026). Кружок не применять
- [x] 3.6 Задать доступное имя, **точно равное** названию сети из перечня, и скрыть графику
      марки от вспомогательных технологий. Сегодня имя даёт видимый текст внутри ссылки
      `web/src/components/Footer.astro:60`, `target="_blank" rel="noopener noreferrer"`
      — после замены текста иконкой его не останется
- [x] 3.7 Задать область нажатия не менее 24×24 CSS-пикселей. Невидимое расширение области
      оправой не считается — иначе требование о размере цели и запрет единой оправы встают
      друг против друга на узких и низких официальных марках
- [x] 3.8 Задать видимый индикатор фокуса по **уже принятой в проекте мере**:
      `web/tests/a11y.spec.ts:204`, `const hasOutline = info.outlineStyle !== 'none'` плюс
      условие о заметной тени. Автоматическая проверка доступности индикаторы фокуса не
      проверяет: живой дефект этого класса — снятый `outline`, компенсированный тенью в 8 %
      чёрного — уже проходил мимо двух зелёных гейтов
- [x] 3.9 **Измерить контраст доминирующей заливки каждой марки к фону подвала в КАЖДОЙ
      теме** (целевой размер — фактический размер отрисовки при `deviceScaleFactor = 1`,
      Решение 20) и записать восемь чисел (четыре марки × две темы). Фон в темах
      противоположен, и заранее неизвестно, какая марка пройдёт порог на обоих фонах, а
      какая только на одном. Порог не пройден и разрешённая альтернативная версия есть —
      записать применяемый в этой теме файл в поле `themeAssets` реестра (Решение 18);
      разрешённой версии нет — исход выбирает владелец (Решение 8), перекрашивать марку
      нельзя
- [x] 3.10 **Измерить цветовое расстояние ΔE76 в CIE Lab между заливками Youtube и Rutube на
      официальных марках** и переизмерить контрольную пару **тем же фиксированным якорем**
      (Решение 19: `#ff0000` и тот же цвет с каждым каналом sRGB, умноженным на 0,8 —
      **не** цвет одной из сетей, у тёмного фирменного цвета то же правило дало бы
      расстояние около нуля). Сессия тестов уже сделала это на мокапных цветах —
      `web/tests/helpers/contrast.ts`, `COLOR_DISTANCE_THRESHOLD = 25` при контрольной паре
      `#ff0000`/`#cc0000` = 19,4 и мокапной паре Youtube/Rutube 58,8 — контрольная пара уже
      совпадает с зафиксированным правилом, менять её не нужно; переизмерить нужно только
      расстояние между Youtube и Rutube на официальных цветах: порог 25 подтверждён лишь
      оценкой порядка на мокапе. Если новое число сдвинет соотношение, поправить
      `COLOR_DISTANCE_THRESHOLD`, не трогая метрику, якорь и правило пары — они закреплены
      требованием, а не решением реализации
- [x] 3.11 Своим кодом на существующем стеке, **без новых зависимостей** (Решение 13).
      `web/tests/helpers/contrast.ts` уже несёт `contrastRatio`, `deltaE76`,
      `dominantOpaqueRgb` — измерение графического объекта сессия тестов дописала туда же
- [x] 3.12 Проверить, что автоматическая проверка доступности видит подвал и не находит
      нарушений: `web/tests/a11y.spec.ts:57`, `template has no critical` — она входит в
      обязательный прогон. Убедиться, что страница действительно отдалась: этот гейт уже
      однажды разбирал страницу 404 на 10 шаблонах из 14
- [x] 3.13 **Проверить число дорожек `grid-template-columns` у сетки подвала на всех трёх
      полосах:** 4 шире 768 px, 2 в полосе 481–768, 1 на 480 и уже
      (`web/src/components/Footer.astro:259`, `@media (max-width: 768px) {`). Считать блоки
      нельзя — их четыре на любой ширине. **Третий вьюпорт для полосы 481–768 уже добавлен
      сессией тестов** — проект `footer-tablet`, 600×800, в `web/playwright.config.ts`
- [x] 3.14 Новые браузерные проверки берут **канонические адреса без слэша на конце**:
      `web/tests/e2e-addresses.test.ts` ловит любой строковый литерал-адрес, являющийся
      источником редиректа, и покраснеет раньше, чем дойдёт до иконок
- [x] 3.15 **Владелец 2026-08-25:** Telegram остаётся **маркой** (не `text-link`) при
      контрасте ~2,4:1 к фону подвала в теме `dark`. Записать в реестре `outcome: 'mark'`,
      `contrastWaiverThemes: ['dark']`, обоснование в `conditionsOutcome`. Обновить
      `footer-social` так, чтобы waiver читался из реестра, а не молчал
- [x] 3.16 **Гейт TD-45 / Decision 17:** `assertMarkAstroBodiesMatchAssets()` — нормализованное
      тело `<svg>` в каждом `*Mark.astro` совпадает с ассетом (допустимы XML-prolog и
      `aria-hidden="true"` на корне). Вызов рядом с `assertMarkFilesMatchRegistry` в
      `Footer.astro`. Негативно: правка только `.astro` обязана ронять сборку. Закрыть TD-45
- [ ] 3.17 **Приёмка отличимости Youtube и Rutube по форме** (сценарий спеки): кадры всего
      подвала в целевом размере в обеих темах лежат в
      `docs/design/acceptance/social-accounts/`
      (`footer-distinctiveness-default-1280.png`,
      `footer-distinctiveness-dark-1280.png`). Владелец подтверждает отличимость по этим
      кадрам — без подтверждения сценарий не закрыт

## 4. Документы и согласования

- [x] 4.1 Поправить пункт чек-листа паритета —
      `discovery/functional_parity_checklist.md:48`, `- [ ] **Social media icons** — 4 links, each opening in new tab`
      — это go-live gate, и он поимённо требует Instagram и Facebook. Формат правки уже
      принят в этом же файле для решения D12: пункт приводится к текущему составу с
      пометкой, что паритет с живым сайтом он больше не задаёт, и со ссылкой на решение
- [x] 4.2 Записать вклад этого change в запись **TD-40** файла `docs/tech-debt.md`
      — изменён подвал, а он присутствует на **всех 12 из 12** эталонов
      (6 страниц × 2 вьюпорта, снимки `fullPage`). Эталоны здесь **не переснимаются**;
      обоснование и **оформленная отмена** пункта входного документа — в Решении 10
- [x] 4.3 **Согласовать покрытие с change `visual-regression-gate`.** Он кладёт подвал в
      манифест покрытия обязательного job с допуском в ноль пикселей, маскируя только
      строку копирайта. Договориться **до** внесения его job в обязательные: кто обновляет
      манифест и эталоны блока «футер» и в каком порядке приземляются два change. Работа по
      внешним виджетам такое обязательство у себя называет — здесь оно должно быть названо
      тоже

## 5. Негативная проверка каждого нового гейта

Правило проекта: гейт, не проверенный негативно, гейтом не считается, и это требуется **по
каждому** новому гейту, а не по одному. Мутации выполняются в своём worktree, фикс к моменту
мутации закоммичен, каждая записана воспроизводимо — команда или патч плюс два числа.
Судить по **имени** покрасневшего теста, а не по цвету прогона: рядом живут другие проверки
подвала, и они покраснеют от той же мутации.

- [x] 5.1 Состав, сторона «исчезло»: удалить `{ label: 'Youtube', ... }` из
      `web/src/lib/social.ts` (не ВКонтакте — тот дал бы `hrefOf('ВКонтакте')` throw при
      сборке, это отдельный отказ, не предмет этой пробы), пересобрать. Вхождений
      `TheKinesiology` в `dist`: **270 → 0**. Покраснел по имени —
      `tests/social-accounts-ci-dist.test.ts`, `в подвале каждой страницы ровно принятый состав`
      (`Test Files 1 failed`, список из 10 страниц в сообщении). Восстановлено
      `git checkout HEAD -- src/lib/social.ts`, diff к `cf2af14` — 0 строк
- [x] 5.2 Состав, сторона «вернулось»: добавить `<p style="display:none">` со ссылкой на
      Instagram в `src/pages/kontakty.astro` (не на главной). Вхождений на `/kontakty`:
      **0 → 1**, на `/` — 0 (проба не на главной подтверждена). Покраснел
      `снятых сетей нет ни на одной странице вывода` (`ci-dist`). Восстановлено,
      diff — 0 строк
- [x] 5.3 Доступное имя: буквальная проба («снять `aria-hidden` с `<svg>`») **гейт не
      красит** — измерено: `YoutubeMark.astro` без `aria-hidden="true"`, оба кандидата
      (`доступное имя…`, `автоматическая проверка доступности…`) остались зелёными. Причина
      измерена, не гадание: явный `aria-label` на `<a>` побеждает в вычислении accessible
      name независимо от потомков (спецификация accname), поэтому графика без имени
      результат не меняет — это не декоративность гейта, а более сильная защита, чем
      требовал буквальный текст задачи. Настоящая несущая мутация —
      `aria-label={link.label}` → `{link.label + " !"}` в `Footer.astro`: покраснел
      `доступное имя ссылки точно равно названию сети` (`ВКонтакте` vs `ВКонтакте !`),
      `автоматическая проверка доступности` осталась зелёной (независимое подтверждение
      изоляции). Восстановлено, diff — 0 строк
- [x] 5.4 Размер цели: `24px` → `23px` у `.footer-social a` и `.footer-social-icon`.
      Покраснел `область нажатия каждой ссылки не меньше 24×24`: **только Telegram**
      (`62×23`) — на SHA мутации Telegram был `text-link` без SVG-распорки; марки
      (ВК/Youtube/Rutube) не сжались из‑за `height: 24px` на SVG. **История первого
      прохода:** после 3.15 Telegram тоже марка — повтор той же CSS-мутации на текущей
      голове даст другой набор покраснений. Восстановлено, diff — 0 строк
- [x] 5.5 Индикатор фокуса: `.footer-social a:focus-visible { outline: none; }` — покраснели
      все 4 ссылки в `каждая ссылка получает фокус табуляцией и показывает видимый
      индикатор`. Добавила `box-shadow: 0 0 0 2px rgba(0,0,0,0.08)` — **тень не успокоила
      гейт**, все 4 остались в списке (`shadow rgba(0, 0, 0, 0.08) 0px 0px 0px 2px`) —
      сценарий из TD, ради которого проверка и написана, воспроизведён и подтверждён.
      Восстановлено, diff — 0 строк
- [x] 5.6 Контраст: `fill="#FF0000"` → `fill="#1f1f1f"` в `YoutubeMark.astro` (близко к фону
      темы по умолчанию `#1a1a1a`). Покраснел `контраст доминирующей заливки… не ниже 3:1
      в каждой теме`: `тема по умолчанию, Youtube: 1.06:1` — **только эта тема**, как и
      требует формулировка задачи (тема `dark`, где фон далёк, не покраснела). **Побочная
      находка (на момент мутации, до 3.16):** мутация ТОЛЬКО `.astro` не краснела хеш-гейтом
      ассета — два независимых экземпляра разметки. Закрыто в этом change задачей 3.16
      (`assertMarkAstroBodiesMatchAssets`). Восстановлено, diff — 0 строк
- [x] 5.7 Отличимость: `fill="white"` фонового `<rect>` в `RutubeOnDarkMark.astro` →
      `fill="#FF0000"` (тот же цвет, что и Youtube). Покраснел `доминирующие заливки Youtube
      и Rutube различаются…`: `ΔE76 между марками 0.0 ниже порога 25`; тест контраста
      (`не ниже 3:1`) остался зелёным — изоляция подтверждена по обеим сторонам.
      Восстановлено, diff — 0 строк
- [x] 5.8 Раскладка: `@media (max-width: 768px) { grid-template-columns: 1fr 1fr }` →
      `1fr`. Покраснел `число дорожек сетки подвала соответствует полосе ширины` **только**
      на проекте `footer-tablet` (600px, `1 дорожек, а не 2`); `desktop` и `mobile` остались
      зелёными — три полосы проверяются независимо, а не одной пробой. Восстановлено,
      diff — 0 строк
- [x] 5.9 Постусловие по каждому пункту — `git diff HEAD -- <файл>` даёт 0 строк после
      каждого `git checkout HEAD --` (пункты 5.1–5.8, восемь мутаций отслеживаемых файлов,
      мутаций внутри `web/dist` не было — вывод пересобирался с нуля перед каждым замером).
      Финальный прогон на восстановленной голове —
      `npm run test:e2e:footer-social -- --project=desktop --project=footer-tablet --project=mobile`:
      **26 passed, 4 skipped**, дословно совпадает с прогоном в CI на `cf2af14`

**Побочная находка §5.6 → TD-45:** на момент мутации хеш ассета не сверял рендер
`*Mark.astro`. **Закрыто в этом change** (задача 3.16, `assertMarkAstroBodiesMatchAssets`;
запись TD-45 в `docs/tech-debt.md` помечена ЗАКРЫТ 2026-08-25).

## 6. Гейты и ревью

- [x] 6.1 Локально в `web/`: `npm run lint`, `npm run typecheck`, `npm run build`,
      `npm test`, `npm run test:build`. Затронуты стили и раскладка, поэтому `test:build`
      обязателен
- [x] 6.2 Локально `npm run test:demo` — без него не проверяется то, что требует задача
      2.8: `test:build` демо-сборку не делает
- [x] 6.3 Локально `npm run test:e2e:a11y` и `npm run test:e2e:smoke` — предмет правки
      живёт в подвале каждой страницы
- [x] 6.4 **Lighthouse.** `Lighthouse budgets (4 templates, median of 5)` — обязательный
      контекст защиты ветки, и он мерит именно то, что change трогает:
      `web/lighthouserc.cjs:24`, `'categories:accessibility': ['error', { minScore: 0.9, aggregationMethod: 'median-run' }],`
      плюс порог сдвига раскладки. Подвал есть на всех четырёх шаблонах бюджета. Прогнать
      локально или явно записать, что проверяется только в CI
- [x] 6.5 `audit:prod` в затронутых пакетах — **только если** дерево зависимостей или
      lockfile всё-таки менялись. Решение 13 говорит новых зависимостей не вводить; если
      решение изменено, это тянет ещё два обязательных контекста.
      **N/A на этой ветке:** lockfile/`web/package.json` зависимостей не менялись — прогон
      `audit:prod` не требовался
- [x] 6.6 Локально `./bin/check-spec-refs`. Перед выводом «у меня зелено» —
      `git fetch --prune`: достижимость ревизий меряется по `refs/remotes/origin/*`, а они
      обновляются только выборкой
- [x] 6.7 Обновить реестры `openspec/.spec-ref-debt` и `openspec/.spec-ref-absent`
      флагами `--write-debt` / `--write-absent` **в этом же PR** и перечислить добавленные
      строки в описании PR — по строкам реестра обоснования не видно.
      Прогон на `b748946`: `--write-debt` / `--write-absent`, расхождений 0; **новых строк
      в реестрах этот PR не добавил** (состав уже совпадал) — в описании PR перечислять
      нечего
- [x] 6.8 `./bin/openspec validate --all --strict --no-interactive`
- [ ] 6.9 **Ревью двумя независимыми исполнителями с разными уклонами** (шаги 2 и 5
      конвейера), ни один — не автор проверяемого. Ревью относится к SHA: если после него
      ветка обновлена, утверждение «это уже ревьюировали» на новую голову не переносится.
      **История:** первый проход на `b748946` — блокеры Decision 17/TD-45 и Telegram
      `text-link` (закрыты в `045286f`); hygiene на `2ef9238`. Факт-чек на `2ef9238`
      (docs leftovers ниже) — продукт OK; цикл 6.10 не закрыт, пока оба уклона чисты на
      **неизменной** голове после этой правки
- [ ] 6.10 **Реализация ведётся циклом «правка → гейты → e2e → ревью → правки» до
      устранения всех находок** (требование владельца от 23.08.2026). Один проход не
      считается: цикл закрывается не по исчерпанию попыток, а по отсутствию новых находок
      на неизменной голове, и каждый круг называет, что проверено и что осталось

## 7. Приёмка и архив

- [ ] 7.1 Предъявить оба состояния по каждому новому гейту: как краснел до фикса и как
      проходит после
- [ ] 7.2 Проверить, что состав в выводе именно четыре, и число получено **измерением по
      выводу**, а не переписано из спеки
- [ ] 7.3 Проверить отношение к основным спекам **применением** дельты в одноразовом
      worktree, а не только строгой валидацией: в OpenSpec 1.7.0 строгая валидация
      проверяет структуру, а не существование требования в основной спеке. Отдельно
      проверить, что требование о ролях артефактов не противоречит `deploy-gating`
- [ ] 7.4 Заархивировать change; требования переезжают в `openspec/specs/social-accounts/`.
      При архивировании реестры ссылок пересобрать заново: ссылки архива уходят из
      проверки, и храповик это заметит
