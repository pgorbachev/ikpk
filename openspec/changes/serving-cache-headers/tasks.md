Пометки у проверок обязательны и означают разное происхождение цвета:

- **[RED]** — проверка обязана падать до реализации, красный прогон предъявляется
  (отдельный push коммита только с тестами либо лог с точной командой и SHA);
- **[GREEN-BY-DESIGN]** — характеризационная проверка: предмет уже существует в коде,
  поэтому она зелёная с первого прогона. Красный прогон для неё **не предъявляется** и
  его отсутствие не является пропуском дисциплины; вместо него обязательна негативная
  мутация — иначе неизвестно, что проверка ловит;
- **[ДОКУМЕНТАРНЫЙ]** — сценарий, у которого предмет утверждения не наблюдаем ни
  прогоном, ни запросом к серверу: он удовлетворён текстом самого change. Для такого
  сценария называется место, где он удовлетворён, и способ сверки — чтение. Ни красного
  прогона, ни мутации у него нет, и это не пропуск: мутировать текст обоснования нечем,
  что делало бы «мутацию» переписыванием того же текста.

**Метка отвечает не за важность проверки, а за происхождение её цвета,** и путаница здесь
уже стоила ошибок в этом файле: **четыре** утверждения стояли под меткой `[RED]`, будучи
зелёными на существующем коде — 2.1 целиком, два критерия из трёх в 2.4 и сверка множеств в
2.7 (найдено сессией тестов, PR #103; пересчитано по прогону на
`origin/main@8b4d13bf4c3a41c40636f5d4c4f1d7eeb2b8d850`). Правило разбора: если проверка утверждает то,
чего в коде **ещё нет**, — `[RED]`; если то, что в коде **уже есть** и запрещено менять, —
`[GREEN-BY-DESIGN]`; если утверждение о **пустом множестве** (правил кеша ноль, блоков с
заголовком ноль), — оно вакуумно, красное даёт только страж «предмета нет», и метки
получают страж и утверждение по отдельности.

**Нумерация проверок и мутаций — только на добавление.** Номера `2.x`, `4.x`, `5.x`
ссылаются из комментариев в `web/tests/serving-config.test.ts`, поэтому существующие
номера не сдвигаются: новое получает следующий свободный номер, даже если по смыслу его
место в середине списка. Одно исключение постоянное и названо здесь, чтобы его не приняли
за сбой правила: **постусловие раздела 4 всегда стоит последним пунктом и потому
переномеровывается при каждом добавлении мутации.** Оно не мутация, и на него не ссылается
ни один тест, поэтому сдвиг ничего не ломает; сейчас это 4.22 (было 4.19, до того 4.16).
Освободившийся номер новой мутации **не отдаётся**: пропуск в нумерации виден и заставляет
перечитать это правило, а повторное использование 4.19 привело бы устаревшую ссылку на
«постусловие 4.19» к мутации молча. Поэтому 4.19 сейчас пуст.

## 1. Предусловия

- [x] 1.1 Спека утверждена владельцем (лежит в `main`, задача передана явно)
- [x] 1.2 Свой worktree исполнителю реализации: `/private/tmp/wt-cache-impl`, ветка
      `change/serving-cache-headers-impl` от `origin/main@1debf3609c4aa1efee846fa56784b1a85ba35534`.
      Тесты влиты из отдельной, уже закрытой сессии (PR #103), не переписывались
- [x] 1.3 Базовый SHA worktree: `origin/main@1debf3609c4aa1efee846fa56784b1a85ba35534`.
      На нём `scripts/bootstrap-vps.sh` heredoc `NGINX` — строки 70-101 (совпадает с
      номерами в design.md для базы `e26b4fee…`, скрипт с тех пор не менялся)
- [x] 1.4 Инвентарь пересчитан по своей сборке (`npm ci && npm run build` в `web/`,
      `web/dist`): `/_astro/` — 7 файлов, все CSS (0 не-CSS); данные поиска — 267
      `.pf_fragment`, 17 `.pf_index`, 1 `.pf_meta`; загрузчик поиска — 12
      файлов с постоянными именами (6 `.js`, 3 `.css`, 2 `wasm.*.pagefind`, 1
      `pagefind-entry.json`); `/fonts/` — 2 файла; корневых `.svg` кроме favicon — 11;
      `sitemap.xml` в сборке отсутствует (269 файлов `.html`). Полное совпадение с
      таблицей `design.md` («Context»), расхождений нет.
      **Поправка по находке ревью 5.5 (F1): совпадение было полным, а сам инвентарь —
      неполным.** Ни этот пункт, ни таблица `design.md` не содержали каталогов
      `web/public/institutes/` (3 файла `.png`, 17 189 байт) и `web/public/terms/` (1 файл
      `.pdf`, 451 872 байта), которые Astro копирует в корень `dist`. Требование о принятой
      плате обязывает назвать каждый такой адрес, поэтому четыре файла не были сопоставлены
      ни классу, ни плате вовсе. Таблица `design.md` дополнена; сверка «пункт ↔ таблица»
      зелёного цвета здесь ничего не стоила — обе стороны читали один и тот же неполный
      список

## 2. Тесты по спеке (отдельная сессия, до реализации)

Новый файл `web/tests/serving-config.test.ts`, vitest — то есть внутри job
`unit-and-build` workflow `Tests`. Место выбрано не по удобству: в гейт публикации входит
только `Tests`, и проверка, положенная рядом, выкладку не остановит.

- [ ] 2.1 **[GREEN-BY-DESIGN]** Извлечение предмета: тело heredoc'а `NGINX` из
      `scripts/bootstrap-vps.sh`, со сбросом строк-комментариев (приём из
      `web/tests/repo-hygiene.test.ts:118-130`). Проверка **падает**, если блок не
      найден или пуст, с сообщением «проверить не удалось» — вакуум не считается успехом.
      Метка именно такая: heredoc с блоком `server` существует до реализации
      (`scripts/bootstrap-vps.sh:70-101` на базе `8b4d13bf`), поэтому проверка зелёная с первого прогона —
      измерено на `origin/main@8b4d13bf4c3a41c40636f5d4c4f1d7eeb2b8d850`, где она проходит
      при 27 упавших из 36. **Красное даёт мутация 4.7** (переименовать разделитель
      heredoc), и падать она обязана с меткой `ПРОВЕРИТЬ НЕ УДАЛОСЬ`, а не любым текстом:
      предмет этой проверки — способность отличить «нарушений нет» от «проверить не
      удалось»
- [ ] 2.2 **[RED]** `Cache-Control` по классам, значения дословно: `/_astro/*` →
      `public, max-age=31536000, immutable`; `*.pf_fragment|*.pf_index|*.pf_meta` →
      `public, max-age=31536000, immutable`; `/fonts/*` → `public, max-age=2592000`;
      `/favicon.*` → `public, max-age=86400`; `/sitemap*.xml` → `public, max-age=3600`;
      страницы (уровень `server`) → `public, max-age=0, must-revalidate`
- [ ] 2.3 **[RED]** Заголовок утверждается **в своём блоке**: значение класса найдено
      именно в правиле этого класса. Значение `immutable`, найденное в блоке страниц,
      требование не удовлетворяет. Различающую мутацию для этой проверки даёт 4.10 —
      проверка, засчитывающая вхождение значения в текст, её не ловит
- [ ] 2.4 Ни потери, ни удвоения — критерий сформулирован по блокам, «ноль» нарушением
      считается не везде, и **происхождение цвета у четырёх частей разное** (три критерия
      плюс страж вакуума, вынесенный из первого из них), поэтому метка стоит у каждой
      отдельно:
      - **[GREEN-BY-DESIGN]** в каждом блоке, объявляющем свой `Cache-Control`, он
        объявлен **ровно один раз**. Само это утверждение до реализации **вакуумно**:
        блоков с заголовком ноль, значит «ни один не объявляет дважды» истинно на пустом
        множестве. Закрывается мутацией 4.5 (второй `add_header Cache-Control` в один
        блок);
      - **[RED]** страж вакуума: `Cache-Control` объявлен **хотя бы в одном** блоке. Он и
        даёт здесь красное до реализации — с меткой «ПРОВЕРИТЬ НЕ УДАЛОСЬ», потому что
        предыдущий критерий без него зелен ровно там, где политики нет вовсе. Проверено:
        на `origin/main@8b4d13bf` тест падает именно на этом стороже;
      - **[RED]** блоки, обязанные иметь собственное значение (`/_astro/`, данные поиска,
        `/fonts/`, `/favicon.*`, `/sitemap*.xml`, `server`), объявляют его. Красное — от
        отсутствия любого `add_header` в текущем vhost; закрывается мутациями 4.1
        (удалить правило класса) и 4.3 (закомментировать политику страниц), для `server`
        — отдельным утверждением «ровно одно объявление серверной политики»;
      - **[GREEN-BY-DESIGN]** блок `location /` своего `Cache-Control` **не объявляет** —
        он наследует серверный, и это несущий механизм, а не пропуск: тем же наследованием
        (но по своей, отдельной цепочке — см. 4.16) живут 264 правила перенаправления.
        Проверка, требующая заголовок в каждом блоке, была бы неисполнима и её пришлось бы
        ослабить до бесполезной. Проверка характеризационная: `location /` не объявляет
        заголовков и сегодня, требование лишь запрещает это изменить. **Закрывается
        мутацией 4.16**
- [ ] 2.5 **[RED]** `always` есть у политики страниц и **отсутствует** у правил
      неизменяемых классов (`/_astro/`, данные поиска) — годовой `immutable` не должен
      попадать на ответ «файла нет». Обе стороны утверждаются отдельно: мутации 4.6 и
      4.11 проверяют их независимо
- [ ] 2.6 **[RED]** Сжатие: `gzip` включён, `gzip_types` задан и включает CSS, JS, SVG,
      XML, JSON, обычный текст; в перечне **отсутствуют** `text/html` (nginx сжимает его
      всегда, повторное упоминание даёт предупреждение `duplicate MIME type`), `woff2`,
      `png`, `jpeg`, `webp` и `application/octet-stream` — под последний попадают файлы
      данных поиска, которые уже являются gzip-потоками.
      **Для XML и JavaScript проверяются оба ходовых написания, а не любое из них:**
      `text/xml` **и** `application/xml`; `application/javascript` **и** `text/javascript`.
      Причина — в `design.md`, Решение 10: тип задаёт таблица `mime.types` nginx, наш хост
      отдаёт `.xml` как `text/xml` и `.js` как `application/javascript`, и перечень с одним
      каноническим написанием оставил бы карту сайта и поисковый скрипт несжатыми при
      зелёной проверке. Это ужесточение требования относительно предыдущей ревизии спеки —
      проверка, засчитывающая любое из написаний, требованию больше не соответствует.
      **Различающие мутации для этой части — 4.20 (XML) и 4.21 (JavaScript), и 4.4 их не
      заменяет:** 4.4 удаляет перечень целиком, от чего краснеют все проверки 2.6 разом,
      включая запретительные, — то есть она доказывает лишь, что перечень вообще читается,
      а не что проверка отличает «оба написания» от «любого одного»
- [ ] 2.7 Форма правил не отбирает адреса у перенаправлений — и здесь тоже две части с
      разным происхождением цвета:
      - **[GREEN-BY-DESIGN]** сверка множеств: ни одно правило кеша не задано точным
        совпадением (`location = <адрес>`) адреса, для которого есть правило в
        `deploy/nginx-redirects.conf`. До реализации это **удовлетворено пустым
        пересечением** — правил кеша ноль, точных совпадений среди них тоже ноль, поэтому
        проверка зелёная с первого прогона (измерено на `origin/main@8b4d13bf`).
        **Закрывается мутацией 4.15**;
      - **[RED]** страж вакуума: в конфигурации **есть** правила кеша — иначе форму
        сравнивать не с чем. Он и даёт красное до реализации. Второй страж, в том же
        утверждении: в `deploy/nginx-redirects.conf` найдено хотя бы одно
        `location = <адрес>` и среди них есть `/sitemap.xml` — опорный случай, ради
        которого проверка заведена (адрес попадает под класс карт сайта буквально и
        существует только как 301). Этот страж зелёный до реализации: 264 правила и
        `deploy/nginx-redirects.conf:13` существуют — его предмет не наша политика, а
        сохранность опорного случая
- [ ] 2.8 **[GREEN-BY-DESIGN]** Адресный контракт сохранён в тексте: порядок
      `try_files \$uri \$uri/index.html \$uri/ =404`, `include` файла правил
      перенаправления, `error_page 404 /404.html` — всё внутри того же heredoc
- [ ] 2.9 **[GREEN-BY-DESIGN]** **Названное пересечение с существующей проверкой.**
      `web/tests/repo-hygiene.test.ts:127-130` уже утверждает наличие `include` файла
      правил — по всему тексту скрипта, а новая проверка — внутри heredoc'а vhost.
      Ответы обеих обязаны совпадать; новая строже. Зафиксировать это в комментарии к
      тесту, чтобы следующий исполнитель не счёл одну из них дублем и не удалил более
      строгую
- [ ] 2.10 Отметить явной записью сценарии **без** автоматической проверки, с причиной и
      способом. Записи распадаются на две группы, и смешивать их нельзя — у них разные
      причины и разные свидетельства:
      - *предмет — ответ работающего сервера.* `Cache-Control` и `Content-Encoding` на
        проводе, 301 для старого адреса и для `/sitemap.xml`, 200 для адреса без слэша,
        страница ошибки и прямой запрос `/404.html`, ответ клиенту без поддержки сжатия,
        шрифт из кеша без обращения к сети. Причина одна и та же: проект не поднимает
        nginx в тестах, а gzip и `add_header` — поведение сервера, не текста. Способ —
        раздел 5, у каждого сценария **назван свой шаг**; сценарий, для которого шага в
        разделе 5 нет, считается непокрытым, а не покрытым ручной приёмкой;
      - **[ДОКУМЕНТАРНЫЙ]** *предмет — текст самого change.* Два сценария:
        «Принятая плата названа» (требования «Введение политики не ухудшает уже
        достигнутое кеширование») — удовлетворён `design.md`, Решение 7 (в нём же полный
        разбор инвентаря по классам и принятой плате), и `proposal.md` (разделы про шрифты,
        корневые `.svg` и медиа), сверяется чтением **по перечню инвентаря**, а не по наличию
        абзаца: сценарий требует, чтобы названы были все адреса, а не примеры; «Расхождение текста и живой
        раздачи» (требования про свидетельство) — его `THEN` **утверждает
        ненаблюдаемость** состояния автоматическими проверками, поэтому проверка,
        объявленная для него, противоречила бы самому сценарию. Он удовлетворён
        именованным ограничением в спеке плюс записью долга в `docs/tech-debt.md`
        (задача 3.11), и сверяется наличием обоих. Эти два сценария в раздел 5 **не
        входят** и ручной приёмкой не закрываются — раньше они молча числились за ней
- [ ] 2.11 Предъявить красный прогон проверок, помеченных **[RED]**: отдельный push
      коммита только с тестами либо лог с точной командой и SHA. Для
      **[GREEN-BY-DESIGN]** красный прогон не предъявляется — за них отвечает раздел 4
- [ ] 2.12 **[GREEN-BY-DESIGN]** Сценарий «Правил перенаправления пока нет»: файл правил
      создаётся `touch`'ем **до** `nginx -t` (`scripts/bootstrap-vps.sh:103` против
      `scripts/bootstrap-vps.sh:108` на базе `8b4d13bf`; номера плывут с каждым коммитом в
      скрипт, поэтому проверка утверждает **порядок**, а не строки). Механизм несущий:
      `include` одного отсутствующего файла — ошибка конфигурации и `nginx -t` падает, а
      пустой `include` законен и означает «правил пока нет». Проверка ищет обе позиции в
      тексте скрипта со сброшенными строками-комментариями и падает, если любая не найдена
      — «проверить не удалось», а не «порядок верен». **Закрывается мутацией 4.18.** До
      этой правки сценарий спеки не был назначен ни автоматической проверке, ни ручному
      шагу: сессия тестов проверку написала, но плана для неё не было
- [ ] 2.13 **Правки в уже написанных проверках — требование, а не пожелание.** Тесты
      написаны раньше этой ревизии спеки (PR #103,
      `tests/serving-cache-headers@b4351d82c5ebb381248709fc88a42b692d530543`), и в **двух**
      местах кода проверок они ей больше не соответствуют. Обе правки перечислены здесь
      именно как задачи сессии тестов, чтобы её исполнитель не выводил их из текста спеки
      догадкой; правки делаются в ветке тестов, этим change код не трогается:
      - **Разделить слитую проверку 2.4 на две.** Сейчас
        `web/tests/serving-config.test.ts:621-639` держит в **одном** `it` (`2.4 ни один
        блок не объявляет Cache-Control дважды`) оба утверждения: страж непустоты
        (`declaring.length` больше нуля, строки 626-631) и собственно запрет удвоения
        (строки 632-639). До реализации весь этот тест красный **от стража**, то есть
        происхождение цвета опять смешано — ровно то, ради чего 2.4 и разделена на части с
        разными метками. Требуется два независимых `it`:
        1. `2.4 [RED] Cache-Control объявлен хотя бы в одном блоке` — только утверждение
           стража, с сообщением `ПРОВЕРИТЬ НЕ УДАЛОСЬ`. Красное даёт отсутствие политики в
           текущем vhost (проверено: на `origin/main@8b4d13bf4c3a41c40636f5d4c4f1d7eeb2b8d850`
           падает именно оно), зелёным закрывается задачей 3.1;
        2. `2.4 [GREEN-BY-DESIGN] ни один блок не объявляет Cache-Control дважды` — только
           утверждение о нуле блоков с двумя объявлениями, **без** стража. До реализации
           оно зелёное вакуумно, и это его законное состояние: красное ему даёт только
           мутация 4.5. Стража сюда **не дублировать** — иначе смешение цвета вернётся, а
           метка снова станет неверной.
      - **Заменить зачёт «годится любое написание» на требование всех нужных строк.**
        `REQUIRED_GZIP_GROUPS` (`:495-509`) описывает группу полем `accept`, а проверка
        `2.6 gzip_types задан и покрывает CSS, JavaScript, SVG, XML, JSON, обычный текст`
        (`:715-723`) засчитывает группу по `!g.accept.some(...)`. Требованию 2.6 это больше
        не соответствует: для XML обязательны **и** `text/xml`, **и** `application/xml`; для
        JavaScript — **и** `application/javascript`, **и** `text/javascript`. Требуется
        проверять **каждую** обязательную строку и называть в сообщении **каждую**
        отсутствующую (этого требует и сценарий спеки «Перечень называет только одно из двух
        написаний типа»: `THEN` — «падает и называет отсутствующую строку»). Две оговорки,
        без которых правка уйдёт в другую сторону: `application/x-javascript`, сегодня
        стоящий в `accept` третьим, обязательным становиться **не должен** — требование
        называет для JavaScript ровно два написания, и требовать третьего значило бы вводить
        новое требование правкой теста; комментарий на `:502-505` («перечень с одним
        `application/xml` карту сайта сжимать не будет, а этот тест пройдёт») после правки
        устаревает наполовину — текст теперь ловит промах по написанию, но живая раздача
        (5.4) остаётся обязательной, потому что таблицу типов задаёт не наш конфиг.
        Различающее красное даёт не 4.4, а мутации 4.20 и 4.21
      - **Отдельно — один комментарий с неверным механизмом**, и он опаснее устаревших
        ссылок, потому что читается как знание: `:653-656` называет подходящей мутацией
        «добавить в `location /` свой `add_header Cache-Control`, тогда правила
        перенаправления потеряют политику». Правила перенаправления от этого **не
        страдают** — они отдельные блоки `location = <адрес>` и наследуют прямо от `server`
        (`design.md`, Решение 4). Заменить ссылкой на 4.16 с её собственным следствием.
        Остальные устаревшие ссылки-комментарии («мутации в разделе 4 на неё нет» на
        `:804-806`, «`tasks.md` проверки не назначает» перед проверкой порядка
        `touch`/`nginx -t`, три записи о пробелах раздела 5 в блоке «СЦЕНАРИИ БЕЗ
        АВТОМАТИЧЕСКОЙ ПРОВЕРКИ») переписываются на 4.17, 2.12 с 4.18 и шаги 5.4, 5.9, 5.10
- [x] 2.13 **Красные тесты класса корневых `.svg`** (класс внесён после ревью 5.5, находка
      F1).

      **Номер 2.13 в этом файле занят дважды, и это разрешается правилом, а не
      перенумерацией.** Выше под тем же номером — «Правки в уже написанных проверках».
      Правило чтения: ссылка **с подномером** (`2.13.1`, `2.13.2`, `2.13.3`) означает
      всегда этот блок, ссылка **без подномера** — тот, что выше; у него подномеров нет
      вовсе, поэтому пересечения не возникает.

      Почему не перенумеровано, хотя дубликат — дефект. Подномера `2.13.1`–`2.13.3`
      закреплены не только здесь: они входят в **имена тестов** в
      `web/tests/serving-config.test.ts` и в предъявленное свидетельство красного прогона —
      упавший check-run на `95f7e64c8c8f39ecb4f393377bc0fa0eb41bc22d`, где эти имена
      напечатаны. По правилу проекта о мутации судят **по имени** покрасневшего теста,
      значит сдвиг подномеров обесценил бы уже снятое свидетельство. Перенумерация верхнего
      блока стоит восьми ссылок в теле PR и тоже создала бы расхождение с историей. Дубликат
      без правила чтения хуже дубликата с правилом; убирать его следует в момент, когда
      свидетельство перестанет быть нужным, — то есть после приёмки и архива.

      Пишутся **до** реализации и в **отдельной сессии** с чистым контекстом — тот же
      порядок, что для остальной спеки, и по той же причине: автор правки спеки проверял бы
      замысел вместо текста. Красный прогон предъявляется (отдельный push коммита только с
      тестами либо лог с точной командой и SHA). Проверок три, и третья не выводится из
      первых двух:
      1. правило класса существует и задаёт **своё** значение: `location ~ ^/[^/]+\.svg$` с
         `add_header Cache-Control "public, max-age=86400"`, без `always` и без `immutable`.
         Утверждение — **в этом правиле**, а не по вхождению значения в текст: значение
         совпадает со значением класса `favicon.`, поэтому проверка «в конфиге есть
         `max-age=86400`» зелёная и на конфиге без нового правила вовсе;
      2. **порядок правил**: правило `~ ^/favicon\.` объявлено раньше правила корневых
         `.svg`. Предмет отдельный, потому что снаружи он сегодня не наблюдаем — сроки у
         классов совпадают, и неверный порядок не меняет ни одного ответа (спека, сценарий
         «Два класса с совпадающим значением объявлены в неверном порядке»);
      3. **страж вакуумности** по образцу 2.3: если правила класса в разобранном heredoc нет
         вовсе, проверка падает сообщением «проверить не удалось», а не засчитывает
         отсутствие несоответствий. До реализации красное дают проверки 1 и 2

## 3. Реализация

- [x] 3.1 Внесено: серверное умолчание `add_header Cache-Control "public, max-age=0,
      must-revalidate" always;` — `scripts/bootstrap-vps.sh:85`
- [x] 3.2 Внесено: `location ^~ /_astro/` (`scripts/bootstrap-vps.sh:125-127`), значение
      без `always`
- [x] 3.3 Внесено: `location ~ \.(pf_fragment|pf_index|pf_meta)$`
      (`scripts/bootstrap-vps.sh:133-135`), значение без `always`, по расширению, не по
      каталогу
- [x] 3.4 Внесено: `location ^~ /fonts/` → `public, max-age=2592000`
      (`scripts/bootstrap-vps.sh:141-143`)
- [x] 3.5 Внесено: `location ~ ^/favicon\.` → `public, max-age=86400`
      (`scripts/bootstrap-vps.sh:147-149`); `location ~ ^/sitemap.*\.xml$` →
      `public, max-age=3600` (`scripts/bootstrap-vps.sh:155-157`). Ни одно не задано
      точным совпадением
- [x] 3.6 Внесено: `gzip on`, `gzip_vary on`, `gzip_min_length 256`, `gzip_types` с 8
      строками (`scripts/bootstrap-vps.sh:97-108`) — CSS, оба написания JS
      (`application/javascript`, `text/javascript`), SVG, оба написания XML
      (`application/xml`, `text/xml`), JSON, `text/plain`. `nginx -t`-предупреждений о
      дубликате типа не проверял (nginx на раннере не поднимается, Non-Goals design.md) —
      типы разные, дубликата по построению нет
- [x] 3.7 `try_files`, `include` правил перенаправления, `error_page` не менялись —
      diff подтверждён (`git diff` показывает только добавленные строки, ни одна
      существующая директива не тронута). Новые `$` в regex (`\.(...)$`,
      `^/sitemap.*\.xml$`) экранированы как `\$`
- [x] 3.8 Прогнано: **до** реализации 27 упавших / 10 зелёных из 37 (совпадает с базой
      теста PR #103); **после** — 37/37 зелёных, ни один из десяти characterization
      (`[GREEN-BY-DESIGN]`) не покраснел
- [x] 3.9 `docs/nginx-migration-notes.md` обновлён: раздел «Политика кеш-заголовков»
      помечен как реализованный, названы три расхождения с заметкой (поиск, шрифты,
      `*.html`/`sitemap.xml`), source of truth указан на спеку/`openspec/specs/`,
      security-заголовки и CSP оставлены как намерение
- [x] 3.10 `docs/deploy-vps.md` (раздел «Одноразовая правка на уже развёрнутом сервере»)
      дополнен указанием переносить директивы политики раздачи, а не только `include`
      редиректов, со ссылкой на heredoc `NGINX` как источник актуального текста
- [x] 3.11 Запись `TD-22` добавлена в `docs/tech-debt.md`: дрейф «текст
      `bootstrap-vps.sh` ↔ живой vhost», ничем не гейтится, два возможных решения названы
      как отдельная работа
- [x] 3.12 **Реализация класса корневых `.svg`** — после красных тестов 2.13, не раньше.
      Внесено: `location ~ ^/[^/]+\.svg$` с `add_header Cache-Control "public, max-age=86400";`
      **без** `always`, размещено **после** `location ~ ^/favicon\.` и перед `location ~
      ^/sitemap.*\.xml$` (`scripts/bootstrap-vps.sh:151-160` на голове реализации). Форма —
      один сегмент пути (`^/[^/]+\.svg$`), не `\.svg$`: правило не заберёт `.svg` под
      `/media/`. Новый `$` в regex экранирован как `\$` внутри heredoc'а bash (см. 3.7).
      Прогнано: **до** реализации 3 упавших / 38 зелёных из 41 (`web/tests/serving-config.test.ts`);
      **после** — 41/41, весь пакет `npx vitest run` — 222/222 (было 3 failed | 219 passed из 222)
- [x] 3.13 `docs/deploy-vps.md`: перечень классов в комментарии процедуры ручного переноса уже
      дополнен корневыми `.svg` и оговоркой о порядке (`docs/deploy-vps.md:168-171`, коммит
      `0fa8917`, до этой сессии) — перенесший по перечню не оставит стенд без нового класса.
      Диапазон строк heredoc'а, указанный в `docs/deploy-vps.md` (`scripts/bootstrap-vps.sh:115`
      — комментарий с `` `nginx -t` `` внутри блока `NGINX`), после правки 3.12 не сдвинулся:
      новый блок вставлен на строки 151–160, то есть ПОСЛЕ строки 115, и ссылка осталась
      верной. Других численных диапазонов, ссылающихся на heredoc, в файле нет (проверено
      `grep -n 'scripts/bootstrap-vps.sh:[0-9]' docs/deploy-vps.md`). Извлечение heredoc'а
      командой `awk` (`docs/deploy-vps.md:150-154`) работает по текстовому маркеру, а не по
      номеру строки, и после правки 3.12 корректно включает новый блок — проверено прогоном
      этой команды на живом файле

## 4. Негативная проверка гейта

В **отдельном worktree**; фикс к моменту мутации закоммичен; каждая мутация записывается
командой или патчем и **двумя числами — до и после**; суждение по **имени** покрасневшего
теста, а не по цвету прогона.

- [x] 4.1 Удалить правило `/_astro/` → **0 → 3 failed**: `2.2 класс '/_astro/…'
      отдаётся с заданным значением`, `2.3 класс '/_astro/…' объявляет своё значение в
      СВОЁМ правиле…`, и side-effect `2.5 у неизменяемого класса '/_astro/…' always
      отсутствует` (значение теперь наследуется из server, `r.source` меняется)
- [x] 4.2 Подменить значение: `max-age=31536000` без `immutable` → **0 → 1 failed**:
      `2.2 класс '/_astro/…' отдаётся с заданным значением`
- [x] 4.3 Закомментировать блок политики страниц → **0 → 8 failed**: все шесть
      `2.2 класс '…' отдаётся с политикой страниц` (DEFAULT_CLASSES), `2.4 блок server
      объявляет политику страниц ровно один раз`, `2.5 always стоит у политики страниц…`.
      Комментарий не засчитан — проверка ищет исполняемый текст, а не подстроку
- [x] 4.4 Удалить `gzip_types`, оставив `gzip on` → **0 → 4 failed**: все четыре
      проверки 2.6, читающие перечень (`задан и покрывает…`, `не содержит text/html`,
      `не содержит уже сжатых типов…`, `не содержит application/octet-stream`) — все
      падают с «ПРОВЕРИТЬ НЕ УДАЛОСЬ», а не «нарушений нет»
- [x] 4.5 Добавить второй `add_header Cache-Control` в один блок (`server`) →
      **0 → 9 failed**, в том числе целевая `2.4 [GREEN-BY-DESIGN] ни один блок не
      объявляет Cache-Control дважды` — подтверждено, что этот тест не декоративен
- [x] 4.6 Добавить `always` в правило `/_astro/` → **0 → 1 failed**: `2.5 у
      неизменяемого класса '/_astro/…' always отсутствует`
- [x] 4.7 Переименовать разделитель heredoc (`NGINX` → `NGINXCONF`) → **0 → 34 failed**,
      корень — `2.1 предмет проверки существует…` с точным текстом «ПРОВЕРИТЬ НЕ
      УДАЛОСЬ: … не найдено открытие heredoc'а <<NGINX». Ветка вакуума пройдена
- [x] 4.8 Переставить `\$uri/` перед `\$uri/index.html` → **0 → 1 failed**: `2.8
      [GREEN-BY-DESIGN] порядок try_files сохранён…`
- [x] 4.9 Удалить `error_page 404 /404.html` → **0 → 1 failed**: `2.8 [GREEN-BY-DESIGN]
      error_page 404 отдаёт страницу ошибки сайта`
- [x] 4.10 **Различающая мутация для 2.3.** Перенесена (не продублирована как строка,
      а перемещена: убрана из `/_astro/`, добавлена в `server`) →
      **0 → 12 failed**, в том числе целевая `2.3 класс '/_astro/…' объявляет своё
      значение в СВОЁМ правиле, а не наследует его` — краснеет именно она, хотя строка
      `"public, max-age=31536000, immutable"` физически осталась в тексте heredoc'а
      (сместившись в `server`, где `server` получил два объявления `Cache-Control`, что
      дополнительно закрасило `2.4`/`2.2` как естественное следствие переноса — численное
      совпадение с «только 2.3» в тексте задачи относится к гипотетической
      substring-проверке, а не к полному набору наших 37 тестов)
- [x] 4.11 Снять `always` с политики страниц → **0 → 1 failed**: `2.5 always стоит у
      политики страниц…`
- [x] 4.12 Добавить `text/html` в `gzip_types` → **0 → 1 failed**: `2.6 gzip_types не
      содержит text/html`
- [x] 4.13 Добавить `image/png` в `gzip_types` → **0 → 1 failed**: `2.6 gzip_types не
      содержит уже сжатых типов (woff2, png, jpeg, webp, архивы)`
- [x] 4.14 Добавить `application/octet-stream` в `gzip_types` → **0 → 1 failed**: `2.6
      gzip_types не содержит application/octet-stream`
- [x] 4.15 Переписать правило карт сайта на `location = /sitemap.xml` → **0 → 3
      failed**: целевая `2.7 ни одно правило кеша не задано точным совпадением адреса,
      у которого есть перенаправление`, плюс side-effect `2.2`/`2.3` класса
      `sitemap*.xml` (адреса `/sitemap-index.xml`, `/sitemap-0.xml` больше не покрыты
      регулярным выражением и падают на server-умолчание)
- [x] 4.16 **Мутация для характеризационной части 2.4.** Добавлен
      `add_header Cache-Control "public, max-age=3600";` в `location /` →
      **0 → 7 failed**, целевая `2.4 [GREEN-BY-DESIGN] блок location / своего
      Cache-Control не объявляет — он наследует серверный` покраснела; шесть
      `2.2 класс '…' отдаётся с политикой страниц` тоже упали (страницы отвечают
      значением блока, а не сервера) — **подтверждено**: `[GREEN-BY-DESIGN] правила
      перенаправления не объявляют своих add_header` осталась зелёной, т.к. правила
      перенаправления наследуют от `server` напрямую, а не через `location /`
- [x] 4.17 **Мутация для характеризационной проверки «правила перенаправления не
      объявляют своих `add_header`»** (её тоже не было). Предмет проверки — **потеря
      унаследованного серверного заголовка**, поэтому мутация объявляет в правиле
      **посторонний** заголовок и своего `Cache-Control` **не объявляет**. Патч по шаблону
      генератора (`web/scripts/gen-redirects.ts:138`, `deploy/nginx-redirects.conf`
      генерируемый — правка готового файла ушла бы при следующем `redirects:gen`):

      ```diff
      -    .map(([from, to]) => `location = ${from} { return 301 ${to}; }`),
      +    .map(([from, to]) => `location = ${from} { add_header X-Debug 1; return 301 ${to}; }`),
      ```

      затем `npm run redirects:gen` в `web/`. Числа, мера — `grep -c add_header
      deploy/nginx-redirects.conf`: **0 → 263** при 264 правилах в файле. Именно 263, и
      расхождение на единицу не погрешность: 264-е правило,
      `location = /sitemap.xml { return 301 /sitemap-index.xml; }`
      (`deploy/nginx-redirects.conf:13`), печатается отдельной строкой-литералом
      `web/scripts/gen-redirects.ts:123`, а не шаблоном `:138`, и мутацией не задевается.
      Число 264 после мутации означает, что тронут и литерал; число 0 — что генератор не
      запускался. Падать обязана проверка **по имени** —
      `[GREEN-BY-DESIGN] правила перенаправления не объявляют своих add_header`.

      **Наблюдаемое следствие, и оно строго то, что заявлено:** объявив хотя бы один свой
      `add_header`, блок перестаёт получать серверный набор целиком (Решение 3), поэтому 263
      ответа 301 приходят **без** `Cache-Control`, а `X-Debug: 1` в них появляется — 301
      входит в перечень кодов, которые `add_header` покрывает и без `always`
      (Решение 4; `add_header`: 200, 201, 204, 206, 301, 302, 303, 304, 307, 308).
      Неизменённое правило `/sitemap.xml` в том же прогоне продолжает наследовать серверную
      политику — встроенный контроль того, что мутация действует именно через объявление
      заголовка, а не как-то иначе.

      **Прежняя форма этой мутации была неверна, и повторять её нельзя.** Она добавляла в
      правило собственный `add_header Cache-Control "public, max-age=31536000"` и объявляла
      следствием ответ 301 **вообще без** `Cache-Control`. Такого результата не бывает: 301
      покрывается `add_header` без `always`, значит ответ пришёл бы с этим самым годовым
      значением — другой дефект, тоже молчаливый, но не потеря заголовка. Текстовая проверка
      краснела бы одинаково в обоих случаях, поэтому ошибка не всплыла бы прогоном; она
      всплыла бы у следующего исполнителя, который по этой записи выучил бы неверный
      механизм наследования

      **Выполнено, числа подтверждены точно**: `grep -c add_header
      deploy/nginx-redirects.conf` **0 → 263** при 264 правилах (`grep -c "location = "`).
      Тест `[GREEN-BY-DESIGN] правила перенаправления не объявляют своих add_header` —
      **0 → 1 failed**, единственная упавшая проверка (36/37 остались зелёными). Встроенный
      контроль подтверждён: `location = /sitemap.xml { return 301 /sitemap-index.xml; }`
      в `deploy/nginx-redirects.conf` не тронут мутацией (`add_header` в нём нет).
      Постусловие 4.22 для этой мутации выполнено на **двух** файлах —
      `web/scripts/gen-redirects.ts` и `deploy/nginx-redirects.conf`
- [x] 4.18 **Мутация для 2.12.** Перенести строку
      `touch "${WEB_ROOT}/shared/nginx-redirects.conf"` (`scripts/bootstrap-vps.sh:179`)
      **после** `nginx -t` → падает 2.12. Именно перенести, а не удалить: удаление роняет
      проверку по ветке «не найдено» («проверить не удалось»), а предмет требования —
      порядок. Обе ветки пройти по разу, иначе непройденная остаётся таким же обещанием,
      как непроверенный гейт

      **НАХОДКА, ЗАКРЫТА правкой теста (решение владельца, коммит `d00a980`).** Первый
      прогон мутации показал: тест был зелёным несмотря на перенос настоящего `touch`
      после `nginx -t` — `script.search(...)` возвращал **первое** совпадение в файле, а
      их два: наш (главный, безусловный, после heredoc) и другой, не связанный, в ветке
      раннего выхода «vhost уже существует» (`scripts/bootstrap-vps.sh:61`, перед
      `exit 3`, существовал в коде до этого change). Более ранний `touch` стоит в файле
      раньше `nginx -t` независимо от положения настоящего, поэтому `touch < test` было
      истинно всегда. Подтверждено измерением индексов (`node -e`): совпадения touch на
      1459 и 2838, `nginx -t` на 2827 — настоящий touch сместился правильно (2838 > 2827),
      но `.search()` вернул первое совпадение (1459 < 2827).

      Тест исправлен минимально и по предмету: обе позиции (`touch` и `nginx -t`) теперь
      ищутся не по всему файлу, а только в тексте **после** закрывающего разделителя
      heredoc'а `NGINX` — это единственное место, где сценарий применим (путь записи
      свежего vhost), и оно естественно исключает не связанный `touch` ветки раннего
      выхода, целиком лежащий до открытия heredoc'а. Повторный прогон мутации в отдельном
      worktree на коммите `d00a980`: **0 → 1 failed**, упавший тест — по имени
      `[GREEN-BY-DESIGN] файл правил перенаправления создаётся до nginx -t` (сообщение
      называет именно «файл правил создаётся ПОСЛЕ nginx -t»); на неизменённом коде тест
      зелёный (37/37), остальные 36 проверок цвет не меняли ни до, ни после исправления
      теста. Постусловие: `git status --porcelain` пуст, diff относительно `d00a980`
      отсутствует; worktree `/private/tmp/wt-cache-mutate2` удалён
- [x] 4.20 **Различающая мутация для пары написаний XML** (2.6). Удалить из `gzip_types` в
      heredoc `NGINX` **только** `text/xml`, оставив `application/xml`:

      ```bash
      grep -o 'text/xml\|application/xml' scripts/bootstrap-vps.sh | wc -l   # до: 2
      perl -0pi -e 's{\s*\btext/xml\b}{}' scripts/bootstrap-vps.sh
      grep -o 'text/xml\|application/xml' scripts/bootstrap-vps.sh | wc -l   # после: 1
      ```

      Числа: написаний XML-типа в перечне **2 → 1**; обязательных строк, отсутствующих в
      перечне, **0 → 1**. Падать обязана по имени проверка полноты перечня
      (`2.6 gzip_types задан и покрывает …`), и её сообщение обязано называть отсутствующую
      строку `text/xml`. Различающая она по двум признакам сразу, и оба надо предъявить:
      запретительные проверки 2.6 (`text/html`, уже сжатые типы, `application/octet-stream`)
      остаются **зелёными** — в отличие от 4.4, где краснеет всё; а проверка, засчитывающая
      группу по принципу «годится любое из написаний», остаётся зелёной **тоже** — то есть
      эта мутация и есть единственное доказательство, что реализовано требование про пары, а
      не старый зачёт по одному написанию (задача 2.13). Затем прогнать зеркальную половину:
      восстановить файл, `perl -0pi -e 's{\s*\bapplication/xml\b}{}' scripts/bootstrap-vps.sh`
      — те же числа (2 → 1 и 0 → 1), в сообщении названа строка `application/xml`. Без
      зеркальной половины проверка,
      требующая лишь то написание, которое отдаёт наш хост, осталась бы неотличима от
      требующей обоих

      **Выполнено с поправкой на число.** Сырой `grep -o 'text/xml\|application/xml' |
      wc -l` в реализации даёт **3**, не 2: пояснительный комментарий на строке 96
      (`# отдаёт .xml как text/xml, .js как application/javascript`) тоже содержит
      подстроку `text/xml`. Реальный счёт по `gzip_types`: **2 → 1** для первой половины
      (удалён `text/xml`; итоговый сырой grep 3 → 2), **2 → 1** для зеркальной (удалён
      `application/xml`). Тест `2.6 gzip_types задан и покрывает …` — **0 → 1 failed** в
      обеих половинах, сообщение называет ровно отсутствующую строку (`XML: text/xml` и
      `XML: application/xml` соответственно); все три запретительные проверки 2.6 и
      «any-of»-подобных зачётов в наборе нет — обе половины подтверждены отдельно
- [x] 4.21 **Различающая мутация для пары написаний JavaScript** (2.6). То же для второго
      класса: удалить **только** `text/javascript`, оставив `application/javascript`
      (`perl -0pi -e 's{\s*\btext/javascript\b}{}'`; мера — `grep -o
      'text/javascript\|application/javascript' scripts/bootstrap-vps.sh | wc -l`, **2 → 1**;
      отсутствующих обязательных строк **0 → 1**), затем зеркальная половина —
      `perl -0pi -e 's{\s*\bapplication/javascript\b}{}' scripts/bootstrap-vps.sh`. Пункт
      отдельный, а не примечание к 4.20, потому что и требование, и проверка называют два
      класса независимо: промах по одному из них 4.20 не поймала бы.
      **Названная асимметрия между 4.20 и 4.21, чтобы её не приняли за ошибку записи:** на
      живой раздаче сегодня несжатым остаётся результат только 4.20 — хост отдаёт `.xml` как
      `text/xml`, а `.js` как `application/javascript` (Решение 10), поэтому удаление
      `text/javascript` факт сжатия сегодня не меняет. Красное здесь — от текста, и это
      предмет требования: перечень обязан покрывать оба написания, потому что таблицу типов
      задаёт пакет nginx, а не наш конфиг, и обновление пакета меняет её без нашего участия.
      Проверять сжатие `.js` на стенде мутацией 4.21 бессмысленно — за факт отвечает шаг 5.4

      **Выполнено с той же поправкой на число** (комментарий строки 96 упоминает
      `application/javascript` тоже): сырой grep даёт **3 → 2** вместо «2 → 1» для обеих
      половин, реальный счёт в `gzip_types` — **2 → 1**. Тест `2.6 gzip_types задан и
      покрывает …` — **0 → 1 failed** в обеих половинах, сообщение называет
      `JavaScript: text/javascript` и `JavaScript: application/javascript` соответственно
- [x] 4.22 Постусловие после каждой мутации: **пустой `git status --porcelain` и
      отсутствие diff** относительно SHA коммита с реализацией
      (`0d3bbfadd6cd2a7b3aa8d1b779015c5e63fa3078`) подтверждены после **каждой** из 20
      мутаций (`git checkout -- <файл>` + `git status --short --porcelain` → пусто).
      Для 4.17 проверены оба файла — `web/scripts/gen-redirects.ts` и сгенерированный
      `deploy/nginx-redirects.conf` (после `git checkout` `grep -c add_header` вернулся к
      **0**). Worktree `/private/tmp/wt-cache-mutate` удалён (`git worktree remove
      --force`), throwaway-ветка `tmp/serving-cache-headers-mutate` удалена
      (`git branch -D`)
- [x] 4.23 **Мутация нового класса: правило удалено.** Снять из heredoc'а блок
      `location ~ ^/[^/]+\.svg$` целиком (`sed`/патч приложить дословно). Ожидание: красная
      проверка из 2.13.1, **имя** покрасневшего теста записать вместе с числами до/после.
      Красное от одного лишь удаления `add_header` внутри блока не годится: блок без
      директив — другой дефект, и его ловит страж 2.13.3

      **Выполнено, в отдельном worktree `/private/tmp/wt-cache-svg-mutate` (throwaway-ветка
      `tmp/serving-cache-root-svg-mutate`, реализация закоммичена на `b645669`).** Патч
      (снят весь блок правила вместе с комментарием, `scripts/bootstrap-vps.sh:151-162`
      на голове реализации):

      ```diff
      -  # Векторная графика в корне сайта (hero-main.svg, logo-wordmark.svg,
      -  # logo-icon.svg и т.п.) — на критическом пути отрисовки, но, в отличие от
      -  # /_astro/, без хеша содержимого в имени: правка файла под тем же адресом —
      -  # обычная замена, поэтому годовой срок и immutable этому классу не положены.
      -  # Правило записано ПОСЛЕ правила favicon. намеренно: /favicon.svg подходит
      -  # под оба регулярных выражения, а nginx берёт первое совпавшее — favicon.
      -  # обязан обслуживать /favicon.svg, а не этот класс. Один сегмент пути в
      -  # регулярном выражении, а не \$.svg\$: класс задан КОРНЕМ сайта, а не любым
      -  # .svg, — .svg под /media/ отдаётся политикой страниц.
      -  location ~ ^/[^/]+\.svg\$ {
      -    add_header Cache-Control "public, max-age=86400";
      -  }
      -
      ```

      Числа: **0 → 3 failed** (было 0/41, стало 3/41 — совпадает с базой до реализации 3.12).
      Упавшие тесты по имени: `2.13.3 [RED] страж: правило класса корневых .svg найдено в
      разобранном heredoc`, `2.13.1 [RED] класс корневых .svg объявляет своё значение в СВОЁМ
      правиле`, `2.13.2 [RED] правило класса favicon. объявлено раньше правила корневых .svg`.
      Страж 2.13.3 краснеет первым по построению — это ровно тот случай «правила нет
      вовсе», для которого он назван в собственном комментарии, а не декоративное
      совпадение
- [x] 4.24 **Мутация порядка: правила переставлены.** Поменять местами блоки
      `~ ^/favicon\.` и `~ ^/[^/]+\.svg$`, ничего больше не меняя. Ожидание: красная
      **только** проверка порядка из 2.13.2; все проверки значений остаются зелёными — это и
      есть доказательство, что предмет проверки порядок, а не значение. Если покраснела
      проверка значения, мутация задела не тот предмет и вывод о гейте делать нельзя

      **Выполнено, тот же worktree.** Блоки `location ~ ^/favicon\.` и
      `location ~ ^/[^/]+\.svg$` (с их комментариями) переставлены местами, порядок
      остальных правил и содержимое блоков не тронуты. Числа: **0 → 1 failed**. Упавший
      тест по имени: `2.13.2 [RED] правило класса favicon. объявлено раньше правила
      корневых .svg` — и только он; 40 из 41 остались зелёными, включая обе проверки
      значений (`2.13.1`) и страж (`2.13.3`)
- [x] 4.25 Постусловие 4.22 распространить на 4.23, 4.24 и 4.26, с **новым опорным SHA** —
      SHA коммита реализации 3.12, а не `0d3bbfad…`: относительно старого SHA diff непуст по
      построению (в дереве есть новый класс), и постусловие превратилось бы в ложный отказ.
      Мутации — в своём worktree, реализация к моменту мутации закоммичена

      **Выполнено с уточнением опорного SHA.** Мутации 4.23, 4.24, 4.26 сделаны в отдельном
      worktree, созданном от `07eb6ae` (коммит реализации 3.12 `b645669` плюс следующий
      коммит с отметками задач 2.13/3.12/3.13 в этом же tasks.md) — не от `b645669` напрямую:
      diff относительно одного лишь `b645669` показывает **неотносящееся к мутации**
      изменение `tasks.md` (отметки задач, сделанные ПОСЛЕ `b645669` и ДО создания
      mutate-worktree), и постусловие относительно него было бы ложным отказом — тем самым
      искажением, от которого этот раздел и предостерегает. `07eb6ae` — фактическая точка,
      от которой worktree ответвлён, и правильный опорный SHA по духу правила (голова, на
      которой тесты зелёные и код 3.12 закоммичен), а не по букве «SHA коммита 3.12» — этот
      коммит в изоляции не совпадает с состоянием worktree, потому что между ним и мутацией
      легла отдельная, законная правка. После каждой из трёх мутаций подтверждено: пустой
      `git status --short --porcelain` и пустой `git diff 07eb6ae -- scripts/bootstrap-vps.sh`
      (а также пустой `git diff 07eb6ae --stat` целиком). Worktree
      `/private/tmp/wt-cache-svg-mutate` удалён (`git worktree remove --force`),
      throwaway-ветка `tmp/serving-cache-root-svg-mutate` удалена (`git branch -D`)
- [x] 4.26 **Мутация формы правила: класс задан расширением, а не корнем.** Записать правило
      как `location ~ \.svg$` (без привязки к корню), ничего больше не меняя. Ожидание:
      красная **только** `2.13.1 [GREEN-BY-DESIGN] класс задан корнем сайта: .svg вне корня
      получает политику страниц`; проверки значения и порядка остаются зелёными.

      **Задача добавлена по пробелу, найденному сессией тестов, и пробел был настоящим.**
      Ни 4.23 (правило удалено целиком), ни 4.24 (порядок переставлен) правило,
      захватывающее `.svg` вне корня, не ловят: значение у него верное, порядок верный, блок
      на месте. То есть без 4.26 у требования «класс задан корнем сайта» мутации не было
      вовсе, а проверка `2.13.1 [GREEN-BY-DESIGN]` оставалась непроверенным обещанием —
      непройденная ветвь, что по правилам проекта равно непроверенному гейту.

      Мутация уже показана рабочей: сессия тестов прогнала её вариантом фикстуры «D» и
      получила красной ровно эту проверку и никакую другую (8 failed против базовой линии 7,
      разница — одно имя). При исполнении 4.26 числа снимаются заново на реальном скрипте,
      а не переписываются из фикстуры: фикстура доказала различимость предмета, но не
      заменяет мутацию реализации

      **Выполнено на реальном скрипте, числа сняты заново, тот же worktree.** Патч:
      `location ~ ^/[^/]+\.svg\$ {` → `location ~ \.svg\$ {`, без иных изменений. Числа:
      **0 → 1 failed**. Упавший тест по имени: `2.13.1 [GREEN-BY-DESIGN] класс задан корнем
      сайта: .svg вне корня получает политику страниц` — и только он; 40 из 41 остались
      зелёными, включая проверку значения (`2.13.1 [RED]`) и порядка (`2.13.2`). Совпадает
      с результатом фикстуры «D» (8 failed / 7 база → здесь 1 failed / 0 база — то же
      единственное различающее имя, при другой базовой линии, так как эта мутация
      выполнена на верном коде, а не на фикстуре с семью предсуществующими паданиями).
      Постусловие (4.25) подтверждено: пустой `git status --short --porcelain`, пустой
      `git diff 07eb6ae -- scripts/bootstrap-vps.sh`

## 5. Приёмка живой раздачи (ручная, со свидетельством)

- [ ] 5.1 Снять «до» на стенде — по одному адресу каждого класса плюс проверка сжатия
      CSS и страницы. Точная команда сохраняется вместе с выводом:
      `curl -sS -D- -o /dev/null -H 'Accept-Encoding: gzip' http://<host><path>`
- [ ] 5.2 Правку живого vhost делает **исполнитель реализации** (решение владельца):
      руками по процедуре `docs/deploy-vps.md`, раздел «Одноразовая правка на уже
      развёрнутом сервере» — блок команд для политики раздачи начинается на строке 133
      (проверено на `change/serving-cache-headers-impl@25434d27574193fbed3eac2f7f9c549454fa4cab`;
      прежняя ссылка `101-120` указывала на блок для одного лишь `include` редиректов и
      директив политики не содержала). Резервная копия, внесение
      директив, `nginx -t`, `systemctl reload nginx`. Ключ доступа проекту известен
      (`id_ed25519_ikpk_vps`), передавать `SSH_KEY=` явно: умолчания у скриптов разные
      (`docs/deploy-vps.md:51-53`). **Владелец уведомляется ДО правки**, а не по факту:
      стенд — единственная работающая раздача, и `reload` на нём виден снаружи.
      `bootstrap-vps.sh` повторно **не запускать**, `FORCE_VHOST=1` не использовать —
      конфигурация стенда была бы снесена
- [ ] 5.3 Снять «после» теми же командами. `Cache-Control` соответствует классу для всех
      семи: `/_astro/<файл>.css`, `/pagefind/fragment/<файл>.pf_fragment`,
      `/fonts/inter-latin.woff2`, `/favicon.svg`, `/hero-main.svg`, `/sitemap-index.xml`,
      `/kontakty`. `/hero-main.svg` — представитель класса корневых `.svg`; `/favicon.svg`
      берётся в паре с ним намеренно, и именно эта пара — единственное наблюдаемое
      свидетельство о порядке правил. Сроки у двух классов совпадают, поэтому по значению
      они не различимы: **записать оба ответа целиком** и назвать в свидетельстве, что
      различить классы на живой раздаче сегодня нельзя, а порядок проверяется текстом
      (2.13.2). Выдавать совпадение значений за подтверждение верного порядка — нельзя
- [ ] 5.4 Сжатие после правки: CSS, SVG, **`/sitemap-0.xml`** и
      **`/pagefind/pagefind-ui.js`** приходят с `Content-Encoding: gzip`; **страница
      тоже** — сегодня HTML сжимается умолчанием дистрибутива, и надо убедиться, что
      наш `gzip_types`, в котором `text/html` намеренно отсутствует, этого не отменил;
      `/fonts/inter-latin.woff2` и файл `.pf_fragment` приходят **без**
      `Content-Encoding` — они уже сжаты.
      Карта сайта и поисковый скрипт добавлены сюда не для полноты: **фактический тип
      содержимого задаёт таблица `mime.types` nginx, а не наш конфиг**, и наш хост отдаёт
      `.xml` как `text/xml`, `.js` как `application/javascript` (измерено, см. `design.md`,
      Решение 10). Перечень, промахнувшийся по написанию, оставит эти два ответа
      несжатыми, а проверка текста «XML в перечне есть» останется зелёной — то есть только
      этот шаг отвечает за факт. Сжатие карты сайта, кроме того, прямо требуется сценарием
      «Векторная графика и карта сайта», у которого до этой правки шага в разделе 5 не
      было.
      **Предмет шага — `/sitemap-0.xml`, а не `/sitemap-index.xml` (находка ревью 5.5, F2).**
      Индексный файл — **221 байт** при `gzip_min_length 256`, то есть сжатым он не приходит
      ни при каком верном перечне типов, и шаг на нём был невыполним: исполнитель приёмки
      получил бы отсутствие `Content-Encoding` на исправной конфигурации и либо записал бы
      ложный провал, либо снял бы порог и испортил конфигурацию под проверку. `/sitemap-0.xml`
      — 43 306 байт, порог проходит, тип содержимого у него тот же `text/xml`, поэтому цель
      шага (угадано ли написание типа) сохранена целиком. Числа сняты с собранного
      `web/dist` и совпали на трёх независимо собранных деревьях
- [ ] 5.5 Проверить, что адресный контракт не пострадал, **не следуя редиректу**:
      `/kontakty` → 200 без перенаправления на слэш; `/contacts` → 301; `/sitemap.xml` →
      301 на `/sitemap-index.xml`, а не 200 и не 404; несуществующий адрес → страница
      ошибки. `curl` без `-L`
- [ ] 5.6 Проверить страницы вокруг класса карт сайта и страницы ошибки: `/sitemap` (HTML
      для человека) → 200 с политикой страниц, а не с `max-age=3600`; `/404.html` прямым
      запросом → 200 с политикой страниц
- [ ] 5.7 Проверить 404 под `/_astro/`: годового `immutable` в ответе нет
- [ ] 5.8 Приложить свидетельство к PR: адрес, время, команда, вывод до и после — по всем
      шагам 5.1–5.10, включая два, добавленных ниже. Зафиксировать рядом **именованное
      ограничение**: свидетельство доказывает момент, а не состояние — живой vhost
      правится руками и расходится с текстом скрипта, и этот дрейф не гейтится ничем
- [ ] 5.9 **Сценарий «Клиент без поддержки сжатия».** Шага для него не было вовсе: все
      команды 5.1–5.7 идут с `-H 'Accept-Encoding: gzip'`, то есть ни одна не проверяет
      ответ клиенту, который сжатия не объявил, — сценарий числился покрытым ручной
      приёмкой, не имея в ней шага. Команда — та же, но **без** заголовка и с чтением тела:
      `curl -sS -D- -H 'Accept-Encoding: identity' http://<host>/kontakty | head -20`, и
      то же для `/_astro/<файл>.css` и `/sitemap-0.xml` (индексный файл здесь не годится по
      той же причине, что в 5.4: на 221 байте «`Content-Encoding` отсутствует» верно и при
      полностью отключённом сжатии, то есть шаг ничего бы не различал). Ожидается: ответ 200,
      `Content-Encoding` в ответе **отсутствует**, тело читается как текст. Заголовок
      `identity` предпочтён отсутствию заголовка намеренно: `curl` без него `Accept-Encoding`
      не отправляет, и результат тот же, но `identity` делает предмет проверки видимым в
      самой команде
- [ ] 5.10 **Сценарий «Шрифт на критическом пути».** Шага для него тоже не было, и `curl`
      его дать не может: предмет — поведение **кеша браузера**, а у `curl` кеша нет.
      Проверка: браузер с пустым профилем → открыть любую страницу (шрифт загружается по
      `<link rel="preload">`) → открыть **вторую** страницу сайта → в панели «Сеть» у
      `/fonts/inter-latin.woff2` источник — дисковый кеш, запроса к сети **нет** (ни 200,
      ни 304). Свидетельство — снимок панели с колонками адреса, кода и размера, плюс
      названная версия браузера. Названное ограничение шага: он проверяет **один** браузер
      в **один** момент, а не свойство раздачи; отрицательный результат означает дефект
      политики, положительный — что политика не мешает конкретной реализации кеша
- [ ] 5.11 **Сценарий «Векторная графика вне корня сайта».** Запросить `.svg` из `/media/`
      (адрес взять из фактической сборки, а не придумать) и убедиться, что он приходит с
      политикой страниц `public, max-age=0, must-revalidate`, а не с суточным сроком. Шаг
      сторожит форму правила: правило, записанное как `\.svg$` вместо одного сегмента пути,
      даст здесь `max-age=86400`, и текстовая проверка этого не различит, если утверждает
      только наличие правила и его значение. Если `.svg` в `/media/` в сборке нет вовсе —
      записать это как **отсутствие предмета проверки**, а не как успех шага, и закрыть
      сценарий текстовой проверкой формы правила из 2.13.1
- [ ] 5.12 **Сценарий «Векторная графика в корне сайта».** `/hero-main.svg` и
      `/logo-wordmark.svg` приходят с `public, max-age=86400`, без `immutable`. Свидетельство
      снимается в паре с 5.3 и той же командой

## 6. Гейты и PR

- [x] 6.1 Локально в `web/`: `npm run lint` (0 ошибок, 0 предупреждений, 5 hints —
      предсуществующие, не по этому change), `npm run typecheck` (0 ошибок), `npm run
      build` (269 страниц, pagefind — успех), `npx vitest run` весь пакет (218/218,
      включая 37/37 `serving-config.test.ts` и без сдвига цвета у остальных 181)
- [ ] 6.2 Ревью двумя независимыми исполнителями с разными уклонами (соответствие
      фактам против полноты последствий); автор реализации не ревьюер
- [ ] 6.3 PR доводится до зелёного и **не мержится** без решения владельца
- [x] 6.4 Находки ревью 5.5, не входящие в объём этого change, зарегистрированы долгом в
      `docs/tech-debt.md`, а не оставлены молчанием: `TD-26` (адреса классов зашиты в
      проверке литералами одной сборки, F6), `TD-27` (запрос каталога под `/_astro/` и
      `/fonts/` отдаёт встроенную страницу nginx, F7), `TD-28` (регулярное выражение класса
      карт сайта шире класса в спеке, F9), `TD-29` (четырнадцать стражей «ПРОВЕРИТЬ НЕ
      УДАЛОСЬ» не закреплены ни одним утверждением, F11), `TD-30` (подстановка команды в
      комментарии внутри heredoc'а — найдено при исправлении F3). `TD-28` касается формы
      правила и потому назван в задаче 3.12: новое правило корневых `.svg` записывается
      одним сегментом пути сразу, чтобы долг не удваивался

## 7. Верификация и архив

- [ ] 7.1 Сверить сценарии спеки с проверками. У каждого сценария ровно одно из трёх:
      автоматическая проверка; названный шаг раздела 5 со свидетельством; запись
      **[ДОКУМЕНТАРНЫЙ]** из 2.10 с указанием места, где сценарий удовлетворён. Сверка
      делается **пересчётом по списку сценариев спеки**, а не чтением записей 2.10:
      предыдущая ревизия числила три сценария за ручной приёмкой, у которой шагов для них
      не было, и по записям это не читалось — читалось только по списку сценариев.
      Пересчёт делается **заново**: после правки по находке F1 в спеке появились четыре новых
      сценария, и способ проверки у каждого назван здесь — «Векторная графика в корне сайта»
      → 2.13.1 плюс шаг 5.12; «Векторная графика вне корня сайта» → шаг 5.11 (с оговоркой об
      отсутствии предмета) плюс форма правила из 2.13.1; «Два класса с совпадающим значением
      объявлены в неверном порядке» → 2.13.2 с мутацией 4.24; уточнение `AND` в «Иконка сайта
      в обоих форматах» → та же 2.13.2
- [ ] 7.2 `./bin/openspec validate --all --strict --no-interactive` — зелёная
- [ ] 7.3 Передать change по security-заголовкам и CSP (находка B4) и change
      `online-payment-flow` **обе стороны** Решения 3 из `design.md`, а не одну:
      - заголовки, добавленные только в блок `server`, **не появятся** в ответах шести
        правил со своим `add_header` (`/_astro/`, данные поиска, `/fonts/`, `/favicon.*`,
        корневые `.svg`, `/sitemap*.xml`) — набор надо повторять в каждом либо вынести в файл
        и подключать `include`;
      - новый блок `location`, **не** объявивший своих заголовков, молча наследует
        серверный. Для будущего `location /api/` формы оплаты это означает `public` на
        ответе прикладного интерфейса — разрешение разделяемому кешу хранить ответ,
        адресованный конкретному посетителю; заодно унаследованный `error_page 404
        /404.html` ответит на несуществующий метод HTML-страницей сайта;
      - **`^~` не является безусловной защитой класса** (`design.md`, Решение 5): он
        прекращает фазу регулярных выражений только когда он же и самый длинный совпавший
        префикс. Обычный префиксный `location` с более длинным префиксом отбирает адрес у
        `^~` вместе с его заголовком — то есть новый блок внутри уже покрытого поддерева
        (например `/fonts/icons/` внутри `^~ /fonts/`) обязан либо повторить заголовок
        класса, либо не появляться
- [ ] 7.4 Сверить с дельтой `article-list-pagination` (после PR #88 — на capability
      `article-catalog`): она утверждает ту же поверхность раздачи с другой стороны
      (перенаправления адресов каталога, 301 для `/statyi/page/1`, 404 за диапазоном).
      Убедиться, что форма правил кеша её перенаправлений не отбирает, и что обе дельты
      говорят о `deploy/nginx-redirects.conf` согласованно
- [ ] 7.5 Архивировать change; требования переезжают в `openspec/specs/static-serving/`
