## 1. Предусловия

- [x] 1.1 Зафиксировать текущий состав обязательных проверок ветки `main` (девять имён) —
      задачи группы 6 его меняют, и без исходного состояния изменение не предъявить
- [x] 1.2 Получить для рабочего токена `gh` scope `workflow`, если он потребуется для
      слияния PR, меняющих файлы в `.github/workflows/`

## 2. Тесты по спеке (отдельная сессия, до реализации)

- [x] 2.1 Написать проверки инварианта покрытия линтера: покрытие сохранилось, упало ниже
      порога, **упало относительно базы в PR, меняющем только зависимости**, **законное
      удаление исходника при покрытии выше порога — проходит**, линтер не отработал
- [x] 2.2 Написать проверки инварианта платформенных записей: дедупликация вложенной
      копии, **потеря бинарника одного имени пакета при сохранении платформы другим**,
      потеря кортежа целиком, **узаконенная потеря проходит**, **та же потеря без записи
      падает**, **повторная потеря по старой записи падает**, **запись при вернувшемся
      кортеже падает**, нечего сравнивать
- [x] 2.3 Написать проверки инварианта числа выполненных тестов: объём сохранился, часть
      перестала запускаться в PR только с зависимостями, законное удаление теста выше
      порога — проходит
- [x] 2.4 Написать проверки требования о вершине `main`: промежуточный коммит не
      опубликован — не нарушение; вершина не опубликована за отведённое время — сигнал
- [x] 2.5 Написать проверки однородности групп: `group-by: dependency-name`; одинаковая
      зависимость при совместимых constraints в `web` и `scripts` приходит одним PR;
      позиция `cms` приходит отдельно; секция `dependencies`/`devDependencies` не
      влияет на группировку; patch/minor внутри одной зависимости допустимы; разные
      зависимости приходят разными PR; мажор не попадает в группу с patch/minor
- [x] 2.6 Отметить явной записью те сценарии, для которых автоматической проверки не
      будет, с названной причиной и способом ручной проверки. Кандидаты: фактический
      состав обязательных проверок ветки, поведение группировки Dependabot
- [x] 2.7 Предъявить красный прогон: отдельный push коммита только с тестами либо лог с
      точной командой и SHA

## 3. Инвариант: покрытие статического анализа

- [x] 3.1 Реализовать измерение числа проанализированных файлов по машинному отчёту
      линтера для `web`, `cms`, `scripts`
- [x] 3.2 Зафиксировать пороги в репозитории, сняв текущие значения (на момент написания:
      `web` — 128, `scripts` — 10; для `cms` снять при реализации)
- [x] 3.3 Реализовать сравнение головы с измерением на базовой ветке — в дополнение к
      порогу, а не вместо него, только для PR, где изменены исключительно манифесты
      зависимостей и файлы блокировки. Признак определять по составу изменённых файлов, а
      не по автору PR
- [x] 3.4 Реализовать различение «ноль нарушений» и «линтер не отработал»: разбор кода
      выхода по значению, пустой или неразбираемый отчёт — неуспех
- [x] 3.5 Разместить проверку так, чтобы она попала и в условие публикации, и в
      обязательные проверки: шагом внутри `Unit and build tests` либо отдельным job'ом с
      внесением в обязательные (см. 6.1)
- [x] 3.6 Негативная проверка: временно сузить область линтера так, чтобы часть файлов
      выпала; записать мутацию, число файлов до и после, имя покрасневшей проверки;
      подтвердить восстановление постусловием
- [x] 3.7 Негативная проверка второго условия: в PR, меняющем только зависимости,
      оставить покрытие выше порога, но ниже базового; убедиться, что проверка краснеет
      именно на сравнении с базой
- [x] 3.8 Проверить, что область второго условия ограничена верно: PR, удаляющий
      исходник, при покрытии выше порога проходит; тот же PR при покрытии ниже порога —
      падает. Предъявить оба состояния

## 4. Инвариант: платформенные записи lockfile

- [x] 4.1 Реализовать извлечение кортежей `(имя пакета, os, cpu, libc)`: имя — часть пути
      записи после последнего `node_modules/`, платформа — поля `os`/`cpu`/`libc` записи;
      без выделения «базовой части» имени
- [x] 4.2 Реализовать сравнение с lockfile базовой ветки по множеству кортежей
- [x] 4.3 Реализовать неуспех при отсутствии записей с метаданными платформы в любом из
      двух lockfile
- [x] 4.4 Реализовать узаконивание потери: файл в репозитории, где принятая потеря
      записывается **поимённым** кортежем с причиной; запись класса кортежей («любые
      потери в пакете») не допускается
- [x] 4.5 Сделать разрешение **одноразовым**: учитывать только записи, добавленные
      относительно базовой ветки; запись, уже присутствующая в базе, разрешения не даёт
- [x] 4.6 Реализовать инвалидизацию: наличие записи для кортежа, присутствующего в
      lockfile головы, — неуспех с требованием убрать устаревшую запись
- [x] 4.7 Разместить проверку в условии публикации и в обязательных проверках для всех
      трёх пакетов (см. 6.1)
- [x] 4.8 Негативная проверка на трёх мутациях: удалить все записи одного имени пакета для
      платформы, оставив её представленной другим именем (ожидание — **красное**);
      удалить кортеж целиком (красное); удалить вложенную копию, оставив верхний уровень
      (зелёное). Для каждой записать множества кортежей до и после
- [x] 4.9 Проверить обе ветви узаконивания: потеря с записью — зелёное; та же потеря без
      записи — красное
- [ ] 4.10 Проверить одноразовость на полном цикле: узаконить потерю и слить; вернуть
      кортеж (ожидание — **красное**, запись противоречит lockfile); убрать запись
      (зелёное); потерять кортеж снова без новой записи (**красное**). Предъявить все
      четыре состояния — именно этот цикл бессрочная запись пропускала бы молча

## 5. Инвариант: число выполненных тестов

- [x] 5.1 Реализовать чтение числа выполненных тестов из машинного отчёта модульного
      прогона и браузерного прогона
- [x] 5.2 Зафиксировать пороги в репозитории, сняв текущие значения
- [x] 5.3 Реализовать сравнение с базовой веткой в дополнение к порогу, с той же областью
      применения, что и в 3.3
- [x] 5.4 Обеспечить, что пропущенные тесты не засчитываются как выполненные
- [x] 5.5 Разместить проверку в условии публикации и в обязательных проверках (см. 6.1)
- [x] 5.6 Негативная проверка: пометить часть тестов пропускаемыми; записать число
      выполненных до и после и имя покрасневшей проверки
- [x] 5.7 Проверить область второго условия: PR, удаляющий тест, при числе выше порога
      проходит

## 6. Контур обязательных проверок

- [x] 6.1 Внести три новых гейта в обязательные проверки ветки `main` — либо встроив их
      шагами в уже обязательный `Unit and build tests`, либо оформив job'ами и добавив
      имена в защиту ветки
- [x] 6.2 Внести `Scripts unit tests` в обязательные: сейчас job живёт внутри `Tests`,
      останавливает публикацию, но слияние не останавливает
- [ ] 6.3 Проверить негативно: PR с падающим `Scripts unit tests` не сливается; до правки
      он сливался — предъявить оба состояния

## 7. Публикация вершины

- [x] 7.1 Реализовать проверку того, что вершина `main` опубликована; допустимое время
      отставания — **30 минут** от соответствующего push-run workflow `Tests`
- [x] 7.2 Реализовать сигнал при превышении этого времени, называющий неопубликованный
      коммит
- [ ] 7.3 Негативная проверка: искусственно оставить вершину неопубликованной, убедиться,
      что сигнал подан и называет верный коммит

## 8. Форма подачи обновлений и SHA-пины

- [x] 8.1 Перевести `.github/dependabot.yml` на общую запись
      `directories: [/web, /scripts]` с `group-by: dependency-name`, чтобы одинаковый
      подъём одной зависимости при совместимых constraints приходил одним PR через оба
      разрешённых пакета
- [x] 8.2 Оставить `cms` отдельной записью и исключить major из patch/minor-групп;
      разные зависимости SHALL приходить разными PR, а секция манифеста не участвует в
      группировке
- [ ] 8.3 Проверить на живых PR, что `typescript-eslint` при совместимых constraints
      обновляется одним PR через `web`/`scripts`, позиция `cms` приходит отдельно, мажоры отделены от
      patch/minor, а разные зависимости не смешиваются
- [x] 8.4 **Перенос между секциями манифеста не должен сокращать область
      runtime-аудита.** Достигать однородности переносом теперь нельзя по построению —
      признак секцию не читает, — но при любом переносе измерить объём
      runtime-аудита до и после и предъявить оба числа. Ориентир: prod-дерево `web` — 304
      записи из 819 и держится на `astro`; его перенос оставил бы гейту почти пустоту при
      зелёном цвете
- [x] 8.5 Заменить во всех workflow `uses: owner/repo@vN` на полный идентификатор коммита
      с версией в комментарии рядом
- [x] 8.6 Добавить проверку целостности: подвижная ссылка на внешнее действие — неуспех;
      ловить общий признак, а не список известных действий
- [x] 8.7 Негативная проверка проверки целостности: вернуть один подвижный тег, убедиться,
      что проверка называет файл и строку
- [x] 8.8 Обновить `docs/tech-debt.md`: долг про мажорные теги actions закрыт

## 9. Приёмка и архив

- [x] 9.1 Прогнать локальные quality gates затронутых пакетов и перечислить их в отчёте
- [x] 9.2 Свести в отчёт негативные проверки всех новых гейтов: мутация, два числа, имя
      покрасневшей проверки
- [x] 9.3 Независимое ревью двумя исполнителями с разными уклонами, находки внесены,
      отклонённое названо с причиной
- [x] 9.4 Сквозной проход по парам требований, говорящих об одном предмете (пороги,
      группировка, обязательные проверки): убедиться, что они не противоречат друг другу.
      Строгая валидация семантических конфликтов не ловит — за десять кругов ревью их
      нашлось два
- [x] 9.5 Строгая валидация change зелёная
- [ ] 9.6 Заархивировать change, требования переехали в `openspec/specs/`
- [ ] 9.7 **После архивирования** обновить дельту `dependabot-auto-merge` относительно
      появившейся основной спеки и проверить её **применимость** — не только строгую
      валидацию: она не проверяет существование требования в основной спеке
