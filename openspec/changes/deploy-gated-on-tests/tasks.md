## 1. Тесты по спеке (отдельная сессия, до реализации)

- [ ] 1.1 Написать проверки на сценарии спеки, различая три вида: что проверяется разбором
      конфигурации workflow, что — наблюдением реального события на `main`, что — только в
      форке. Для каждого сценария указать вид явно
- [ ] 1.2 Проверка конфигурации: условие публикации по-событийное, а не голое сравнение
      `conclusion` (иначе `push` и `workflow_dispatch` молча выключаются)
- [ ] 1.3 Проверка конфигурации: у checkout при `workflow_run` стоит явный `ref` на
      `head_sha`, а не значение по умолчанию
- [ ] 1.4 Проверка конфигурации: guard сравнивает `head_sha` с вершиной основной ветки
- [ ] 1.5 Проверка конфигурации: dry-run-файл не объявляет `permissions: pages` и не
      входит в группу `concurrency: pages`
- [ ] 1.6 Предъявить красный прогон этих проверок на текущем `deploy.yml` — отдельным push
      только с тестами либо логом с точной командой и SHA
- [ ] 1.7 Для сценариев, которые конфигурацией не проверяются (реальные события, откат по
      guard), записать способ ручной приёмки: команда, адрес прогона, что считается
      подтверждением

## 2. Шаг 1 — dry-run рядом с действующим деплоем

- [ ] 2.1 В `deploy.yml` добавить триггер `workflow_run` на workflow `Tests`,
      `types: [completed]`, `branches: [main]`, сохранив действующий `push: main` и
      `workflow_dispatch`
- [ ] 2.2 Сделать условие джобов по-событийным:
      `github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'`
- [ ] 2.3 Задать `ref` у `actions/checkout`: `head_sha` для `workflow_run`, `github.sha`
      для остальных событий
- [ ] 2.4 Добавить guard: сравнить `head_sha` с вершиной `main`, при устаревшем коммите
      завершить job без публикации и сказать об этом в логе
- [ ] 2.5 Публикацию на новом пути НЕ включать: job доходит до сборки и печатает выбранный
      SHA
- [ ] 2.6 Создать отдельный workflow-файл dry-run без `permissions: pages` и без группы
      `concurrency: pages`
- [ ] 2.7 Прогнать проверки из группы 1 — те, что разбирают конфигурацию, должны позеленеть

## 3. Наблюдение на основной ветке

- [ ] 3.1 После merge шага 1 дождаться успешного `Tests` на `main` и убедиться: job
      отработал, напечатанный SHA совпадает с вершиной
- [ ] 3.2 Дождаться события с неуспешным `Tests` (или воспроизвести в форке) и убедиться,
      что job не стартовал
- [ ] 3.3 Проверить guard в форке: перезапустить старый прогон и убедиться, что публикации
      нет
- [ ] 3.4 Убедиться, что действующий push-деплой всё это время работал и сайт обновлялся
- [ ] 3.5 Зафиксировать свидетельства: ссылки на прогоны и что именно они доказывают

## 4. Шаг 2 — перевод публикации на проверенный путь

- [ ] 4.1 Снять `push: main` из `deploy.yml`
- [ ] 4.2 Включить публикацию на `workflow_run`-пути
- [ ] 4.3 Удалить временный dry-run-файл
- [ ] 4.4 Проверить, что `workflow_dispatch` по-прежнему публикует текущее состояние
      основной ветки
- [ ] 4.5 Убедиться, что после merge реальный коммит опубликовался именно по новому пути

## 5. Приёмка

- [ ] 5.1 Прогнать локальные гейты затронутых пакетов и `./bin/check-openspec-integration`
- [ ] 5.2 Пройти два независимых ревью с разными уклонами (соответствие фактам против
      полноты последствий), находки внести, отклонённое назвать с причиной
- [ ] 5.3 Записать в описании PR ограничение проверяемости: красный сценарий в боевом
      репозитории невоспроизводим, проверен в форке — не выдавать пропуск за прохождение
- [ ] 5.4 Записать в спеке или в `docs/tech-debt.md`, что гейт покрывает только `Tests`, а
      `Lint`, `Lighthouse` и `Security` в него не входят
- [ ] 5.5 Заархивировать change, требования переезжают в `openspec/specs/`
