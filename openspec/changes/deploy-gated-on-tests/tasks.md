## 1. Тесты по спеке (отдельная сессия, до реализации)

- [x] 1.1 Написать проверки на сценарии спеки, различая три вида: что проверяется разбором
      конфигурации workflow, что — наблюдением реального события на `main`, что — только в
      форке. Для каждого сценария указать вид явно
- [x] 1.2 Проверка конфигурации: условие публикации по-событийное, а не голое сравнение
      `conclusion` (иначе `push` и `workflow_dispatch` молча выключаются)
- [x] 1.3 Проверка конфигурации: у checkout при `workflow_run` стоит явный `ref` на
      `head_sha`, а не значение по умолчанию
- [x] 1.4 Проверка конфигурации: guard сравнивает `head_sha` с вершиной основной ветки
- [x] 1.5 Проверка конфигурации: права `pages` и группа `concurrency: pages` есть только
      у публикующего workflow. Правило общее, а не про конкретный файл: изначально оно
      писалось под временный dry-run, но после перехода на один шаг продолжает стеречь
      тот же предмет — посторонний workflow в группе `pages` вытеснил бы публикацию
- [x] 1.6 Проверка конфигурации: происхождение (`head_repository` против самого
      репозитория) проверяется ДО шага `actions/checkout`, а не после. Проверка обязана
      падать, если шаг выгрузки стоит раньше — иначе она не о том
- [x] 1.7 Проверка Req5 «объём гейта назван явно»: в документации процесса присутствует
      перечень проверок, входящих и не входящих в условие публикации. Это автоматизируемо
      (разбор `docs/tech-debt.md` или описания change), ручной приёмки не требует
- [x] 1.8 Предъявить красный прогон этих проверок на текущем `deploy.yml` — отдельным push
      только с тестами либо логом с точной командой и SHA
- [x] 1.9 Для сценариев, которые конфигурацией не проверяются (реальные события, откат по
      guard), записать способ ручной приёмки: команда, адрес прогона, что считается
      подтверждением

## 2. Реализация одним шагом

- [x] 2.1 В `deploy.yml` снять триггер `push: main`, добавить `workflow_run` на workflow
      `Tests` с `types: [completed]` и `branches: [main]`, сохранить `workflow_dispatch`
- [x] 2.2 Поставить проверку происхождения ПЕРВЫМ шагом, до `actions/checkout`: при
      источнике вне основного репозитория завершить job, не выгружая и не исполняя код
- [x] 2.3 Сделать условие джобов по-событийным:
      `github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'`
- [x] 2.4 Задать `ref` у `actions/checkout`: `head_sha` для `workflow_run`, `github.sha`
      для остальных событий
- [x] 2.5 Добавить guard: сравнить `head_sha` с вершиной `main`, при устаревшем коммите
      завершить job без публикации и сказать об этом в логе
- [x] 2.6 Решить и записать, что делать с `cancel-in-progress` в группе `pages`, чтобы
      устаревший запуск не вытеснял идущую легитимную публикацию
- [x] 2.7 Прогнать проверки из группы 1 — все должны позеленеть (число живёт в тесте,
      а не в чек-листе: на момент закрытия задачи их 23)

## 3. Негативная верификация

Каждый аспект конфигурации ломается по очереди, и проверяется, что краснеет тест **с
нужным именем**, а не соседний. Мутация выполняется в своём worktree, фикс к моменту
мутации закоммичен, восстановление подтверждается пустым `git status --porcelain` и
отсутствием diff относительно SHA реализации.

- [x] 3.1 Убрать триггер `workflow_run` → краснеет проверка про подписку на событие
- [x] 3.2 Заменить по-событийное условие голым сравнением `conclusion` → краснеет проверка
      про ручной запуск, а не что-то иное
- [x] 3.3 Убрать `ref` у выгрузки → краснеет проверка про проверенный коммит
- [x] 3.4 Убрать шаг сверки с вершиной → краснеет проверка про устаревший коммит
- [x] 3.5 Переставить проверку происхождения после выгрузки → краснеет проверка про
      порядок, и отдельно — про выгрузку чужого кода
- [x] 3.6 Вернуть `push: main` → краснеет проверка «push сам по себе не публикует»
- [x] 3.7 Записать по каждой мутации точную команду или патч и два числа: до и после

## 4. Проверка на живой системе после merge

- [x] 4.1 Убедиться, что первая реальная публикация прошла по новому пути и опубликован
      именно `workflow_run.head_sha`.
      **Выполнено:** merge #41 → `Tests` на `8f60e4797dfc1d7edeb0c0597fa4f17f608664b9`
      завершился успехом → пришло событие `workflow_run` → прогон
      https://github.com/pgorbachev/ikpk/actions/runs/31248760022 собрал и опубликовал
      именно этот SHA, оба джоба `success`
- [ ] 4.2 Убедиться, что `workflow_dispatch` по-прежнему публикует состояние основной
      ветки
- [ ] 4.3 Дождаться события с неуспешным `Tests` и убедиться, что публикация не
      запустилась. Различать `failure`, `cancelled`, `skipped` и `timed_out` явно
- [ ] 4.4 Проверить в форке: guard от устаревшего коммита и отказ выгружать код из
      стороннего репозитория — в боевом репозитории эти сценарии невоспроизводимы
- [ ] 4.5 Зафиксировать свидетельства: ссылки на прогоны и что именно они доказывают

## 5. Приёмка

- [x] 5.1 Прогнать локальные гейты затронутых пакетов и `./bin/check-openspec-integration`
- [x] 5.2 Пройти два независимых ревью с разными уклонами (соответствие фактам против
      полноты последствий), находки внести, отклонённое назвать с причиной
- [x] 5.3 Записать в описании PR ограничение проверяемости: сценарии форка и устаревшего
      коммита в боевом репозитории невоспроизводимы, проверены в форке — не выдавать
      пропуск за прохождение
- [x] 5.4 Записать в спеке или в `docs/tech-debt.md`, что гейт покрывает только `Tests`, а
      `Lint and Typecheck`, `Lighthouse CI` и `Security` в него не входят
- [ ] 5.5 Заархивировать change, требования переезжают в `openspec/specs/`
