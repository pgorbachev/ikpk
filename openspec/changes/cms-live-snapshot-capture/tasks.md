# Задачи

## 1. Красные тесты (отдельная сессия, чистый контекст)

- [ ] 1.1 Тесты по восьми сценариям спеки. Живая система управления поднимается локально
      (`cms`, sqlite) либо подменяется заглушкой HTTP — выбрать в сессии тестов и назвать.
- [ ] 1.2 Предъявить красный прогон: точная команда и SHA.

## 2. Соответствие полей

- [ ] 2.1 Таблица соответствия «поле снимка ← источник» как данные.
- [ ] 2.2 Проверка полноты относительно контракта снимка, а не относительно фикстуры.
- [ ] 2.3 Отказ при исчезнувшем источнике с названием обоих имён.
- [ ] 2.4 Печать полей схемы, не участвующих в соответствии.

## 3. Живой захват

- [ ] 3.1 Чтение всех типов через REST с постраничным обходом: `pageSize` по умолчанию 25,
      и без обхода снимок молча потеряет записи.
- [ ] 3.2 Отказ вместо подмены фикстурой при заданном адресе.
- [ ] 3.3 Снимок не записывается на диск, пока не прошёл контракт.
- [ ] 3.4 Отметка происхождения в поле `origin`: живой либо закреплённый, адрес и время для
      живого. **Не `provenance`** — то имя занято числами журнала и читается гейтом публикации.
- [ ] 3.5 Судьба `fixtures/content-snapshot/url_map.csv` и
      `fixtures/content-snapshot/collapsible_panels.json` в живом пути: обе — части артефакта
      снимка, и сценария для них нет. Решить явно: переносятся из фикстуры, собираются из CMS
      или объявляются вне объёма.

## 4. Проверка на настоящих данных

- [ ] 4.1 Живой захват из локальной CMS, наполненной `scripts/import.ts`.
- [ ] 4.2 Сверка состава с закреплённой фикстурой: расхождения названы поимённо, а не «примерно
      совпадает».
- [ ] 4.3 Сборка сайта из живого снимка проходит.

## 5. Приёмка

- [ ] 5.1 Правка в системе управления отражается на собранном сайте — критерий владельца.
- [ ] 5.2 Негативная проверка каждого нового гейта.
- [ ] 5.3 TD-56 снят: писатель журнала на событие правки перестал быть невызываемым.
