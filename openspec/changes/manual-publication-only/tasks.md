## 1. Утверждение и предусловия

19.09.2026: два независимых ревью спецификации завершены; дополнительное Copilot Balanced
проверяется отдельным циклом исправлений. Владелец поручил полный цикл до реализации,
merge и деплоя. Операционные предусловия ниже остаются обязательными.

- [x] 1.1 Провести независимое ревью proposal/spec/design двумя исполнителями с разными
  уклонами и внести подтверждённые находки; отдельно проверить непротиворечивость с основными
  `deploy-gating`, `cms-content-source`, `static-serving` и `online-payment`.
- [x] 1.2 Получить утверждение владельца на четыре breaking-следствия: никакое автоматическое
  событие не публикует сайт; публикации из GitHub Actions не остаётся вовсе; свежесть сайта не
  ограничена до явной команды; право записи на боевой хост переходит к машине оператора.
- [x] 1.3 Перед началом реализации обновить ветку от свежего `origin/main` и потребовать, чтобы
  канонический журнал change-flow был доступен из репозитория; затем записать переход change в
  `active`, не превышая действующий лимит из журнала (на срезе актуализации — четыре). Отсутствие журнала в основной ветке — блокер учёта, а не
  разрешение продолжать по локальной копии.
- [ ] 1.4 Проверить готовность предусловий рабочего места и раздачи: непривилегированный
  deploy-user, каталог релизов и глубина их хранения, закреплённый признак хоста, способ
  передачи учётных данных вне аргументов команд, механизм исключительной блокировки.
  Отсутствующее предусловие блокирует настоящую публикацию и операционную приёмку;
  разработку и проверки на изолированном тестовом назначении оно не блокирует.
- [x] 1.5 Зафиксировать имя обязательного прогона и **строгий признак его успеха**:
  основной репозиторий, событие push или расписание для основной ветки (не предложение
  изменений), названный workflow, перечень его обязательных джобов, ни одного пропущенного
  или отменённого. Зафиксировать способ чтения по SHA и убедиться, что чтение не требует прав
  записи.
- [ ] 1.6 Согласовать поставку единственного launcher: его код реализуется здесь,
  `server-hardening` отвечает за установку и защиту на машине оператора. Проверить
  получение `origin/main` и выдачу учётных данных только подтверждённому источнику.
  Отсутствие — блокер операционной приёмки; тесты используют изолированное назначение.
- [ ] 1.7 Получить свидетельства задачи 5.3 `cms-live-snapshot-capture`: две быстрые правки
  дают две записи, снимок несёт ревизию журнала. Не считать живой REST доказательством
  событийного журнала. При отсутствии запретить настоящую публикацию.
- [x] 1.8 Перед merge реализации подтвердить, что Pages не обслуживает публичный сайт,
  а существующий релиз VPS остаётся доступен при удалении workflow. Иначе этот порядок
  внедрения остановить и согласовать план переноса действующей площадки.
  Свидетельство: `docs/testing/manual-publication-only/site-topology-readonly.md` —
  GitHub сообщает `has_pages: false`, production разрешается в `158.160.147.32`,
  главная отвечает 200. Проверка после merge остаётся частью операционной приёмки.

## 2. Красные тесты в отдельной сессии

- [x] 2.1 Проверки конфигурации: ни один размещённый прогон не содержит шага, передающего
  содержимое сборки на хост раздачи; `workflow_run`, `workflow_dispatch`-публикация, Pages
  upload/deploy actions, environment `github-pages`, права `pages: write`/`id-token: write`
  отсутствуют; учётные данные раздачи не объявлены ни в одном прогоне.
- [x] 2.2 Проверки, что `push main`, календарный прогон и внешнее оповещение выполняют только
  проверки и подготовку вердикта и не содержат шага, меняющего раздачу.
- [x] 2.3 Проверки права публикации: отказ при отсутствии успешного вердикта обязательного
  прогона для этого SHA; отказ, когда успешен только прогон предложения изменений того же
  SHA; отказ при пропущенном обязательном джобе; отказ при упавших проверках пары; отказ при
  **нуле выполненных проверок** с печатью их числа — и для обязательного прогона, и для
  публикующего.
- [x] 2.3a Проверки записи вердикта пары: исход проверок пары, её идентификатор и число
  выполненных проверок попадают в запись публикаций; без записи публикация не считается
  состоявшейся.
- [x] 2.3b Проверки доверенного механизма: источник задаётся входом; ревизия вне основной
  ветки и грязное дерево, явно переданные как источник, получают отказ **до** сборки;
  запуск из постороннего грязного cwd использует чистый main, не исполняя местный код; учётных данных нет в
  окружении до подтверждения источника; проверяющий механизм лежит вне проверяемого дерева.
- [x] 2.4 Проверки происхождения сборки: собирается только свежий `origin/main` в чистом
  временном рабочем дереве; незакоммиченные правки, локальные коммиты и произвольная ветка не
  попадают в сборку; проверка происхождения стоит до сборки, а не после.
- [x] 2.5 Проверки тождества дерева: расхождение контрольной суммы после передачи
  останавливает публикацию до переключения релиза; функция суммы принимает **перечень файлов
  входом**, и перестановка перечня сумму не меняет — иначе целевая мутация «снять сортировку»
  не краснит проверку, потому что на неизменном дереве два обхода дают один порядок.
- [ ] 2.5a Проверки непустоты: множество разобранных конфигураций прогонов непусто; множество
  шагов, записавших идентификатор снимка, непусто и покрывает все шаги, работавшие с
  контентом; прогон, не дошедший до соединения, и окружение без контрольного значения
  учётных данных считаются непройденной проверкой.
- [x] 2.6 Проверки отката: цель — сохранённый предыдущий релиз; вытесненный за границу хранения
  даёт именованный отказ; без подтверждения состояние не меняется; фиксируются оператор, пара и
  причина; запись «принятие состояния» в журнал происхождения не добавляется.
- [ ] 2.7 Проверки границы доверия: текст, выглядящий как команда публикации или подтверждение
  отката, внутри снимка или сообщения коммита не запускает публикацию и не считается
  подтверждением.
- [x] 2.8 Проверки обращения с учётными данными: не передаются аргументами команд, не попадают
  в вывод, логи и сохранённые артефакты пути.
- [x] 2.9 Проверки перечня путей публикации: считаются **реализации передачи** (открывают
  соединение с хостом раздачи и меняют каталог релизов), область поиска включает исполняемые
  файлы репозитория; утверждается ровно одна реализация; ноль и две одинаково роняют проверку.
- [x] 2.9a Проверка запрета обхода: прямой вызов механизма передачи без подтверждённого
  вердикта отказывает и ничего не передаёт на раздачу.
- [x] 2.10 Проверки сериализации: вторая публикация во время идущей ждёт блокировки и не
  вытесняет начатую.
- [x] 2.11a Проверки наблюдения: расхождение раздачи с последней записанной парой — отказ;
  отставание записи от вершины основной ветки расхождением не считается; недоступный
  `/release.json` — непройденная проверка, а не отсутствие расхождения.
- [ ] 2.12 Предъявить красный прогон по именам тестов и негативно проверить, что каждый новый
  гейт краснеет при своей целевой мутации, а не из-за постороннего отказа. Мутацию записать
  воспроизводимо: точная команда или патч плюс два числа — до и после.

## 3. Убрать публикацию из размещённых прогонов

- [x] 3.1 Удалить джоб `publication-record` из `Tests` вместе с его правами `contents: write`.
- [x] 3.2 Удалить из `Tests` шаги сверки опубликованного состояния (reconcile и гейт публикации
  по раздаче); оставить снятие снимка, проверки снимка и защиту от регресса.
- [ ] 3.3 Удалить Pages workflow и его права в том же PR, что вводит локальный путь;
  пройти локальные и PR-гейты конечного контракта. Настоящий зелёный `Tests` на main
  проверяется после merge в 7.1, а не предполагается из удаления одного джоба.
- [x] 3.4 Переписать под новый контракт 20 проверок в четырёх файлах, выводивших обязательный
  прогон из наличия `workflow_run` у публикующего workflow: `web/tests/demo-gate.test.ts` (9),
  `web/tests/browser-test-gating.test.ts` (5), `web/tests/deploy-gating.test.ts` (5), `web/tests/social-accounts.test.ts` (1).
  Не `.skip`: они проверяют настоящий инвариант, у которого меняется предмет, а не исчезает.
- [x] 3.5 Проверить по всем `.github/workflows`, что успешный push, schedule,
  `repository_dispatch` или изменение контента не вызывают внешнюю выкладку прямо либо косвенно.

## 4. Локальный entrypoint публикации

- [x] 4.1 Реализовать выбор коммита и сверку вердикта: чтение результата обязательного прогона
  по SHA, отказ при отсутствии успеха, отмене или пропуске.
- [x] 4.2 Реализовать код единственного доверенного launcher для защищённой установки
  вне рабочего дерева средствами `server-hardening`: принимает источник входом,
  получает и сверяет `origin/main`, создаёт чистое временное дерево, выдаёт учётные данные
  только подтверждённому источнику, убирает дерево после. Код репозитория исполняется только
  после подтверждения.
- [x] 4.2a Реализовать в механизме передачи отказ без подтверждённого вердикта: прямой вызов
  ничего не передаёт на раздачу.
- [x] 4.3 Снять снимок контента один раз одним шагом; передать его локальным проверкам и сборке
  как неизменяемый артефакт; запретить повторное обращение к системе управления после фиксации.
- [ ] 4.4 Реализовать `test:publication` для новой публикации по пяти обязательным группам дельты deploy-gating.
  Один раз собрать сайт из снимка; переиспользовать проверки контракта, медиа/ссылок,
  rich-content-safety, режимов и платёжного назначения; browser smoke — desktop/mobile
  на том же дереве без пересборки. Потребитель проверяет полный состав, число и успех
  каждой группы, назначение и сумму; пропуск любой группы и чужой отчёт дают отказ.
  Роль оплаты независима от CRM: `ci` положительно проверяет отсутствие формы/endpoint
  без API-запросов; активные роли требуют readiness/preflight. Проверить production+CRM
  вместе с ролью `ci`, не приравнивать PAYMENT_ROLE к DEPLOY_MODE.
- [x] 4.5 Посчитать сумму готового дерева по относительным путям в устойчивом порядке;
  после проверок и перед передачей подтвердить её неизменность. Отчёт хранить вне дерева.
- [x] 4.6 Внутри подготовки артефакта записать `release.json` с `commit` и `snapshotId`
  до вычисления суммы и проверок готового дерева; после них файл не изменять.
- [x] 4.7 Передать дерево на раздачу под исключительной блокировкой; сверить сумму на хосте;
  расхождение — отказ до переключения релиза.
- [x] 4.8 Повторно сверить вершину, последнюю запись журнала и высшую отметку
  непосредственно перед атомарным переключением. Сохранить проверенные значения;
  покрыть событие CMS до сверки (отказ) и после её завершения (остаточное окно контракта).
- [x] 4.9 После переключения прочитать `/release.json` с фактической раздачи, запретить смену
  имени хоста по перенаправлению и потребовать точного совпадения пары; неуспех завершает
  публикацию красным с указанием действующего релиза.
- [x] 4.10 Обеспечить, что учётные данные передаются окружением, конфигурацией клиента или
  агентом ключей, и не появляются в аргументах команд, выводе, логах и артефактах.

## 5. Запись публикаций и откат

- [ ] 5.0 Подключить журнал, поставленный `cms-live-snapshot-capture`: проверить две записи
  для двух правок между публикациями и отказ при снимке без ревизии. Сам писатель событий
  реализуется в задаче 5.3 соседнего change; здесь меняются потребитель, индекс и запись явного принятия текущего состояния CMS.
- [x] 5.1 Перенести запись ИНДЕКСА в локальный путь: добавляемая запись пары, исхода
  проверок пары и момента публикации; прежние записи не изменяются; сериализация — той же
  исключительной блокировкой на хосте раздачи. Убрать CI-токен записи; сохранить повтор после конфликта push и слияние
  непересекающихся записей с событийным писателем, как требует дельта cms-content-source.
- [x] 5.1a Реализовать поведение при отказе записи после переключения релиза: публикация
  завершается неуспехом с названием пары, следующие штатные публикации отказывают до
  восстановления записи.
- [x] 5.2 Реализовать защиту от регресса: при явном принятии текущего восстановленного
  состояния локальный путь записывает acceptance через общий протокол журнала. При новом
  событии между подтверждением и записью старое подтверждение не принимается; повторить
  публикацию после принятия без повторного подтверждения. Откат релиза журнал не меняет.
- [x] 5.2a Разделить историю, незавершённые операции и цели отката по назначению; записывать
  сумму проверенного дерева. Публикация стенда после production не меняет его ожидаемую
  пару, чужая цель или изменённое дерево отката дают отказ.
- [x] 5.3 Реализовать подтверждённый откат на сохранённый предыдущий релиз: подтверждение и
  причина — отдельные входы; фиксируются оператор, пара и причина; журнал происхождения не
  меняется; цель вне границы хранения даёт именованный отказ. Отдельный набор отката проверяет
  исходное свидетельство CI/локальных групп, назначение, сумму, режимы, smoke и платёжную роль
  сохранённого релиза. CMS, снимок, старый run и зелёная текущая main не требуются; её код не
  исполняется. Проверить недоступность этих источников и отказ при отсутствии самой записи.
- [x] 5.4 Автоматизировать интеграционную проверку записи публикаций на временном стенде:
  первая запись, накопление второй, отсутствие перезаписи прежних, чистое дерево записи.

## 6. Перечень путей и документация

- [x] 6.1 Расширить проверку перечня путей публикации на локальные исполняемые entrypoint'ы;
  объявить перечень в одном месте; утвердить ровно один путь, где ноль и два — отказ.
- [x] 6.2 Обновить `docs/deploy-vps.md`: как запросить штатную публикацию, как увидеть выбранную
  пару и вердикт, как выполнить подтверждённый откат, и почему прямая передача файлов
  низкоуровневым скриптом не является штатной публикацией.
- [ ] 6.3 После проверки удаления Pages и `publication-record` закрыть TD-54 и TD-61
  в `docs/tech-debt.md`; прочие причины падений CI не считать закрытыми. Записать отсутствие
  SLA свежести и автоматического отката как принятые свойства.
- [x] 6.4 Записать в документации, что штатным путём является entrypoint, а оператор — его
  исполнитель, и что требования адресованы скрипту.

## 7. Операционная приёмка после merge

Удаление Pages выполняется в 3.3 до первой настоящей публикации. Оно не переключает VPS:
его текущий релиз сохраняется. Тестовая проверка кода в PR не требует зелёного main для ещё
неслитого SHA; штатная команда всегда требует настоящий вердикт для свежего main.

- [ ] 7.1 После merge предъявить зелёный `Tests` для актуального main с полным SHA.
  При падении назвать причину и оставить раздачу без изменений; не подменять его успехом PR.
- [ ] 7.2 После подтверждения 1.4–1.7 выполнить штатную публикацию на тестовое назначение:
  SHA, вердикт CI, число локальных проверок, сумма дерева и ответ `/release.json` сохранены.
- [ ] 7.3 На тестовом назначении опубликовать второй проверенный релиз, затем выполнить
  подтверждённый откат на первый: запись оператора, пары и причины есть; журнал не изменён.
  Каталог без записи проверенной пары не признаётся допустимой целью.
- [ ] 7.4 Перед первой боевой выкладкой явно сообщить оператору, есть ли проверенная цель
  отката. При пустой истории первый релиз создаёт первую запись; неизвестный прежний
  каталог не выдаётся за проверенный, запрос отката без допустимой цели отказывает.
- [ ] 7.5 После отдельного подтверждения готового production-назначения и подтверждения
  владельца выполнить публикацию на него и сверить
  страницы, ресурсы, формы и `/release.json`: конечный 200 без смены хоста, пара совпадает.
  На срезе 19.09.2026 production-назначение нового пути не установлено; успех стенда
  193.124.115.99 не закрывает эту задачу. Перенос трафика/DNS сюда не входит.
- [ ] 7.6 Проверить неудавшуюся запись после переключения: неуспех с названием активной пары,
  следующая публикация запрещена до восстановления; сохранены доказательства восстановления.
- [ ] 7.7 Проверить, что push, правка контента, расписание и ручной запуск CI без локальной
  команды не меняют сайт; в репозитории одна реализация передачи, Pages и его прав нет.

## 8. Гейты, ревью и архив

- [ ] 8.1 Прогнать `./bin/check-spec-refs`, строгую валидацию OpenSpec, проверку применимости
  дельты применением в одноразовом worktree, lint, typecheck, полный vitest, build и
  релевантные scripts-гейты; результаты перечислить по командам.
- [ ] 8.2 Провести независимое ревью кода двумя исполнителями, не писавшими реализацию; после
  любого rebase повторить применимость дельты, локальные гейты и ревью нового полного SHA.
- [ ] 8.3 Получить приёмку владельца на локальный путь и доказательство, что обычный push
  больше не меняет сайт.
- [ ] 8.4 Архивировать change, перенести требования в основные спеки и добавить в канонический
  журнал change-flow переход `completed` отдельной append-only записью.

## Свидетельства текущей реализации (19.09.2026)

Закрытые пункты выше относятся к реализованным библиотекам, launcher и конфигурации CI.
Исполняемая связка worker, установленный откат, recover и accept-state проверены на
изолированных назначениях. Реальные предусловия и операционная приёмка ещё не завершены;
эти отметки не являются разрешением настоящей публикации. Пункт 6.1 дополнительно выявил
конфликт старого restore-server-state.sh с единственным механизмом публикации. Решение
владельца 19.09.2026: восстановление из резервной копии **ставит** копию в каталог релизов
(`restored/restore-<время>`, вне окна удержания `releases/`) и подтверждает её побайтно, но раздачу не переключает —
активация любого дерева идёт только через launcher. Скрипт переписан под этот контракт,
`manual-publication-restore-bypass.test.ts` и инвентарный гейт на реальном репозитории зелёные,
сценарий контейнера «восстановление подтверждается сравнением» проверяет теперь и то, что
действующий релиз не тронут. Граница названа: восстановленный каталог не является
проверенной парой индекса, и подтверждённый откат на него не сработает; для содержимого,
которого нет среди сохранённых релизов, путь один — новая публикация из `main`.
Принятое отклонение, названное вслух: до первой проверенной пары в индексе production
(сегодня `/release.json` там нет) восстановленную копию показать посетителям нельзя ничем —
это цена единственного пути, и она закрывается первой проверенной публикацией (пункты 1.4,
1.6), а не возвратом второго пути. Новое состояние на диске назначения (`.candidates/`,
`restored/`) описано в `docs/deploy-vps.md`; провижининг (`bootstrap-vps.sh`, change
`server-provisioning`) его пока не создаёт и не проверяет — долг того change.

- Доверенный источник и launcher: 32/32 Vitest; транспорт и его независимые регрессии: 48/48 Node.
- Runner, Git store, миграция workflow и PR #252: целевой прогон семи файлов — 94/94.
- Независимые RED и мутации сохранены в `web/tests/evidence/` и `scripts/tests/`;
  разбор последнего ревью — `web/tests/evidence/manual-publication-runner-review.md`.
- После синхронизации с PR #252 строгая валидация — 22/22, гейт ссылок — 0 расхождений.
  Финальные проверки и два полных ревью всей связки остаются в 8.1–8.2.

- Текущая интеграция: полный unit-прогон 1852 PASS / 2 FAIL; оба отказа были ложным
  определением type-only импортов как чтения двух сборок. Исправленный demo-гейт: 10/10 PASS.
  Новые независимые recovery-тесты: исходно 1 PASS / 3 RED, после исправления 70/70
  recovery/state-store/rollback; дополнительные отказы state-store: 3/3 PASS.
- Retention: независимый P1 отменённые кандидаты вытесняли последнюю активную цель отката;
  исправление проверено 50/50 native тестами. Полный native-прогон 168 PASS / 1 FAIL
  выявил гонку сообщения об отказе SSH; исправление и повторный прогон остаются в работе.
- Свежий typecheck: 424 файла, 0 errors / 0 warnings / 8 hints. Web lint и audit:prod зелёные.
  Финальные 8.1–8.2 ещё не закрыты; промежуточные числа не подменяют эти гейты.
