# Долг неизмеримых ссылок: сколько ссылок с номером строки идут БЕЗ фрагмента в кавычках,
# то есть не проверяемы по содержимому. Гейт ./bin/check-spec-refs сравнивает текущее число
# с этим и падает при росте — храповик. Уменьшили долг? Впишите новое число, иначе он не
# защёлкнется.
#
# Почему долг, а не немедленный запрет: ссылка «путь:строка» без фрагмента — принятая в
# проекте форма, её несут и уже слитые спеки. Требовать разом переписать все было бы
# запретом на существующее состояние вместо защиты от регресса.
54
